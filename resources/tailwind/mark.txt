$attributes->class([$classes])
$attributes->getAttributes());
$classes
$scope);
$slot
',
'base',
'bg-[var(--color-wk-warning-bg)]',
'px-0.5',
'rounded-[var(--radius-wk-sm)]',
'scope'
'text-[color:var(--color-wk-text)]',
(`color-mix(in_oklch,…)_14%`).
(silent
--}}
//
0.058
0.06
102.1)`
80)`.
<mark
=
=>
@endphp
@php
@props([
@props.
Declared
Dev-only
Fully
It
Measured
NEITHER
No
Pushery\WireKit\WireKit;
Renders
So
Tailwind
The
WireKit::resolveClasses('mark',
[
\Pushery\WireKit\WireKit::warnUnknownProps('mark',
])
]),
`_`
`oklch(0.905_0.093_102.1)`,
`oklch(32%
`oklch(96.4%
a
action
all,
already
and
any
arbitrary
asked
asserted:
at
attribute
auto-derived
background
be
block,
both
browser
came
cannot
chose,
class
color
compiled
component's
could
dark
debug
declared
directory
does
early.
element,
exists
fallback
fallback.
file
flags
for
for,
form
from
function
garbage
guard
highlight
implode('
imports
in
inside
interactive
is
it
later
light
list
live
mark
matches
may
modes,
n/a
never
no
not
null,
of
on
one
one.
optimistic-ui:
out
palette
presentational
prod).
props
qualified:
rather
raw
reach
read
reason
refutes
rendered
renders
rest
result
say
see
shown
silently.
situation
so
so.
spaced
spaces
splits
than
that
the
theme
there
this
three
to
token
tokens
unknown
use
value
value:
view's
was
way
which
whitespace,
whose
with
writes
{{
{{--
}}</mark>
}}>{{
—
