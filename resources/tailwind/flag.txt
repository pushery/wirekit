!==
$alt
$attributes->class([$classes,
$attributes->class([$classes])
$attributes->getAttributes());
$classes
$code
$format
$format);
$heightClass
$heightClass,
$intrinsicHeight
$intrinsicHeight]
$intrinsicWidth
$scope);
$shape
$shape,
$size
$size,
$src;
$url
&&
'')
'')));
'');
'',
',
'1x1';
'4x3'
'alt'
'aspect-[4/3]'
'aspect-square',
'base',
'bg-[var(--color-wk-bg-muted)]'])
'circle'
'circle']);
'country'
'h-3',
'h-4',
'h-5',
'h-6',
'h-8',
'inline-block
'lg'
'lg',
'md'),
'md',
'rect'
'rect'),
'rounded-[calc(var(--radius-wk-sm)/2)]',
'rounded-full'
'scope'
'shape'
'shape',
'size'
'size',
'sm'
'sm',
'square',
'src'
'w-auto',
'xl'
'xl']);
'xs'
($alt
($country
($size)
($url
(FlagPackage::manifest()
(is_string($src)
(silent
(string)
--}}
//
/>
1:1
20px
24]
24];
3166-1
4:3
:
<img
<span
=
===
=>
></span>
?
??
@else
@endif
@endphp
@if
@php
@props([
A
Case
Dev-only
Empty
FlagPackage::reportMissingOnce();
FlagPackage::reportUnknownOnce($code);
FlagPackage::url($code,
Fully
Give
Heights
ISO
It
Measured
No
Pushery\WireKit\Support\FlagPackage;
Pushery\WireKit\WireKit;
Renders
The
URL
What
Wins
WireKit::resolveClasses('flag',
WireKit::validateProp('flag',
[
[$intrinsicWidth,
['rect',
['xs',
[24,
[32,
\Pushery\WireKit\WireKit::warnUnknownProps('flag',
])
]),
`circle`
`country`.
`de`,
`eu`
`gb-sct`.
`rect`
`square`
`src`
a
above.
action
align-middle',
alignment
alone.
alpha-2
alt="{{
an
and
announcement
any
aria-hidden="true"
aria-label="{{
artwork
artwork's
artwork,
as
asserted:
attributes
be
before
beside
box
box.
build
by
call,
caller's
can
carries:
carry,
carry.
class
code
codes
component's
config('wirekit.components.flag.shape',
config('wirekit.components.flag.size',
corners
could
countries
country
country's
crop
crops
data-wk-flag
data-wk-prose-skip
debug
decoding="async"
decorative
decorative:
default
default,
depend
does
drawn
early.
element,
else
elseif
every
file
flag
flag's
flags
follows
for
for,
for.
from
given
given.
goes
guard
halved,
has
height
height="{{
icon
if
image
implode('
imports
in
interactive
into
is
it
it;
its
itself.
keeps
ladder,
like
loaded.
loading="lazy"
makes
manifest,
match
matter.
missing.
moment
n/a
name
name,
name.
next
no
noise.
not
null)
null,
null;
of
on
one
one.
only
optimistic-ui:
or
organization
other
over
own
own,
package
package,
pill.
placeholder
presentational
prod).
props
qualified,
radius
rather
ratio
ratio,
reason
refutes
region
renders
repeat
result
role="img"
round.
row
same
second
shipped
show.
shown
shows
shrink-0
sits
size
so
soften
src="{{
stands
states
stretched
strtolower(trim((string)
stylesheet
such
taken
text
than
that
the
theme
theme,
there
thing
this
through
to
trim($src)
turning
unknown
unless
use
used
was
when
where
which
whose
width
width="{{
with
without
would.
your
{
{{
{{--
}
};
}}
}}"
—
