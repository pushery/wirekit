!
!!}
!==
"default"
"heroicon-m-inbox"
$animateAttr
$attributes->class([$classes])
$attributes->class([$sizeClasses]);
$attributes->get('aria-hidden');
$attributes->get('aria-label');
$attributes->get('aria-labelledby');
$attributes->get('role');
$attributes->getAttributes());
$axis
$axis,
$callerAriaHidden
$callerAriaHidden;
$callerAriaLabel
$callerAriaLabelledBy
$callerRole
$chipSizeClasses
$classes
$cleaned
$cleaned);
$e)
$e;
$hasIconSlot
$iconSizeProp]
$iconSlot
$iconSlot->hasActualContent();
$intent
$isInformative
$isInformative;
$level
$level)
$level,
$levelValue
$m):
$m[1]);
$mergedAttributes
$mergedAttributes->getAttributes())->toHtml();
$mergedAttributes->merge($placeholderAttributes)
$mergedAttributes->merge(['aria-hidden'
$name,
$name];
$placeholderAttributes
$placeholderAttributes['role']
$resolved
$resolved);
$resolvedAriaHidden
$scope);
$shouldSetHidden
$size
$size,
$sizeClasses
$sizeMap
$sizeMap[$validSize];
$sizeMap[$validated];
$slot
$svgHtml
$svgHtml,
$title
$tone
$tone,
$tone;
$toneAliases
$toneAliases[$tone]
$toneClasses
$toneMap
$toneMap[$validTone];
$validSize
$validTone
$validated
&&
'
''
'')
'',
',
'.
'/<svg\b([^>]*)>/i',
'/<svg\b/',
'2',
'2xl'
'3',
'4',
'5',
'6']);
'<svg
'<svg'.$cleaned.'>';
'Rendering
'Run:
'[WireKit]
'accent'
'accent');
'accent',
'animateIn'
'base',
'bg-[color-mix(in_srgb,var(--color-wk-accent)_12%,var(--color-wk-bg))]
'bg-[color-mix(in_srgb,var(--color-wk-danger)_12%,var(--color-wk-bg))]
'bg-[color-mix(in_srgb,var(--color-wk-success)_12%,var(--color-wk-bg))]
'bg-[color-mix(in_srgb,var(--color-wk-warning)_12%,var(--color-wk-bg))]
'bg-[var(--color-wk-accent)]
'bg-[var(--color-wk-bg-muted)]
'danger'
'feature');
'flex
'gap-[var(--gap-wk-sm)]',
'h-12
'h-3
'h-4
'h-5
'h-6
'h-8
'icon'
'img';
'info'
'intent'
'level'
'level',
'lg'
'lg'],
'md'
'md');
'md'],
'name'
'neutral'
'primary'
'scope'
'size'
'size',
'sm'
'sm'],
'soft'
'soft',
'success'
'title'
'tone'
'tone';
'true';
'true']);
'warning'
'xl'
'xl'],
'xs'
(!
($callerAriaHidden
($isInformative
($levelValue
($resolvedAriaHidden
($shouldSetHidden)
($size
($svgHtml
('xs')
(1-6).
(64×64
(\BladeUI\Icons\Exceptions\SvgNotFound
(\Pushery\WireKit\Support\StrictnessGate::shouldThrowOnInvalid())
(\Throwable
(array
(button
(custom
(default,
(e.g.
(function_exists('svg')
(int)
(or
(paths,
(preserving
(provides
(regex
(replaces
(same
(shouldSetHidden=true),
(silent
(string)
(the
(true
(validateProp
);
+
-
--color-wk-accent-fg
--}}
...>
/
//
/>
1
2,
2rem,
3
3,
32×32
3;
3rem.
4,
5,
500s
6],
:
:name="$icon"
:size="$iconSizeProp"
</div>
</h{{
</p>
<div
<h{{
<p
<span
<svg
<svg…>)
<use>,
<x-wirekit::icon
<x-wirekit::icon>
=
===
=>
>
?
??
??=
@else
@elseif($icon)
@endif
@endif>
@endphp
@if
@if($animateAttr)
@if($hasIconSlot)
@if($slot->hasActualContent())
@if($title)
@php
@props([
@props.
A
A11y
ALSO
AND
API:
AT
ATTEMPTED
Adding
An
BECAUSE
BELOW,
Blade
Blade-heroicons'
BladeUI\Icons\Svg
CALL
CAN
CANNOT
CONDITION
CTA
Catching
Check
DO
Declared
Defaults
Dev-only
Drop
ENT_QUOTES).'"',
EVERY
Escape
FAIL
Feature
Fully
Graceful
HERE,
HTTP
Heading
Hero).
Htmlable,
IN
INSIDE
IS
IT
Icon
IconResolver,
If
In
It
Lighthouse.
Logger
MOST
Measured
NOT
Named
Nothing
Null
ONE
ONLY
OPENING
OR
OUTER
Operating
Optional
PERFECTLY
Preferring
Pushery\WireKit\Icons\IconResolver;
Pushery\WireKit\WireKit;
RENDER
Reapply
Render
Renders
Reported
Resolve
Resolved
RuntimeException
SEE.
SITE
SVG
SVG,
SVG:
See
Set
Silent
Size
Strip
SvgNotFound
SvgNotFound.
Symmetric
TAG
THE
The
This
Throwing
Tone
WAY
WCAG
We
WireKit
WireKit::resolveAnimateIn($animateIn,
WireKit::resolveClasses('feature',
WireKit::validateProp('feature',
WireKit::validateProp('icon',
With
[
[$chipSizeClasses,
['1',
['data-wk-icon-missing'
['w-10
['w-12
['w-16
['w-8
[1,
[chipDimensionClasses,
\Pushery\WireKit\Icons\IconSetPackages::reportMissingSetOnce((string)
\Pushery\WireKit\WireKit::warnUnknownProps('feature',
\Pushery\WireKit\WireKit::warnUnknownProps('icon',
])
]),
];
__toString
`<x-wirekit::feature
`Svg
`aria-hidden="true"`
`aria-label`
`aria-labelledby`
`class="h-12
`heading-order`
`heroicon-m-inbox`
`inbox`
`info
`info`
`intent`
`primary
`primary`
`role="img"`.
`tone`
a
a11y
about
above.
absent,
accent
accent`
accept
accepts
accessibility
accessible
action
actual
add
after
against
alert
alert/badge/callout/message/reaction/toast-region.
alias
all
all,
allowed
already
an
anchored
and
animation
any
app(IconResolver::class)->resolve($name);
are
aria-hidden
aria-hidden,
aria-hidden="'.htmlspecialchars($resolvedAriaHidden,
aria-hidden="..."
aria-hidden="false".
aria-label
aria-labelledby
array_keys($sizeMap));
array_keys($toneMap));
as
asserted:
at
attribute
attribute,
auto-derived
auto-switching
axe
axe's
axis,
back
back-compat
backgrounds,
badge
badge,
baked
bakes
bare
be
because
becomes
below
below.
best-practice
blade-heroicons
blade-icons
blade-icons,
blade-ui-kit/blade-heroicons.
blade-ui-kit/blade-icons
blade-ui-kit/blade-icons.
block,
bootstrapped
both
branch's
break
bug
build
but
button
button,
buttons).
buttons,
by
call
called
caller
caller's
callout
can
cannot
canonical
card
carries
carry
case
catch
caught
check
checks
chevrons,
chip,
choice
chose
chosen
class
class="flex
class="text-[length:var(--text-wk-lg)]
class="text-[length:var(--text-wk-sm)]
cleanly.
close
close-icons,
collapsed
color
color-mix(in_srgb,
component
component's
components
composer
condition
config('wirekit.components.feature.size',
config('wirekit.components.feature.tone',
config('wirekit.components.feature.{key}')
configured
conflict
console
console).
consuming
content
context
contract.
contrast
could
covered
covers
crashed
danger
danger.
data-replayable="true"
data-wk-prose-skip
debug
debug,
decided
decision,
decision:
declared
declaring
decorative
decorative,
default
default.
default:
defaults:
define('WIREKIT_BLADE_ICONS_WARNED',
defined('WIREKIT_BLADE_ICONS_WARNED'))
degradation:
degrades,
deliberate.
dense
dev
developer
developer's
did
did-you-mean)
didn't
direct
docs
document
does
does.
doing
down
draws
dropdown
dropdowns
early
early.
either
either,
element
element,
else
elseif
embedded
emit
empty
empty-state
entry
even
every
exhausting
exists.
explicitly
factory:
fallback
fallback,
fallback.
falls
false)
false),
feature
feature-grid.
features
file
files
finds
finished
first
first-allowed
first:
fix
fix.
fixed
flags
flex-col',
followable.
font-[number:var(--font-wk-heading-weight)]
for
forbidden
forcing
foreground
found.`
framework
from
full
function
function).
function_exists('svg'))
get
gets
global
glyph
glyph.
goes
grid
guarantees
guard
guessed
h-10
h-12
h-16
h-5
h-8
h/w
h1,
half
hand-written
hard
hardcoded
harder
has
hatch:
have
have)
helper
here
hero-row
hidden
historical
house
icon
icon's
icon);
icon-only
icon;
iconSizeProp].
icons
icons).
icons,
identifier
if
illustration,
image
implements
implode('
imports
in
in_array((int)
including
individual
inert
info
informative
informative.
injected
inside
installed
installed.'
intact
intent
intent.
inter-attribute
interactive
into
invalid
invisible
is
isn't
isset($iconSlot)
isset($sizeMap[$size])
isset($toneMap[$tone])
it
it,
it.
items-center
its
justify-center
keeps
kill
kit
kit's
know
labels).
ladder
later
layout
leading-relaxed">
leave
level
line
list
lists;
literal
live
lives
loads.
log
logger()->warning(
look
looking
lost
loudly,
make
makes
map
map.
marker
marks
match
matching
may
mb-[var(--space-wk-xs,0.25rem)]">
md
merge
message
migrated.
migration.
missing
missing,
missing:
modal
modals,
most
moves
moving
n/a
name
name.
named
needs
neither
nested
neutral
never
no
nobody
non-null
none,
nor
not
nothing
nothing.
now.
null
null)
null,
null;
occurrence
of
older
on
one
one,
one-time
one.
one;
only
onto
open
opening
optimistic-ui:
or
other
our
outline
outline:
over
override.
own
own.
package
page
page.
pages
path
pattern
per-set
place
placeholder
post-process
pre-checking
preg_replace(
preg_replace('/\s*aria-hidden="[^"]*"/i',
preg_replace('/\s+/',
preg_replace_callback(
present
presentational
preserves
preset
primary
problem
prod).
produce
production
production.
progress)
progress,
project,
prop
prop-naming-conventions
props
proven
provides
put
qualified:
rather
raw
re-inject
reach
reaches
reader
real
reapply
reason
reason:
refutes
registered
registry
removed
render
render,
rendered
renders
replaced
replaces
reports,
reproduced
request
require
requires
resolved
resolves
respect
rest
result
return
returns
reveal
role
role)
role,
role="img"
role="img".
root
root;
rounded-[var(--radius-wk-lg)]',
rounded-[var(--radius-wk-md)]',
rounded-[var(--radius-wk-sm)]',
rounded-[var(--radius-wk-xl)]',
rule
run
rung
same
screen-reader-skipping
scrolls
sees
semantic
sends
sensible
set
set.
sets
should
shown
signals
single
site
sits,
size
size,
skipped
sm
so
soft
soft`
somebody
source
source-baked
space).
span
spell:
spelling.
static
stay
still
stopped
storage/logs/laravel.log.
string
strip
style="display:inline-block;"></span>
success
suites
supplied
surrounding
svg($resolved,
svg()
svg(),
sweep
table
tag
tag's
tag.
tags
takes
taking
teaches
test
text
text-[color:var(--color-wk-accent-fg)]',
text-[color:var(--color-wk-accent-text)]',
text-[color:var(--color-wk-danger-text)]',
text-[color:var(--color-wk-success-text)]',
text-[color:var(--color-wk-text)]">
text-[color:var(--color-wk-text)]',
text-[color:var(--color-wk-text-muted)]
text-[color:var(--color-wk-warning-text)]',
text-white
than
that
the
their
then
there
they
third
this
though
three
through
throw
throws
tile
tinted
tinted-accent
title
to
to,
toHtml().
tone="primary">`
too.
touches
transitively
treatment)
tree:
true)
true);
try
two
unchanged.
unknown
unknown-alias
unknown.
unless
until
use
used.
useful
uses
utility
v1.2.x
v1.5.0-identical).
valid
validateProp
validateProp's
value
value,
via
view's
view.
visible
vocabulary
w-12"`
w-12',
w-3',
w-4',
w-5
w-5',
w-5';
w-6',
w-8',
warning
was
way
ways
we
what
when
when:
where
wherever
whether
which
whitespace
who
whoever
whose
wins
with
working
works
worse
would
wrote
xl
yet.
{
{!!
{{
{{--
|
||
}
},
}}
}}>
—
…)
…).
→
⚠️
