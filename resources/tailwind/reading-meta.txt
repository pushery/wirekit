"11",
"12",
"9
"9":
"~0"
"~12
"~5
$attributes->getAttributes());
$attributes->merge(['style'
$cjkCharsPerMinute);
$cjkCpm
$paragraphMinWords);
$paragraphMinWordsInt
$perParagraph
$perParagraphBool
$remainingLabel
$resolvedTarget
$rootClass
$scope);
$showRemaining
$showRemainingBool
$target
$totalLabel
$wpm);
$wpmInt
',
'base',
'cjkCharsPerMinute'
'false'
'font-size:
'inline-flex
'main,
'paragraphLabelTemplate'
'paragraphMinWords'
'perParagraph'
'remainingLabel'
'scope'
'showRemaining'
'target'
'totalLabel'
'wk-reading-meta',
'wpm'
($showRemainingBool)
(--cjk-chars-per-minute
(after
(first-match
(initial)
(int)
(re-computed
(resources/js/components/reading-meta.js).
(silent
(the
+
--}}
/
//
225,
30,
500).
500,
</div>
</span>
<code>,
<div
<figcaption>,
<figure>,
<img>/<picture>/<svg>
<p>
<pre>,
<span
=
=>
>
??
@endif
@endphp
@if
@php
@props([
@props.
A
Alpine's
Annotations
Blade
BooleanProp::from($perParagraph,
BooleanProp::from($showRemaining,
CHANGED
CJK
CSP
Declared
Default
Dev-only
FILTER_VALIDATE_BOOL);
FROM
Fully
Its
Method
Normalized
On
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
Reading-meta
SR
Same
Skips
That
The
UNBOUND
WireKit::resolveClasses('reading-meta',
Without
[
[data-language],
\Pushery\WireKit\Support\AlpinePayload::from($cjkCpm)
\Pushery\WireKit\Support\AlpinePayload::from($paragraphLabelTemplate)
\Pushery\WireKit\Support\AlpinePayload::from($paragraphMinWordsInt)
\Pushery\WireKit\Support\AlpinePayload::from($perParagraphBool)
\Pushery\WireKit\Support\AlpinePayload::from($resolvedTarget)
\Pushery\WireKit\Support\AlpinePayload::from($showRemainingBool)
\Pushery\WireKit\Support\AlpinePayload::from($wpmInt)
\Pushery\WireKit\WireKit::warnUnknownProps('reading-meta',
])
]),
__('wirekit::min
__('wirekit::{n}
`<span
`aria-atomic`
`paragraphMinWords`
`prop="false"`
a
against
an
and
annotation
annotations
announce
announces
anticipate
aria-atomic="true"
aria-hidden
aria-hidden="true">·</span>
aria-live="polite"
article'
article';
articles
as
as,
at
attribute
auto-derived
back
back.
bare
before
block,
blocks
build
build,
call
canonical
captions
cast
changes
character-based
cjkCpm:
class="wk-reading-meta-paragraph">N
class="wk-reading-meta__remaining">
class="wk-reading-meta__separator"
class="wk-reading-meta__total">
client-only
code
color:
compiles
component
component's
convention
count,
count.
count;
debug
default
display
does
don't
each
element
enabled,
estimate
estimated
factory
failed
falls
false);
false,
family
feature
figure
filter_var($perParagraph,
filter_var($showRemaining,
flags
flips
for
from
gap-1',
heard
here
idea
immediately
implode('
imports
in
injects
inside
integers
is
items-center
itself
languages
later
least
list
live
long-form-publishing
makes
markup
max(1,
max(100,
max(50,
may
mean
measures
min
min'),
min</span>`
mode
mount,
n/a
never
no
not
nothing
null,
number.
object
of
off;
on
on.
one.
only
onward
opposite
opt-in.
optimistic-ui:
optionally
own
owns,
pace.
paragraph
paragraphMinWords:
paragraphTemplate:
parse
pattern):
per-paragraph
perParagraph
perParagraph:
polite
prod).
progress.
prop's
props
prose
qualified:
reach
read
read"
read'),
reader
reading
reading-bookmark.
reading-spine
reading-time.
reads
region
remaining"
remaining".
remaining'),
remaining-time
resolves
role="status"
roll
scroll).
scrolling
server
ships.
shorthand
show
showRemaining:
showing
silently.
site
small
so
state
static
stayed
string,
target
target's
target:
target=null
text
textContent
that
the
then
there
they
this
this,
three
time
to
tokenization
total/remaining
truthy
under
underestimates,
unit
unknown
use
used
value
var(--reading-meta-color);'])->class([$rootClass])
var(--reading-meta-text-size);
view's
was
what
when
where
which
whitespace
whole,
wins).
with
word
words.
wpm:
x-data="wirekitReadingMeta({
x-text="remainingMinutes"></span>
x-text="totalMinutes"></span>
{{
{{--
})"
}}
}},
~<span
—
