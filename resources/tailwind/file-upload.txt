!
!$hasError)
!==
"Drop
"the
$accept
$announceError
$announceErrors
$attributes
$attributes->except(['announceErrors',
$attributes->except(['aria-label',
$attributes->get('aria-describedby',
$attributes->get('aria-label')
$attributes->getAttributes());
$attributes->whereStartsWith('wire:model')
$attributes->whereStartsWith('wire:model')->first();
$boundModel
$boundModel));
$capture,
$captureValue
$compactSizing
$compactSizing,
$describedBy
$disabled
$dropzoneClasses
$dropzonePadding
$dropzonePadding,
$dropzoneShape
$dropzoneShape,
$error
$errorId
$errorMessage
$errorMessage;
$errors->first($name)
$fileItemClasses
$hasError
$hasError)`,
$hint
$hintId
$iconClasses
$id
$label
$labelClasses
$labelIsVisible
$labelText
$listClasses
$multiple
$name
$removeLabel)
$removedMessage
$removedMessage)
$required
$scope);
$showsError
$uploadId
$variant,
$variantValue
&&
'
''
'')
'')));
''));
'');
',
'-error';
'-hint';
'.((string)
'[]'
'accept'
'announce-errors']);
'announceError'
'aria-describedby'])->whereDoesntStartWith('wire:model')->class([
'bg-[var(--color-wk-bg-muted)]',
'border-2
'border-[length:var(--border-wk-width)]
'border-[var(--color-wk-accent)]
'border-[var(--color-wk-border-error)]'
'border-[var(--color-wk-border-strong)]').'
'capture'
'capture',
'compact'
'compact',
'compact']),
'cursor-pointer',
'default',
'disabled'
'dropzone',
'duration-[var(--transition-wk-duration)]',
'environment'
'environment']),
'error'
'false'
'flex
'gap-[var(--padding-wk-x-sm)]',
'group
'h-4
'h-[var(--size-wk-lg)]
'h-[var(--size-wk-md)]
'h-[var(--size-wk-sm)]
'has-[:focus-visible]:ring-[length:var(--ring-wk-width)]',
'has-[:focus-visible]:ring-[var(--color-wk-ring)]',
'hint'
'id'
'inline-flex
'label'
'lg'
'list',
'list-none
'max-w-full',
'md'),
'min-h-[var(--size-wk-touch-target)]',
'multiple'
'name'
'p-[var(--padding-wk-y-lg)]',
'p-[var(--padding-wk-y-md)]',
'p-[var(--padding-wk-y-sm)]',
'px-[var(--padding-wk-x-sm)]
'removeLabel'
'required'
'rounded-[var(--radius-wk-lg)]',
'rounded-[var(--radius-wk-md)]',
'scope'
'size'
'sm'
'sr-only'
'text-[length:var(--text-wk-sm)]
'text-[length:var(--text-wk-sm)]',
'text-center',
'transition-colors',
'user',
'variant'
'variant',
'w-8
'w-full'
'w-full',
'wk-touch-target
'wk-upload-'
($capture)
($errors
($hasError
($name
($showsError
($size)
($variant)
($variantValue
(@aware
(`error=""`
(`wire:model.live`,
(`wk-upload-photos`),
(axe
(developer
(not
(p-0.5
(resources/js/components/file-upload.js).
(silent
(some
(standard
(truncated),
(when
+
--size-wk-touch-target
--}}
.
.($variantValue
/
//
/>
0
0"
00.75-.75v-2.5a.75.75
00.75.75h9.5a.75.75
01-.7-.7L5.29
01-.7.7L6
01-1.06
01-2.25
01.7
01.7.7L6.71
01.75-.75z"/>
01.75.75v2.5a.75.75
01.75.75v6.69l2.72-2.72a.75.75
010-.7z"/>
011.5
0110
013
0;
0;"
0L6
0l-4-4a.75.75
0v2.5a2.25
1.06l-4
111.06
111.06-1.06l2.72
12
12"
12x12
13a.75.75
14px
16.25v-2.5a.75.75
1x1
2.25
2.25a.5.5
2.25h-9.5A2.25
2.5.5
2.72V3.75A.75.75
20
20"
2px
3.05
3.05a.5.5
3.3.1
3.5
3.75
3.75a.5.5
3a.75.75
3z"
44x44
4a.75.75
5.29l2.25-2.24a.5.5
6
6.71
6l2.24
8.95a.5.5
:
::before
:aria-label="removeLabel.replace(':name',
:class="dragging
:key="file.name">
:name'),
:name');
</button>
</div>
</label>
</li>
</svg>
</template>
</ul>
<button
<div
<input
<label
<label>
<li
<p
<path
<span
<svg
<template
<ul
<ul>
<x-wirekit::button>
<x-wirekit::tags-input>:
=
===
=>
>
?
??
??=
@aware(['announceErrors'
@change="handleFiles($event.target.files)"
@click="removeFile(index)"
@dragleave.prevent="dragging
@dragover.prevent="dragging
@drop.prevent="handleDrop($event)"
@endif
@endphp
@if($accept)
@if($announceError)
@if($attributes->get('aria-label'))
@if($captureValue)
@if($describedBy
@if($disabled)
@if($hasError)
@if($hint
@if($multiple)
@if($name)
@if($required)
@if($showsError)
@php
@props([
@props.
A
A11y:
AAA
ANY
API
AREA
Accessible
Alpine
Alpine's
Alpine:
An
Android
Android-era
Assembled
At
BOTH
Blade
BooleanProp::from($disabled,
BooleanProp::from($multiple,
BooleanProp::from($required,
BooleanProp::stripFalseHtmlFlags($attributes);
Both
CLICKABLE
CSP
CSS
Compact
Compose
Content-Security-Policy
DECLARED
DROP
Declared
Deliberately
Dev-only
Dropzone
EMPTY,
Each
Enforced
Enter
Error
File
Filename
Fully
HTML
Hidden
Hiding
IDREFS
IS
Icon
In
Inline
Internal
It
JavaScript
LABEL,
Laravel
ListStyleAntiDriftTest.
Livewire
Livewire's
MEANS
MIME
Margin-top
Matches
Mirrors
NAME
NOT
Native,
Normalized
Null
OWN
On
Overridable
Own
PRESENCE,
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
REPLACE
REQUIRED
Remove
Resolved
Reusing
Same
Seeded
Selected
Set
Shape,
So
StrictnessGate
Strip
Tailwind
The
This
TranslatableDefaultsGuardTest
UA
UI
UNBOUND
UX).
Unconditional
Undeclared,
Upload
WCAG
We
What
WireKit::resolveClasses('file-upload',
WireKit::stableId('wk-upload',
WireKit::validateProp('file-upload',
X
X),
[
['default',
['user',
\Pushery\WireKit\Support\AlpinePayload::from($boundModel)
\Pushery\WireKit\Support\AlpinePayload::from((string)
\Pushery\WireKit\Support\AlpinePayload::string(($hasError
\Pushery\WireKit\WireKit::warnUnknownProps('file-upload',
])
]),
]);
__('wirekit::Drop
__('wirekit::Remove
__('wirekit::Removed
`$hasError`
`.blur`)
`.value`
`:name`
`<div>`
`<input
`@aware`
`@if($hint
`@props`
`MessageBag::has(null)`
`_focusAfterRemoval`
`accept="image/*;capture=camera"`,
`accept`.
`any()`,
`aria-describedby=""`,
`aria-invalid`
`aria-live`
`aria-valid-attr-value`)
`before:content-['']`
`border-2`:
`camcorder`
`camera`
`capture`
`compact`
`config()`
`const`,
`default`
`disabled="false"`
`flex`)
`focus-within`
`has-[:focus-visible]`
`has-[:focus-visible]`,
`inline-flex`
`items-center`
`list-none`
`livewire-upload-finish`.
`m-0`
`mt-[var(--padding-wk-y-sm)]`
`multiple="false"`
`multiple`
`name[]`,
`name`
`p-0`
`prop="false"`
`relative`
`required`
`size="sm"`
`sr-only`
`w-full`
`whereStartsWith`
`wire:model*`
`wire:model`
`{{
a
above,
absent
accept="{{
accepting
accepts
accessibility
accessible
across
action.
actually
adds
after
against
align-middle'
aligned
all
all.
already
also
always
an
anchors
and
announce-error
announceErrors)
announced.
announces
any.
anything
apart
apart,
app
appears
appears:
are
area
area,
area.
area;
aria-atomic="true"
aria-describedby
aria-describedby'd
aria-describedby="{{
aria-hidden
aria-hidden="true">
aria-hidden="true">*</span>@endif</span>
aria-invalid="true"
aria-label="{{
aria-live="polite"
aria-required,
aria-required="true"
array
arrives
as
as,
asked
assistive
asterisk.
at
at.
attached
attribute
attribute,
attributes
auto-derived
available
available.
back
background
badges
bag
bag,
bag.
base:
baseline
be
became
because
becomes
before
before.
before:-translate-x-1/2
before:-translate-y-1/2
before:absolute
before:content-['']"
before:h-[var(--size-wk-touch-target)]
before:left-1/2
before:top-1/2
before:w-[var(--size-wk-touch-target)]
behaves
belongs
below
below.
beneath
beside
beyond
bg-[var(--color-wk-bg-muted)]'
binding
binds
block,
block-level
blowing
boolean
border
border,
border-dashed',
bordered
both
bound
boundaries.
box.
browse",
browse');
browser
browser's
browser-default
bug:
build
bullets
business,
but
button
button's
button,
button.
buttons
buttons.
by
byte
call
caller
caller's
caller's.
caller-supplied
camera
can
cannot
capture
capture="{{
card
carrier
carries
carry
cast
catalog
catalog's
catalogs.
centered
change
changed
check,
checkbox
children
chip
chip-style
chip;
chose
chrome.
claiming
claims
class
class-based
class="flex-1
class="h-3.5
class="mt-[var(--padding-wk-y-xs)]
class="relative
class="sr-only"
class="text-[color:var(--color-wk-danger-text)]
class="text-[color:var(--color-wk-text-muted)]
class="{{
classes
click
click-to-browse
clicking
clip
clip-rule="evenodd"/>
clipped
clips,
clutter.
color,
column
comfortable
compact
compiles
complain
component
component's
component,
component.
computed
concatenated
config('wirekit.a11y.announce_error',
config('wirekit.components.file-upload.accept',
config('wirekit.components.file-upload.capture',
config('wirekit.components.file-upload.multiple',
config('wirekit.components.file-upload.size',
config.
constraint
constraint,
construction.
container
container,
content
content.
context
contexts
contract,
control
control's
control,
control.
copy
correctly.
could
counted
couple
current
cursor-pointer
d="M10
d="M3.05
d="M3.75
danger
dashed
data-wk-file-remove
data-wk-prose-skip
date-picker,
debug
decides
declares
decorative,
decorative;
default
default:
dense
described
described-by
describes
description.
destroys
developer
dialog
did
differ
different
differs.
directly
disabled
disables
disc
distinguished.
div,
docs
document
documentation
does
does.
doesn't
doing
down.
drag
drag-drop
drag-highlight
drag-over
drawn
draws
drifting
driven
drop
dropped
dropping
drops
dropzone
dropzone,
dropzone-gap
duplicate
duplicate.
duration-[var(--transition-wk-duration)]
each
edge
eight
either
either,
either:
element
element's
element.
element:
else
elsewhere)
emits.
emitted
empties
empty
empty,
enables
enlarged.
enough
ensures
error
error.
errors
event.
events
ever
every
everything
everywhere
exactly
exist
exist.
exists
expands
explicit
extend
factory
fail.
fails
failure
fallback
fallback.
falls
false
false"
false),
false);
false,
feature
feedback
field
field.blade.php
file
file's
file-upload
file-uploader
file.name)"
filename
filenames
files
files"
files.
fill
fill-rule="evenodd"
fill="currentColor"
filled
fills
filter
fine
fire
fires
first
first,
fit,
fixed
flags
flex
flex-1
flex-col
flips
floor
flow.
flow:
focus
focus-visible:outline-hidden
focus-visible:ring-[length:var(--ring-wk-width)]
focus-visible:ring-[var(--color-wk-ring)]
focusable
folded
follows
for
for.
for="{{
form
form.
formatting
four
fresh
from
full
full-width
gap
gap-[var(--padding-wk-x-sm)]
gap-[var(--padding-wk-y-sm)]',
gap-[var(--space-wk-sm)]',
gap-sm
gap.
gesture:
get
give
gives,
global
glyph
gone.
got
group
growing
grows
guarded
h-8').'
had
half
hand
handed
handler
handlers
handling
hard
has
have
heard
heavier
height
height,
here
here.
hidden
hint
hit),
horizontal
hover
hover,
hover-reveal
hover.
hover:bg-[var(--color-wk-bg-subtle)]
hover:border-[var(--color-wk-accent)]')
hover:text-[color:var(--color-wk-danger-text)]
hugs
icon
id
id="{{
idref
ids
if
iframe;
ignores,
implementation.
implode('
imports
in
in,
in.
including
indent;
index)
inline
inline-flex
input
input,
input.
inside
instead
into
invalid
invisible,
is
is_string($boundModel)
it
it,
it.
it:
it;
item
items
items-center
items-start
its
itself
itself,
itself:
joins
just
justify-center
keep
keeps
kept
key
keyboard
label
label:
landed
lands
language.
languages
large
last
later
lay
layer
least
left
legacy
legitimate
less
like
line
line.
list
list,
list-none
literal
literal,
live
lives
load
long
long.
looked
looks
lower
m-0
made
make
makes
margin
margin:
marker
markers
markup
match
matches
matches.
max-w-full
may
mean
message
metadata
middle
min-w-0
min-w-0"
min-w-0'
min-w-0',
mirrors
missing
model
model:
modifiers
moment
morph
mouse
move
moves
ms-0.5"
mt-[var(--padding-wk-y-sm)]
multiple
must
n/a
name
name="{{
named
names
naming
narration
native
need.
neighbor
neighbors
neither
neither.
never
new
next
no
nobody
node
node,
none;
nor
not
not,
nothing
nothing"
nothing,
nothing.
notices
now
null
null),
null);
null)?->has($name));
null,
null;
null])
numbers
object
of
of.
off
omitted
on
on.
once
once.
once:
one
one,
one.
ones:
only
only,
onto
opened
opposite
opt
optimistic
optimistic-ui:
or
or,
order
other
out
out.
over
overflowing
own
own.
owns
p-0
p-0.5
padding
padding:
page
page,
painted
painting.
paints
paragraph
paragraph,
parent
parity.
parse
parser
part
passing
passthrough
path
path,
pattern
payload
peeled
per
picker.
pixel-identical
pixels.
place
placed
placeholder
plain
plausible
plus
point
pointed
pointer
pointer,
pointer/touch
pointing
polite
position
posts
precedence:
prevents
preview.
prod).
progress
project
prop
prop's
prop,
property
props
protocol
public
push
pushed
put
px-[var(--padding-wk-x-lg)]
px-[var(--padding-wk-x-md)]
px-[var(--padding-wk-x-sm)]
py-[var(--padding-wk-y-xs)]',
qualified:
queries
query
quiet
radio
radius
rather
re-attached
reach
reaches
read
reader
reading
reads
reads:
real
reason
received
recognizes
rectangle
red
redundant
reference
region
region.
removal
removal's
remove
removeLabel:
removed
removedMessage:
render
render.
rendered
rendering
renders
replaced
replaces
report
reported
reporting.
reports
required
resolve,
resolves
resolves).
resolving
resort,
result
returns
reword.
right
ring
role="list"
round
rounded
rounded-[var(--radius-wk-md)]',
rounded-[var(--radius-wk-sm)]
rounded-[var(--radius-wk-sm)]',
row
row's
row,
row.
rows
rows,
runtime,
said
same
sandbox
sandbox-iframe
satisfied,
save
saw
say
saying
says,
scope
screen
see
seeds
segmented-control's
selected
sell
sentence
server
server-side
set,
sets
setting
shadow,
shape
shapes
share
shift
shipped
shorter
show.
shown
shows
shrink-0
shrink-0"
shrink-0'
shrink-0',
shrink-to-fit,
shrinks
side
silent
silently
silently.
site
site.
sits
size
size,
sizing
small
so
somewhere
somewhere.
space
spacing.
spec's
spelling
spellings
stack
stand
starting
state
statements
states
stay
stays
still
stops
stored
stored.
stray
stretching
strict
string,
strip
strongest
style
style="list-style:
styling.
submit
submits
submitted
subtle
such
surely
surface
survives
system
tabbing
table
tabular-nums
tag,
tags
tags-input
takes
tall
tap
taps
target
technology,
tell
template
text
text".
text-[color:var(--color-wk-danger-text)]">{{
text-[color:var(--color-wk-text-muted)]
text-[color:var(--color-wk-text-muted)]">{{
text-[color:var(--color-wk-text-muted)]'
text-[color:var(--color-wk-text-subtle)]';
text-[length:var(--text-wk-xs)]
than
that
the
their
them
themselves
then
there
they
this
this,
those
through
to
to,
together
together.
token
token,
token.
told
took
touch
tracks
transition,
transition-colors
translated
translators
tree,
triggers
trim($describedBy.'
trim(($hint
trip
true
true"
true);
truncate
truncates
truthy
two
type
type:
type="button"
type="file"
type="file">`
unbound
undefined,
under
unguarded
unknown
unlike
unrelated
up,
upload
use
used
user
uses
valid
validated.
validation
value
value.
values.
var(--padding-wk-y-sm)
variable
variant
variant's
variants
variants.
verb).
vertical
very
via
view's
viewBox
viewBox="0
visible
visible,
visual
visually
w-3.5"
w-4
w-full
was
watches
way
way.
well
were
what
whatsoever:
when
where
whether
which
while
who
whole
whose
why
wider
width
width;
wirekit.css
with
without
word
word:
wording,
worked
worked.
would
wouldn't
wrap
wrapper
written
wrong
x-bind:class.
x-cloak>
x-data="wirekitFileUpload({
x-for="(file,
x-ref="input"
x-show="files.length
x-text="file.name"></span>
x-text="fileAnnouncement"></div>
x-text="formatBytes(file.size)"></span>
xmlns="http://www.w3.org/2000/svg"
{
{{
{{--
||
})"
};
}}
}}"
}}">
}}">{{
}},
}}</p>
}}@if($required)<span
}}`
—
