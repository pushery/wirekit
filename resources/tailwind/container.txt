$as
$as);
$attributes->class([$classes])
$attributes->getAttributes());
$center
$classes
$max,
$maxClasses
$maxClasses,
$padding,
$paddingClasses
$paddingClasses,
$scope);
$slot
'',
',
'2xl'
'2xl',
'as'
'base',
'center'
'div',
'false'
'full'
'full']),
'lg'
'lg',
'max'
'max',
'max-w-[var(--size-wk-container-2xl,96rem)]',
'max-w-[var(--size-wk-container-lg,64rem)]',
'max-w-[var(--size-wk-container-md,48rem)]',
'max-w-[var(--size-wk-container-sm,40rem)]',
'max-w-[var(--size-wk-container-xl,80rem)]',
'max-w-full',
'md'
'md'),
'md',
'mx-auto'
'none'
'padding'
'padding',
'px-[var(--padding-wk-x-lg,1rem)]',
'px-[var(--padding-wk-x-md,0.75rem)]',
'px-[var(--padding-wk-x-sm,0.625rem)]',
'px-[var(--padding-wk-x-xl,1.5rem)]',
'scope'
'sm'
'sm',
'w-full',
'xl'
'xl'),
'xl',
'xl']),
($max)
($padding)
(developers
(silent
(string)
(which
--}}
//
:
</{{
<{{
=
=>
?
@endphp
@php
@props([
@props.
Blade
Blade's
BooleanProp::from($center,
Declared
Dev-only
Fully
Inline
Measured
Normalized
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
Renders
See
UNBOUND
Vertical
WireKit::resolveClasses('container',
WireKit::validateProp('container',
['none',
['sm',
\Pushery\WireKit\WireKit::tagName('container',
\Pushery\WireKit\WireKit::warnUnknownProps('container',
])
])),
`--padding-wk-x-*`
`--padding-wk-x-lg`
`--space-wk-*`
`<x-wirekit::container>`
`<x-wirekit::main
`=`
`as`
`prop="false"`
`py-*`
a
action
against
an
and
any
apply
array_filter([
as
as,
asserted:
attribute
attribute.
auto-derived
be
block,
call
cast
compiles
component
component's
config('wirekit.components.container.max',
config('wirekit.components.container.padding',
container
content
content's
content-edge
could
data-wk-prose-skip
debug
default
docs/extending/spine-contract.md
does
each
early.
edge
element,
escaping
exactly
feature
file
flags
flips
for
from
guard
implode('
imports
in
inherits
inner
inside
interactive
interpolated
into
is
joins
later
left
list
live
main's
match
may
mean
n/a
nested
never
no
not
null,
of
on
on.
one.
opening
opposite
optimistic-ui:
or
own
padding
padding="lg">`
page-edge
presentational
prod).
prop's
props
qualified:
rather
reach
reads
reason
refutes
renders
result
rhythm).
same
section
shown
silently.
single
site
sits
so
space
spine
spine.
still
stop
straight
string,
tag,
than
that
the
themselves
there
they
this
to
token)
true);
true,
truthy
unknown
unvalidated
use
used
uses
value
view's
visible-text
want
was
what
when
where
which
whose
wirekit:spine-participant
would.
{
{{
{{--
};
}}
}}>
—
