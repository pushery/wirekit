$actor
$attributes->class([$classes])
$attributes->getAttributes());
$badge
$classes
$detail
$dotColor
$kind,
$kindLabel
$kindText
$kinds
$kinds[$kind]
$scope);
$slot
$timestamp
',
'actor'
'base',
'comment'
'commit'
'deploy'
'flex
'font-[family-name:var(--font-wk-sans)]',
'kind'
'kindLabel'
'merge'
'py-[var(--padding-wk-y-sm)]',
'scope'
'system'
'system',
'text-[length:var(--text-wk-sm)]',
'timestamp'
'user'
'var(--color-wk-accent)',
'var(--color-wk-success)',
'var(--color-wk-text-muted)',
'var(--color-wk-text-muted)';
("2
($kind)
(WCAG
(aria-hidden)
(array)
(no
(silent
+
--}}
/
//
1.4.1).
</div>
</span>
<div
<div>
<span
<span>
=
=>
></span>
??
@endif
@endif{{
@endisset
@endphp
@if($actor)<span
@if($timestamp)
@isset($badge)
@isset($detail)
@php
@props([
@props.
A
Accessible
Activity
Any
Block
Declared
Decorative
Dev-only
Developer
Fully
Leading
Measured
Optional
Pushery\WireKit\WireKit;
Renders
The
Unknown
What
WireKit::resolveClasses('activity-row',
[
[]));
\Pushery\WireKit\WireKit::warnUnknownProps('activity-row',
])
]),
],
__('wirekit::Comment'),
__('wirekit::Commit'),
__('wirekit::Deployment'),
__('wirekit::Merge'),
__('wirekit::System'),
__('wirekit::User'),
`config('wirekit.components.activity-row.kinds')`.
`kindLabel`
`release
a
above
action
activity
actor
add
ago"),
an
and
announced
announces
another
any
app
are
aria-hidden="true"
array_merge([
asserted:
at
auto-derived
back
be
below,
beside
block,
bold
built-in
by
caller's
can
cannot
catalog
catalog,
catalog;
changed
class="flex
class="font-[number:var(--font-wk-heading-weight)]">{{
class="min-w-0
class="mt-1
class="mt-1.5
class="shrink-0
class="sr-only">{{
color
color.
column,
come
comment
comment.
commit
component
component's
config
config('wirekit.components.activity-row.kinds',
content
conveyed
could
debug
default
defaults,
deploy
developer
distinguishable
does
dot
dot's
dot-color
dot.
drives
early.
element,
entry
equivalent
error
etc.
extend
fall
falls
fields,
file
finds
flags
flex
flex-1">
flex-wrap
flush
for
from
gap-1">
gap-2">
gap-3',
gave
guard
h-2
hears
hold
hours
implode('
imports
in
inline
inline-block
instead
intentionally
interactive
is
it
it.
items-baseline
items-center
items-start
its
justify-between
key
key,
key:
keys
kind
kind's
kind,
kinds
label
language
later
leading
line.
line:
list
literally,
live
map
match
may
merge
merged
min-w-0">
muted
muted.
n/a
name
name,
names
no
not
null,
of
one
one.
open).
optimistic-ui:
or
other
out
over
override.
own
page
perception
place
presentational
prod).
props
qualified:
quoted
rather
reach
reader
reason
recolor
refutes
relative
rendered
renders
result
right-aligned
rounded-full"
row.
screen
search
set
shown
shrink-0
sits
six
slot
so
span
spoken
sr-only
stands
start
style="background-color:
system
tabular-nums">
text
text-[color:var(--color-wk-text)]">
text-[color:var(--color-wk-text-muted)]
text-[length:var(--text-wk-xs)]
textual
textually
than
that
the
there
this
timestamp
to
token.
translation
under
unknown
unless
untranslated
use
used
used.
user;
var(--color-wk-warning)`
via
view's
w-2
where
which
whose
wins.
with
without
word
written
{
{{
{{--
};
}}
}};"
}}</span>
}}>
—
→
