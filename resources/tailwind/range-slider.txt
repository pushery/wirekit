!
!==
"0
"<label>
"Could
"Phantom
"maximum
"min
"shrink
$announceError
$announceErrors
$attributes
$attributes->class([$classes])
$attributes->class([$wrapperClasses,
$attributes->except($wireModel)
$attributes->except(['announceErrors',
$attributes->except(['aria-describedby',
$attributes->get('aria-describedby');
$attributes->get('aria-label');
$attributes->get('name'),
$attributes->get('name',
$attributes->getAttributes());
$attributes;
$bindRangeValueText
$callerAriaLabel
$callerAriaLabel;
$callerDescribedBy
$classes
$disabled
$disabled)
$error
$errors->first($name)`,
$event)"
$groupLabel
$groupLabel)])
$hint
$i
$i++)
$id
$id);
$id.'-hint'
$initialMax
$initialMin
$key
$key;
$label
$mLabel)
$mLabel;
$mValue
$mValue]
$max
$max)
$max;
$maxThumbLabel
$maxValue
$max]
$min
$min)
$min;
$minThumbLabel
$minValue
$min]
$name
$optimistic,
$optimisticArgs),
$optimisticConfig
$priceRange
$rangeValueTextMap
$rangeValueTextMap)
$rangeValueTextMap[(string)
$required
$resolvedShowValues
$scope);
$segments
$segments)
$segments;
$showValues
$slot
$step
$step)
$step;
$thumbClasses
$thumbDescribedBy
$tickPercent
$tickPercent)
$tickPercents
$tickPercents[]
$val)
$val;
$wireModel
$wireModelKey
$wrapperClasses
${minPercent}%`
&&
'#'.$id.'-error',
''
'')));
'').'
'']"
''])
',
'-1'
'.((string)
'0'
'Enterprise'].
'Free',
'absolute
'action'
'announce-errors']);
'announceError'
'args'
'base',
'bg-[var(--color-wk-accent)]',
'bind'
'block',
'border-2
'cursor-not-allowed'
'cursor-pointer',
'debug'
'disabled'
'error'
'errorRegion'
'false'
'focus-visible:outline-hidden
'font-[family-name:var(--font-wk-sans)]',
'font-[number:var(--font-wk-body-weight)]',
'font-medium',
'hint'
'label'
'max'
'maxVal'],
'maxVal']`.
'maxValue'
'messages'
'min'
'min-w-[min(16rem,100%)]',
'minValue'
'mode'
'name']);
'opacity-0'
'opacity-0',
'opacity-100'
'opacity-100',
'opacity-[var(--opacity-wk-disabled)]
'optimistic'
'optimisticArgs'
'pending'
'priceRange'
'range-slider'
'range-slider-');
'reject',
'required'
'reverted'
'rounded-full',
'scope'
'shadow-[var(--shadow-wk-sm)]',
'showValues'
'space-y-2',
'step'
'text-[color:var(--color-wk-text)]',
'text-[length:var(--text-wk-md)]',
'tracking-[var(--font-wk-letter-spacing)]',
'transition-opacity
'transition-shadow
'valueTextMap'
'wire:model'
'wire:model'))
'wire:model.live'
'wk-range-thumb',
(!
("<label>
("bubble")
($attributes->getAttributes()
($callerDescribedBy
($errors
($i
($max
($min
($optimistic
($segments
($step
($valueTextMap
(-translate-x-1/2
(.live,
(20px
(<
(@aware
(Alpine
(Strategy
(The
(WAI-ARIA
(Written
(_measureBubbles),
(a
(and
(announced
(auto
(bool)
(centering
(decorative)
(exactly
(is_array($valueTextMap))
(no
(not
(now
(only
(see
(silent
(so
(step
(string)
(the
(they
(top-1/2
(track
(transparent
(vertically
(via
(when
(which
(wired
*
*/
+
-
--opacity-wk-disabled.
--}}
->
-top-8
-translate-x-1/2
-translate-x-1/2',
-translate-x-1/2`
-translate-y-1/2
-translate-y-1/2`
.blur)
.debounce.500ms,
.lazy,
/
/*
//
/>
0%
0%).
0)
0),
0;
1
1)
1),
10"),
100
100",
100%
100%)`,
100),
100,
16rem
16rem.
20
20)
20,
20rem
256px
280px
288px:
28px
2px
2rem;"
32px
4);
50%
80];
8px
8px-tall
90,
:
:aria-valuemax="minThumbCeiling"
:aria-valuemin="maxThumbFloor"
:aria-valuenow="maxVal"
:aria-valuenow="minVal"
:aria-valuetext="valueTextFor(maxVal)"
:aria-valuetext="valueTextFor(minVal)"
:class="[_merged
:from
:required="$required">{{
:style="maxBadgeStyle()"
:style="maxThumbStyle()"
:style="mergedBadgeStyle()"
:style="minBadgeStyle()"
:style="minThumbStyle()"
:style="rangeFillStyle()"
:to'))
:value="maxVal"
:value="minVal"
<
</div>
</label>
<=
<div
<div>
<div>.
<input
<input>
<label
<p
<select>
<span
<span>{{
<textarea>
<x-wirekit::label
=
===
=>
>
></div>
></span>
>=
>{{
?
??
??=
@aware(['announceErrors'
@disabled($disabled)
@elseif($hint)
@endforeach
@endif
@endphp
@endunless
@foreach($tickPercents
@if($announceError)
@if($bindRangeValueText)
@if($disabled)
@if($error)
@if($label)
@if($optimisticConfig)
@if($required)
@if($resolvedShowValues)
@if($thumbDescribedBy)
@if($wireModel)
@keydown.arrow-down.prevent="stepMax(-1)"
@keydown.arrow-down.prevent="stepMin(-1)"
@keydown.arrow-left.prevent="stepMax(-1)"
@keydown.arrow-left.prevent="stepMin(-1)"
@keydown.arrow-right.prevent="stepMax(1)"
@keydown.arrow-right.prevent="stepMin(1)"
@keydown.arrow-up.prevent="stepMax(1)"
@keydown.arrow-up.prevent="stepMin(1)"
@keydown.end.prevent="jumpMax(true)"
@keydown.end.prevent="jumpMin(true)"
@keydown.home.prevent="jumpMax(false)"
@keydown.home.prevent="jumpMin(false)"
@keydown.page-down.prevent="stepMax(-10)"
@keydown.page-down.prevent="stepMin(-10)"
@keydown.page-up.prevent="stepMax(10)"
@keydown.page-up.prevent="stepMin(10)"
@php
@pointerdown="startDrag('max',
@pointerdown="startDrag('min',
@props([
@props.
@unless($disabled)
A
A11y:
ABOVE
AND
APG
ARRIVED
Absent
Accessibility
Active
Alpine
An
Anchoring
And
ArrowUp
B
BEATS
BELOW
BOTH
BOUNDS
Blade
Blink,
BooleanProp::from($disabled,
BooleanProp::from($required,
BooleanProp::stripFalseHtmlFlags($attributes);
Both
Change
Compute
Concatenating
DECLARED
DENSE,
DO
DOCUMENTED
DOM
DOM.
Declared
Decorative;
Description
Dev-only
Disable
ENDS
EXACT
EXACTLY
Each
Edge
Edge-flush
End
English
Every
Extra
Fully
GROUP
HARD
HTML
Handles
Hidden
Home
Home/End
IDREF
INSIDE
IS
In
It
Its
JS
Keeping
Label
Laravel
Livewire
MIRRORS
MUST
Max
Measured
Merged
Min
Mirrored
Mirrors
Modifiers
NESTS
NEVER
NO
NOT
Native
Net:
No
Normalized
ON
ONE
OPACITY
ORDER
OUTSIDE
OWN
On
One
PHP
PRESENCE,
Page
Pass
Per-thumb
Pre-fix
Prop
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
Putting
REPLACES
REQUIRED
Rendered
Renders
Reported
Required
Routing
SAME
SIZE
Same
Set
Sharing
Sizing
Snap-to-step
So
Spoken
Spoken-value
Static
StrictnessGate
Strip
Structure
Support\DomId
THIS
THUMB's
TOP
TRACK
TUPLE:
Tailwind
The
This
Thumb
Track
UNBOUND
Undeclared,
Up/Down,
Usability
Visibility
WHICH
We
When
Whichever
WireKit::resolveClasses('label',
WireKit::resolveClasses('range-slider',
WireKit::warnUnknownProps('range-slider',
Without
Y
[
['label'
['min'
['minVal',
['show_values'
[0
[],
[];
[min,
\Pushery\WireKit\Support\AlpinePayload::from((object)
\Pushery\WireKit\Support\AlpinePayload::from([
\Pushery\WireKit\Support\AlpinePayload::from(__('wirekit::Range:
\Pushery\WireKit\Support\DomId::unique($attributes->get('id')
\Pushery\WireKit\WireKit::warnUnknownProps('label',
])
]),
]);
],
__('wirekit:::label
__('wirekit::Could
__('wirekit::Maximum');
__('wirekit::Minimum');
__('wirekit::Saving'),
_ready
`!_merged`)
`$attributes->has('disabled')`:
`$disabled`,
`$error
`$this->validate()`
`-top-8
`-top-8`
`16rem`.
`:class`)
`:error`
`:max-value`
`:style`
`<div>`,
`@aware`
`@props`
`Math.min(value,
`['minVal',
`[min,
`_merged`
`_setMax`
`_setMin`,
`args`
`aria-disabled`
`border-2`
`class`
`cursor-pointer`
`disabled:`
`disabled="false"`
`disabled`
`disabled`,
`display:
`display:none`
`div[role="slider"]`,
`error`
`id="…-hint"`
`input`:
`label`
`left:
`left`,
`max()`
`maxVal
`maxVal`
`min()`
`min(16rem,
`min-width`
`minVal
`minVal`,
`minVal`/`maxVal`,
`ms-0.5`
`name`
`null`
`optimistic="method"`
`prop="false"`
`radiogroup`
`relative`
`required`
`role="group"`
`role="slider"`
`role="slider"`:
`run()`.
`stepMin`
`style="overflow:
`style`
`this`,
`top-1/2
`top-1/2`
`translateX(-${minPercent}%)`
`width:
`wire:model{modifiers}="prop.max"`.
`wire:model{modifiers}="prop.min"`
`x-show`
`x-show`.
`{{
a
able
about
above
above)
above.
above:
accepts
accessibility
accessible
across
action
actually
adopting
after
against
against,
aligns
all
all.
along
already
also
always
ambiguous
amount
an
anchor
and
announce-error
announceErrors)
announced
announced.
announcement
announces
announcing
answer
any
anyway,
app
appears
appended
application.
apps
arbitration
are
argued
arguments
aria-*
aria-atomic="true"
aria-describedby
aria-describedby.
aria-describedby="{{
aria-disabled="true"
aria-hidden="true"
aria-hidden="true">*</span>@endif
aria-invalid="true"
aria-label.
aria-label="{{
aria-labelledby)
aria-labelledby="{{
aria-live
aria-live="assertive"
aria-live="polite"
aria-live="polite">
aria-required,
aria-required="true"
aria-valuemax
aria-valuemax="{{
aria-valuemin="{{
aria-valuenow
aria-valuenow="{{
arming
around.
array
array_values((array)
arrive
arrives
arrows
arrows,
as
as,
as.
asserted:
assistive
asterisk.
at
at.
attribute
attribute,
attributes
audible
auto
auto-
auto-derived
axe-core's
badge
badge's
badge:
badges
badges).
badges.
bag
bag,
bag.
bare
be
because
become
been
before
before,
behaves
behavior
belongs
below
between
beyond
bg-[var(--color-wk-accent)]"
bg-[var(--color-wk-bg-elevated)]
bg-[var(--color-wk-bg-muted)]"
bg-[var(--color-wk-border)]"
bind
binding
binding,
bindings
binds
block
block,
block.
boolean
boots,
border
border-2
border-[var(--color-wk-bg-elevated)]',
border-[var(--color-wk-border)]
border-box
border-transparent
both
both,
bound
bound,
boundaries
boundary
bounds-labels
box
bracketed
break;
bubble
bubble's
bubble,
bubbles
build
builds
bullet
but
by
byte.
calculation.
call
call,
call.
caller
caller's
caller-supplied
can
can't
cannot
capability
card
carries
carry
cast
catalog,
ceiling
cell,
center
centered
centers
change
check.
child
children,
circle's
claim.
clamp
clamp.
clamps
class
class)
class="absolute
class="mt-3
class="relative
class="relative"
class="sr-only"
class="text-[color:var(--color-wk-danger-text)]
class="text-[length:var(--text-wk-sm)]
class="{{
classes
classes:
clipping
clobbering
close
closes
clutters
coarse
coarse-pointer
collapses
collapsing
colliding
colors
column,
come
comes
comment
comments.)
commit
commonest
compares
compiled
compiles
complain
complete
component
component's
component,
component.
components
composed
config
config('app.debug'),
config('wirekit.a11y.announce_error',
config('wirekit.components.range-slider.max',
config('wirekit.components.range-slider.min',
config('wirekit.components.range-slider.show_values',
config('wirekit.components.range-slider.step',
config.
config/wirekit.php:
constant).
constraint,
consulted.
contact,
container
container.
containing
contents">
contents`
context
continue;
control
control.
control:
controls
correctly.
costs
could
count
current
cursor,
cursor-not-allowed'
danger-text
dark
dark:
data-wk-optimistic-announcer
data-wk-prose-skip
debug
declared
declares:
default
default,
defaults
defaults:
defers
dense.
depend
dependency
described
describedby
describes
description
design
deterministic
developer
did
different
dim
dims
direction
directive
disabled
disables
disagree.
discrete
dist/wirekit.css.
div
div)
div,
div.
document
documentation
does
does),
doesn't
doing.
double-announce
double-render
down
down.
drag
drag,
drag.
dragged.
drags.
drawn
drift
drift.
drop
dropped
drops
dual-handle
dual-thumb
duplicate
duplicating
duration-[var(--transition-wk-duration)]'
duration-[var(--transition-wk-duration)]',
e.g.
each
early.
edge
edge,
editable.
either
either,
element
element's
element,
element.
elsewhere)
embeds
emitter,
empty
empty,
end
end,
end.
ends
ends:
enough
equivalent
error
estimate
even
event,
every
everywhere
exactly
exclusive.
existed
expanding
explicit
explicitly,
exposed
expression
extend
extract
extras:
fact
factory
factory,
failed
failing
fallback
fallback,
false
false);
false,
false],
far
far-left
far-right
feature
feeds
few
field
field's
fight
file
fill
fit-content
fit-content`
fixed-size
fixes
flag
flags
flex
flex/grid
flight
flight.
flips
floor
floor,
floor.
flow
flow.
flush
focus
focus):
focus-visible:ring-[length:var(--ring-wk-width)]
focus-visible:ring-[var(--color-wk-ring)]',
focusable
focuses.
folded
follow
font-[number:var(--font-wk-body-weight)]
for
for.
foreach
form
form,
formatting.
forms.
free
from
full
full-width
full.
fully
further
gap
generic
gesture
get
gets
getter
give
gives
global
globally
gluing
goes
grabbed.
grammar
grid
grid.
group
group)
group,
group.
grouping
guarantees
guard
h-2
h-full
habit
half
half-thumb
half-translated
hand-mount
handle
handle's
handle),
handle,
handle.
handlers,
handles
hang
happened
hardcoded
has
have
hear
hears
hears,
height
here
here)
here,
here.
hidden
hidden.
hide
hint
hint,
hint-only
hold,
horizontal
horizontally
how
hung
hydrated
i.e.
id
id;
id="{{
identical
idref
if
ignore
implode('
imports
in
in),
indicator
individual
initial
inline
inlined
inner
input
input.blade.php.
inputs
inside
instead
instead.
integration
intended
interactive
interchangeable:
interrupt
into
intrinsic
invalid
invisible
invite
is
is_string($key)
it
it,
it.
item,
its
itself
itself,
justify-between
keeps
kept
key
key,
keyboard
keyboard.
keyed
keypress
keys
keys,
label
label.
labels
labels,
land
landed
landing
lands
larger
last
later
layer
layer's
leaves
leaving
left
left,
left-1/2
legitimate
let
lets
like
line
line).
list
listener
live
load-bearing:
locale
logic
long
longer
looked
lookup
low
lower
lower,
make
making
map,
marked,
marks
marks:
marksMap:
matches
matching
max
max"
max($min,
max:
maxPercent
maxVal
maxValue
maxValue:
max].
max]`,
maximum").
maximum',
maximum.
may
mean
means
meant,
measured
merely
merged
merged.
message
method
middle
middle.
min
min($max,
min,
min-width
min:
minValue
minValue:
minimum"
minimum',
minted
mirror
mis-trace
mode,
model
model,
moments.
mount:
move
moved,
moved.
moves
ms-0.5"
multiplier
must
mutually
n/a
name
name,
name="{{
named
names
naming
narrow
narrower
native
nearer
needed).
needs
nested
nested-key
nests
network
never
new
newline
no
no-op.
nobody
node,
noise.
non-interactive.
none
normal
not
not,
not.
note
nothing
nothing:
now,
nudged
null
null)?->first($name);
null,
null;
null])
number
number,
numeric
obeying
object
obstacle:
of
off
off.
older
omitted
on
on,
on.
once
one
one.
one:
only
onto
opacity
operable
operated.
opposite
opt
optimistic
optimistic-ui:
optimistically.
or
or,
order
order,
origin
other
otherwise
out
out.
outer
outer-left
outer-right
over
overlap
override
overrides
own
own,
owns
padding
page
page-unique
page.
pair
pair,
paragraph
parent
parent",
parent's
parses
part
partial
passed
passthrough
past
path
pattern
pattern):
pattern,
pending
per
per-thumb
percent
percentages
perf
phone
picks
pixel-identical:
pixels
place
placed
placeholder
plain
platform
plus
point
pointed
pointer
pointer-events-none"
pointers)
pointerup
pointing
polite
politely
positioning
positions
pre-Alpine-init
precedence:
presentational
presses
preview
price
printed
prod).
produces
promise
prop
prop's
properties
property
props
props-table
public
purpose:
pushed
put
put:
puts
px-[var(--padding-wk-x-sm)]
py-0.5
qualified:
range
range),
range,
rather
re-emit
re-renders
reach
reachable
reaches
reaching
reactively.
read
read.
readable
reader
reading
reads
reads:
real
reappear,
reason
reason:
receive.
receives
recipe
reference
refuses.
refutes
regardless
region
region)
region,
region.
relative
remain
remove
removing
render
rendered
rendering
renders
repeating
replace
report
required
resolve
resolved
resolves
restore
restores
result
reverse-diff
right
right,
role
role-less
role="group"
role="slider"
rollback
room
room,
roughly
round(($i
rounded-[var(--radius-wk-sm)]
rounded-full
routed
row
row,
row.
rule.
run
runs
same
sat
save"
save.
says
says,
scales
scan
scanner
scope
scope,
scope;
scopes
screen
screen-readers
second
see
sees
segmented
selection
sends
sent
server
server-side
set
sets
seven
shadow-[var(--shadow-wk-sm)]
shape
shape.
share
shared
shifts
shipped
shipping
should
show
showed
shown
shown,
shows
shrink-to-fit
siblings
side
silence,
silently
silently.
single-value
sit
site
sits
sitting
size
size.
sized
sizes
slider
slider's
slider,
slider;
sliders
slides
slot
slot,
small
snaps.
snapshot
so
so.
something
source
space
span
specific
spellings
split-min/split-max):
spreads
sr-only
standalone
standing,
start.
starting
state
state,
static
static-fallback
stay
stayed
stays
stays.)
step
step)`,
step,
step:
step`
step`,
stepping,
steps.
still
stop,
stops
str_starts_with($key,
stray
strictly
string,
string-keyed
strings).
style
style="display:
style="left:
style="overflow:
style="padding-top:
stylesheet
submission.
submit
submit.
submits
submitted
such
suite's
summary
supported
surface
survives
sync
syntax
tab
tabindex="{{
table
tabular-nums
tabular-nums">
tag
tag,
tag.
tags
takes
targets
teaches,
technology
tell
ten
test
text
text-[color:var(--color-wk-danger-text)]">{{
text-[color:var(--color-wk-text)]
text-[color:var(--color-wk-text-muted)]
text-[color:var(--color-wk-text-muted)]">{{
text-[length:var(--text-wk-xs)]
than
that
that,
that:
the
their
them
them.
themselves
then
there
there.
they
thing
this
through
thumb
thumb":
thumb's
thumb).
thumb,
thumb-centered
thumb-level
thumb-sized
thumb.
thumb:
thumbs
thumbs,
thumbs.
thumbs:
tick
ticks
ticks)
tier.
time
timer).
timing,
to
together
toggle
tokens
too
tooltip
tooltip-style
top
top-1/2
touch-action:none,
track
track's
track,
track.
tracks
transform
transition
translated
travel,
tree
trim(($hint
trim((string)
true);
truth;
truthy
tuple
turns
twice
two
two-way
type="hidden"
unchanged
unconditionally
uncorrected
undeclared
under
undone.'),
unknown
unlike
unreachable,
untestable.
until
untouched
untrue
unusable
up
updates
upper
use
use.
used
user
uses
utilities)
utility
utility.
valid
validation
value
value,
value-badge
value.
value:
value="{{
valueTextFor(maxVal))">Range:
valueTextFor(minVal)).replace(':to',
valuemax
valuemin.
values
values.
variable
variant
variant,
verbatim
vertical
vertically
very
via
view's
visibility.
visible
visible;"
visible;"`
visibly
visual
visually
vs
w-px
wants.
was
watches
way
way.
well
were
what
when
whenever
where
wherever
whether
which
whichever
while
whitespace,
whitespace-nowrap
who
whole
whose
why
wide
wider
widest
width
width,
width/height
win
window
winning
wins,
wins;
wire:model
wire:model*
wirekit.js
wiring.
with
withdrawal.
without
wk-range-thumb
wk-range-thumb)
word
works
would
wrapper
wrapper)
wrapper,
wrapper.
write
writes
written
wrong
wrote,
x-bind:aria-busy="isPending"
x-data="wirekitOptimistic({{
x-data="wirekitRangeSlider({
x-effect="remeasureOnValueChange()"
x-ref="maxBubble"
x-ref="maxInput"
x-ref="minBubble"
x-ref="minInput"
x-ref="track">
x-show's
x-text="announcement"></div>
x-text="mergedBadgeText()"
x-text="valueTextFor(maxVal)"
x-text="valueTextFor(minVal)"
x-text="{{
you
z-10"
z-20"
z-30"
{
{{
{{--
||
}
})"
}}
}}"
}}%;"></span>
}})"
}},
}}-error"
}}-hint"
}}-label"
}}.max"
}}.min"
}}.replace(':from',
}}</p>
}}</span>
}}</x-wirekit::label>
}}="{{
}}>
}}@if($required)<span
}}[max]"
}}[min]"
}}`
~2px
~4px
~60px:
–
—
…
→
⚠️
