!==
$attributes->class([$classes])
$attributes->getAttributes());
$chipClasses
$classes
$entry
$groupLabel
$index
$item)
$itemLabel
$itemLabel),
$itemLabel,
$item['label']
$item['value']
$items
$items[]
$label
$reply
$reply)
$reply];
$scope);
'')
'');
',
'base',
'bg-[var(--color-wk-bg)]
'border-[length:var(--border-wk-width)]
'chip',
'cursor-pointer',
'disabled'
'disabled:opacity-[var(--opacity-wk-disabled)]
'flex
'focus-visible:outline-hidden
'font-[family-name:var(--font-wk-sans)]',
'gap-[var(--gap-wk-xs)]',
'hover:bg-[var(--color-wk-bg-muted)]',
'inline-flex
'label'
'min-h-[var(--size-wk-target-min)]',
'px-[var(--padding-wk-x-sm)]
'replies'
'rounded-[var(--radius-wk-full)]',
'scope'
'text-[length:var(--text-wk-sm)]',
'transition-colors
'value'
($entry['label']
($entry['value']
($itemLabel
($items
($replies
(defaults
(silent
(string)
--}}
//
2.5.8
:
</div>
<button
<div
=
===
=>
>
>{{
?
??
@disabled($item['disabled'])
@endforeach
@endif
@endphp
@endunless
@foreach
@if
@php
@props([
@unless($attributes->has('x-data'))
A
Alpine
AlpinePayload::string($item['label'])
AlpinePayload::string($item['value'])
An
Blade
BooleanProp::from($entry['disabled']
DOES
Dev-only
Emitted
FIRST
Fully
HTML
Measured
NOT
Names
Normalized
Nothing
On
Pushery\WireKit\Support\AlpinePayload;
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
The
WCAG
WireKit::resolveClasses('quick-replies',
Wrapping
[
['label'
[])
[],
[];
\Pushery\WireKit\WireKit::warnUnknownProps('quick-replies',
])
]),
];
__('wirekit::Suggested
`disabled`.
`label`,
`toolbar`:
`value`
`x-data`
`x-on:click`
a
all.
also
an
and
announce
announced
announces
answer
answer,
answer:
answered
any
application
application's
are
aria-label="{{
array
arrow-key
arrows
as
asserted:
at
attributes,
back
back.
be
behind
below
block,
border-[var(--color-wk-border)]',
broken
brought
button
buttons
buttons,
call
can
caption.
case:
chip
chosen
class="{{
client-only
common
component
continue;
control
could
data-value="{{
data-wk-quick-replies
data-wk-quick-reply
debug
decision,
default
directive
disabled:cursor-not-allowed',
discard
dispatches
does
duration-[var(--transition-wk-duration)]
early.
ease-[var(--transition-wk-easing)]',
edge
either
element
empty
entry
every
falls
false),
false,
file
filled($label)
find
flags
flex-wrap
floor,
focus-visible:ring-[length:var(--ring-wk-width)]
focus-visible:ring-[var(--color-wk-ring)]',
for
foreach
four
from
gets
group
group.
guard
hands
hardcoding
has
have
here
hide
hit.
horizontal
how.
identical
if
implode('
imports
in
index:
initializes:
inside
is
is_array($reply)
it
items-center
items-center',
its
itself:
justify-center',
keeps
know
label
label)
label,
label:
last
later
live
loop,
makes
malformed
markup
may
mobile
model,
mutates
mutation
n/a
name
named
named.
never
no
none
none.
not
nothing
nothing,
nothing.
null,
of
offer
offered.
once.
one
one.
only
optimistic-ui:
optional
or
order
own
own.
page
performs
phone
phone,
pill
plain
pointer
press
primary
prod).
promise
promises
props
py-[var(--padding-wk-y-xs)]',
qualified:
rather
reach
reader
reader,
reason
refutes
rendered
replies');
replies,
replies.
reply
result
rich.
role
role="group"
root,
runs
same
says
screen
scroller
sees
segmented
sent
set
shown
silence.
silently
site
skipped
so
stays
still
string
suggested
suggestion
suggestions
takes.
target
text-[color:var(--color-wk-text)]',
than
that
the
theirs.
then
there
they
this
three
to
token
translated
tree
tries
two
type="button"
unconditional,
unknown
unset
use
value
value:
view's
walks,
way
what
when
which
who
wider
widget
with
without
words
would
x-data
x-on:click="$dispatch('wirekit:quick-replies-select',
{
{{
{{--
}
})"
}}
}}"
}},
}}</button>
—
