!==
"bar
"tried
$attributes->getAttributes());
$attributes->merge(['style'
$boundary
$boundary,
$boundary;
$boundarySelector
$boundarySelector)
$dispatch
$edgeClass
$edgeStyle
$effectiveIntent
$effectiveIntent,
$heightToken
$indicator
$intent
$intentPropName
$intentPropName,
$milestones
$milestonesEnabled
$pct
$pos
$pos)
$position
$positionMode
$resolvedBoundary
$rootClass
$scope);
$segmentColor
$segmentColor.'
$segmentsArray
$segmentsStyle
$showAfter
$stops
$stops).');';
$stops[]
$target)
$useSticky
$v
$variant;
$variantColor
$variantValue
&&
'
'')
'')])->class([$rootClass])->merge(['aria-label'
'';
',
'.$edgeClass
'.$edgeClass),
'.$edgeStyle.';
'.$pct.'%';
'.$segmentsStyle
'.($heightToken).';'.($segmentsStyle
'.($positionMode).';
'.implode(',
'<css-selector-string>']
'<selector>'
'accent'
'auto'
'auto']
'background-image:
'bar',
'base',
'bottom'
'bottom-0'
'bottom:
'boundary'
'boundary',
'container'
'container')
'container').
'container';
'danger'
'danger',
'dot'
'dot')
'false'
'fixed
'fixed';
'height'
'indicator'
'info'
'info',
'intent'
'inverse'
'lg'
'md',
'milestones'
'neutral'
'neutral',
'position'
'position:
'primary'
'primary',
'reading-progress',
'scope'
'segments'
'selector';
'showAfter'
'sm'
'sticky
'sticky'
'success'
'success',
'target'
'top',
'top-0'
'top-[var(--wk-strip-inset,0px)]');
'top:
'transparent
'true'
'var(--reading-progress-fill,
'var(--reading-progress-height-lg)',
'var(--reading-progress-height-md)',
'var(--reading-progress-height-sm)',
'var(--reading-progress-segment,
'variant'
'variant';
'warning'
'warning',
'wk-reading-progress',
'wk-reading-progress--dot'])->merge(['aria-label'
($boundary
($effectiveIntent)
($height)
($indicator
($segmentsArray
($useSticky
($v)
($variantValue)
(0..1)
(`.wk-reading-progress.wk-reading-progress`)
(`announcement-banner
(`milestones=false`);
(a
(action-bar,
(background-
(bar)
(consistent
(default
(default)
(default),
(default):
(default,
(developer-set
(docs-sandbox
(dot)
(iframes,
(int)
(is_string($boundary)
(legacy)
(or
(progress
(resources/js/components/reading-progress.js).
(same
(see
(silent
(string)
(the
(typically
(v2.4.0
(which
),
);
*
+
-
--reading-progress-fill
--}}
-rotate-90"
/
//
/>
0
0'
0)
0,
0;
0px)');
0px)`
1))
1):
10%
100%
100%;
100)
100.53"
100;
150ms
1px
1px)';
1px-tall
2)
2)`
2.5rem
25/50/75/100%
2π
36
36"
3px
40%-opacity
4×
5000-line
6-set
65ch
:
:root
</div>
</svg>
<=
<circle
<div
<length-percentage>`
<svg
=
===
=>
>
></div>
>=
?
??
@else
@endif
@endphp
@if
@php
@props([
@props.
A
ARE
Alpine
Alpine's
BOTH
BYTE
Bar
Blade
BooleanProp::from($milestones,
Both
Brutalist
Build
CALLER
CPU
CSP
CSS
CSS-only
CSS.
Class
DARK
DERIVED
DOM
Declared
Dev-only
Developer
Developers
Disabled
Dot
Doubled-class
END
EVERY
Each
Every
Ext
FILTER_VALIDATE_BOOL);
FIXED
FOR
Fully
GPU-accelerated,
HERE
HTML,
Half
IN
INVISIBLE
IS
Inline
Inline-style
It
Its
JS
Marker
Milestones
NAMED
NN%`
NO
NOT
Normalized
Not
OBJECT
ON
ONCE
ONCE,
ORIGIN
One
PINNED
Position:
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
REPLACES
Reading-progress
Renders
Resolve
STATIC
SVG
Same
Segments
Set
Sticky
TEXT,
THE
THEME.
THIS
Tailwind
Tested
That
That's
The
This
Three
Tokens
Two
UNBOUND
Under
Uses
Validation
Variant
WAS
WHICH
What
When
WireKit::resolveClasses('reading-progress',
WireKit::validateProp(
Without
[
['container',
['primary',
[];
\Pushery\WireKit\Support\AlpinePayload::from((string)
\Pushery\WireKit\WireKit::warnUnknownProps('reading-progress',
])
]),
__('wirekit::Reading
`!important`
`!important`.
`"container"`
`--color-wk-border-strong`
`--reading-progress-fill:
`--reading-progress-fill`
`--reading-progress-segment`
`--wk-strip-inset`
`.prose
`.wk-scroll-fade`
`0ms`
`75ms`.
`@tailwindcss/typography`
`[dir="rtl"]`
`bg-transparent`,
`bind:style`
`boundary`
`calc(…
`dir="rtl"`
`env(safe-area-inset-bottom,
`indicator="bar"`
`indicator="dot"`
`intent`
`left
`left:0;
`max-width:
`overflow:
`page-progress`
`pointer-events-none`
`position:
`progress`
`progress`,
`prop="false"`
`rgba(0,0,0,0.4)`,
`segments`
`stroke-dasharray`
`style`
`transform-origin`
`transform:
`variant`
`width:
`wk-reading-progress--dot`
`x-on:wirekit:reading-progress:milestone.window`.
a
above
above);
above,
accept
across
actual
advances.
after
against
alert/callout's
alias
alias.
aliases
already
also
alternative
an
ancestor
ancestor.
anchor
anchored
and
animates,
animating.
animation
animation.
anticipate
any
anywhere
applies
arbitrary-value
are
aria-hidden="true">
aria-valuemax="100"
aria-valuemin="0"
array
array_values(array_filter($segments,
article
as
as,
asserts
assignment,
at
attribute
auto
auto-derived
auto.
auto`
away
axis:
axis;
back
back-compat
back.
background
background-color
background-color:
background-gradient
bar
bar's
bar/dot
base
beats
because
becomes
behavior
below
below,
between
bg-transparent
binding
black
block,
block.
blowing
body
body,
both
bottom
bottom)
bottom-[calc(var(--padding-wk-x-lg)_+_env(safe-area-inset-bottom,0px))]'
bottom-[calc(var(--padding-wk-x-lg)_+_env(safe-area-inset-bottom,0px))]')
bottom-right).
bottom-right.
bottom:
boundaries
boundary
boundary.
boundary;
boundarySelector:
branch
branch:
branches.
break
browser
build.
but
by
calc('.$pct.'%
calc(var(--transition-wk-duration)
calculation
call
can
canonical
cap
carries
carry
cast
center
center;
change).
changes
chapter
child
children
circle's
circular
class
class-level
class="block
class="wk-reading-progress__fill
classes
cleanly.
click
client-only
clockwise
color
color,
color.
comment
common
compile
compiles
component's
component:
components
compositor-only
constrains
container,
container.
content
contexts
contexts,
continuously,
corner
correctly
count($segmentsArray)
currentColor
currentColor)',
custom
cx="18"
cy="18"
danger
dark
debug
decides.
declaration
dedicated
default
defeats
defensive
defer
defined
deliberate
deliberate.
desktop.
detection
developer
developer's
developer-side
did
diff
different
differs.
digits
direct
direction-encoded
disappears
dispatch
dist/wirekit.css,
dividers
divides,
document
does
doesn't
dot
dot's
down,
down.
drift
dropped
duplicated
duration
duration,
during
each
earlier
ease-out;"
edge
edge,
edge.
edges
element
else
else,
elseif
elsewhere.
embedded
emitted
entire
environments
equivalent
error
even
events
every
everywhere
exemption
existing
exists;
explicit
explicitly
extensions)
extensions).
extra
factory
fade.
fails
falls
false);
false,
family's
feature
file
fill
fill.
fill="none"
filled
fills
filter_var($milestones,
fired
first
fix
fixed
fixed`.
flags
flips
flow,
fn
folding
follows
for
foreach
form
form,
fractional
from
full
further
gates
gating
generate
gesture
given
gone,
gracefully).
grammar
grows
guard
h-full
had
hand.
has
hasn't
height
height,
height.
height:
height;
here
home
hook
hook;
horizontal
hover
if
iframe-srcdoc
iframe-srcdoc,
ignores
implode('
imports
in
indicator
individual
info
init()
inline
inline-style
inset
inside
instead
intent
intercepts
invisible.
is
is_array($segments)
is_numeric($v)
it
it's
it,
it.
its
itself
keeps
kept
keyword
kicks
lag
land.
lands
later
layout,
left
left-0
left:
letters
like
line
linear-gradient
linear-gradient(to
lines
list
listed
listen
literal
live
logical
long-tasks
look
loses
machine;
marker
markup
match
matching
math,
max-width
max-width:
may
mean
measured,
merges
milestone
milestonesEnabled:
mirror
mirrors
missed
modal
more,
must
n/a
name
names
nav).
near-black
nearest
needed
neither,
neutral
never
no
nodes
non-empty
non-scrolling
none;
none`
not
note
nothing
now
null
null"
null)
null,
null;
numeric
object-form
object:
occasionally
of
offset
old
on
on.
once
one
one.
only
opposite
optimistic-ui:
or
origin
other
otherwise.
over
overflow
overlaid
overlap
override
own
owns,
padding
page
paint,
paint.
paints
pane,
parent
parse
part
pattern,
per
phone
physical
picks
pinned
pins
plain
plugin's
plus
pointer-events-none
pointer-events:
position,
position.
position:
positioned
positioning
positions
pre-requisite
preserved.
preserving
preview
previews).
primary
primary,
primary==info
print
print-stylesheet
prod).
produced
production
progress
progress')])
prop
prop's
prop.
properties
property
props
prose
public
punctuation;
purpose,
purpose:
push
puts
qualified:
r
r="16"
rather
reach
reactive
read
reader
reading
reading-bookmark);
reads
records
red
reduced-motion
regenerates
regression
relative
release,
render.
rendered
rendering
renderings.
repairing
replaces
requires
resolve
resolved
resolves
respects
responsible
rest
retheming).
retinting
reuse
reverse
right
right,
right-0
right-[calc(var(--padding-wk-x-lg)_+_var(--wk-scrollbar-inset,0px))]
right-[var(--padding-wk-x-lg)]
right:
right:0`
rightward,
ring
role="progressbar"
roll
rule
rules
rules.
runtime
same
says
scaleX
scaleX(0.5)`,
scaleX`
scaled
scanner
scans
scope
scroll
scroll"
scroll)
scroll-to-top,
scroll;
scrolling
scrolls
scrolls.
sees
sees.
segment
segment.
selector
semantic).
sentence.
separator
server
serves
session
set
sets
setting
settle
shape
shapes
share
shipped
short
shorter
showAfter:
shrinks
sibling
siblings
sidebar
silently.
sit
site
sits
size,
sizing
sliver
smooth
so
source
specifically.
specificity
specificity.
spellings
spells
standalone
standard
start
start.
starts
state
state,
static
static`
stay
stays
sticky
sticky/fixed
sticky`
still
stops
stops.
string
string,
strings
strip
strip)
strip,
strip-height
strip`)
stroke
stroke-dasharray
stroke-dasharray="100.53"
stroke-dashoffset
stroke-linecap="round"
stroke-width="2.5"
stroke="var(--color-wk-border)"
stroke="{{
style
style="height:
style="transition:
styles
stylesheet
stylesheet,
success
supported
surface
surface).
surfaces
surrounding
swallows
swap
symptom
system
takes
tall
tap.
target:
targeted
template
template.
test
text
than
that
the
their
theme-aware.
theme-wide
theme.
theme:
then
there
thin
this
those
threshold.
throttle:
throw
time
to
token
token.
too.
top
tracks
transform
transform-origin,
transform-origin:
transition).
transition:
transitions
transparent
travels
travels.
true,
truthy
typography
under
unknown
up
update,
use
used
useful
user
uses
uses.
using
utility
v4's
value
value.
var(--color-wk-accent))',
var(--color-wk-border-strong))';
var(--color-wk-danger))',
var(--color-wk-success))',
var(--color-wk-text)`
var(--color-wk-text-muted))',
var(--color-wk-warning))',
var(--padding-wk-x-lg);
var(--reading-progress-dot-size);
var(--reading-progress-dot-size);'])->class([$rootClass,
var(--wk-strip-inset,
var(--z-wk-sticky);
variant
variant:
variants
verify
very
via
view's
viewBox="0
viewport
viewport's
viewport,
viewport-fixed
viewport-pinned
visible
visibly
visual-synonym
w-full
w-full"
wanting
wants
warning
warns
was
way
we
went
were
what
whatever
when
where
whether
which
while
whose
why
wide
width).
width,
width.
width:
wins
wins;
with
without
work".
would
wrapper
wrapper's
wrappers
writes
written
wrote,
x-bind:aria-hidden="progress
x-bind:aria-valuenow="roundedProgress()"
x-bind:stroke-dashoffset="100.53
x-bind:style
x-bind:style="fillStyle()"
x-data,
x-data="wirekitReadingProgress({
your
z-[var(--z-wk-sticky)]
z-index:
zero
{
{{
{{--
{}
|
||
}
})"
};
}`
}}
}}"
}},
}};
~12.
~150
×
—
→
⚠️
