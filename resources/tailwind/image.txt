!==
"1/1",
"4/3",
"the
$alt
$attributes->class([$figureClasses])
$attributes->getAttributes());
$caption
$figureClasses
$fit,
$fitClass
$fitClass,
$fitValue
$imgClasses
$loading
$ratio
$rounded
$roundedClass
$roundedClass])
$roundedClass]);
$scope);
$src
&&
'
''
'';
',
'.(string)
'WireKit
'alt'
'base',
'caption'
'contain'
'contain']),
'cover'
'cover',
'false'
'fit'
'fit',
'font-[family-name:var(--font-wk-sans)]',
'lazy',
'loading'
'm-0',
'object-contain'
'object-cover';
'ratio'
'rounded'
'rounded-[var(--radius-wk-md)]'
'scope'
'src'
("16/9",
($fit)
($src
(--radius-wk-md).
(CLS-safe)
(a
(absolute-fill
(alt="").
(e.g.
(fill
(letterbox,
(modern
(none)
(renders
(silent
);
+
--}}
.'Pass
.'src:
/
//
/>
0
:
</figcaption>
</figure>
<figcaption
<figcaption>
<figure
<img
<img>
=
===
=>
?
??
@endif
@endphp
@if($caption)
@if($ratio)
@php
@props([
@props.
A
ACL-protected
Accessible
An
BEFORE
Blade
BooleanProp::from($rounded,
Corner
DIRECTLY
Debug
Declared
Dev-only
Eager
Fully
HTML
IS
Ignored
Image
Measured
Native
Normalized
Null
ONLY
Optional
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
REQUIRED
Renders
Sizes
The
UNBOUND
URL
URL.
WireKit::resolveClasses('image',
WireKit::validateProp('image',
With
[
['block
['cover',
[image]:
\Illuminate\Support\Facades\Log::warning(
\Pushery\WireKit\WireKit::warnUnknownProps('image',
])
]),
`alt=""`
`alt`
`prop="false"`
`width:
a
above-the-fold
absolute-fill
absolutely-
action
against
alt=""
alt="{{
alt="…"
an
and
announces
answer
any
apart.
are
as
as,
aspect-ratio
asserted:
attribute
auto-derived
automated
be
because
before
block,
box
boxes
byte-identical
call
can
cannot
caption
case
case.
cases
cast
check
class="mt-[var(--space-wk-xs)]
class="{{
collapses
compiles
component
component's
config('app.debug'))
content
context
context.
contributes
correct
could
crop)
crop).
data-wk-prose-skip
debug
decoding="async"
decorative
decorative"
default
default,
developer
dimensions
docs
does
download
each
early.
either,
element,
empty
even
exception:
failure
false
false);
false,
feature
file
fill
fit-content`
flags
flips
for
forgot"
forgotten
from
given,
guard
h-auto
hero
if
image
image,
image.
image:
image;
images,
images.
implode('
imports
in
in-flow
incorrect
inside
interactive
intrinsic
is
it
it).
it.
its
itself,
itself.
keeps
later
layout
lazy-loading.
line
list
live
lives
load.
loading="{{
loads,
log
make
marked
markup
match
max-w-full',
may
mean
measures
n/a
name
name.
natural
never
nine
no
not
nothing
nothing,
nothing.
null
null,
number).
object-fit
object-fit)
of
omit
on
on.
one
one.
only
only,
opposite
optimistic-ui:
or
other
own
parent's
pass
positioned
presentational
preview
prod).
produced
prop's
props
purely
purpose.
qualified:
rather
ratio
ratio-boxed
ratio.
reach
read
readers
reads
ready
reason
refutes
renders
repo's
reserved
result
rounding
say
says
screen
see
shell)
shifts
shown
signed
silently.
site
sites
sized
sizes
skip
so
space
src="{{
string
string,
style="aspect-ratio:
supplies
text-[color:var(--color-wk-text-muted)]">
text-[length:var(--text-wk-sm)]
than
that
the
there
this
throwing
to
token:
told
true
truthy
twenty-two
two
unchanged).
under
unknown
unknown.
unusable
use
used
valid
view's
visible
visible.
w-full
was
what
when
where
which
whose
width,
will
with
without
works
would
wrapper
wrapper)
{
{{
{{--
}
};
}}
}}"
}}>
—
→
