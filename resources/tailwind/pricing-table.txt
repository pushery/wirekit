!==
"3
"this
$attributes->getAttributes());
$attributes->merge(['style'
$attributes->whereStartsWith('wire:model')
$classes
$columnClasses
$columnClasses,
$columns)
$defaultInterval
$defaultInterval;
$interval
$intervalKey
$intervalKey)
$intervalLabel
$intervalLabelText
$intervalLabelText)
$intervalMap
$intervalSelectedClasses
$intervalUnselectedClasses
$intervals
$label
$name
$scope);
$serverInterval
$slot
&&
',
'1'
'2'
'4'
'Annual']".
'Monthly',
'annual'
'base',
'bg-[var(--color-wk-bg-elevated)]',
'columns'
'false'
'false'"
'grid
'grid-cols-1
'grid-cols-1',
'interval'
'interval',
'interval-selected',
'interval-unselected',
'intervalLabel'
'intervals'
'items-stretch',
'label'
'list-none',
'list-style:
'name'
'scope'
'shadow-[var(--shadow-wk-sm)]',
'text-[color:var(--color-wk-text)]',
'text-[color:var(--color-wk-text-muted)]',
'true'
((string)
(An
(never
(silent
(string)
(the
+
--}}
/
//
0;
0;'])->class([$classes])
0;'])->whereDoesntStartWith('wire:model')->class([$classes])
1
2
2-plan
3
3,
390px
:
:aria-pressed="interval
:class
:class="interval
:intervals="['monthly'
</div>
</ul>
<button
<div
<form>.
<input
<ul
<ul>
<ul>,
=
===
=>
>
>{{
?
??
@else
@endforeach
@endif
@endphp
@foreach($intervalMap
@if($interval
@if($intervalMap
@php
@props([
@props.
A
Accessible
Alpine
BEFORE
Billing
Blade
Both
CSP
Column
DOES
Declared
Default
Dev-only
Distinct
Full
Fully
Getting
Given,
How
Inside
It
Keeping
Livewire
NOT
Name
Normalize
OUTSIDE
Omitted
One
Only
PROP,
Pushery\WireKit\WireKit;
SERVER
STATIC
Same
Tailwind
The
This
UA
Until
WITHOUT
What
WireKit::resolveClasses('pricing-table',
WireKit::scope(),
[
[]
\Pushery\WireKit\Support\AlpinePayload::from($defaultInterval)
\Pushery\WireKit\Support\AlpinePayload::from((string)
\Pushery\WireKit\Support\AlpinePayload::string($intervalSelectedClasses)
\Pushery\WireKit\Support\AlpinePayload::string($intervalUnselectedClasses)
\Pushery\WireKit\WireKit::warnUnknownProps('pricing-table',
])
]),
__('wirekit::Billing
__('wirekit::Pricing
`$serverInterval`,
`aria-pressed`
`data-wk-server-value`
`default`
`interval`
`intervals`,
`list-none`
`prices`.
`wire:model`
`x-data`,
a
about
above
across
against
all
all,
alone,
already
an
and
answer
anything
appearance
are
aria-label="{{
aria-pressed,
aria-pressed="{{
array_key_first($intervalMap)
arriving,
as
assistive
at
attribute
auto-derived
backwards
be
because
before
being
below.
beside
bg-[var(--color-wk-bg-subtle)]
bind
binding
block,
blocks
border-[length:var(--border-wk-width)]
border-[var(--color-wk-border)]
both
bound
branches
breakpoint.
build,
bulleted
bullets.
bundle
button
by
byte-identical
byte-stable.
caller's
can
cannot
card.
cards
carries
case
change,
changes
checkout
choice
claimed
class
class:
class="cursor-pointer
class="mb-[var(--space-wk-md)]
client-only
comment
compares,
component
component's
components
config('wirekit.components.pricing-table.label')
contain
content
correct
could
data-wk-pricing-interval-toggle="{{
data-wk-pricing-intervals
data-wk-pricing-table
data-wk-prose-skip
data-wk-server-value="{{
dead
debug
decided
default
default),
default:
deliberate:
developer's
directive
disc
dist/wirekit.css
divs:
do
do;
docs
document
does
duration-[var(--transition-wk-duration)]
e.g.
earlier
effect
element
evaluates
every
exactly
factory
fill
first
first,
fix
flags
focus-visible:ring-[length:var(--ring-wk-width)]
focus-visible:ring-[var(--color-wk-ring)]"
focus:outline-hidden
for
from
gap-[var(--gap-wk-md)]',
gap-[var(--space-wk-xs)]
gappy
generates
given.
grid.
group
grow
had
halved
has
hear
here.
hidden
historical
hold
identically
if
iframe
implode('
imports
in
in.
inert
init.
inline
inline-flex
input
interpolated
interpolated)
interval
interval'),
interval-keyed
intervals
intervals,
into
is
is_array($intervals)
it
it.
items
items"
items,
items-center
just
keeps
key
knows:
label,
ladder.
ladder;
last.
later
leave.
leaves
lg:grid-cols-3',
lg:grid-cols-4',
list
list,
list-none
list-none:
list-style
listed
literal
literals:
live
load
loaded.)
locally.
make
many
map
margin:
markers
match
may
md:grid-cols-2
md:grid-cols-2',
meets
monthly/yearly
morph
most.
n/a
name
name="{{
needs.
never
nine
no
noise
none;
not
nothing
nothing.
now.
null
null)
null,
null;
observe
observeServerValue
observed
of
off
on
one
one.
only
opening
opposite;
optimistic-ui:
other
otherwise
out
owns
p-[var(--space-wk-xs)]"
padding:
paint.
pass
passthrough
path.
pattern
per
personalized.
phone
pick
pile
plan
plans
plans'),
plans.
position.
position:
pre-morph
precedence
preview.
previews
priced
prices
prod).
prop
props
pure
put
px-[var(--padding-wk-x-md)]
py-[var(--padding-wk-y-sm)]
qualified:
queued
quietly
rather
re-initializes
reach
reaches
read
reader
reads
reason
redundant
render
rendered
renders
renders.
resolve
resolveClasses
resolves
resources/views/_safelist.blade.php
rewrites
right
role="group"
role="list"
rounded-[var(--radius-wk-full)]
row
rule
runtime
same
sandbox
sandbox-iframe
says
scanner
scope
scope.
screen
screen.
see
seed,
seeded
sees
segmented-control.
segmented-control;
selected
selected"
selected.
semantic
server
server's
server-rendered
set
sheet
should
shown,
side
sit
sits
sitting
slot
so
specifically
state
stays
still
strict
strings
surrounding
survive
switch
symptom,
table
table.
taken
tech,
ternary,
text-[length:var(--text-wk-sm)]
than
that
the
their
them.
then
there
this
three-column
through
tiers
tint:
to
to,
toggle
toggle.
token
transition-colors
type="button"
type="hidden"
under
unknown
update
use
used
value
value="{{
version
view's
wading
was
well
what
when
where
which
who
whole
why
widest
wins;
with
with.
work
would
wrapper
writes
written
wrong
x-data="wirekitPricingTable({
x-on:click="interval
x-ref="hiddenInput"
{
{{
{{--
})"
};
}}
}}"
}}</button>
—
