!
!!}
!==
"2
"default"
"heroicon-m-inbox"
$active
$active,
$attributes->class([$sizeClasses]);
$attributes->get('aria-hidden');
$attributes->get('aria-label');
$attributes->get('aria-labelledby');
$attributes->get('role');
$attributes->getAttributes());
$attributes->whereDoesntStartWith(['wire:model',
$attributes->whereStartsWith(['wire:model',
$buttonClasses
$callerAriaHidden
$callerAriaHidden;
$callerAriaLabel
$callerAriaLabelledBy
$callerRole
$cleaned
$cleaned);
$config
$count);
$current));
$e)
$e;
$groupLabel
$isInformative
$isInformative;
$label
$loop
$loop,
$m):
$m[1]);
$mergedAttributes
$mergedAttributes->getAttributes())->toHtml();
$mergedAttributes->merge($placeholderAttributes)
$mergedAttributes->merge(['aria-hidden'
$name,
$name];
$navClasses
$placeholderAttributes
$placeholderAttributes['role']
$resolved
$resolved);
$resolvedAriaHidden
$scope);
$shouldSetHidden
$size
$size,
$sizeClasses
$sizeMap
$sizeMap[$validated];
$svgHtml
$svgHtml,
$total
$total,
$total])
$validated
&&
'
''
'')
'',
',
'.
'/<svg\b([^>]*)>/i',
'/<svg\b/',
'2xl'
':current',
':total']),
'<svg
'<svg'.$cleaned.'>';
'Rendering
'Run:
'[WireKit]
'announcement'
'base',
'button',
'count'
'countLabel'
'current'
'disabled:cursor-not-allowed
'focus-visible:outline-hidden
'h-12
'h-3
'h-4
'h-5
'h-6
'h-8
'hover:bg-[var(--color-wk-bg-muted)]',
'img';
'inline-flex
'label'
'lg'
'loop'
'md'
'name'
'scope'
'size'
'size',
'size-[var(--size-wk-sm)]
'sm'
'text-[color:var(--color-wk-text-muted)]
'text-[length:var(--text-wk-sm)]
'total'
'true';
'true']);
'wk-transition',
'x-model'])->class([$navClasses])
'x-model'])->whereDoesntStartWith('x-modelable')
'xl'
'xs'
(!
($callerAriaHidden
($isInformative
($resolvedAriaHidden
($shouldSetHidden)
($size
($svgHtml
('xs')
(\BladeUI\Icons\Exceptions\SvgNotFound
(\Pushery\WireKit\Support\StrictnessGate::shouldThrowOnInvalid())
(\Throwable
(array
(button
(e.g.
(function_exists('svg')
(int)
(or
(paths,
(preserving
(provides
(regex
(same
(shouldSetHidden=true),
(silent
(string)
(true
);
+
-
--}}
...>
/
//
/>
0
0)
0,
1
1,
1-based
1-based,
2rem,
3",
3rem.
500s
:
:current
:total',
</button>
</div>
<button
<div
<span
<svg
<svg…>)
<use>,
<x-wirekit::icon
<x-wirekit::icon>
=
===
=>
>
></span>
?
@else
@endif
@endphp
@if
@if($total
@php
@props([
@props.
A
A11y
ALSO
AND
API:
AT
ATTEMPTED
Adding
AlpinePayload::from([
An
BECAUSE
BELOW,
Blade
Blade-heroicons'
BladeUI\Icons\Svg
BooleanProp::from($loop,
CAN
CANNOT
CONDITION
Catching
Check
Clamped,
DO
Declared
Dev-only
Drop
ENT_QUOTES).'"',
EVERY
FAIL
Fully
Graceful
HERE,
HTTP
How
Htmlable,
IN
INSIDE
IS
IT
Icon
IconResolver,
If
In
It
Logger
MOST
Measured
Moving
NOT
Names
Nothing
ONE
ONLY
OPENING
OR
OUTER
Off
Operating
Out
PERFECTLY
Pushery\WireKit\Icons\IconResolver;
Pushery\WireKit\Support\AlpinePayload;
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
RENDER
Reapply
Render
Rendered
Renders
Reported
Required
Resolve
RuntimeException
SEE.
SVG
SVG:
Silent
Size
Strip
SvgNotFound
SvgNotFound.
Symmetric
TAG
THE
The
This
Throwing
WAY
We
Whether
Which
WireKit
WireKit::resolveClasses('branch-switcher',
WireKit::validateProp('icon',
With
[
['current'
['data-wk-icon-missing'
\Pushery\WireKit\Icons\IconSetPackages::reportMissingSetOnce((string)
\Pushery\WireKit\WireKit::warnUnknownProps('branch-switcher',
\Pushery\WireKit\WireKit::warnUnknownProps('icon',
])
]),
]);
];
__('wirekit:::current
__('wirekit::Next
__('wirekit::Previous
__('wirekit::Response
__('wirekit::Showing
__toString
`Svg
`aria-hidden="true"`
`aria-label`
`aria-labelledby`
`class="h-12
`cursor-pointer`
`cursor:
`heroicon-m-inbox`
`inbox`
`resources/js`
`role="img"`.
`wire:model`
`wire:model`:
`x-modelable`
a
a11y
about.
above.
absent,
accessibility
accessible
action
actual
add
after
afterwards.
against
alias
all
all,
allowed
already
an
anchored
and
announced
announcement
announcer
answer
answer.
answers
any
app(IconResolver::class)->resolve($name);
are
are.
aria-hidden
aria-hidden,
aria-hidden="'.htmlspecialchars($resolvedAriaHidden,
aria-hidden="..."
aria-hidden="false".
aria-label
aria-label="{{
aria-labelledby
aria-live
aria-live="polite"
array_keys($sizeMap));
as
asserted:
assistant
assistive
at
attribute
attribute,
auto-derived
back
baked
bakes
bare
be
because
becomes
below
below.
between
binding
blade-heroicons
blade-icons
blade-icons,
blade-ui-kit/blade-heroicons.
blade-ui-kit/blade-icons
blade-ui-kit/blade-icons.
block,
bootstrapped
branch's
break
bug
build
but
button
buttons).
buttons,
by
by.
call
called
caller
caller's
can
canGoNext()"
canGoPrevious()"
cannot
carries
carry
case
catalog
catch
caught
changes
check
checks
chevrons,
choice
chose
clamped
class="sr-only"
class="{{
client-only
close
close-icons,
collapsed
component's
components
composer
condition
configured
conflict
console
console).
consuming
content
context
control.
controls
copy
could
count
count.
covered
created
cursor-pointer
data-wk-branch-announcer
data-wk-branch-count
data-wk-branch-next
data-wk-branch-previous
data-wk-branch-switcher
debug
debug,
decided
decision
decision,
decision:
declared
declaring
decorative
decorative,
default
default.
default:
default`,
define('WIREKIT_BLADE_ICONS_WARNED',
defined('WIREKIT_BLADE_ICONS_WARNED'))
degradation:
degrades,
deliberate.
dev
developer
developer's
direct
disabled:hover:bg-transparent',
disabled:opacity-50
does
down
down.
draws
dropdown
dropdowns
drops
early
early.
either
element
element,
else
elseif
embedded
emit
empty
empty-state
ends
entry
every
exhausting
exists
exists.
expects
explicitly
factory
factory:
fallback
fallback,
fallback.
falls
false)
false),
false);
false,
feature
file
files
filled
filled($label)
fills
finds
first
fix
fix.
flags
floor
focus-visible:ring-[length:var(--ring-wk-width)]
focus-visible:ring-[var(--color-wk-ring)]',
followable.
for
forbidden
forcing
found.`
framework
from
front
function
function).
function_exists('svg'))
gap-[var(--gap-wk-xs)]',
get
gets
gives
global
glyph
glyph.
goes
group
guarantees
guard
h-5
h/w
hand
hand-written
handed
hard
harder
has
have
have)
helper
here
hidden
historical
house
hover:text-[color:var(--color-wk-text)]',
icon
icon-only
icon;
icons
icons).
icons,
identifier
if
image
implements
implode('
imports
in
including
index
index.
inert
informative
informative.
injected
inner
inside
installed
installed.'
intact
intent.
inter-attribute
interactive
interrupt
into
is
isn't
isset($sizeMap[$size])
it
it,
it.
items-center
its
justify-center
keeps
kill
know
label
labels).
ladder
last
later
layout
leave
legitimately
lets
line
list
literal
live
loads.
log
logger()->warning(
longer
loses
lost
loudly,
makes
many
map.
marks
matching
max(0,
max(1,
may
mean.
means
merge
mid-move.
min($total,
missing
missing,
missing:
modal
modals,
move
n/a
name
name.
name="chevron-left"
name="chevron-right"
named
navigates
needs
neither
nested
never
no
nobody
non-input
non-null
none,
nor
not
nothing
nothing,
nothing.
now.
null
null)
null,
null;
numbers
occurrence
of
on
once
one
one,
one-time
one.
one;
only
onto
open
opening
optimistic-ui:
or
ordinary
other
our
over
override.
own
own.
package
page
page.
pages
path
per
per-set
place
placeholder
placeholders
plain
post-process
practice:
pre-checking
preflight
preg_replace(
preg_replace('/\s*aria-hidden="[^"]*"/i',
preg_replace('/\s+/',
preg_replace_callback(
present
presentational
preserves
preset
problem
prod).
production.
project,
props
provides
purpose:
put
qualified:
race,
range
rather
raw
re-inject
re-renders
reach
reaches
reader
real
reapply
reason
reason:
reasons
records,
refused
refused.
refutes
regenerate
regenerates
region
region:
registered
registry
removed
render
render,
rendered
renders
replaced
replaces
reproduced
request
require
requires
resolved
resolves
respect
response
response')
rest
result
return
returns
reused
role
role,
role="group"
role="img"
role="img".
role="status"
root
root;
rounded-[var(--radius-wk-full)]',
rule
rung
same
say
says
screen-reader-skipping
sees
semantic
sensible
sent
sentence
server
server,
set
sets
settled.
should
show
showing,
shown
silent
single
site
size
size,
size="sm"
smallest
so
somebody
source
source-baked
space).
span
speaks
speaks,
stale
static
stay
step,
step.
stepping
still
stopped
storage/logs/laravel.log.
string
strip
style="display:inline-block;"></span>
substituted
svg($resolved,
svg()
svg(),
switcher
table
tag
tag's
tag.
take
takes
taking
target
teaches
technology.
test
text
text-[color:var(--color-wk-text-muted)]',
than
that
that.
the
their
them,
then
there
therefore
these
third
this
three
through
throw
throwing
throws
tile
to
toHtml().
together
touch
touches
transitively
translated
translator,
travel
tree:
true);
try
turn
turn's
two
type="button"
unknown
unknown-alias
unknown.
unless
unset
until
untranslated
up
use
useful
uses
utility
valid
validateProp
value
value,
variant
variants
variants');
via
view's
w-12"`
w-12',
w-3',
w-4',
w-5
w-5',
w-5';
w-6',
w-8',
warning
was
way
ways
we
what
when
when:
wherever
whether
which
while
whitespace
who
whole
whose
wins
with
wk-touch-target',
would
wrap-around
wrap.
x-bind:disabled="!
x-data="wirekitBranchSwitcher({{
x-modelable="current"
x-on:click="next()"
x-on:click="previous()"
x-on:keydown.arrow-left.prevent="previous()"
x-on:keydown.arrow-right.prevent="next()"
x-text="announced"
x-text="label()">{{
yet.
{
{!!
{{
{{--
||
}
},
}}
}}"
}})"
}}</span>
—
⚠️
