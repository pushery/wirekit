"Resizable"
"splitter,
$attributes->class([$classes,
$attributes->class([$classes])
$attributes->getAttributes());
$attributes->merge(['style'
$attributes->missing('aria-label')
$attributes->missing('aria-labelledby');
$classes
$defaultPct
$defaultPct,
$defaultSize));
$direction
$directionClass
$directionClass])
$label
$maxPct
$maxPct,
$maxSize));
$minPct
$minPct,
$minSize));
$needsName
$scope);
$slot
$style
$style])->class([$classes])
%d%%;
%d%%;',
&&
'',
',
'--wk-default-size:
'base',
'border-[length:var(--border-wk-width)]',
'border-[var(--color-wk-border)]',
'defaultSize'
'direction'
'flex
'flex-col'
'flex-row';
'font-[family-name:var(--font-wk-sans)]',
'horizontal'
'horizontal',
'index'
'label'
'maxSize'
'minSize'
'overflow-hidden',
'rounded-[var(--radius-wk-lg)]',
'scope'
'vertical'
(e.g.
(int)
(resources/js/components/
(see
(silent
);
+
--wk-max-size:
--wk-min-size:
--}}
/
//
0,
10,
1`
50%"
50,
90,
:
</div>
<div
<span
=
===
=>
>
?
??
@endif
@endphp
@if($needsName)
@php
@props([
@props.
APG's
ARIA
Accessible
Alpine
Blade
Blade's
CSS
Capture
Declared
Dev-only
Dragging
Fully
Inline
It
Its
JS
JavaScript
Measured
NOT
NOTE:
Not
Nothing
Pushery\WireKit\WireKit;
Renders
Resizable
Safari
Splitter
That
The
This
WAI-ARIA
WHICH
We
Window
WireKit::resolveClasses('resizable',
WireKit::resolveClasses('resizable.handle',
WireKit::resolveClasses('resizable.panel',
[
[data-wk-resizable]
\Pushery\WireKit\WireKit::warnUnknownProps('resizable',
\Pushery\WireKit\WireKit::warnUnknownProps('resizable.handle',
\Pushery\WireKit\WireKit::warnUnknownProps('resizable.panel',
])
]),
__('wirekit::Resize
`<x-wirekit::resizable.handle>`
`aria-label`
`aria-labelledby`
`body
`contain`
`dist/wirekit.css`
`dist/wirekit.css`.
`flex:
`index`
`lostpointercapture`
`previousElementSibling`
`resize:
`resize`
`role="separator"`
`setPointerCapture`
`tabindex="0"`,
`wirekitResizableHandle`
a
absorb
accompanying
action
actual
adapts
after
again
against
already
an
and
announced
another
answer
anticipate
any
aria-controls,
aria-hidden
aria-hidden="true"></span>
aria-label="{{
aria-valuenow
arrow-key
as
asserted:
at
attach
attaches
attribute
attributes
auto-derived
avoid
back.
backward
be
because
behind.
being
belief
beside
better
between
block,
both
bound
branching.
browser's
browser-native
but
by
caller
can
capture,
carries
carry
caveat
centered,
changes).
child
children
clamp
client-only
comment
compatibility
component
component's
container
container,
copied
corner
could
custom
data-wk-default-size="{{
data-wk-direction
data-wk-direction="{{
data-wk-max-size="{{
data-wk-min-size="{{
data-wk-resizable
data-wk-resizable-grip
data-wk-resizable-grip>
data-wk-resizable-handle
data-wk-resizable-panel
debug
declaration
decorative
decorative:
default
delegated
descendant
described
developer
direction-aware
dist/wirekit.css
dist/wirekit.css.
divider
does
doesn't
drag
drives
early.
edge
element
element,
emitted
events
every
exposes
file
first
flags
flex
flex-col
flex-row
flip
floats
floor.
for
from
gesture:
gives
grip,
growth.
guard
half,
handle
handler
handlers,
handles
has
have
heading
heading,
heading;
hears
horizontal/vertical`
horizontal`
identically.
implementation
implode('
imports
in
init
inline
inner
instead.
integers
interactive
interactive.
into
is
is:
it
it.
its
itself
just
keep
keeps
kept
keyboard
last
later
layout
layout,
leave
lets
life.
line
list
live
lives
locate
locks
logic
long
lose
lost
marks
matching
max($minPct,
max(0,
may
mid-drag,
min(100,
more
mouse.
movable
moves
n/a
name
named
names
native
needs
neither
new
no
non-last
none
not
nothing
nowhere.
null,
of
old
on
one
one,
one.
one:
only
onto
optimistic-ui:
or
ordinal,
orientation,
orientation.
orientations
other
others
own
owns
owns,
page
page's
pane
pane's
panel
panel')
panel.
panels,
pass
per-direction
percentages
pill
plugin
plus
point
pointer
pointer-drag
pointerup
pointing
position.
presentational
previous
prod).
prop
properties
property
proportionally
props
put
qualified:
range
rather
reach
reachable
read
reader
reading
reason
refutes
reload,
removed
renders
reports
resizable.js)
resizable.js);
resizes
resources/js/components/
rest
rest.
restore
result
role,
role.
roll
rule
rules
same
sanitize
saying
scoped
second
section):
selected
selection,
selectors
sending
sentence
separator
separator.
server
sets
shown
single
size
sized
sizes,
so
space
split
sprintf(
stale
state
stopped
style
supplied
tabs
takes
target
template
text
than
that
the
them
them,
there
these
this
three-dot
three-panel
time.
to
to.
too,
true,
two
under
unknown
unnamed
untested
until
up
us.
use
used
user-select:
uses
usually
value
vertical`
via
view's
viewport
visible
visual
visually.
w-full',
was
whatever
when
whenever
which
whose
with
without
worth
would
wrapped
wrapper
writing
x-data="wirekitResizableHandle"
x-on:keydown="onKeyDown($event)"
x-on:lostpointercapture="onPointerUp($event)"
x-on:pointercancel="onPointerUp($event)"
x-on:pointerdown="onPointerDown($event)"
x-on:pointermove.passive="onPointerMove($event)"
x-on:pointerup="onPointerUp($event)"
you
{
{{
{{--
}`,
}}
}}"
—
→
