$attributes->getAttributes());
$attributes->merge($headerStyle
$classes
$container
$headerStyle
$headerStyle's
$headerStyle]
$innerStyle
$scope);
$slot
$sticky
$stickyClasses
$stickyClasses])
'';
',
'base',
'bg-[var(--color-wk-bg-elevated)]',
'border-b
'container'
'display:
'false'
'flex
'gap-[var(--gap-wk-md)]',
'min-h-16',
'px-[var(--padding-wk-x-lg)]',
'py-[var(--padding-wk-y-sm)]',
'scope'
'sticky
'sticky'
'w-full',
'wk-header',
(40)
(50);
(Dashboard
(`--wk-strip-inset`,
(`min-h-16`)
(see
(silent
(the
+
--z-wk-dropdown
--z-wk-sticky
--}}
/
//
/>`
0px
100%
100%;
2xl
390px
75ch`
96rem);
:
</div>
</header>
<div
<header
=
=>
?
@else
@endif
@endphp
@if($container)
@php
@props([
@props.
A
BELOW
Blade
BooleanProp::from($container,
BooleanProp::from($sticky,
CHROME
CSS
Declared
Desktop
Dev-only
Fully
Header
INSIDE
Inline
MIN-height
Measured
Normalized
Projects
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
Renders
Reports
See
Settings
Sticks
Tailwind
Tailwind-aware
Team
The
UNBOUND
When
WireKit::resolveClasses('header',
Without
[
['style'
[])->class([$classes,
\Pushery\WireKit\WireKit::warnUnknownProps('header',
])
]),
`:container="true"`,
`<x-wirekit::spacer
`display:
`dist/wirekit.css`
`flex-wrap:
`flex-wrap`
`margin-left:
`max-width:
`prop="false"`
`w-full`
`wk-header`
a
above
above,
action
actually
against
align-items:
an
anchored
and
any
app-shell
are
area
arithmetic,
as
as,
asserted:
at
attribute
auto
auto-derived
auto;
auto`
bar
be
because
becomes
behavior
below
block,
border-[var(--color-wk-border)]',
brand
break
but
call
can't
canonical
cast
center;
center`
centers
chart-wrapper,
children
children's
clamps.
class="flex
classes
cluster
cluster's
collided
compiles
component
component's
container
content
context
contexts
correct.
could
debug
decoration,
default
developer
docs-sandbox
docs.wirekit.app
docs/extending/spine-contract.md
does
doesn't
downward
each
early.
edge
element,
engages
even
example,
false);
false,
feature
file
fit,
fixes:
flags
flex
flex-row
flex-wrap
flex-wrap:
flex;
flex`
flips
footer.blade.php
for
from
full
full-width
gap).
gap-[var(--gap-wk-md)]"
gap:
guard
h-16
header
header.
hits
idioms
iframe
implode('
imports
in
inline
inline,
inlined
inner
inside
instead
interactive
is
isn't
it
it).
it,
it.
items
items-center
items-center',
its
itself
joins
keeps
later
layer,
layout
layouts.
lesson
lets
limit
line-height.
list
live
load-bearing
long
lost;
margin
margin-left:
margin-right:
margins.
marker
max-w-[var(--size-wk-container-2xl,96rem)]
max-width
max-width:
may
mean
mirrors
modal
must
mx-auto
n/a
narrow
nav
never
no
not
notes
nothing
null,
of
on
on.
once
one),
one.
only
opened
opens
opposite
optimistic-ui:
outer
over
overflows.
overlaps
own
page
page-edge
painted
panel
parent.
phone
pick
popover
presentational
preview
primitive
prod).
profile)
prop's
properties
props
prose
push
pushed
qualified:
quirk.
rather
rationale).
reach
reading-spine
reads
reason
refutes
renders
result
right
row
rule
same
sandbox
sandbox-inline
scope
second
see
shared
shown
silently.
single
sit
site
slot
so
sparkline,
spine.
squeeze
stack
sticky
still
string,
strip
style
style="{{
takes
teleported
tenant-isolated
than
that
the
them
there
therefore
they
this
to
top
top-[var(--wk-strip-inset,0px)]
top-level
truthy
unaffected:
under
unknown
up
use
used
utility
var(--gap-wk-md);';
var(--size-wk-container-2xl,
vertically
view's
viewports
w-full
was
what
when
whenever
which
whose
why
width
width:
wirekit:spine-participant
with
without
work
wrap
wrap;
wrap`
wrapped
wrapper
z-[var(--z-wk-chrome)]'
{{
{{--
}}
}}">
}}>
—
