!==
"1
"26831d").
"73y
"box"
"days
"none"
"now"
"text"
"urgent"
"years
$activeUnits
$activeUnits[]
$animate
$animateOn
$animateStyle
$attributes->class([$baseClasses])
$attributes->getAttributes());
$attributes->has('aria-labelledby'))
$autoMode
$baseClasses
$countdownLocale
$expiredLabel
$expiredText
$humanDeadline
$locale
$requested
$requested,
$scope);
$separators
$separators)
$showSeconds
$slot
$target
$target->getTimestampMs()
$targetIso
$targetMs
$unitLabels
$unitOrder
$unitPhrases
$unitPhrases)
$units))
$until
$until)
$until));
$variant,
$variantValue
$warnSeconds
$warnThreshold
',
'-',
'0'
'0',
'animate'
'auto'
'auto')
'auto',
'base',
'box'
'box',
'days'
'days',
'expiredText'
'false'
'false',
'flex
'font-[family-name:var(--font-wk-sans)]',
'hours'
'hours',
'inline'
'inline',
'inline-flex
'locale'
'minutes'
'minutes',
'minutes'];
'none'
'none',
'none'.
'none';
'scope'
'seconds'
'seconds';
'seconds'];
'segments'
'segments')
'segments':
'segments']),
'separators'
'showSeconds'
'tabular-nums',
'text'
'text',
'text'.
'text-[color:var(--color-wk-danger-text)]'
'text-[color:var(--color-wk-text)]')"
'text-[color:var(--color-wk-warning-text)]'
'text-[length:var(--text-wk-2xl)]
'text-[length:var(--text-wk-md)]',
'units'
'until'
'variant'
'variant',
'warnThreshold'
'years'
("1
($activeUnits
($showSeconds)
($target
($u)
($units
($until
($variant)
('false'
(aria-hidden)
(border
(default):
(false
(full
(ignored
(int)
(is_numeric($until)
(like
(min-w-[3.5rem]
(no
(or
(resources/js/components/countdown.js).
(seconds
(segKey
(silent
(the
(true)
(urgent
(warning-tinted)
+
--}}
-translate-y-1"
/
//
0
0),
09s".
1
12h
15m
190d
1`.
29px
3.6px
393px
398px
3px
7px
90px
:
:class="expired
:count
:key="segKey(seg)">
:separators="false".
</div>
</span>
</template>
<div
<span
<template
<time
<time>.
<time>/units
=
===
=>
>
><span
?
?:
??
@class(['wk-countdown-pulse'
@class(['wk-countdown-text-flash'
@else
@endif
@endphp
@endunless
@if($animateOn)
@if($slot->hasActualContent())
@if($variantValue
@php
@props([
@props.
@unless($attributes->has('aria-label')
A
ABSOLUTE
AND
API
APPLICATION's
Absolute,
Alpine
Alpine's
Alpine.
An
Arabic,
BCP-47
Blade
BooleanProp::from($separators,
BooleanProp::from($showSeconds,
BooleanProp::from(),
Both
CONTROL
COPIED.
CSP
Carbon
Carbon,
Carbon.
Carbon::createFromTimestamp((int)
Carbon::now())->isoFormat('LLL');
Carbon::now())->toIso8601String();
Carbon::now()->getTimestampMs();
Carbon::parse((string)
Carbon\Carbon;
Change
DEAD
Declared
Decorative
Dev-only
EXAMPLE
Each
English
English-configured
Every
FIRST
For
Fully
German
German,
German-Germany.
German.
Getters
HTML
Headless
INTERPOLATE,
ISO-8601
IT
Include
Intl.PluralRules.
Invalid
It
Its
JavaScript
Keeping
LAST
Laravel
Live
Locale-aware
Localized
Machine-readable
Making
NOT
Normalized
Nothing
Null
Number-agreeing
ONE
OPERATOR`
Only
Or
Overdue
Passing
PluralPhrases
Polish,
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
Resend
Resolve
Russian
SCREEN-READER
SEGMENTS
Same
Seconds-remaining
Six
Style
THAT
THAT.
TO
Text
That
The
Their
These
Turn
UNBOUND
USED
WAS
WHEREVER
Where
Which
WireKit
WireKit::resolveClasses('countdown',
WireKit::validateProp('countdown',
Without
Wrapping
[
['days',
['inline',
['years',
[])
[];
\Pushery\WireKit\Support\AlpinePayload::from($activeUnits)
\Pushery\WireKit\Support\AlpinePayload::from($animateOn)
\Pushery\WireKit\Support\AlpinePayload::from($autoMode)
\Pushery\WireKit\Support\AlpinePayload::from($countdownLocale)
\Pushery\WireKit\Support\AlpinePayload::from($expiredLabel)
\Pushery\WireKit\Support\AlpinePayload::from($targetMs)
\Pushery\WireKit\Support\AlpinePayload::from($unitLabels)
\Pushery\WireKit\Support\AlpinePayload::from($warnSeconds)
\Pushery\WireKit\Support\AlpinePayload::from((bool)
\Pushery\WireKit\Support\AlpinePayload::from((object)
\Pushery\WireKit\Support\PluralPhrases::from('wirekit::{1}
\Pushery\WireKit\WireKit::warnUnknownProps('countdown',
])
]),
];
__('wirekit::Days'),
__('wirekit::Deadline')
__('wirekit::Hours'),
__('wirekit::Minutes'),
__('wirekit::Overdue');
__('wirekit::Seconds'),
__('wirekit::Years'),
`Intl.NumberFormat`
`Intl.PluralRules`
`Unexpected
`animate`
`aria-label`
`de_DE`,
`done`
`done`,
`expiredText`
`expired`,
`lang:extract`
`locale:`
`locale="fr-FR"`
`prop="false"`
`remaining`
`srText`,
`true`
`units`
`until`
`urgent`,
`value
a
a11y
about
above
absolute
accent
accent-tint
accepts
active
activeUnits:
actually
adjustment
after
against
agree
agreed.
all
already
also
an
and
animate
animate:
animates
animation).
animation.
animation:
answers
any
anything
anywhere,
app
app()->getLocale());
appear
appears.
are
aria-hidden="true"
aria-label="{{
around
array_values(array_filter($unitOrder,
as
as,
as-is
assignments
assistive
at
attaches
attribute
attributes,
auto-derived
autoMode:
available
back
bag
be
because
being
belonged
belongs
below
bg-[var(--color-wk-bg-elevated)]
big
bind
binding
blindness
block
block,
bool
boolean
border-[length:var(--border-wk-width)]
border-[var(--color-wk-border)]
both
box
box).
box.
box;
boxed
breakdown
briefly
browser
browser's.
build
build,
buy
by
cached;
call
caller
caller's
can
canonical
capitalizing
caption
carries
carrying
case
casing
cast
cast.
catalog.
categories.
change
change-animation
change;
changed
changing
choose
chooses
class="mt-[var(--space-wk-xs)]
class="sr-only"
class="tabular-nums"
class="{{
classic
client
client-only
client-side;
clock
clock,
coarse
code
coincidence
collapse
color
color.
compiles
component's
compute
computed"
concatenated,
convention
copy
cosmetic
count
countdown
counted
covered
crawlers.
dashboard
datetime="{{
days":
days'),
day|[2,*]
dead
deadline
debug
default
default.
default:
degrades
deliberate:
design
despite
developer's
device
did
digits
directives
discarded,
dist/wirekit.css.
distinguish
distinguishes,
do
docblock
does
down
drift
drifts
drop
dropping
drops
duplicated
duration
duration-[var(--transition-wk-duration)]"
duration:
e.g.
each
ease-out
else
elsewhere
enter
enters
entries
error
estimated.
evaluated.
event
event,
every
everyone
exactly
example
expired">
expiredText:
expiry
explicit
expression
factory
fade
failed
false
false,
false;
falsey
feature
feeding
five
fix,
fixed
flags
flash
flashes
flex-col
flex-wrap
flex-wrap,
flips
fn
font-[number:var(--font-wk-heading-weight)]
for
form
forms
from
full
gap-[var(--space-wk-sm)]
gap-[var(--space-wk-xs)]'
gap-[var(--space-wk-xs)]',
gated
genuinely
go
grows
guard
half
handles
hard
hardcoded
has
have
here
holds
honest
honest.
honored
honored,
horizontal
hours
hours'),
hour|[2,*]
identical
if
immediately-overdue
implode('
imports
in
in_array($u,
includes
index)
index)"></span>
index)"></span><span
inline
instanceof
instant
intentionally
intentions:
interpolation
into
introduced
invisible
is
it
it)
it,
it.
items-baseline
items-center
items-stretch
its
just
justify-center
keeps
key
key,
keyed
keys
known
label
label,
labels
ladder
ladder,
lang/en.json
language
large
largest
largest-to-smallest
later
leading
leading-none
let
line
lines
list
list).
listed
literal
literal,
literally
literally:
little
live
lives
locale
locale,
locale.
locale:
locales
logs,
look.
looked
looks
looped
lowercased
machine
make
markup
match
may
mean
meaningful
measured,
method
min-w-[3.5rem]
minimum
minutes"
minutes'),
minute|[2,*]
misconfigured
mobile
mode
mode.
mode:
moment
more
motion
must
n/a
name
names
navbar.
needed,
needs
never
no
node
none
normalization
normalized
not
not.
nothing
noun
nouns;
null
null)
null,
null;
number
number's
number,
number;
object
object.
of
off
off.
old
on
on),
on.
once
one
one.
only
only:
opposite
optimistic-ui:
optional)
or
order.
ordered
other.
out
outermost
output,
over
overflow
overruns
own
owns
padding
page
parent
parse
parsed
parser
passed.
paste,
people
per
phrase
phrases
picked
picked.
placeholder
plays
plural
pluralize
pop)
pop).
prefers-reduced-motion
preg_split('/[\s,]+/',
price).
prod).
prop
prop's
prop-reading
property
property.
props
pulse
pulses
puts
px-[var(--padding-wk-x-sm)]
px-[var(--space-wk-xs)]'
py-[var(--padding-wk-y-sm)]'])
qualified:
rather
re-mounts
reach
reachable
reaches
read
reads
real
recomputed
regardless
remaining
rendered
renders
repetition
replacement
replaying
reported
reserving
resolve
resolves
resync,
right
rise
role="timer"
room
rounded-[var(--radius-wk-md)]
row
runs
same
say
scale
scale(1.08),
scope
scope,
screen-reader
second",
seconds
seconds").
seconds'),
second|[2,*]
see.
segment
segments
sentence
separator,
separators
separators.
separators:
shape,
shorthand
show.
shown
shows
shrinking
side,
silently
silently.
simply
single
singular
sit
site
six
size;
sleeps
smaller
smallest
smallest,
so
source
spells
sr
standalone
state.
state:
stay
stays
still
still.
stood
str_replace('_',
string
string),
string,
stringly-false
strings
style.
style:
subset,
substitute
suffix
supported
surface:
surrounding
survived.
surviving
sweep
tab
tabular-nums'])
tag.
target
target:
tech
template
text
text,
text-[color:var(--color-wk-text-muted)]"
text-[length:var(--text-wk-xs)]
text-only
than
that
the
then
there
this
thousands
three
threshold
through
throwing.
throws,
tick,
tick.
ticking
ticks
time
timer
timer.
timestamp.
to
token:
tool
totalSeconds),
tracking-wider
transform:
transformation.
transition
translatable
translate-y-0"
translation,
trap
travel
tri-state
trim((string)
true
true)
true)));
true);
true,
true;
truncates.
truth.
truthy
two
two.
unbound-attribute
under
underscore
unit
unitPhrases:
units
units,
units.
unix
unknown
unnoticed.
unreadable.
uppercase
urgent
use
used
value
value,
value.
values
variable
variable,
variant
variant,
verbatim
version
view's
visible
warnSeconds:
was
were
what
whatever
when
whenever
where
wherever
which
whichever
while
who
whole
why
width
with
without
wk-countdown-pulse
wk-countdown-text-flash
words
would
wrapping,
written
wrong
x-data
x-data="wirekitCountdown({
x-for="(seg,
x-if="!
x-modelable="done"
x-show="expired"
x-text="expiredText"></span>
x-text="remaining.totalSeconds"></span>s.
x-text="segValue(seg,
x-text="srText"></time>
x-text="unitSuffix[seg.unit]"></span></span>
x-text="{{
x-transition:enter-end="opacity-100
x-transition:enter-start="opacity-0
x-transition:enter="transition
year
years
years'),
year|[2,*]
zero-units
{
{{
{{--
||
}
})"
};
}}
}}"
}}">
}},
}}:
}}[seg.unit]"></span>
—
→
⚠️
