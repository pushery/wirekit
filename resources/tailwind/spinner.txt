$attributes->class(['wk-spinner',
$attributes->getAttributes());
$colorClass
$colorClass,
$intent
$intent,
$intentColors
$intentColors[$resolvedIntent]
$label
$resolvedIntent
$scope);
$size
$size,
$sizeClass
$wrapperClasses
$wrapperClasses])
'')
'';
',
'accent'
'base',
'danger'
'h-4
'h-5
'h-6
'h-8
'info'
'inline-flex
'intent'
'intent',
'label'
'lg'
'lg',
'md'),
'md',
'neutral'
'primary'
'scope'
'size'
'size',
'sm'
'success'
'text-[color:var(--color-wk-accent)]',
'text-[color:var(--color-wk-danger)]',
'text-[color:var(--color-wk-success)]',
'text-[color:var(--color-wk-text-muted)]',
'text-[color:var(--color-wk-warning)]',
'warning'
'xl'
'xl']);
'xl'],
(!
($intent
($size)
(WCAG
(aria-hidden).
(h-4
(like
(not
(null)
(silent
(then
+
--}}
/
//
0
018-8V0C5.373
1.4.11,
12a8
12h4z"></path>
24
24"
3:1)
5.373
8
:
</span>
</svg>
<circle
<path
<span
<svg
=
===
=>
?
??
@endphp
@php
@props([
@props.
A
An
Color:
Declared
Default
Dev-only
FILL
Full
Fully
GRAPHIC
Glyph
INSTEAD
It
Measured
NOT
OUTSIDE
Optional
Pushery\WireKit\WireKit;
Renders
SVG
Screen-reader
Set
Tailwind
Tailwind's,
The
Validate
Visual
WireKit
WireKit's
WireKit::resolveClasses('spinner',
WireKit::validateProp('spinner',
[
['sm',
\Pushery\WireKit\WireKit::warnUnknownProps('spinner',
])
])),
];
__('wirekit::Loading'),
`wk-spinner`
a
accent
accent-as-text
accent-text
accessibility
accessible
action
alias
allowed
an
and
animate-spin
animation
animation.
announced
any
arc
are
aria-hidden="true">
aria-live="polite"
array_filter([
array_keys($intentColors));
asserted:
auto-derived
badge
base
be
below)
below),
block,
borrows
brand
button
by
cannot
class
class="animate-spin
class="opacity-25"
class="opacity-75"
class="sr-only">{{
color
color,
color.
colored
component's
component.
config('wirekit.components.spinner.intent',
config('wirekit.components.spinner.size',
configured
context
continuous
convention
could
currentColor,
currentColor;
cx="12"
cy="12"
d="M4
debug
decorative
default
defaults
developer
dimensions
does
drops
early.
element
element,
else
etc.).
even
every
exactly
exempts.
fallback
file
fill="currentColor"
fill="none"
flags
for
from
front
glyph
glyph.
guarantee
guard
has
helper
icon
if
implode('
imports
in
in_array($size,
indicator.
info
inherits
inline
inside
instead.
intent
interactive
interpolated)
into
invalid
is
isset($intentColors[$intent])
it
items-center
its
itself
justify-center',
keyframes,
label
later
library
links
list
literal
live
loading
makes
mapped
maps
marker
match
matches.
may
must
n/a
name
name,
near-black,
no
non-text
not
null
null),
null,
null/''
of
on
one
one.
optimistic-ui:
override
override.
own
placed
polite
presentational
prod).
props
purpose:
qualified:
r="10"
rather
raw
reach
readable
real
reason
reduced-motion
refutes
region.
region;
removable
renders
repoint
resolveClasses
resolves
restyled
result
returns
ring
role="status"
rotation
rule
same
scaled
scanner
sees
selects,
semantic
shown
sits
size
sizes
so
spinner
spinner,
spun
standalone
state
string,
string.
strings
strip
stroke-width="4"></circle>
stroke="currentColor"
structural
stylesheet
stylesheet's
success/warning/danger
surrounding
text
than
that
the
them.
themeable
there
this
though
to
token
token.
tokens)
true))
unknown
up
use
use;
uses
uses,
utility.
value
vanish
via
view's
viewBox="0
visually-hidden
w-4
w-4',
w-5',
w-6',
w-8',
which
who
whose
with
would
xmlns="http://www.w3.org/2000/svg"
{
{{
{{--
||
}
};
}}"
}}</span>
}}>
—
