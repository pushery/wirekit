$ariaLabel
$attributes->getAttributes());
$attributes->merge(['style'
$attributes->only('class')
$classes
$label
$label;
$listLabel
$muted
$scope);
$slot
',
'ariaLabel'
'base',
'false'
'gap-[var(--space-wk-lg,1.5rem)]',
'grid
'grid-cols-2
'label'
'list-none',
'list-style:
'muted'
'scope'
'wk-logo-cloud
'wk-logo-cloud',
("Trusted
(it
(silent
--}}
//
0;
0;'])->except('class')->class([$classes])
:
</div>
</p>
</ul>
<div
<p
<ul
=
=>
>
?
??
@endif
@endphp
@if($label)
@if($listLabel)
@php
@props([
@props.
A
Accessible
Blade
BooleanProp::from($muted,
DOES
Declared
Dev-only
Drain
Falls
Fully
Measured
Normalized
Optional
Purely
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
Rendered
Renders
Styling
Tailwind
The
They
UNBOUND
WITHOUT
WireKit::resolveClasses('logo-cloud',
[
\Pushery\WireKit\WireKit::warnUnknownProps('logo-cloud',
])
]),
`list-none`
`prop="false"`
a
about
above
action
against
alone.
alt
an
and
announced
any
arbitrary
are
aria-label="{{
as
as,
asserted:
at").
attribute
auditor
auto-derived
back
bare
be
being
block,
brand
build,
by
call
cannot
carries
cast
children
claim
claimed.
clashing
class
class="mb-[var(--space-wk-md,1rem)]
color
color.
colors
comments,
companies,
company's
compete
compiles
component
component's
could
data-wk-logo-cloud
data-wk-prose-skip
dead
debug
default
deliberately
depends
developer's
dist
dist/wirekit.css
dist/wirekit.css.
docs
does
drift
each
early.
element,
emission.
emit
every
feature
file
filter
flags
flips
for
from
full
guard
hangs
here
here:
hover.
how
identity
iframe
implied
implode('
imports
in
including
inline
interactive
is
it
items-center
its
itself
itself.
justify-items-center',
label.
later
layout
lead-in
learns
lg:grid-cols-5',
list
list-none:
list-style
live
lives
load
logo
logos
makes
many
margin:
marker
may
mean
n/a
name
names
need
never
no
none;
not
nothing
null,
of
on
on.
one.
one:
opposite
optimistic-ui:
out
own
own.
padding:
page's
presentational
previews
prod).
prop's
props
prose
qualified:
rather
reach
readable
reads
real
reason
redundant
refutes
renders
resolve).
result
return
role="list"
row
sandbox
scans
see
seeing
shown
silently.
site
sm:grid-cols-3
so
someone
source
still
string,
stylesheet
teams
text
text,
text-[color:var(--color-wk-text-muted)]">
text-[length:var(--text-wk-sm)]
text-center
than
that
the
there
this
to
tokens
trace
treatment
true);
true,
truthy
unknown
use
used
uses.
utility
variant,
view's
visual:
wall
wall,
was
what
which
who
whose
why
with
wk-logo-cloud-muted'
word
would
written
{{
{{--
}}
}}"
}}>
—
