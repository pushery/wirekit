!
!!};
!!};@endif
!==
"<family>
"local('".$n."')",
$attributes->getAttributes());
$css
$css,
$customFallbacks
$customFallbacks[]
$displayMismatch
$fallback)
$fallback['family']
$fallback['src']
$family
$family));
$file
$fontConfig
$fontConfig['mono']
$fontConfig['sans']
$fontConfig['serif']
$fontDisplay
$fontDisplay);
$fontHref
$fontHref($monoPreset)
$fontHref($sansPreset)
$fontHref($serifPreset)
$locals
$locals)),
$m)
$monoPreset
$monoPreset->fontFamily()
$n):
$name)),
$nonce
$overrides
$overrides,
$preloadHref
$preloadHref($sansPreset)))
$preloadSans
$preset
$preset)
$preset):
$property
$published
$publishedCss
$safeFamily
$safeFamily,
$sansPreset
$sansPreset->fontFamily()
$serifPreset
$serifPreset->fontFamily()
$source
$spec)
$spec['ascentOverride']
$spec['descentOverride']
$spec['lineGapOverride']
$spec['sizeAdjust']
$v
$v)
$value
$value)
$version
$warnMissing
$wkNonce
&&
''
'')
'',
',
'Cascadia
'Fira
'ascent-override'
'descent-override'
'family'
'line-gap-override'
'nonce'
'overrides'
'size-adjust'
'src'
'ui-serif,
'unsafe-inline'
'{{
(!
($fontConfig['fallbacks']
($fontConfig['mono']
($fontConfig['sans']
($fontConfig['serif']
($fontDisplay):
($locals
($name):
($preloadSans
($safeFamily
($spec['local']
($v):
('{{
((array)
(?FontPreset
(FontPreset
(`/wirekit/fonts/...`),
(`Inter-Regular.woff2`,
(array)
(file_exists($published))
(null
(preg_match("/src:\s*url\(['\"]?([^'\")]+\.woff2)['\"]?\)/",
(silent
(string
(string)
)));
*
*/
-->
--font-wk-serif:
--force
--}}
-apple-system.
/*
//
1)
1);
2
400
:
:root
<!--
</style>
<link
<number>
<percentage>
<style
<style>
=
===
=>
?
??
?string
@endforeach
@endif
@endphp
@font-face
@foreach($customFallbacks
@foreach($fallback['overrides']
@if(!
@if($displayMismatch($monoPreset))
@if($displayMismatch($sansPreset))
@if($displayMismatch($serifPreset))
@if($monoPreset)
@if($monoPreset)--font-wk-mono:
@if($sansPreset)
@if($sansPreset)--font-wk-sans:
@if($serifPreset)
@if($warnMissing($monoPreset))
@if($warnMissing($sansPreset))
@if($warnMissing($serifPreset))
@if($wkNonce)nonce="{{
@php
@props([
@props.
A
AND
AS
Blade's
Both
But
CONFIGURED
CSP
CSS
Cache-busted
Code',
Custom
Declared
Dev-only
Escaping
Every
FAMILY
FILE.
FIRST
Fallback"
Fallback';
Font
FontCss::display();
FontCss::matchesConfiguredDisplay((string)
FontRegistry::get($fontConfig['mono'])
FontRegistry::get($fontConfig['sans'])
FontRegistry::get($fontConfig['serif'])
Fully
Georgia,
HTML
Helper:
Inter
It
LCP
LOOKS
Left
Level
Livewire
Measured
Metric-matched
NAME
NOT
NOTHING,
Narrowed
Not
Nothing
Now
ONLY
OTHER
OWN
Omitting
On
Only
PHP
PHP.
PUBLISHED
Parsing
Pass
Preloading
Properties
Pushery\WireKit\Fonts\FontCss;
Pushery\WireKit\Fonts\FontPreset;
Pushery\WireKit\Fonts\FontRegistry;
Read
Registers
Regular
Renders
Resolve
Run:
SHORTER
STATIC
Serif
So
TWO
That
The
This
Those
URL
URL,
VALIDATED
Vite
WHEN
WireKit:
WireKit::cspNonce().
Without
[
[])
[]);
[];
\Pushery\WireKit\WireKit::cspNonce();
\Pushery\WireKit\WireKit::warnUnknownProps('fonts',
])
],
];
_-]/',
__DIR__.'/../../fonts/'.$preset->cssFile;
`&#039;`
`--font-wk-mono`
`--font-wk-sans`
`--font-wk-serif`.
`105%;
`:root`
`;`,
`<link
`<style>`
`@wirekitStyles`
`FontSystemTest`
`VT323-Regular.woff2`),
`composer
`crossorigin`
`dist/wirekit.css`
`size-adjust`
`src:
`src`
`swap`
`ui-monospace,
`vendor:publish`
`wire:navigate`,
`{`
`{{
`}`
a
about
above
above,
action
activated
actually
after
against
alive
all
all,
already
also
always
an
and
and,
answering
answers
any
anyway
anywhere
application
application's
are
argument
array
array_filter([
array_map(static
array_values(array_filter(array_map(
artisan
as
as="font"
ascending
asked
asks
assembled
asserted:
assertion.
asset($file)
asset($preset->publishedCssPath()).'?v='.filemtime($published);
at
auto-derived
back
baked
bandwidth
be
beats
because
before
being
below.
better
binding
block
block,
block.
body
bool
box
brace
break
breaks
browser
builds
bundled
but
by
bytes
bytes.
cache
cache-busting
cache.
came
can
cannot
capability
carries
carry
cascade.
case
category
category,
category.
changes
choice
choice:
classic
cleaned
comes
comment
comparison
competes
component
component's
computed
config
config('wirekit.fonts',
configured
configured)
consuming
contain,
container
continue;
copies
copy
copy,
correct
costs
could
crossorigin>
database.
day
debug
declaration
declares
decode
deep:
default
default.
delete
deliberate
developer
developer's
did
did.
different
differently-keyed
direction
directive
directive,
directly
dirname($preset->publishedCssPath()).'/'.basename($m[1]);
disagree.
discard
discarded.
discovered
display:
documented
does
doing
download.
drop
dropped,
drops
drops.
early.
either
element
element,
else,
emit
emits
emitted
emitted,
ends
entities,
entry
environment:
escaped
escaped.
escaping
even
ever
every
exactly
exception,
exists
exists,
explaining
explicitly
face
face,
face.
faces
fact
fails:
failure
fallback
fallbacks
falls
false;
families
families.
family
fast
faster.
fell
fetch
fetched
fetches
file
file's
file,
file.
file_exists($published)
file_exists($publishedCss))
file_exists(public_path($file))
file_exists(public_path($preset->publishedCssPath()));
file_get_contents($published),
file_get_contents($publishedCss);
filemtime($source)
files
filtered:
finds
first
flags
fn
font
font,
font-display
font-display,
font-family
font-family:
fonts
fonts.
for
for,
for.
foreach
found
four
from
from:
function
gains
generated
generator.
given
got
gradual
guard
guessed
hand,
hands
hardcoded
has
head
header
header.
here,
here:
however
href="{{
identical
if
ignore
ignored
immutable
implode(',
imports
in
included.
inert
inline
input.
inside
installed
installed.
instead
integration
interactive
into
invalidates
invented:
is
is_array($spec))
is_file($source)
it
it,
it.
it:
its
itself
job
job.
joins
just
keep
keeps
key,
key:
last
later
layout
leads
leads.
learns
leave
left
lets
like
line.
list
literal
live
load
loaded
loads,
local
locally,
look
looks
loses
lost
makes
may
means
measured
measurements.
merges
metric-matched
metrics
mints
mistake:
mono
monospace
monospace`
monospace`,
month's
moves
mtime
mtime,
n/a
name
named
names
narrowed
narrowing
navigation,
needs
neither.
never
new
no
nobody
nonce
nonce="{{
nonced
none
not
not,
nothing
nothing.
now
nowhere.
null
null)
null,
null;
numbers
occupies
of
of.
omitting
on
on,
on.
once
one
one-year
one.
only
open;
opposite
optimistic-ui:
optional
or
order
ordering
other
others.
otherwise
out
out,
over
overrides
own
own.
package
page
page's
page,
painted
paints
paragraph
parse
parses
pass
passes
path
path,
paying
per
php
pins
place
plain
point.
position.
precisely
preg_match('/^-?(?:\d+\.?\d*|\.\d+)%?$/',
preg_replace('/[^A-Za-z0-9
preload
preload.
presentational
preset
preset's
presets
previous
primary
prod).
produce
production
project
props
public_path($preset->publishedCssPath());
publish
published
published.
published?
publishes
qualified:
query,
question
quote
quote,
ran
rather
re-publish
reach
reaches
reader
reading
reads
reads,
reason
refuses.
refutes
rel="preload"
rel="stylesheet">`,
rel="stylesheet"@if($wkNonce)
remove.
renders
replaces
request
request()->headers->has('X-Livewire-Navigate')
requests
resolves
response
restate
result
return
right
right,
round
route
route-served
route.
rule
rule,
rule.
safe
same
same-origin
sans
sat
say
says
screen,
scripts,
second
second,
see
self-hosted
semicolon
serif
serif'
served
server
serving
serving:
setup
shape
shift
shipped
ships
shown
shows
silent:
silently
sink
so
soften
someone
something
specificity,
split,
src:
stack
stand-ins
stand;
standing
stands,
static
stays
still
stopped
stops
straight
string
style-src:
stylesheet
stylesheet's
stylesheet:
stylesheets
substitutes
such
suffix
surface
swap
system
take:
tells
test
text
than
that
that,
the
then
there
therefore
these,
they
thing
this
this,
those
three
through
throws,
time
time();
time.
to
to.
token
tool
touching
transition
trim((string)
trip,
trips
true
trusting
turn
twenty-one
two
type="font/woff2"
typography
unconditionally,
unconfigured
undefined
under
unknown
unlayered
unpublished
unpublished-font
untouched.
up
update`
update`.
url('wirekit/fonts/'.$preset->cssFile).'?v='.$version.'-'.$fontDisplay;
url(...)`
use
use,
use.
used
user
uses.
value
values
vendor:publish
verbatim,
verbatim.
view's
waiting
was
way
way,
web
weight
weight's
well,
were
what
when
where
whether
which
whichever
while
who
whole
whose
why
wirekit.fonts.display
wirekit:publish-fonts
with
without
won:
works,
worse
worse,
worst
would
write
wrong
year,
yet
{
{!!
{`
{{
{{--
}
};
}}
}}"
}}">
}}"@endif
}}"@endif>
}}'
}}')
}}:
}};
}}`
—
⚠️
