$attributes->class([$classes])
$attributes->getAttributes());
$classes
$scope);
'base',
'grow',
'scope'
(silent
--}}
//
<div
=
=>
@endphp
@php
@props([
@props.
Declared
Dev-only
Fully
Measured
Pushery\WireKit\WireKit;
Renders
WireKit::resolveClasses('spacer',
\Pushery\WireKit\WireKit::warnUnknownProps('spacer',
])
a
action
any
aria-hidden="true"></div>
asserted:
auto-derived
be
block,
component's
could
debug
does
early.
element,
file
flags
for
from
guard
imports
in
interactive
is
later
list
live
may
n/a
no
not
null,
one.
optimistic-ui:
presentational
prod).
props
qualified:
rather
reach
reason
refutes
renders
result
shown
so
than
that
the
there
this
unknown
use
view's
which
whose
{{
{{--
}}
—
