!
!$name)`,
!==
"/".
"Acme
$attributes->except('rel')->class([$classes])
$attributes->get('rel',
$attributes->get('target',
$attributes->getAttributes());
$attributes->has('aria-label');
$attributes->merge()
$bpBlock
$bpHidden
$bpInlineFlex
$classes
$computedRel
$darkLogo
$darkLogo)
$finalRel
$hasVisibleName
$href
$isLink
$logo
$logoAspect
$logoAspect)
$logoAspect).';
$logoOnlyNeedsLabel
$mobileBreakpoint,
$mobileLogo
$mobileLogo)
$name
$opensNewTab
$relAttr
$relAttr;
$resolvedBreakpoint
$scope);
$size
$size)
$size))
$size,
$slot
$slot->hasActualContent();
$targetAttr
$wkLogoAspect
$wkLogoFloor
$wkLogoHeight
$wkLogoHeight.'
$wkLogoHeightClass
$wkLogoHeightStyle
$wkLogoHeightStyle.$wkLogoAspect.'min-width:
$wkLogoHeightStyle.$wkMobileAspect.'min-width:
$wkLogoLength
$wkLogoStyle
$wkMobileAspect
$wkMobileStyle
&&
'
''
'');
'',
'';
',
'.$wkLogoFloor.';';
'.$wkLogoLength.';
'.e($logoAspect).';
'.e($mobileLogoAspect
'/',
'2rem',
';
'_blank');
'a
'a'
'aspect-ratio:
'base',
'darkLogo'
'flex
'gap-[var(--gap-wk-sm)]',
'h-8',
'h-[var(--size-wk-lg)]',
'h-[var(--size-wk-md)]',
'h-[var(--size-wk-xs)]',
'height:
'href'
'lg'
'lg',
'lg:block',
'lg:hidden',
'lg:inline-flex',
'logo'
'logoAspect'
'md'
'md',
'md:block',
'md:hidden',
'md:inline-flex',
'mobileBreakpoint'
'mobileBreakpoint',
'mobileLogo'
'mobileLogoAspect'
'name'
'no-underline',
'noopener')
'scope'
'size'
'size',
'sm'
'sm',
'sm';
'sm:block',
'sm:hidden',
'sm:inline-flex',
'span'
'text-[color:var(--color-wk-text)]',
'var(--size-wk-lg)',
'var(--size-wk-md)',
'var(--size-wk-xs)',
'wk-brand',
'xl'
'xl'.
'xl'])
'xl:block',
'xl:hidden',
'xl:inline-flex',
'xs'
($mobileLogoAspect
($relAttr
($resolvedBreakpoint)
($size)
($wkLogoLength
(0,1,1).
(0,2,0)
(1.5,
(640px)
(`alt=""`
(`merge()`
(`sm:inline-flex`
(a
(alt=""
(an
(back-compat).
(byte-identical
(fragile).
(no
(or
(silent
(string)
(the
(typical
(via
+
-
--}}
.
/
//
/>
0
0,1,0
1
1.75rem']);
2,
2.5
28px
32px
3rem),
55px,
:
</span>
</{{
<a>'s
<img
<img>
<img>:
<img>s
<span
<span>
<{{
=
===
=>
>
?
?:
??
@elseif
@elseif($logo
@elseif($logo)
@endif
@endphp
@if
@if($computedRel)
@if($isLink)
@if($logo
@if($logoOnlyNeedsLabel)
@if($name)
@if($opensNewTab)
@if($slot->hasActualContent())
@php
@props([
@props.
A
ALONGSIDE
AND
Accessibility
Additive:
Almost
Auto-inject
Blade
Blueprint
Both
Branch
Brand
CSS
CSS-first
Caller-provided
Children
ComponentSlot
Composes
Console"
DEFAULT,
DOM.
Dark-mode
Declared
Default
Defaults
Dev-only
EXPLICITLY,
Emitting
Empty
Every
FIRST
FULL
Fully
HTML
IndieAuth/Mastodon
Intrinsic
It
LINK
Measured
Mode-aware
Mode-neutral
NAMED
NO
Navigation
Not
Null
Numbers
ONE
On
Only
PHP
Pre-fix
Pushery\WireKit\WireKit;
Rendered
Responsive
Responsive-logo
SR
SVG
Same
Same,
See
Splitting
TEXT
TWO
Tailwind
The
This
URL
URL-string
Unset
Until
Validates
Values:
WIDTH
We
When
Where
Whole
WireKit
WireKit::resolveClasses('brand',
WireKit::validateProp('brand',
Without
[
['sm',
['xs',
[null,
\Illuminate\View\ComponentSlot)
\Pushery\WireKit\WireKit::warnUnknownProps('brand',
])
]),
__('wirekit::(opens
__('wirekit::Home')
`!important`.
`"{$resolvedBreakpoint}:hidden"`.
`$colsMap`.
`$darkLogo`,
`$hasVisibleName`
`$logoAspect`
`$logo`
`$logo`.
`$mobileLogo`
`$mobileLogo`.
`$mobileLogo`:
`'4/1'`
`'width/height'`
`--size-wk-*`
`.dark`
`.dark`-scoped
`.wk-brand.wk-brand`
`.{prose-class}
`1.75rem`,
`:href="false"`
`<a
`<a>`
`<img>`
`<x-slot:logo>...</x-slot:logo>`
`@if(!$logo
`@source`
`alt=""`
`aria-hidden`
`aria-hidden`)
`aria-label="{{
`aria-label`
`aspect-ratio`
`avatar`
`dark:`
`dist/wirekit.css`
`em`
`false`
`h-8
`href`
`logo`
`multi-select`
`name`
`no-underline`
`null`:
`panelWidth`,
`px`
`rem`,
`sm`
`sm`.
`truthy-string`
`w-auto`
`width`/`height`
`wk-brand`
`wk-dark-only`
`wk-light-only`
`xs`
`{bp}:block`
a
a`
above
accessibility
accessible
accident
across
against
alone
already
already,
also
alt=""
always
an
and
announce
anti-prose-
anything
anyway.
app
appears
applies
are
aria-hidden="true"
aria-hidden="true")
aria-label
aria-label="{{
arrive
as
aspect
assembled
at
attribute
attribute,
attributes
authors
auto-derived
auto-inject
avatar
away.
axes
back
back-compat).
badge)
bag
be
be.
because
before
before.
behavior
being
below
below,
below;
beside
between
block,
both
branches
branches'
brand
brand-bar
breakpoint
breakpoint,
breakpoint.
breakpoint;
broke
browser
build
built
bump,
but
by
byte-identical,
bytes
caller
callers
can
cannot
carry
carrying
case
central
checked,
chevron,
children
class
class.
class:
class="font-[number:var(--font-wk-heading-weight)]
class="hidden
class="sr-only">{{
class="wk-dark-only
class="wk-light-only
class="{{
classes
classes:
clear
code
collapsed
colored
column
combine
combo
completeness
component
component's
components
computed
content
content),
content,
contract.
corpus,
costs
counts
crashed
custom
dark
data,
data-wk-prose-skip
debug
debug,
decided
declared
decoration:
decorative
default
default's.
default).
default,
defeats
derive
describe
design
design-token
desktop
destination.
deterministic.
developer
developer's
directly,
discoverable
dist/wirekit.css
dist/wirekit.css).
div
documented
does
does.
doesn't
doubled-class
drives
drop
dropdown/item.blade.php.
dropped
e.g.
eats
echoed
either
element
else
else.
elsewhere
empty
enum
enum;
every
everything
exactly
except('rel'):
exists
fails
fall
fallback
false)
false],
file
finds
fixed
flags
flips
floor
follows
footer
for
forced
from
from,
full
full-width
further
gate.
generated
generic
gives
goes
grid.blade.php's
group
guard
had,
handles
happens
has
have:
header
header's
height
height,
held
here
here.
hidden
hint
home
home,
href="{{
href>`
icon
icons.
if
image
image-src
images
img
imgs
implode('
imports
in
in_array($href,
inline
inner
input
inside
instanceof
instead
interpolated
into
invalid
is
it
it.
it:
items-center
items-center">
its
jumps.
keeps
known
later
later,
leaves
leaving
left
length
less
light
light/dark
light↔dark
like
line.
lines
link
link)
link,
link:
links
links.
list
literal
literals
live
lives
load
logo
logo's
logo-only
logo-only;
logo.
logos
longer
lose
loses
made
main
makes
mark
mark's
marker
markup
match
matches
may
means
mechanism,
min-width
missing
mobile
mobile-first
mode
mode,
mode-aware.
mode-neutral
moment
most
mutation.
n/a
name
name,
name.
name:
named
names
navigation
navigation,
needs
never
new
no
no-op.
non-class
noopener
noreferrer"
noreferrer')
not
nothing
nothing.
now.
null
null)
null);
null,
null;
of
off
often
on
once.
one
one,
one.
one:
only
only:
only;
onto
optimistic-ui:
or
order
ordinary
other
otherwise,
out
output
outside
over-reservation.
overrides
own
own.
package's
page
page,
pair
passed
path
pattern:
place.
placed
precedent
preg_match('/^\d+(?:\.\d+)?(?:rem|em|px)$/',
presence
presentation
presentation,
prod
prod).
product
profile
prominent
prop
props
prose
prose-stylesheet
provided
px
qualified:
rail
rail's
rather
ratio
raw
reach
reaches
readers
reads
reappears
recognized
refused
rel="me"
rel="noopener
rel="{{
render
rendered
renders
replace
replaced
reservation
reserves
resolve
resolved
resorting
responsive
responsive-logo
rest
root,
routinely
row
rule
rule).
runtime
said
same
say
scale
scale.
scan
scanned
scanner
screen
selector
set
set,
sets
shape
shape.
share
shift
shift:
ships
show/hide
shrink-0',
sidebar
sidebar.
silently
since
single
single-logo
sits
size:
slot
sm+
so
something
space,
span
specificity
square
sr-only
src="...">
src="{{
statically
stays
still
stops
str_contains($relAttr,
str_contains($targetAttr,
strictness
string
string)
string.
stringified
strings
strings,
style
style="{{
stylesheet
styling
such
suite
suppressed
swap
swap.
swap:
swaps
tab
tab)')
tags:
take
taken
taking
tall,
tallest
target.
target="_blank".
targets
template
test
text
text-
text-[length:var(--text-wk-lg)]
text.
than
that
the
their
them
them.
then
there
thing
this
throw
thrown
tie
time)
to
toggle
tokens
too,
trailing
treats
trim($relAttr
trim((string)
true);
turns
two
typically
typography
uncached
under
underline
unknown
unless
untouched.
usable
use
used
using
utility
v4's
value
value.
values
vanishes
variable,
variant
vendor
verification
via
view
view's
view.
viewports.
visibility
visible
vs
w-auto`
w-auto`:
was
was,
way
way.
whatever
when
whenever
where
which
whole
whose
wide
wider
width
widths
will
wins
with
without
wk-dark-only
wk-dark-only)
wk-light-only
wk-rail-hide">{{
word
wordmark
wordmark.
workspace-switcher
worst
would
wrapper
wrapping.
writing
written
yet
{
{{
{{--
||
}
};
}`)
}}
}}"
}}"`
}}</span>
}}>
}}w-auto
}}w-auto"
—
→
