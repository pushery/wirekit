!
!==
"invalid"
"none
$announceError
$announceErrors
$attributes
$attributes->except(['announceErrors',
$attributes->except(['id',
$attributes->get('aria-describedby'),
$attributes->get('name');
$attributes->get('required',
$attributes->getAttributes());
$boxClasses
$describedBy
$error
$errorMessage
$errorMessage)
$errors
$fieldGroup
$fieldGroup,
$groupError
$groupError,
$groupOwnsBagEntry
$hasError
$hideLabel
$hint
$id
$id.'-error'
$id.'-hint'
$id;
$indeterminate
$isInvalid
$label
$labelClasses
$name
$optimistic
$optimistic,
$optimisticArgs),
$optimisticConfig
$rawName
$rawName,
$scope);
$scope,
$sizing
$sizing,
$slot
$textClasses
$variant,
$variantValue
$wkFieldSet
$wkRequiredMarker
&&
'
'#'.$id.'-error',
''
',
'action'
'announce-errors',
'announceError'
'args'
'aria-describedby'])->class(['peer',
'base',
'bg-[var(--color-wk-bg-input)]',
'border-[length:var(--border-wk-width)]',
'border-[var(--color-wk-border-strong)]',
'card'
'card']),
'checkbox',
'checkbox-');
'cursor-pointer',
'debug'
'default'
'default'),
'default',
'duration-[var(--transition-wk-duration)]',
'error'
'errorRegion'
'false'
'group
'groupError'
'hideLabel'
'hint'
'indeterminate'
'label'
'label',
'lg'
'md'),
'messages'
'mode'
'name',
'optimistic'
'optimisticArgs'
'peer-checked:bg-[var(--color-wk-accent)]',
'peer-checked:border-[var(--color-wk-accent)]',
'peer-checked:peer-hover:border-[var(--color-wk-accent)]',
'peer-disabled:cursor-not-allowed',
'peer-disabled:opacity-[var(--opacity-wk-disabled)]',
'peer-focus-visible:ring-[length:var(--ring-wk-width)]',
'peer-focus-visible:ring-[var(--color-wk-ring)]',
'peer-focus-visible:ring-offset-[length:var(--ring-wk-offset)]',
'peer-focus-visible:ring-offset-[var(--color-wk-ring-offset)]',
'peer-hover:border-[var(--color-wk-border-strong-hover)]',
'peer-indeterminate:bg-[var(--color-wk-accent)]',
'peer-indeterminate:border-[var(--color-wk-accent)]',
'peer-indeterminate:peer-hover:border-[var(--color-wk-accent)]',
'pending'
'reject',
'relative
'reverted'
'rounded-[var(--radius-wk-sm)]',
'scope'
'size'
'sm'
'sr-only'])
'text',
'text-[color:var(--color-wk-accent-fg)]',
'text-[length:var(--text-wk-md)]
'transition-colors',
'true'
'value'
'variant'
'variant',
'w-4
'w-5
'w-6
'wk-field-set']);
'wk-touch-target',
'wkFieldSet'
'wkFieldSet',
(!
("tags[]")
($attributes->get('checked')
($errors
($fieldGroup?->covers($name)
($fieldGroup?->isInvalid($errors
($groupError
($groupOwnsBagEntry
($isInvalid)
($size)
($variant)
(@aware
(WCAG).
(`min-w-0
(`x-init="$el.indeterminate
(a
(aligned
(bool)
(card
(consumes
(in
(inline
(inline-flex)
(kept
(links,
(measured:
(nested
(no
(silent
(ten
(the
(w-full
(when
);
+
--}}
-2/-3
.=
.peer
.peer)
.peer,
.sr-only,
/
//
/>
0
12h14"
12px
13l4
155x20
2.5.8's
2.75rem
20px
24
24"
2×
44px
4L19
7"
:
:checked
:disabled
:focus-visible
:has()
:indeterminate
</div>
</label>
</span>
</svg>
<div
<input
<input>
<label
<label>
<label>,
<p
<path
<span
<svg
=
===
=>
>
?
??
??=
@aware(['announceErrors'
@elseif($hint)
@elseif($label)
@endif
@endif>
@endphp
@if($announceError)
@if($describedBy
@if($hasError
@if($isInvalid)
@if($optimisticConfig)
@if($slot->hasActualContent())
@php
@props([
@props.
A
A11y:
AFTER
ALWAYS
Alpine
Announced
Array-style
BETWEEN
BOTH
BOX,
Blade
BooleanProp::from($groupError,
BooleanProp::from($hideLabel,
BooleanProp::from($indeterminate,
BooleanProp::stripFalseHtmlFlags($attributes);
Both
CLASS
Card
Change
CheckboxToggleShiftTest).
Checkmark
DEFAULT
DOM
Declared
Dev-only
Error
Extra
FieldGroup::describedBy,
GDPR
HERE
HTML
Hanging
IS
In
Indeterminate
Invalid
It
Its
LATER
Laravel
Lenient
Livewire
Livewire-compatible.
Measured
Mirrors
NOT
Native
No
Normalized
Note
Null
ON
ON.
ONE
ONLY
ONLY,
OR
OUTSIDE
PLUS
PRESENCE,
Passing
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
ROUTED
Read
Read,
Render
Rendered
Resolve
SEAM,
SOME
SVG
Set
Shared
Siblings
Size
Slot-based
So
Strip
Support\DomId;
THE
THROUGH
TOP
Take
That
The
These
They
Two
UNBOUND
VARIANT
Visual
WHICH
What
Whether
WireKit
WireKit::resolveClasses(
WireKit::resolveClasses('checkbox',
WireKit::validateProp('checkbox',
WireKit::warnUnknownProps('checkbox',
[
['default',
[],
\Pushery\WireKit\Support\AlpinePayload::from([
\Pushery\WireKit\Support\DomId::unique($attributes->get('id')
\Pushery\WireKit\Support\FieldGroup
\Pushery\WireKit\Support\FieldGroup::describedBy(
])
]),
]);
],
__('wirekit::Could
__('wirekit::Saving'),
`$variantValue`
`--text-wk-md`
`:group-error="false"`
`<input>`,
`<label>`,
`@aware`
`@props`
`args`
`base`
`card`
`class="flex
`covers()`
`disabled="false"`
`el.indeterminate`
`flex-1`
`flex`
`group`
`hideLabel`
`hideLabel`.
`indeterminate`
`inline-flex`,
`name`
`peer
`prop="false"`
`required`
`resolveClasses`
`resolveClasses`,
`size`
`sr-only`
`target-size`
`true`.
`{{
a
able
above
absolutely-positioned
accent
accepts
accessible
action
adds
adjustable,
adopting
after
against
against).
agreed
align-top
align-top',
all
all.
already
already-tall
also
always
an
and
announce-error
announceErrors)
announced
announced.
announcement.
answers
any
appears
appended
application
application,
are
area
arguments
aria-atomic="true"
aria-describedby
aria-describedby:
aria-describedby="{{
aria-hidden="true"
aria-hidden="true">
aria-hidden="true">*</span>@endif</span>
aria-invalid="true"
aria-live="assertive"
aria-live="polite"
around
array_values((array)
arrives
as
as,
asked
association
at
attribute
attribute,
attribute.
attributes
auto-derived
axe
bag
bag,
bag.
bare
base,
baseline
baseline)
baseline,
be
became
because
becomes
been
before
behind
being
belongs
below
below,
beside
bg/border
bg/border).
block,
block-level
boolean
border
border-[length:var(--border-wk-width)]
border-[var(--color-wk-border)]
border-[var(--color-wk-border-error)]';
bordered
bordered,
both
box
box's
box,
branch
branches:
browser
build
buildable
builds
but
by
call
call,
call.
caller's
caller-supplied
calls
can
can't
cannot
capability
caption,
card
card's
card.
cards
carry
case
case,
cases
cast
causes
centered
change
changed,
changing
check
check/indeterminate
checkbox
checkbox's;
checkbox,
checkbox.
checkbox:
checkboxes
checked
checked,
checkmark
class
class="hidden
class="space-y-1.5
class="sr-only"
class="text-[color:var(--color-wk-danger-text)]
class="text-[length:var(--text-wk-sm)]
class="{{
clean
clickable
closer
closure,
code
column
combobox
commonest
compiles
component
component's
component,
component.
compound
config('app.debug'),
config('wirekit.a11y.announce_error',
config('wirekit.components.checkbox.size',
config('wirekit.components.checkbox.variant',
config.
confirmation,
conformance
conformant:
consent
consume
consumed:
consumes
container
content:
control
control's
control,
control.
controls
controls:
could
count
counter-check
counts
cursor-pointer
d="M5
dash
data-wk-indeterminate="{{
data-wk-optimistic-announcer
data-wk-prose-skip
debug
decision.
declared
decline
declining
dedup
default
delivers
dense
dense)
described
detection:
did
disables
display
does
drop
dropped
duplicate
duplicated
duration-[var(--transition-wk-duration)]
each
edge
edge,
edge.
either
either:
element
element.
element:
elsewhere)
emitted
emitted,
empty:
entry
error
error,
even
ever
every
exactly
exception
existed
explicit
exposed
factory
false
false));
false),
false);
false,
feature
field.set
field.set's
fill
fill.
fill="none"
first
fixed
flags
flex
flex-1`
flex-centered
flipped
flips
focus
focus-visible.
folding
for
for="{{
for>
form
formatting)
forms
free
from
full-width,
fully
gap
gap-2
gap-3
gated
get
give
gives
global
gray
group
group's
group's,
group's.
group-has-[:checked]
group-has-[:checked]:block
group-has-[:indeterminate]
group-has-[:indeterminate].
group-has-[:indeterminate]:block
group.
group:
grow
h-4',
h-5',
h-6',
h-full
h-full),
hand-mount
handler
hardcoded,
has
has-[:checked]:bg-[var(--color-wk-bg-subtle)]
has-[:checked]:border-[var(--color-wk-accent)]
has-[:focus-visible]:ring-[length:var(--ring-wk-width)]
has-[:focus-visible]:ring-[var(--color-wk-ring)]'
header
hears
here
here.
hidden
hide.
highlights
hint
hint/error
hit
hold.
hover
how
id
id.
id="{{
identical
if
implode('
in
independent
indeterminate
initial
inline-flex
inner
input
input.indeterminate
inside
instanceof
instead
instead,
into
invalid
is
isn't
it
it,
it.
it:
it;
items-center
items-start
its
itself.
justify-center
keep
keeps
kept
key
kills
label
label's
label)
label:
land
lands
layer
layout
leading-tight
leaves
left,
let
library
like
line
line-box
list
listens
literals.
live
lives
longer
make
makes
match
match-with-default
matching
mean
measured
merged
message
message,
message.
method
min-w-0"
mirrors
mode
morphs
ms-0.5"
must
mutation
name
name)
name.
name="{{
names
native
need
needs
needs.
neither
nested
never
new
newly
next
no
node,
none↔block
nor
not
nothing
null
null)
null)?->first($name));
null)?->has($name));
null,
null;
null])
occurrence
of
off
offsets
old
on
on.
once
once,
one
one.
ones.
only
onto
opposite
opt
optimistic
optimistic-ui:
or
order
other
our
out
out.
outrank
over
overlays
override
own
owns
p-0.5
padding
page-unique
parent
past
pattern).
peer-*).
peer-*.
peer-checked
peer-disabled
peer-focus-visible
per
personalization
placed
placement
plain
point
pointer-events-none
pointer.
points
polite
positions
practically
precedence:
primitive
printing
prod).
prop
prop's
prop,
property
props
pt-0.5',
pulling
purpose:
push
put.
px-[var(--padding-wk-x-md)]
py-[var(--padding-wk-y-md)]
question.
radio
ran
rather
re-initialize,
re-rounds
reach
reaches
reacts
read
reader
reads
recolored
redundant.
regardless
region
rejection
relative
remove
render
render,
rendered
rendered.
renders
repeat
replaces
reports
required
required.
reserve
resolve
resolve.
rested
rich
rides
right
right-aligned
ring
room
roughly
round
rounded-[var(--radius-wk-lg)]
routing
row
row's
row,
rows
rule
runs
runs.
same
satisfy.
save.
says
says,
scale
scopes
seam
second
select
select-none
selectable-option
selected
selected"
selectors.
sentence
separate
server
server-side
serves
sets
several
share
shared
shift
shifts
ships
should
show
shrink-0',
sibling
siblings
silently
silently.
simply
sit
site
sites
sitting
size
size.
sizes
slot
so
something
spacing
span
specificity,
spellings
spread
spreads
sr-only
sr-only'
sr-only`
start
starting
state
states
states.
stay
stays
still
stood
stray
stretch
string,
stroke-linecap="round"
stroke-linejoin="round"
stroke-width="3"
stroke="currentColor"
styling.
sub-pixel
submit
such
suffix,
supported
supports
surface
surrounding
survives
switch
table
tag
tag,
take
taps
target
target,
target:
targets
tell
template
text
text-[color:var(--color-wk-accent-fg)]"
text-[color:var(--color-wk-danger-text)]">{{
text-[color:var(--color-wk-text)]
text-[color:var(--color-wk-text-muted)]">{{
text.
textarea
than
that
that:
the
their
them
them).
theme-controller
then
there
therefore
they
third
this
three
three,
through
ticking
time
tinted
to
today,
together
toggle
toggle.
toggle/radio):
toggle:
too,
touch
trailing
transition-colors
trip
true
true"`,
true)
true);
true,
truthy
turn
two
type="checkbox"
unaffected.
unconditionally
under
undo,
undone.'),
unhide
unknown
unlike
unreachable,
up
upper
use
used
uses
validation
value
value,
value.
variant
variant.
variant:
variants
via
viewBox="0
violation
visibility
visible
visually
w-full
w-full"`
w/h.
wants
was
was.
way
way.
well,
went
what
when
which
while
who
whole
whose
wide
will
wire:model.live
with
words
would
wrapper
written
x-bind:aria-busy="isPending"
x-data="wirekitOptimistic({{
x-on:change="commitFromControl()"
x-ref="control"
x-text="announcement"></div>
x-wk-indeterminate
{
{{
{{--
||
}
};
}}
}}"
}}">
}}">{{
}})"
}}-error"
}}-hint"
}}</p>
}}@if($wkRequiredMarker)<span
}}`
}}{{
~0.5px,
—
