!
!==
"Announcement".
"changes
"dismissed"
"staging"
"this
$action
$attributes->class([$classes])
$attributes->class([$classes])->merge(['style'
$attributes->getAttributes());
$classes
$dismissKey
$dismissible
$dismissibleStated
$hasKey
$hue
$hue,
$hueMode
$hueValue
$intent
$intent,
$intentClasses
$intentClasses,
$intentValue
$isDismissible
$isSticky
$label
$persist
$persistValue
$persistValue));
$persistValue;
$persistsDismissal
$position
$position,
$positionValue
$scope);
$slot
$sticky
$stickyClasses
$stickyClasses,
$strip
$surface
$surface,
$surfaceValue
&&
''
'')
''),
'',
'';
',
'--wk-banner-hue:
'.$hueValue.';'])
'.');
'.',
'0'),
'base',
'bg-[color-mix(in_srgb,oklch(var(--wk-banner-hue-l)_var(--wk-banner-hue-c)_var(--wk-banner-hue,var(--theme-hue,264)))_12%,var(--color-wk-bg-elevated))]
'bg-[color-mix(in_srgb,var(--color-wk-accent)_12%,var(--color-wk-bg-elevated))]
'bg-[color-mix(in_srgb,var(--color-wk-danger)_12%,var(--color-wk-bg-elevated))]
'bg-[color-mix(in_srgb,var(--color-wk-success)_12%,var(--color-wk-bg-elevated))]
'bg-[color-mix(in_srgb,var(--color-wk-warning)_12%,var(--color-wk-bg-elevated))]
'bg-[oklch(var(--wk-banner-hue-l)_var(--wk-banner-hue-c)_var(--wk-banner-hue,var(--theme-hue,264)))]
'bg-[var(--color-wk-accent)]
'bg-[var(--color-wk-danger)]
'bg-[var(--color-wk-success)]
'bg-[var(--color-wk-warning)]
'bottom'
'bottom']);
'bottom'],
'danger'
'danger']);
'danger'],
'dismissKey'
'dismissible'
'false'
'flex
'hue'
'hue',
'info'
'info',
'intent'
'intent',
'label'
'persist'
'position'
'position',
'promo',
'px-[var(--padding-wk-x-lg)]
'scope'
'soft',
'solid'
'solid']);
'solid'],
'sticky
'sticky'
'strip'
'success'
'success',
'surface'
'surface',
'text-[length:var(--text-wk-sm)]
'theme')
'top',
'top';
'truncate'
'warning'
'warning',
'wk-announcement-banner',
($dismissibleStated
($hasKey
($hue
($intentValue)
($strip)
(--padding-wk-x-lg),
(`--theme-hue`)
(`--wk-banner-hue`)
(`py-0`
(accent),
(and
(below)
(float)
(indigo)
(is_numeric($hue)
(localStorage).
(same
(session-only),
(session-only).
(silent
(string)
(true)
(true)`:
(what
--wk-banner-hue:
--}}
/
//
/>
0
0-360:
12"
18L18
2,
24
24"
250,
264
300
360
360)
6M6
6l12
:
:root
:root,
</button>
</div>
</svg>
<=
<button
<div
<path
<span
<svg
=
===
=>
>
>=
?
??
@click="dismiss()"
@endif
@endisset
@endphp
@if($isDismissible)
@if($strip)
@if(filled($label))
@isset($action)
@php
@props([
@props.
A
AA
ABOVE
AND
Accessible
Alpine
An
BANNER
BE
BELOW
Blade
BooleanProp::from($dismissible,
BooleanProp::from($persist,
BooleanProp::from($sticky,
BooleanProp::from($strip,
CALLER
Composed
Content-Security-Policy
DEFAULT
DOM,
Declared
Dev-only
ELEMENT
EVERY
Eight
EnumPropsFallThroughTheValidatorTest
FILTER_VALIDATE_BOOLEAN);
For
Full
Fully
HUE
How
In
Independent
It
LANDMARK
LANDMARK.
LINE
Left
Lightness
MADE
Normalized
Not
Null
Null,
OFF
ON
ONE
OR
On
Only
Opts
PERSISTS
PURPOSE,
Page-edge
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
SAME
STICK
Same
Semantic
So
Somebody
Stated
Sticky
THE
TO
Text
The
Turn
UNBOUND
USED
WHICH
WITHOUT
Where
Whether
WireKit::resolveClasses('announcement-banner',
WireKit::validateProp('announcement-banner',
Without
['a
['promo',
['soft',
['top',
\Pushery\WireKit\Support\AlpinePayload::string('wk-banner:'.$dismissKey)
\Pushery\WireKit\WireKit::warnUnknownProps('announcement-banner',
])
])),
__('wirekit::Announcement')
__('wirekit::Dismiss')
`--theme-hue`
`--wk-banner-hue-c`,
`--wk-banner-hue-l`
`--wk-strip-inset`
`.env-staging
`250`
`300`
`__('wirekit::Announcement')`,
`app-shell
`badge`
`dismissible="false"`
`dismissible`
`false`
`filled()`;
`grid`
`intent="info"`
`match
`min`.
`oklch(…
`persist="false"`
`persist`
`position`
`promo`
`prop="false"`
`role="region"`
`soft`
`solid`
`solid`.
`theme`
`warning`
a
able
about
above
accent
accent,
accessible
accident.
across
affordance
against
alert
alert.
all
all,
already
also
always
ambient,
an
and
announcement
announcements
anything
apart,
application
applied:
applies
are
aria-hidden="true">
aria-label="{{
arm
array_filter([
as
as,
asked
at
attribute
attribute.
auditor
auto-derived
back
back,
back.
backdrop
badge's
badge).
banner
banner's
banner,
banners.
bar
bare
bars
be
beats
because
before
being
belongs
below
below,
below;
between
block,
blue
blue,
boolean
booleans,
both
bottom,
bottom-0
box.
break
bring
brings
browser
button
button,
by
call
called
caller's
can
cannot
carry
cast
catalog
centered
chain
change
changes
changes.
characters
choice,
chroma
chrome's
chrome,
claim
class
class="h-4
class="min-w-0
class="ms-auto
class="shrink-0">{{
classes)
clickable
close
color
come
comes
compiles
component
component's
component:
composed
composition
computed
constants
content,
content-edge
contract
contrast-paired
control
cursor-pointer
custom
cut
d="M6
data-intent="{{
data-position="{{
data-replayable="true"
data-wk-announcement-action
data-wk-announcement-banner
data-wk-announcement-content
data-wk-announcement-dismiss
data-wk-viewport-strip
debug
declared,
default
default,
default.
degrees
deliberate
deliberately
demo
derivation
derivation.
derived
derived,
derived.
deserve.
design
developer
dialog.
dismiss
dismissal
dismissed-then-empty
dismissed:
dismissible
dismissible.
do
docs
does
done),
down
drift
duration-[var(--transition-wk-duration)]
each
edge
edge.
either
either,
else
else,
elseif
ends
enforce.
enum
enum,
environment
even
every
exactly
exempting
existing
exists
exists,
explicitly
expression
failure
fallback
false
false);
false,
false;
feature
fight
file
fill
fill,
fill="none"
filled($label)
fills
filter_var($persist,
filter_var($sticky,
find
first
first-class
fit
fixed
fixed,
flag.
flags
flashing
flips
floating
flows
focus-visible:outline-hidden
focus-visible:ring-2
focus-visible:ring-[color:var(--color-wk-ring)]"
folds
follows
font-[family-name:var(--font-wk-sans)]',
font-[number:var(--font-wk-heading-weight)]',
for
for.
foreground.
forgetfulness
forgets
form
formula
frame's
frame.
from
gap-[var(--gap-wk-sm)]',
gate
gated
generic
gets
gives
glyph.
gone
guards
h-[var(--wk-strip-height)]
had
harvest
has
has,
have
having
height
holding
house
hover:opacity-100
hue
hue.
identifies
if
ignore
implode('
imports
in
in_array($intent,
in_array($position,
in_array($surface,
inherit
inline
instance
instead
instead.
intent
intent's
intent,
intents
interpolated
interrupts
into
invalid.
invent
is
is,
it
it,
it.
items-center
its
itself
jump
justify-center
keep
keeps
key
know
labeled
landmark.
landmarks
last
later
left
let
lets
level:
like
line
line,
lines
list
literal
live
lives
lives.
load
load-bearing:
loads
loads.
localStorage.
makes
margin.
marketing
match
may
mean
meaning.
means
measure
measured
meets
mid-sentence.
modal
modality
monochrome
more
most
moves
must
n/a
name
name,
name;
named
names
needs
neither
never
never,
next
no
normalized
not
not.
not:
nothing
nothing"
notice
null
null,
null;
number
of
off
on
on.
once
one
one,
one.
only
opacity-70
opposite
opt-in
optimistic-ui:
options
or
ordinary
other
out
outlier.
over
own
owns
p-[var(--padding-wk-x-xs)]
padding
page
page-chrome
page.
page:
paging
paints
paints.
pair
pair:
passthrough
persist
persistKey:
persistence
pinned
pins
policy
positively
precedence
preset
preview
previously-dismissed
prod).
produced
promo
prop's
property
props
py-0
py-[var(--padding-wk-y-sm)]',
qualified:
ran.
rather
raw
re-mount
reach
read
reader
reader;
reading
reads
real
reappear
regardless.
region
regions
remembered
remembers)
render
replay/reset
resettable,
resolved
returning
role
role="alert":
role="region"
rounded-[var(--radius-wk-sm)]
rtrim(rtrim(number_format((float)
rule
safe
same
say.
saying
says
screen
screen-reader
script
sentence.
session-scoped
sets
several
shapes
shell
should
shrink-0
siblings
silently
silently.
since
site
sits
so
solid
something
specific
spine
stable
stacking
staging
staging",
state
stated
statement
statement.
stay
stayed
stays
stick
sticky
still
stored
string,
string.
strings
strip
strip's
strip;
stripping
stripping,
strips
stroke-linecap="round"
stroke-linejoin="round"
stroke-width="2"
stroke="currentColor"
stronger
strongly
style
styles,
styles;
stylesheet
such
supplied
surface
surfaces
survives
switch
switches
system
takes
tall,
tell
text
text-[color:var(--color-wk-accent-fg)]',
text-[color:var(--color-wk-banner-hue-fg)]',
text-[color:var(--color-wk-danger-fg)]',
text-[color:var(--color-wk-success-fg)]',
text-[color:var(--color-wk-text)]',
text-[color:var(--color-wk-warning-fg)]',
text.
than
that
the
them
them.
theme
theme']);
theme's
then
there
there:
they
this
those
three
through
tint.
tints
to
token
token,
tokens.
tone
top
top-0
top-[var(--wk-strip-inset,0px)]
transition-opacity
transparent.
trap:
treatment,
treatments
true
true)
true);
true,
true;
truthy
turned
turning
turns
two
type="button"
underneath
unguarded
unknown
unless
up
use
used
user
uses.
usual
validates
value
value,
var(--wk-banner-hue))`
via
view's
viewBox="0
viewport
viewport's
viewport,
viewport`
violet.
visible
visit
w-4"
w-full
waits
want
wants
warning
was
was.
way
what
whatever
when
where
whether
which
whichever
whole
whose
with
without
word
worse
would
writing
written
x-cloak
x-data="wirekitDismissible({@if($persistsDismissal)
x-show="shown"
xmlns="http://www.w3.org/2000/svg"
z-30',
z-[var(--z-wk-chrome)]
{
{{
{{--
||
}
})"
},
};
}`
}}
}}"
}}">{{
}}</span>
—
⚠
⚠️
