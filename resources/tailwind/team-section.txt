$attributes->getAttributes());
$attributes->merge(['style'
$classes
$label
$scope);
$slot
',
'base',
'grid
'grid-cols-2
'items-start',
'label'
'list-none',
'list-style:
'scope'
(it
(silent
--}}
//
0;
0;'])->class([$classes])
</ul>
<ul
=
=>
>
@endphp
@php
@props([
@props.
Accessible
DOES
Declared
Dev-only
Fully
Measured
Pushery\WireKit\WireKit;
Renders
Tailwind
The
WITHOUT
WireKit::resolveClasses('team-section',
[
\Pushery\WireKit\WireKit::warnUnknownProps('team-section',
])
]),
__('wirekit::Team'),
`list-none`
a
action
announces
any
are
aria-label="{{
asserted:
auto-derived
be
block,
boundary.
build,
class
component
component's
could
data-wk-prose-skip
data-wk-team-section
dead
debug
developer's
dist/wirekit.css
docs
does
early.
element,
file
flags
for
from
gap-[var(--space-wk-lg,1.5rem)]',
guard
how
iframe
implode('
imports
in
inline
interactive
is
it
later
lg:grid-cols-4',
list
list-none:
list-style
live
load
many
margin:
may
md:grid-cols-3
n/a
name
names
no
none;
not
null,
of
one.
optimistic-ui:
padding:
people
presentational
previews
prod).
props
qualified:
rather
reach
reader
reading
reason
redundant
refutes
renders
resolve).
result
role="list"
roster,
run
sandbox
screen
shown
so
than
that
the
there
this
tokens
unknown
use
view's
which
whose
why
with
{{
{{--
}}
}}"
—
