$attributes->class([$classes])
$attributes->getAttributes());
$classes
$scope);
$size,
$sizeClasses
$sizeClasses,
$slot
',
'base',
'bg-[var(--color-wk-bg-muted)]',
'border-[length:var(--border-wk-width)]',
'border-[var(--color-wk-border)]',
'border-b-2',
'font-[family-name:var(--font-wk-mono,ui-monospace,monospace)]',
'font-normal',
'inline-flex
'leading-none',
'lg'
'lg']),
'md'
'md',
'rounded-[var(--radius-wk-sm)]',
'scope'
'size'
'size',
'sm'
'text-[color:var(--color-wk-text)]',
'text-[length:var(--text-wk-md)]
'text-[length:var(--text-wk-sm)]
'text-[length:var(--text-wk-xs,0.75rem)]
($size)
(silent
--}}
//
<kbd
=
=>
@endphp
@php
@props([
@props.
Declared
Dev-only
Fully
Measured
Pushery\WireKit\WireKit;
Renders
WireKit::resolveClasses('kbd',
WireKit::validateProp('kbd',
[
['sm',
\Pushery\WireKit\WireKit::warnUnknownProps('kbd',
])
]),
a
action
any
asserted:
auto-derived
be
block,
component's
could
debug
default
does
early.
element,
file
flags
for
from
guard
implode('
imports
in
interactive
is
items-center
justify-center',
later
list
live
match
may
min-w-5',
min-w-6',
min-w-7',
n/a
no
not
null,
one.
optimistic-ui:
presentational
prod).
props
px-1
px-1.5
px-2
py-0.5
py-1
qualified:
rather
reach
reason
refutes
renders
result
shown
so
than
that
the
there
this
unknown
use
view's
which
whose
{
{{
{{--
};
}}</kbd>
}}>{{
—
