!==
"animation-delay:
"var(--motion-wk-delay-{$delay})",
$allowedBases
$allowedBases)
$allowedBases),
$allowedBases,
$allowedFull
$allowedFull);
$allowedFull,
$attributes->except('style')->merge(['data-replayable'
$attributes->get('style',
$attributes->getAttributes());
$baseClasses
$baseClasses])
$callerStyle
$delay
$delay,
$delay.'ms',
$delayValue
$duration
$duration,
$internalStyle
$internalStyle],
$mergedStyle
$once
$once,
$optionsJson
$p.'-in',
$p.'-out',
$preset
$preset,
$preset.'-in';
$preset;
$presetAliases
$presetAliases[$preset]
$s
$scope);
$slot
$threshold
$threshold,
$trigger
$trigger,
$validatedDuration
$validatedDuration,
$validatedPreset
$validatedTrigger
$validatedTrigger,
&&
'')
'')));
''),
'';
');
',
'-in'
'125ms'
';
'base',
'bounce',
'class'
'click',
'delay'
'delay',
'duration'
'duration',
'fade'
'fade-down'
'fade-in'
'fade-in');
'fade-left'
'fade-right'
'fade-up'
'fast'
'flip',
'lg',
'manual'
'manual']);
'manual'],
'md',
'none'/'sm'/'md'/'lg'/'xl'
'normal'
'normal');
'normal',
'once'
'preset'
'preset',
'rotate',
'scale',
'scope'
'slide-down',
'slide-down-in',
'slide-left',
'slide-left-in',
'slide-right',
'slide-right-in',
'slide-up',
'slide-up-in',
'slow'
'slow']);
'slow'],
'sm',
'spring'];
'threshold'
'trigger'
'trigger',
'true',
'var(--motion-wk-delay-{token})'
'viewport'
'viewport');
'w-full',
'xl']),
'xl'],
'zoom',
($p)
($s)
(150ms),
(300ms,
(600ms).
(IntersectionObserver),
(and
(auto-suffix
(bool)
(default
(developer
(e.g.
(float)
(in_array($preset,
(only
(raw)
(silent
(true)
(which
);
*
*/
+
--motion-wk-delay-*
--}}
/
/*
//
0
0..1.
0.4);
0ms"`)
1000ms"`.
11
11-preset
:
</div>
<div
<div>
=
===
=>
>
>=
?
??
??=
@endif
@endphp
@if($mergedStyle
@if-rendered
@php
@props([
@props.
A
ASCII
Alpine
Alpine's
Alpine.$dispatch('wirekit:reveal')).
AlpinePayload
Animation
BEEN
Blade's
CONTEXT,
CSP
CSS
Compose
DECLARED,
DID
DOCUMENTED
Declared
Default
Delay
Dev-only
Duration
Every
FIRST
Fully
HAS
HTML5
IS
IntersectionObserver
Its
JS
JSON_HEX_TAG
NOTHING.
No
Normalize:
One
Options
PROP
Per
Proven
Pushery\WireKit\WireKit;
Re-fire
SINGLE
Same
THAT
That
The
This
Trigger
Validate
WireKit::resolveClasses('reveal',
WireKit::validateProp('reveal',
Without
[
['fade',
['fast',
['none',
['reveal'
['viewport',
\Pushery\WireKit\Support\AlpinePayload::from([
\Pushery\WireKit\Support\AlpinePayload::string($validatedPreset)
\Pushery\WireKit\WireKit::warnUnknownProps('reveal',
])
]);
];
`<`
`<`,
`WireKit::scope('marketing',
`animation-delay`
`docs/customization.md`
`fade-*`
`fade-down`
`fade-left`
`fade-right`
`fade-up`
`resolveClasses()`
`reveal`
`scope`
`slide-*-in`
`style="--wk-stagger-step:
`style="animation-delay:
`style=…`
`u003C`.
`warnUnknownProps`
`{{
a
above
accept
adds
after
against
aliases
all,
already
an
and
animate
animation
animation-delay
animations.md
anticipate
any
applied
are
array_filter([$callerStyle,
array_map(fn
array_merge(
arrive
as
at
attribute,
attribute.
attribute:
attributes
auto-derived
back.
backslash
base
be
because
before
begins
begins.
block,
bool
both:
bounce,
browser
but
by
byte
call
caller
caller's
caller-passed
can
carrying
change
choice
chosen
class
class,
class.
client-only
combines
common
component
component's
component,
composes
composition
config('wirekit.components.reveal.delay',
config('wirekit.components.reveal.duration',
config('wirekit.components.reveal.once',
config('wirekit.components.reveal.preset',
config('wirekit.components.reveal.threshold',
config('wirekit.components.reveal.trigger',
consulted
convention
could
custom
debug
declared,
decoration.
default
default),
delay
delay="125"
designers
developer
different
direction)
directive
dispatches
does
does,
drop
dropped
drops
duplicate
effect,
element
emitted
entered
entrance
enum
enum,
equivalent
escapes
every
extension
fade
fade,
flag
flags
flip,
float,
fn
for
four
from
gallery
get
got
hand-picked
happens
harmful.
has
having
helper
here
if
immediately
imports
in
in-presets:
in/out
in_array($delay,
in_array($duration,
in_array($preset,
in_array($trigger,
inline
instead
int
internal
into
is
is_int($delay)
it
it,
keeps
later
learn
letters,
levels.
list
literal.
live
made
makes
maps
match
may
milliseconds
mistake).
mode:
moment
most
motion).
n/a
name
name.
naming
needed.
never
no
not
nothing
null
null);
null,
object
of
of:
on
once
one
one.
only
optimistic-ui:
or
our
override
owns,
parses
parsing,
passed
payload
personalization
picked
plugin
plus
presenting
preset
preview
prod).
prop
props
qualified:
rather
raw
reach
reason).
registered
registering
rendering
resolution:
resolve
resolver
rhythm)
roll
root,
rotate,
routed
same
scale,
scheme.
scope
scoped
second,
server
set
set.
shorthand
sibling
silence:
silent
silently
slide
slide-down,
slide-left,
slide-right,
slide-up,
so
spring
state
stop.
style
style="{{
suffix
supported
template
than
that
the
then
there
this
this,
threshold
through
to
today
token
token:
tokenizer
too.
total
trigger)
trigger='viewport').
trigger?
trim((string)
trim(implode(';
true
true)
true))
true);
two
universally
unknown
unnecessary
up.
use
used
user
validated
value
var
via
view's
viewport.
visual,
vocabulary
warning,
was
way
well
what
when
whenever
whether
which
who
with
without
wk-animate-{preset}
would
wrong
x-data
x-data="wirekitAnimate({{
zoom,
{
{$delayValue}"
{{
{{--
}
};
}}
}}"
}})"
}},
}}`
×
—
…])`
→
⚠️
