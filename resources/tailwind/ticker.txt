!==
"+"
"++8.4%"
"+8.4"
"+8.4%"
"-"
"8,4
"8.4"
"Change:
"Revenue"
"Trend
"up"
"{$sign}{$deltaText}";
"{$sign}{$deltaText}%"
$attributes->class([$baseClasses])
$attributes->getAttributes());
$baseClasses
$delta
$delta,
$delta;
$deltaAriaLabel
$deltaClasses
$deltaFormat
$deltaMagnitude
$deltaMagnitude]),
$deltaText
$footer
$formattedDelta
$intent
$label
$resolvedIntent
$scope);
$sign
$size,
$slot
$slot->hasActualContent())
$tickerId
$trendLabel
$value
$valueSizeClass
&&
'%'
'')
'';
''`
'+'
'+')
'+')).($deltaFormat
',
'base',
'danger'
'danger',
'delta'
'deltaFormat'
'flex
'font-[family-name:var(--font-wk-sans)]',
'intent'
'label'
'lg'
'lg']),
'md'
'md'),
'md',
'neutral',
'percent'
'percent',
'scope'
'size'
'size',
'sm'
'success'
'success',
'text-[color:var(--color-wk-danger-text)]',
'text-[color:var(--color-wk-success-text)]',
'text-[color:var(--color-wk-text-muted)]',
'text-[length:var(--text-wk-2xl)]',
'text-[length:var(--text-wk-lg)]',
'text-[length:var(--text-wk-xl)]',
'ticker-');
'trend'
'trendLabel'
'value'
("+8.4")
("8.4%"
($delta
($resolvedIntent)
($size)
(button
(float)
(silent
(string)
(true)
*-text
+
--}}
/
//
/>`
0
1.4.3
12
24
2rem-tall
:
:value',
<
</article>
</span>
<article
<article>
<span
<span>,
=
==
===
=>
>
?
??
@endif
@endphp
@if($deltaAriaLabel)
@if($formattedDelta
@if($trend
@if(filled($label))
@if(filled($trendLabel))
@if(isset($footer))
@php
@props([
@props.
A
Accept
Auto-derive
BOTH
Blade
CALLER's
Declared
Delta
Dev-only
English
Footer
Format
Fully
German
Given
It
Label
Measured
NOT
Names
Negative
One
Opt-in,
Optional
PHP,
PRUNES
Page-unique,
Prozent"),
Pushery\WireKit\WireKit;
Renders
SLOT
See
Support\DomId.
That
The
Through
Twenty-two
Value
WCAG
WireKit::resolveClasses('ticker',
WireKit::validateProp('ticker',
Without
[
['sm',
['value'
\Pushery\WireKit\Support\DomId::unique($attributes->get('id'),
\Pushery\WireKit\WireKit::warnUnknownProps('ticker',
])
]),
__('wirekit::Change:
`!==
`--color-wk-danger`
`--color-wk-success`
`<article>`
`<svg>`,
`<x-wirekit-chart
`<x-wirekit::ticker
`Blade::render`
`aria-label`
`aria-labelledby`
`generic`
`hasActualContent()`,
`isNotEmpty()`
`isNotEmpty()`,
`md5($label)`
`role="img"
`role="img"`
`shell-bar`
`trendLabel`
`trend`
a
above
abs(is_numeric($delta)
action
against
all,
alone
already
an
and
announced
announcement
announces
answers
any
anywhere
applied
are
aria
aria-label="Chart"`
aria-label="Revenue,
aria-label="{{
aria-labelledby="{{
as
asking,
assembled
asserted:
assistive
at
at:
auto-derived
axe
back
backgrounds.
bare
based
be
because
becomes
before
below
belt-and-braces
bg
bit
bite
blank
block
block,
blueprint
both
box
built
by
calibrated
call.
called
caller
campaign.
cannot
canvas,
cards
carried
carries
catalog
catches
chart
class="flex
class="h-8
class="text-[length:var(--text-wk-sm)]
class="text-[length:var(--text-wk-xs)]
class="{{
classes
color
colors
comment
component
component's
component.
component:
config('wirekit.components.ticker.size',
content
contrast
correctly
could
dark
dashboard
dashboard,
debug
decides,
default
default:
delta
depend
derived
did,
direction
discarded.
display.
documented
does
down
drop
duplicate
early.
element
element,
else,
emitted
empty
etc.)
every
everything
exactly
expands
explicitly
fail
failure
falls
file
first
fix:
fixed
flags
flex-col
follows
font-[number:var(--font-wk-body-weight)]"
font-[number:var(--font-wk-heading-weight)]
for
foundation
fragments
free
from
fully
gap-[var(--space-wk-sm,0.5rem)]">
gap-[var(--space-wk-xs,0.25rem)]',
gave
getting
given
glyph
graphic
guard
happening.
has
heading
here
holding
hours">`
id
id.
id="{{
if
image
implicit
implode('
imports
in
in,
input
inputs
inside
instead
intent
interactive
interpolates
invented
is
is_numeric($deltaText)
is_string($delta)
it
it,
it.
it:
items-baseline
its
itself
itself:
job
keep
key
knowing
label
label.
landmark
language
language,
last
later
layout,
leading
leaving
light
line
list
literal
live
lives
looked
lost
ltrim($delta,
ltrim((string)
magnitude
match
matches
may
means.
measured
mode.
mostly
must
n/a
name
name,
named
named-slot-only
names
need
never
no
nobody
not
nothing
null
null)
null,
null;
number
numeric
of
on
one
one's
one,
one.
only
opens
optimistic-ui:
or
order
original
own
page
passing
path,
per
percent".
plus
point
positives,
prepends
presentational
previews
prod).
prohibited
props
qualified:
question
rather
re-add
reach
reader
reader's
readers
readers.
reason
refutes
render
renders
reported
resolves
result
role
role="img"
row
same
say
say.
says
screen
screen.
second
second,
see
sees
self-closing
sentence
sentence.
set
shape
shown
sign
sign,
signed-string
simply
size
skip
slot
slot.
small
so
span
sparkline
sprite.
staying
stays,
strict
string
strip,
strip.
stripping
subtree
supplies
surface
survive:
tabular-nums"
tabular-nums">
technology
template
test-helper
text
text-[color:var(--color-wk-text)]
text-[color:var(--color-wk-text-muted)]
text-[color:var(--color-wk-text-muted)]">
text-[length:var(--text-wk-sm)]
than
that
that.
the
their
then
there
they
this
this.
ticker
tickers
to
to.
tokens
translated
trend="up"
trim,
trimming
trims
true.
two
unchanged'),
unconditionally,
unit
unknown
unnamed
untranslated
up
use
used
uses:
value.
variant
variants
verbatim.
via
view's
visible
visualization"
w-full"
was
way
we
were
what
whatever
when
which
while
whitespace
whose
with
without
word
words:
worse
would
wrapper
wrapper's
wrote
{
{{
{{--
}
};
}}
}}"
}}-label"
—
→
≥4.5:1
⚠️
