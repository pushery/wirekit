"01"
$attributes->class([$classes])
$attributes->getAttributes());
$classes
$intent
$intent,
$intentClasses
$intentClasses,
$scope);
$size
$size,
$sizeClasses
$sizeClasses,
$slot
',
'accent',
'base',
'bg-[var(--color-wk-accent)]
'bg-[var(--color-wk-danger)]
'bg-[var(--color-wk-success)]
'bg-[var(--color-wk-text)]
'bg-[var(--color-wk-warning)]
'danger'
'danger',
'font-[family-name:var(--font-wk-sans)]',
'font-[number:var(--font-wk-heading-weight)]',
'h-10
'h-12
'h-8
'info'
'info',
'inline-flex
'intent'
'intent',
'leading-none',
'lg'
'lg']);
'md'),
'md',
'neutral'),
'neutral'],
'primary',
'scope'
'shrink-0',
'size'
'size',
'sm'
'step-marker',
'success'
'success',
'warning'
'warning',
($intent)
($size)
(silent
);
--}}
/
//
<span
=
=>
@endphp
@php
@props([
@props.
A
Color
Declared
Dev-only
Every
FILLED
Fully
Given
IS
Literal
Measured
No
Presentational
Pushery\WireKit\WireKit;
Renders
Same
Square-with-soft-corners,
Tailwind's
The
WireKit::resolveClasses('step-marker',
WireKit::validateProp(
WireKit::validateProp('step-marker',
[
['primary',
['sm',
\Pushery\WireKit\WireKit::warnUnknownProps('step-marker',
])
]),
`-fg`,
`01`
`info`
`neutral`
`role`,
a
about
accent
accessibility
action
adds
against
already
always
an
and
announcement
any
as
assembled
asserted:
at
auto-derived
badge
badge's
badge,
badge.
base
be
because
becomes
before
block,
borrows
both
button,
by
call
cannot
carries
chip
chip,
class
component's
components
config('wirekit.components.step-marker.intent',
config('wirekit.components.step-marker.size',
content,
contrast.
correct
could
debug
decorative
default
deliberately.
different
distinguishes
does
drift
early.
element
element,
else.
error.
every
exactly,
fact,
failure
file
fill
flags
for
from
gap.
glance
guard
has
heading
hears
here
here;
implode('
imports
in
intent
interactive
interpolation,
into
inverts
is
it
items-center
its
justify-center',
know
knows
label
later
list
live
map
marker
match
may
meaning
mirror
n/a
name.
never
no
not
nothing.
null,
number
of
omission
one
one.
optimistic-ui:
order.
own
pair
pill
pill:
presentational
prod).
props
qualified:
quiet
rather
reach
reaches
read
reads
reason
refutes
renders
repeats
result
role
role.
rounded-[var(--radius-wk-lg)]',
rounded-[var(--radius-wk-md)]',
rounded-[var(--radius-wk-sm)]',
same
says
scanner
screen-reader
second
shape
shown
sit
site
so
solid
something
stays
step
step's
step.
strings
stylesheet,
surface,
text-[color:var(--color-wk-accent-fg)]',
text-[color:var(--color-wk-bg)]',
text-[color:var(--color-wk-danger-fg)]',
text-[color:var(--color-wk-success-fg)]',
text-[color:var(--color-wk-warning-fg)]',
text-[length:var(--text-wk-md)]
text-[length:var(--text-wk-sm)]
text-[length:var(--text-wk-xs)]
text/background
text:
than
that
the
there
things
this
this.
tint.
to
two
unknown
unstyled
use
user
variable
view's
vocabulary
w-10
w-12
w-8
way
what
which
whole
whose
with
{
{{
{{--
};
}}</span>
}}>{{
—
