!!}</{{
$as
$as);
$attributes->class([$classes])
$attributes->getAttributes());
$classes
$escaped
$highlighted
$markClasses
$part)
$part;
$query)
$query,
$scope);
$segment)
$segment,
$segment;
$slot
$slot,
''
'';
',
'/');
'<'))
'<mark
'as'
'base',
'bg-[var(--color-wk-warning-bg)]',
'font-[family-name:var(--font-wk-sans)]',
'px-0.5',
'query'
'rounded-[var(--radius-wk-sm)]',
'scope'
'span',
'text-[color:var(--color-wk-text)]',
($query)
($segment
(preg_split("/({$escaped})/i",
(preg_split('/(<[^>]*>)/',
(silent
(string)
*
*/
--}}
-1,
.=
/*
//
0.058
0.06
102.1)`
80)`.
:
<{{
=
===
=>
?
?:
@else
@endif
@endphp
@if($query)
@php
@props([
@props.
Assembled
Blade
Blade's
Declared
Dev-only
Fully
HTML
HTML:
INSIDE
IS
It
Measured
NEITHER
No
PREG_SPLIT_DELIM_CAPTURE)
Pushery\WireKit\WireKit;
RENDERED
Renders
SECOND
So
TAG
TEXT
The
Three
WireKit::resolveClasses('highlight',
[
[]
\Pushery\WireKit\WireKit::tagName('highlight',
\Pushery\WireKit\WireKit::warnUnknownProps('highlight',
])
]),
]);
`&amp;`
`&amp;amp;`
`<em>`
`<em>note</em>`
`<mark>`
`=`
`@foreach`:
`as`
`em`
`foo<em>bar</em>`
`oklch(0.905_0.093_102.1)`,
`oklch(32%
`oklch(96.4%
`query="em"`
`{{
a
action
all
alone
already
already-escaped
an
and
angle
any
around
arrives
as
asserted:
attribute.
auto-derived
back
be
because
below.
block,
both
brackets
built
caller
came
cannot
chose,
class="'.e($markClasses).'">'.$part.'</mark>'
color
component
component's
consequences,
content
continue;
correctly
could
cut
dark
data-wk-prose-skip
debug
declared
does
each
early.
element,
emitted
entities
escaped
escaping
every
exists
fallback
fallback.
file
first
flags
for
for,
foreach
fragment
from
function
gaps
guard
had
halves
has
here
highlight
highlighted
if
implementation
implode('
imports
in
inline
intact.
interactive
interpolated
into
is
it
iteration,
later
left
light
list
live
loop
markup
markup,
matched
matches
may
mb_strtolower($part)
mb_strtolower((string)
modes,
moment
n/a
never
newline
no
not
null,
null;
of
on
one
one.
only
opening
optimistic-ui:
or
out
output.
palette
pass
preg_quote((string)
presentational
previous
printed
prod).
props
puts
qualified:
query
rather
raw
raw,
reach
read
reason
refutes
removed:
renders
result
same
say
searched;
see
segments
shown
situation
slot
so
so.
space
space,
split
splitting
stop
str_starts_with($segment,
straight
string
tag
tag,
tags
template
text
than
that
the
theme
there
they
this
three
through
time.
to
token
unknown
untouched
unvalidated
use
value
view's
visible
whatever
which
whose
with
wrapped
wrote
wrote.
{
{{
{{--
||
}
}}
}}</{{
}}>
}}>{!!
}}>{{
}}`,
—
