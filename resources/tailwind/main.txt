!==
$attributes->class([$classes,
$attributes->getAttributes());
$classes
$container
$id
$max
$maxClass
$padding,
$paddingClasses
$paddingClasses])
$resolvedMax
$resolvedMax,
$scope);
$slot
''
'',
',
'2xl'
'2xl');
'2xl',
'base',
'container'
'false'
'flex-1',
'full'
'full']),
'id'
'lg'
'lg'),
'lg',
'max'
'max',
'max-w-[var(--size-wk-container-2xl,96rem)]'
'max-w-[var(--size-wk-container-2xl,96rem)]',
'max-w-[var(--size-wk-container-lg)]',
'max-w-[var(--size-wk-container-md)]',
'max-w-[var(--size-wk-container-sm)]',
'max-w-[var(--size-wk-container-xl)]',
'max-w-full',
'md'
'md',
'none'
'none',
'padding'
'padding',
'px-[var(--padding-wk-x-lg)]
'px-[var(--padding-wk-x-md)]
'px-[var(--padding-wk-x-sm)]
'px-[var(--padding-wk-x-xl)]
'scope'
'sm'
'sm',
'wk-main',
'wk-scrollbar
'xl'
'xl',
'xl']),
($padding)
($resolvedMax)
(browser
(default
(or
(see
(string)
(tabindex="-1")
(the
+
--}}
//
1900px+
75ch`
</div>
</main>
<div
<main
=
=>
>
??
@else
@endif
@endphp
@if($container
@if($id
@php
@props([
Anything
Blade
BooleanProp::from($container,
Default
First-class
Header
Horizontal
INTO
Main
Measured
Normalized
Opt
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
Reads
Renders
Resolve
See
UNBOUND
Vertical
When
WireKit::resolveClasses('main',
WireKit::validateProp('main',
WireKit::warnUnknownProps('main',
[
['none',
])
]),
`--padding-wk-x-{size}`
`--size-wk-container-*`
`--space-wk-{size}`
`2xl`);
`<x-wirekit::container>`.
`<x-wirekit::header>`,
`max-width:
`none`
`null`
`prop="false"`
`wk-main`
a
action
adopting
against
alignment
allow
already
also
an
and
any
app-shell
area
as
as,
asserted:
attribute
back-compat.
be
becomes
behavior.
breathing
by
call
canonical
cap
cap,
cast
clamps
class="{{
compiles
component
component's
config
config('wirekit.components.main.max',
config('wirekit.components.main.padding',
container
content
could
dashboards
default
dev.
developer
docs/extending/spine-contract.md
don't
each
early.
edge-to-edge
element
element,
else
empty
entirely
explicit
false);
false,
feature
file
flips
focus
focusable
footer.blade.php
for
fragment
from
full
full-page
generic
guard
helper.
here
id
id="{{
implode('
in
interactive
is
joins
keyboard
keys
landmark
layout)
layouts
layouts.
line.
load-bearing
main
maps
marker
match
matches
max-width
max="none"
mean
monitors.
most
moves
mx-auto
n/a
navigation
never
no
null)
null,
of
on
on.
one
one.
only
opposite
optimistic-ui:
out
overflow-y-auto',
overrides.
own
padding
page-edge
pair
pairs
per-app
pre-2.3.0
presentational
preserve
primary
programmatically
prop
prop's
prose
py-[var(--space-wk-lg,1.5rem)]',
py-[var(--space-wk-md,1rem)]',
py-[var(--space-wk-sm,0.5rem)]',
py-[var(--space-wk-xl,2.5rem)]',
rather
rationale).
reads
reason
refutes
renders
result
room.
same
scale
scroll).
set,
shares
shown
sibling
silently.
site
skip-link's
skips
slot
so
spine.
stays
stretch
string)
string,
surfaces.
tabindex="-1"
than
that
the
there
this
tier
tier.
to
token
tokens
truthy
two
unbounded
unifies
unknown
use
used
uses
vertical
via
w-full">
warn
was
what
whose
width
wirekit::skip-link
wirekit:spine-participant
with
would
wrap
wrapper
{
{{
{{--
||
};
}}
}}"
—
