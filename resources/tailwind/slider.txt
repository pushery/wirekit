!
!==
"0"
"100"**
"7"
"Array
"Could
"Low"
"is
"label"
"shrink
$announceError
$announceErrors
$attributes
$attributes->except(['announceErrors',
$attributes->except(['class',
$attributes->get('aria-describedby',
$attributes->getAttributes());
$attributes->has(':aria-valuetext');
$attributes->has('aria-label')
$attributes->has('aria-labelledby');
$attributes->has('aria-valuetext')
$attributes->has('x-bind:aria-valuetext')
$attributes->only(['class',
$attributes->whereStartsWith('wire:model')->first()));
$attributes->whereStartsWith('x-model')->whereDoesntStartWith('x-modelable')
$bindValueText
$callerBindsValueText
$callerBindsValueText;
$currentValue
$currentValue)
$describedBy
$disabled
$disabled)
$error
$errors->first($name)`,
$event.target.value"
$explicitValueTextMap
$fallbackLabel
$hasExplicitAriaName
$hasExplicitAriaName;
$hasLabeledMarks
$hasLabeledMarks,
$hasMessage
$hasTrackOverlay
$hint
$hint;
$id
$inputClasses
$isLabeledMarkMap
$isLabeledMarkMap)
$label
$m)
$mDescription
$mDescription,
$mDescription])
$mLabel
$mLabel)
$mLabel,
$mLabel;
$mValue
$mValue),
$mValue,
$mValue]
$m['description']
$m['label']
$m['label'];
$m['value']
$m['value']]
$mark)
$mark['description']
$mark['label']
$mark['pct']
$marks)
$marksIsList
$max
$max]
$min
$min)
$min;
$name
$name)
$name).'-mark-'.preg_replace('/[^a-z0-9-]/i',
$needsSrOnlyFallback
$normalizedMarks
$normalizedMarks[]
$optimistic,
$optimisticArgs),
$optimisticConfig
$pairs
$pct
$pct,
$range
$range)
$required
$scope);
$showValue
$showValue;
$sliderId
$sliderId.'-error'
$sliderId.'-hint'
$spec)
$spec,
$spec['description']
$step
$step),
$tooltip
$trackHeight
$trackHeight,
$v),
$v,
$value
$valueBelow
$valueBelow)
$valueBelow,
$valueClasses
$valueClasses),
$valuePosition,
$valueTextMap
$valueTextMap)
$valueTextMap[(string)
$widestValueText
$wkCandidate)
$wkCandidate;
$wkEnd)
$wkEnd,
$wkStepDecimals
$wkStepDecimals,
$wkStepString
$wkStepString)[1])
$wkValueTextCandidates
$wkValueTextCandidates[]
$wrapperClasses
&&
&&`
'
'#'.$sliderId.'-error',
''
'')
''))
''))),
'')));
'')).'
'');
'',
'';
',
'-2',
'.')
'.');
'.',
'.($label
'.((string)
'0'),
'A',
'B']`
'High']
'High']).
'High']`)
'High']`,
'Hoch']`
'Ja',
'Low',
'Mid']`
'Neutral',
'action'
'after'
'announce-errors']);
'announceError'
'appearance-none',
'args'
'aria-describedby'])->whereDoesntStartWith('x-model')->class([$inputClasses])
'basis-full'
'below'
'below',
'below'])
'bg-transparent',
'bind'
'col-span-full'
'col-start-2'
'current',
'current'`
'cursor-pointer',
'debug'
'descId'
'description'
'disabled'
'disabled:cursor-not-allowed',
'disabled:opacity-[var(--opacity-wk-disabled)]',
'end'
'end',
'error'
'errorRegion'
'false'
'flex
'flex-1',
'flex-wrap
'focus-visible:outline-hidden',
'grid
'grid-cols-[auto_minmax(0,1fr)]'
'grid-cols-[minmax(0,1fr)]')
'h-1',
'h-2',
'h-3',
'hint'
'id'
'input',
'label'
'label'=>,
'lg'
'marks'
'max'
'md'),
'messages'
'min'
'min-w-[min(16rem,100%)]',
'mode'
'mt-2'
'mt-7'
'name'
'optimistic'
'optimisticArgs'
'pb-2'
'pb-6'
'pct'
'pct'=>],
'pending'
'pt-7'
'reject',
'required'
'reverted'
'scope'
'showValue'
'size'
'slider-mark-'
'sm'
'step'
'style',
'style'])->class([$wrapperClasses])
'syncToInput',
'tabular-nums',
'text-[color:var(--color-wk-text)]',
'text-[length:var(--text-wk-sm)]
'text-[length:var(--text-wk-sm)]',
'tooltip'
'value'
'value',
'valuePosition'
'valuePosition',
'valueTextMap'
'w-full'
'wk-slider',
'wk-slider-'
'wrapper',
(!
("0"),
("Single
("−2"),
($attributes->get('id')
($errors
($explicitValueTextMap
($hasLabeledMarks
($hasMessage
($hasTrackOverlay
($hint
($isLabeledMarkMap)
($m)
($max
($name
($normalizedMarks
($optimistic
($pairs
($size)
($spec['description']
($spec['label']
($v)
($v,
($valuePosition)
($wkValueTextCandidates
(($mValue
(-50%)
(4.1.2)
(@aware
(The
([$min,
(`@input="current
(`[0
(`[0,
(`basis-full`),
(`flex-1`
(`w-full`).
(a
(and
(aria-valuetext,
(axe
(bool)
(class
(e.g.
(extends
(flex/grid
(float)
(humanized).
(is_string($step)
(label
(max
(mb_strlen($wkCandidate)
(mirrors
(pb-6
(pt-7
(resources/js/components/slider.js).
(see
(silent
(string)
(the
(tooltip
(value
(verified)
(when
(wire:model,
),
);
*
**477.7px
*/
+
+2,
-
--
--}}
-2
-translate-x-1/2
.
..."`),
...].
/
/*
//
/>
0
0%
0),
0.5rem
0;
0–100
0–100.
1
1),
1.75rem
100
100%
100%)`,
100));
100),
100,
100:
100]
100]`)
16rem
16rem.
1;
2
2.1
20rem
25,
256px
280px
288px:
3.8px.
32px
4.1.2).
481.5px
5
50
50%
50,
500
50]`)
75,
:
:aria-valuetext="valueText"
:style="bubbleShiftStyle()"
:style="bubbleStyle()"
</div>
</span>
<div
<input
<input>
<input>.
<label
<p
<span
=
===
=>
>
?
?:
??
??=
@aware(['announceErrors'
@class([
@elseif($hint)
@elseif($needsSrOnlyFallback)
@endforeach
@endif
@endphp
@foreach($normalizedMarks
@if(!
@if($announceError)
@if($bindValueText)
@if($describedBy
@if($disabled)
@if($error)
@if($hasTrackOverlay)</div>@endif
@if($hasTrackOverlay)<div
@if($label)
@if($mark['description']
@if($mark['label']
@if($name)
@if($optimisticConfig)
@if($required)
@if($showValue)
@if($tooltip)
@input
@input,
@input="current
@php
@props([
@props.
A
A11y:
AND
AT.
Accessible-name
Alpine
Alpine's
An
And
Anything
Assigning
BE
BEATS
BOX
Before
Below
Bind
Binding
Blade
BooleanProp::from($disabled,
BooleanProp::from($required,
BooleanProp::from($showValue,
BooleanProp::from($tooltip,
BooleanProp::stripFalseHtmlFlags($attributes);
Both
Build
CASE,
CONTROL's
CRASHED
CSP
CSS
CURRENT
Caller
Centering
Change
DECLARED
DESCRIPTION
DOM
DURING
Declared
Decorative
Decorative;
Decouple
Dev-only
EACH,
ELEMENT
EVER
Every
Extra
FIRST
Falls
Found
Fully
GUARDED
HARD
HERE
HTML
HTML's
HTTP
IN-FLOW
INSIDE
IS
IT
Illuminate\Support\Str;
In
Initialized
Input-semantic
Inside
It
JS-readable
LIST
LOOK
Labeled
Laravel
Live
Livewire
Livewire's
MAP
MEAN
MEANS
MEASURED
MEASURED,
MIRROR
MOVE
Measured
Mirrors
MutationObserver
NO
NORMALIZED
NOT
NUMBER,
NUMERIC
Native
No
Normalize
Normalized
Null
ON
ONE
ONLY
OWN
On
One
Only
PHP
PHP's
POSITION
POSITION,
PRESENCE,
Positioned
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
RAW
REPLACE
Reading
Reads
Rendered
Reported
Routing
SHOW,
SIBLING
Same
Seeded
Set
Show
Single-handle,
Six
Sizing
Slider
So
Step
Str::headline((string)
StrictnessGate
String(current)`,
Strip
TEXT
TEXT,
THE
THIS
THREE
TRACK.
Tailwind
That
The
They
This
Tick
Ticks
Track
TypeError,
UNBOUND
Undeclared,
Uses
Value
WCAG
WHICH
WIDER
WIDEST
WILL
WITHIN
WORD
WRAPPER,
When
Where
Whether
Which
Whichever
WireKit::resolveClasses('slider',
WireKit::stableId('wk-slider',
With
Without
Wrapper
Written
[
[$mValue,
[$v,
['description'
['end',
['label'
[-2
[...]]`
[0
[0,
[1
[['value'=>,
[]
[],
[];
\Pushery\WireKit\Support\AlpinePayload::from((object)
\Pushery\WireKit\Support\AlpinePayload::from((string)
\Pushery\WireKit\Support\AlpinePayload::from([
\Pushery\WireKit\Support\DomId::unique(
\Pushery\WireKit\WireKit::validateProp('slider',
\Pushery\WireKit\WireKit::warnUnknownProps('slider',
])
])),
]),
]);
])>{{
],
];
__('wirekit::Could
__('wirekit::Saving'),
__('wirekit::Slider');
`!
`$error
`$errors->first(null)`
`$marks`.
`$name`,
`$this->validate()`
`'Neutral'`
`(string)`,
`16rem`.
`:error`
`:value="current"`.
`<input
`@aware`
`@class`
`@props`
`[0
`[1
`after`
`app.css`
`args`
`aria-describedby`
`aria-describedby`,
`aria-describedby`.
`aria-hidden="true"`
`aria-hidden`
`aria-invalid="true"`
`aria-label`
`aria-labelledby`
`aria-live`
`aria-valuetext`
`array_is_list()`
`array_is_list`
`below`
`below`,
`bind:
`bind`
`bottom-full`
`ch`
`change`
`current`
`data-wk-slider-value`
`disabled="false"`
`display:
`el.value`
`end`,
`error`
`hidden`
`input`:
`invisible`
`label`
`left:
`marksMap[current]
`marks`
`min()`
`min(16rem,
`min-w`
`min-width`
`name`
`pct%`
`pct`
`pointer-events-auto`
`pointer-events-none`
`prop="false"`
`required`
`sr-only`
`tests/`.
`this`
`title`
`top-full`
`type="range"`:
`undo`
`valueTextMap`
`valueTextMap`,
`valueText`
`value`:
`w-full`
`wire:model.live`
`wire:model`,
`wk-slider`
`x-model`
`x-modelable`
`xs`
`{{
a
able
about
about.
above
above,
above.
above;
absolute
accent
accepted
accepts
accessibility
accessible
across
action
added
adds
adopting
affordance
after
afterwards
against
against,
against:
agreed
aimed
alike
all
all,
all.
alone
along
already
also
altering
always
an
ancestors
and
announce
announce-error
announceErrors)
announced
announced.
announcement.
announces
announces.
answer
answered,
answers
any
app
appear:
appears
appended
application
applies
apply
arbitration
are
argued
arguments
aria-*,
aria-atomic="true"
aria-describedby.
aria-describedby="{{
aria-hidden="true"
aria-hidden="true">
aria-hidden="true">*</span>@endif</label>
aria-invalid="true"
aria-label
aria-live="assertive"
aria-live="polite"
aria-required
aria-required,
aria-required="true"
aria-valuetext
around
array
array.
array_filter([
array_is_list($marks)
array_keys($marks),
array_map(
array_map('strval',
array_map(fn
array_values($marks)
array_values($valueTextMap));
array_values((array)
arrives
arrow
arrow-key
as
as,
asking
assistive
assume
assumed
assumed,
assuming
asterisk.
at
at.
attribute
attribute,
attribute.
attributes
attributes,
audit
auto
auto-derived
avoid
away
back
bag
bag,
bag.
bare
baseline
be
became
because
been
before
before.
begins
behaves
behavior
behind,
being
belongs
below
below,
below.
beside
between
beyond
bg-[var(--color-wk-border)]"
bg-[var(--color-wk-tooltip-bg)]
bind
binding
binds
block,
blur
boolean
both
bottom-full
bound
boundary
box
box's
box.
box:
branch
browser
bubble
bubble's
bug.
build
but
by
byte-identical
call
call,
call.
caller
caller's
caller-supplied
calls
can
candidate
cannot
capability
card
carries
carry
case
case:
cast
caught
cell
cell,
center
centered
centered.
centers
change
change,
changes
changes,
changing
character
check
child,
choice,
chromium
claim.
clamped
class
class="absolute
class="block
class="col-start-1
class="mt-0.5
class="pointer-events-none
class="relative
class="sr-only"
class="sr-only">{{
class="text-[color:var(--color-wk-danger-text)]
class="text-[length:var(--text-wk-sm)]
class="{{
clause
clipping
clips
coalescing
code
coincidence.
collapses
collapsing
collect($marks)->contains(fn
collect($normalizedMarks)->contains(
color
column
column,
columns
comment
commit
committed
commonest
compile
compiled
compiles
complain
compliant
component
component's
component,
component.
computed
config('app.debug'),
config('wirekit.a11y.announce_error',
config('wirekit.components.slider.max',
config('wirekit.components.slider.min',
config('wirekit.components.slider.size',
config('wirekit.components.slider.step',
config.
constrained
constraint
constraint)
constraint,
consulted.
consumed
container
container,
container.
container;
content
content,
contents">
contents`
context
contiguous
control
control.
control:
controls
controls,
conversion"
conversion",
copy,
correct
correctly,
correctly.
cost
costs
could
counted
counts
covers
current
current:
cut
data-*)
data-wk-optimistic-announcer
data-wk-prose-skip
data-wk-slider-value
debug
decision.
declared
decouples
default
defensive
deliberately
depend
derive
derived
describe
described
describes,
description
description,
descriptions
developer
did
didn't
different
differs
digits
direction
directions.
directive
directly
directory
disabled
disables
discrete
dispatching,
display
display.
distance
div
div,
diverge.
docs
documentation
documented
does
doing.
double-announce
doubles
down.
drag
drag,
drag.
dragging
drags.
drawn
drew
drift
dropped
duplicates
e.g.
each
echo
either
either,
either/or.
el.value
element
element,
element.
else
elseif
elsewhere)
emits,
empty
empty($marks))
empty($normalizedMarks)
empty($normalizedMarks))
empty($normalizedMarks),
empty($normalizedMarks);
empty,
empty:
end
ends
ends,
ends.
entirely.
error
error,
event
event,
every
everything
exact
exactly
except(['class','style']).
excludes
exist,
existed
exists
exit
explaining
explicit
exposed
exposes
extra
extremes
factory
fail
failed
fails
fall
fallback
fallback,
fallback.
false
false);
false,
false;
far
fatal
feature
few
field
field's
field.
file,
fill)
fill.
fills
find
fine
finished
fires
first
first,
fit-content
five
five-move
fix
flags
flex
flex-1">@endif
flex-col
flexes
flight
flips
floated
floats
floor
floor,
flow
flow.
fn
focus
folded
follow
follows
font,
for
for.
for="{{
foreach
form
form,
form:
forms.
fourth
frame
free;
fresh
from
full-width
further
gained
gap
gap)
gap-[var(--gap-wk-sm)]
gap-[var(--padding-wk-x-sm)]
gap-y-[var(--gap-wk-xs)]'
gap.
gaps,
generic
gesture
gesture's
get
gets
getters
ghost
give
gives
global
glyph
goes
grid
grid,
grid:
grouped
guard
guard.
guessing
h-1
had
half
half:
hand-mount
hand-pad
hand-written
handled
handler
handling,
hang
happen
has
has,
have
hear
hears,
height
held.
here
here,
here.
hid
hidden
hide
hint
hint,
hold
honest
hook
hook,
hover
i.e.
id
id,
id="{{
identical
if
ignores
immediately
implode('
imports
in
in:
inert
inert:
initResync()
inline
input
input's
input)
input+change
input,
input.
input.blade.php.
input?"
inset-x-0
inside
inside,
instead
intended
intent.
interrupt
into
into.
intrinsic
invalid
invisible
invisibly
inward/left),
inward/right),
is
is:
is_array($m))
is_array($spec)
is_array($valueTextMap)
is_numeric($m));
is_numeric($step))
it
it,
it.
it:
item
item).
item,
items-center
items-start{{
its
itself.
itself:
just
justify-items-center
justify-items-end
keeps
kept
key
keydown
keyed
keypress
keypress,
keys
know
label
label's
label,
label.
label:
labeled
labels
labels,
labels.
labels:
landed
lands
last
later
layer
layer's
layout
layouts:
learn
leave
leaves
left
left-aligns
legitimate
length:
length;
lengths.
lets
like
likely
line
lines
list
list),
list,
list.
list/map
listen
listener
literal
live
long
longer
longest
looked
lost
made
make
makes
map
map's
map.
mark
mark's
mark.
marker
marks
marks)
marks).
marks,
marks.
marks:
marksMap:
markup
match
math
max(0,
max:
max="{{
may
mb-1.5"
mb_strlen($widestValueText))
mean
meaning
meaning,
meaning.
means
means,
meant,
mechanism
message
method
methods
mid-gesture.
middle.
min(100,
min)
min:
min="{{
minimum
minus
mirror
mirror,
mirror:
misplaced
modes
modes.
moment
more:
morph
most
moved
moves
moves.
ms-0.5"
mt-1"
must
mutated
mutates
naive
name
name,
name.
name="{{
narrow
narrower
native
natural
need
needed
needs
neighboring
neither.
nested
nests
network
never
new
next
no
nobody
node,
noise.
non-focusable
none.
normal
normalized
normalizer
not
not,
not.
not:
note
nothing
nothing.
null
null)
null)?->first($name)
null,
null;
null]
null])
null],
number
number);
number.
number_format((float)
numbering
numbers
numeric
numeric-label
object
occupies
of
off
off.
offered
old
on
on.
once
once,
once.
one
one-cell
one.
only
onto
opposite
opt
optimistic
optimistic-ui:
opts
or
order
ordered
other
otherwise
otherwise,
our
out
out-of-flow
out.
outright.
over
overflow
overflow-hidden
overhanging:
overlap
overlay
overlays,
override
own
owns
owns.
padding
page
pair
parent
parent",
parent's
parent.
parse,
parser
part
passed
passthrough
past
path
pb-2
pct
pct%`
per
per-mark
percentage,
phone
pixels
place
places
plain
plus
point
pointed
pointer
pointer,
pointer-events-auto'
pointer.
pointerup
pointing
policy.
polite
politely
position
positioned
positions
positions-only
precedence
precedence:
press
preview
preview,
prod).
programmatic
programmatically-determinable
prop
prop's
prop.
properties
property
props
purpose,
pushed
pushes
put
putting
px
px-[var(--padding-wk-x-sm)]
py-[var(--padding-wk-y-xs)]
qualified:
question,
quiet.
range
range-slider:
rather
raw
re-assert
re-reads
reach
reached
reaches
read
readable
reader
readers
reading
reads
reads:
real
reason
reason.
recognizes
referenced
refusal
refused
regardless.
region
region,
region.
relative
release
releases
reliable
removal.
remove
render
render:
rendered
rendered.
renders
reports
required
requires
reservation
reservation.
reserve
reserves
resizing
resolve
resolved
resolves
rest.
result
returns
reverse
reverse,
right
right-aligns
roll
rollback
room
room,
roughly
round
round.
rounded-[var(--radius-wk-sm)]
row
row,
row-start-1
row:
rows
rtrim(rtrim(sprintf('%.6F',
rule
rule).
run
runs
said
same
satisfied
save"
save.
saw
say
say.
says
says,
scale
scans
scope,
screen
screen-reader
second
see
see.
sees
selector
semantic
semantic-text
sent
sentence
separate
server
server-side
set
sets
settles
several
shadow-[var(--shadow-wk-sm)]"
shape
shape:
shapes
shapes,
share
shift.)
shifts
shipped
shipping
short-circuiting
should
show
showValue,
showed
showing.
shown
shown,
shrink-to-fit
sibling
side
sides
sighted
signal
silent:
silently
silently.
simple
since
single
site
sits
sits:
size
sizes
slider
slider,
slider.
slider:
sliders
sliders.
small
so
solved
some
something
source
space
span
spec
spec,
special
specific
spelled
spellings
spoken
spoken-text
spread
spreads
sr-only
stale
standing
start
starting
starts
starts,
stay
stays
stays.)
step,
step.
step="{{
steps
still
stop
str_contains($wkStepString,
stray
string
string)
string,
string-keyed
string:
strlen(explode('.',
style
style="display:
style="left:
styled
styling
submit
submit.
submits
subtracted
such
summary
support,
supported
supported.
surface
survives
swallow
switched
table
tabular-nums
tag,
takes
technology
tell
tells
test
text
text,
text-[color:var(--color-wk-danger-text)]',
text-[color:var(--color-wk-text)]">{{
text-[color:var(--color-wk-text-muted)]">{{
text-[color:var(--color-wk-text-muted)]',
text-[color:var(--color-wk-tooltip-text)]
text-[length:var(--text-wk-xs)]
text-center'
text-end',
than
that
that.
that:
the
their
them
then
there
there:
therefore
they
thing
third
this
three.
through
throws
thumb
thumb's
thumb.
tick
tick.
ticks
ticks,
ticks-only).
ticks.
time
timing,
title
title,
title="{{
to
together
together,
token
token.
too
too,
too:
took
tooltip
tooltip,
top):
top-full
touch,
trace
track
track's
track,
track.
track:
tracks
transform:
translateX(-pct%)
translateX(-{{
transparent
trap
tree
trim(($error
trip
trivially
true
true);
true,
true;
truth
truth;
truthy
trying
twice,
two
two-and-a-half
type="range"
type="range">.
type="range">`,
typed
unaffected.
unchanged.
unconditionally
undeclared
undefined
under
undone.'),
units
unknown
unlike
unreachable,
unreachable.
untestable.
until
unusable
up
update
updated
us
usability
use
used
user
users
uses
using
utility
v2.8.0
validation
validation,
value
value's
value).
value,
value.
value/min/max.
value;
value="{{
values
variable
variable.
variables.
variant
verdict").
vertical
via
view's
visible
visual
w-full
w-full',
w-px
warned
was
way
way.
way;
we
weakest
well.
were
what
whatever
when
where
which
whichever
while
whitespace-nowrap
whitespace-nowrap"
whitespace-nowrap">{{
whole
whose
why
wide
widened
widest
width
width,
width.
winning
wins
wins,
wins;
wirekit.css).
with
without
words
work,
worked
would
wrap
wrapped
wrapper
wrapper)
wrapper;
wrapping
wraps,
write
writes
written
wrong
wrote
wrote,
x-bind:aria-busy="isPending"
x-data="wirekitOptimistic({{
x-data="wirekitSlider({
x-effect
x-init="initResync()"
x-modelable="current"
x-on:change="run($event.target.value)"
x-on:keydown="mark()"
x-on:pointerdown="mark()"
x-ref="control"
x-text="announcement"></div>
x-text="valueText"></span>
you
z-10
zero,
zero-based
zero.
{
{{
{{--
||
}
})"
};
}}
}}"
}}">
}}%"></span>
}}%)"
}}%;
}})"
}},
}}-error"
}}-hint"
}}</label>
}}</p>
}}</span>
}}@if($required)<span
}}`
×
—
…]
…],
…]]
→
−
≈
⚠️
