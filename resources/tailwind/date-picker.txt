!
!$hasError)
!==
"2025-01-20/2025-02-09").
"Y-m-d/Y-m-d"
$announceError
$announceErrors
$attributes
$attributes->except('aria-describedby')->class(['wk-field',
$attributes->except(['announceErrors',
$attributes->get('aria-describedby',
$attributes->get('aria-label');
$attributes->get('aria-labelledby');
$attributes->getAttributes());
$attributes->has('aria-label')
$attributes->has('aria-labelledby');
$attributes->whereStartsWith('wire:model')->first()));
$callerAriaLabel
$component`.
$dateId
$dateId.'-format-hint';
$describedBy
$directive
$disabled
$disabled)
$endPairs
$endPairs[$directive]
$endPairs['aria-labelledby']
$endValue
$endValue]
$error
$errorId
$errorMessage
$errorMessage;
$errors->first($name)
$event.target.value"
$fallbackLabel
$fallbackLabel);
$fallbackLabel;
$formatHint
$formatHintId
$hasError
$hasError)`,
$hasExplicitAriaName
$hasExplicitAriaName;
$heightClasses
$heightClasses,
$hint
$hintId
$id
$inputClasses
$inputClasses])
$isRange
$label
$max
$min
$name
$name)
$needsSrOnlyFallback
$optimistic,
$optimisticArgs),
$optimisticConfig
$placeholder
$property
$property)
$property.'.end';
$property.'.start';
$range
$rangeEndAttributes
$rangeName
$rangeName])
$rangeStartAttributes
$rangeWrapperAttributes
$rangeWrapperAttributes->class('flex
$required
$scope);
$showsError
$startPairs
$startPairs[$directive]
$startPairs['aria-labelledby']
$startValue
$value
$value,
$value['end']
$value['start']
&&
'#'.$errorId,
'')
'')));
''),
',
'-',
'-error';
'-hint';
'.($formatHint
'.($showsError
'.((string)
'/'))
'action'
'announce-errors']);
'announceError'
'args'
'aria-describedby']);
'aria-labelledby',
'bg-[var(--color-wk-bg-input)]',
'border-[length:var(--border-wk-width)]',
'border-[var(--color-wk-border-error)]'
'border-[var(--color-wk-border-strong)]',
'debug'
'disabled'
'disabled:cursor-not-allowed',
'disabled:opacity-[var(--opacity-wk-disabled)]',
'duration-[var(--transition-wk-duration)]',
'end'
'error'
'errorRegion'
'false'
'focus:border-[var(--color-wk-accent)]',
'focus:outline-hidden',
'focus:ring-[length:var(--ring-wk-width)]',
'focus:ring-[var(--color-wk-ring)]',
'formatHint'
'h-[var(--size-wk-lg)]
'h-[var(--size-wk-md)]
'h-[var(--size-wk-sm)]
'hint'
'id'
'input',
'label'
'lg'
'max'
'md'),
'messages'
'min'
'mode'
'name'
'optimistic'
'optimisticArgs'
'pending'
'placeholder'
'placeholder:text-[color:var(--color-wk-text-placeholder)]',
'px-[var(--padding-wk-x-md)]',
'range'
'reject',
'required'
'reverted'
'rounded-[var(--radius-wk-md)]',
'scope'
'size'
'sm'
'text-[color:var(--color-wk-text)]',
'transition-colors',
'value'
'w-full',
'wk-date-'
(!
($attributes->has('aria-labelledby'))
($attributes->whereStartsWith('wire:model')->getAttributes()
($errors
($hasError
($hint
($isRange)
($name
($optimistic
($size)
($value
($value[0]
($value[1]
(4.1.2)
(@aware
(`error=""`
(`wire:model.live`,
(arrow
(bool)
(default)
(dist/wirekit.css)
(e)
(e.g.
(filled($callerAriaLabel)
(humanized)
(is_array($value))
(is_string($value)
(s)
(see
(silent
(single-date
(string)
(the
(two
(when
);
+
--}}
->
->except(['aria-label',
->whereDoesntStartWith('wire:model')
.
.'
..,
..]
/
//
/>
/>`
0.25rem.
1.
139/155
16px
2),
2,
2.
2.1
25px
3.
3.3.1
310px
330px,
4.1.2
:
:appDate
:fieldDate.'))
:max="e
:max="{{
:min="s
:min="{{
</div>
</label>
<div
<input
<label
<p
<span
=
===
=>
>
></p>
?
??
??=
@aware(['announceErrors'
@else
@elseif($needsSrOnlyFallback)
@endif
@endif>
@endphp
@endunless
@if($announceError)
@if($describedBy
@if($disabled)
@if($formatHint)
@if($hasError)
@if($hint
@if($isRange)
@if($label)
@if($max)
@if($min)
@if($name)
@if($optimisticConfig)
@if($placeholder)
@if($required)
@if($showsError)
@if($value)
@php
@props([
@props.
@unless($label)
A
A11y:
ABOVE
AND
ANGLE
ANSWER
ANY
APPLICATION'S
Absent
Accessible-name
Alpine
An
BINDING,
BRACKETS,
BROWSER
BROWSER's
Blade
Blade's
BooleanProp::from($disabled,
BooleanProp::from($formatHint,
BooleanProp::from($range,
BooleanProp::from($required,
BooleanProp::stripFalseHtmlFlags($attributes);
Both
Build
CARRIES
CLIPS
COMPONENT.
Caller-provided
Change
Compose
DATES
DIFFERENT
DIRECTIVE
Date
Declared
Dev-only
English
Error
Everything
Extra
FILTER_VALIDATE_BOOLEAN);
FORMAT,
Fully
German.
HERE
HINT
HTML
IF
IN
IS
Illuminate\Support\Str;
It
Livewire
Livewire's
Measured
Measured,
Mirrors
NAME
NAMES,
NO
NOT
NOTHING
Native
No
Normalized
Nothing
ONE
ORDER
OS
OUT,
OWN
Off
On,
Own
PADDING
PAGE,
PHP
PRESENCE,
PageUp/PageDown
Pass
Passing
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
RAW
READS,
REPLACES
Range
Rendered
Reported
SAY
SO
SPACE,
Screen-reader-only
Seeded
Set
Single
Single-date
Sizing
So
Str::headline((string)
Strip
TEMPLATE
TEXT
THAN
THE
TOKEN
TWO
TYPO.
That
The
This
Two
UNBOUND
Visible-label-less
WANT
WCAG
WHEN
WHICH
WRITES
When
WireKit::resolveClasses('date-picker',
WireKit::stableId('wk-date',
YOU
[
[$startValue,
['label'
['start'
[],
[];
\Illuminate\View\ComponentAttributeBag($endPairs);
\Illuminate\View\ComponentAttributeBag($startPairs);
\Pushery\WireKit\Support\AlpinePayload::from($endValue)
\Pushery\WireKit\Support\AlpinePayload::from($max)
\Pushery\WireKit\Support\AlpinePayload::from($min)
\Pushery\WireKit\Support\AlpinePayload::from($startValue)
\Pushery\WireKit\Support\AlpinePayload::from([
\Pushery\WireKit\Support\AlpinePayload::from(__('wirekit::Your
\Pushery\WireKit\Support\AlpinePayload::from(str_replace('_',
\Pushery\WireKit\WireKit::warnUnknownProps('date-picker',
])
]),
]);
],
__('wirekit:::label
__('wirekit::Could
__('wirekit::Date');
__('wirekit::Saving'),
`$attributes`
`$hasError`
`&nbsp;*`.
`--space-wk-xs`
`03.04.2026`
`04/03/2026`
`<input
`<input>`
`<input>`.
`<x-wirekit::date-picker
`@aware`
`@if($hint
`@props`
`MessageBag::has(null)`
`Undefined
`any()`,
`args`
`aria-*`
`aria-invalid`
`aria-label`
`aria-labelledby`
`class`,
`data-*`,
`disabled="false"`
`label`
`locale`
`min-width:
`ms-0.5`
`name="stay[end]"`
`name="stay[start]"`
`name[end]`
`name[start]`
`name`
`optimistic="method"`
`optimistic`
`prop="false"`
`range`
`style`,
`value`
`value`.
`w-full`
`wire:model.blur.debounce.500ms`);
`wire:model.live`
`wire:model="stay.end"`,
`wire:model="stay.start"`
`wire:model`
`x-wirekit::calendar`
`{property}.end`
`{property}.start`
`{{
a
able
above
above).
above.
accepts
accessibility
action
actually
additive
adopting
after
against
agree
alike;
all,
all.
already
also
always
an
and
and,
announce
announce-error
announceErrors)
announced
announced.
announces
answer
any
anything
apart.
app()->getLocale()))
appears
appears,
appended
application
applies.
are
arguments
aria-atomic="true"
aria-describedby
aria-describedby="{{
aria-hidden="true"
aria-hidden="true">*</span>@endif
aria-invalid="true"
aria-label="{{
aria-live="assertive"
aria-live="polite"
aria-required="true"
arm
arm.
array
array.
array_pad(explode('/',
array_values((array)
arrives
as
as,
assistive
asterisks
at
attribute
attributes,
author
authorable
auto-derived
auto`,
automatically.
avoid:
back
bag
bag,
bag.
be
because
becomes
been
before
before,
before.
behaves
below
below.
between
binding
binds
block,
boolean
border
both
bound
browser
browser,
build
but
button).
by
byte
byte.
calendar
call
call,
call.
caller's
caller's.
caller-supplied
came
can
can't
can,
cancels
cannot
capability
carries
carry
case.
cast
change
claims
class="block
class="mt-[var(--padding-wk-y-xs)]
class="mt-[var(--space-wk-xs)]
class="shrink-0
class="sr-only"
class="sr-only">{{
class="text-[color:var(--color-wk-danger-text)]
class="w-full"
class="wk-field
closes
comes
comment
commit
common
commonest
company
compiled
compiles
component
component's
component,
component.
components
composed
computed
conditionally.
config('app.debug'),
config('wirekit.a11y.announce_error',
config('wirekit.components.date-picker.size',
config.
consistency.
console
constrain
container
container.
content
continue;
contract
contributes
control
control,
control.
controls
copied
copy
copying
costs
count
counted
current
data-wk-optimistic-announcer
data-wk-prose-skip
date
date-picker
dates
dates.
day
debt
debug
decided
declared
default
default,
defaults
demos
dense
derive
described
described-by
describedby
describes
description
description,
destroys
determinable
developer
did
dies
different
disabled
disables
disclosure
div
do.
document
documents
does
does.
doesn't
down
draws
dropped
dropped:
duplicate
duplicate.
duplicated:
e:
each
edge,
eight
either
element
element.
elements
else
else;
elseif
elsewhere)
emits
emits.
empty
empty:
end
end',
end.
ends
entered
entry
error
error,
etc.),
even
ever
every
exactly
exceed
excess:
excluded
existed
existing
exists
expensively,
explicit
exposed
factory
fails
fails.
failure
fallback
fallback.
falls
false
false);
false,
family
fault
feature
field
field.blade.php
fields
fields,
file,
filled($label)
filter_var($range,
fine
first
first,
first.
fix:
flags
flex
flex-1
flight
flips
floor
focus
focusable,
follow
followed
follows
for
for="{{
foreach
form
form,
form:
format
formatted
fragile
frame
from
frozen
gains
gap,
gap-[var(--padding-wk-x-sm)]')
gets
give
gives
global
goes
grown
guarded
had
half-applied
half-applied.
half.
hand-mount
has
have
here
here,
here.
hid
hint
hit),
holding
holds
holds.
house
iOS-zoom
id="{{
identical
idref
ids
if
ignored
ignores
immediately
implode('
imports
in
in.
input
input.
inputs
inputs)
inputs.
inside
instead.
into
intrinsic
invalid
invocation,
is
is_string($property)
it
it,
it.
it:
item
items-center
its
itself
itself.
joins
keep
keeps
key
key.
keyboard
keys
keys,
knows
label
label,
landed
lands
language
language;
laptops
large
last
last.
later
layer
leave
less
like
line
linked
list
list,
live
locale
locale:
localized
longer
looks
loses
makes
margin
marker,
markup
match
max="{{
maximum
may
mb-[var(--padding-wk-y-xs)]
mean
meant.
message
method
min-w-0
min="{{
misreading
mode
mode)
model,
modifiers
moment
months,
morph
mounts
move
moved,
ms-0.5"
must
name
name,
name.
name="{{
names
naming
narrow
native
navigation
needs
neither.
network
never
new
no
nobody
node,
non-native
none
normally.
not
notation
note
nothing
nothing.
nothing:
nothing;
now
null
null);
null)?->has($name));
null,
null;
null])
of
off
on
on,
on.
once.
once:
one
one.
only
onto
opens,
operation.
operators
opposite
opt
optimistic
optimistic-ui:
optimistically.
or
or,
order
order.
orders
other
other.
out.
outer
overwhelmingly
own
owns
padding
page
page's
page:
pair
paragraph
paragraph,
paragraph.
parent
parsed
parser
parsing
pass
passed
passes
past
path
per
person
phone.
phones
pick
picked.
picker
placeholder="{{
plus
pointing
polite
popup
post
pre-compiler
precede
precedence:
preview
previous
printed
prod).
programmatically-
programmatically-determinable
prop
prop's
prop,
prop.
prop:
property
props
provides
put
qualified:
question,
range
ratchet
rather
reach
reached
reached.
reaches
reactively:
read
reader
readers
reading
reads
real
reason
record
red
region
registration
rejected,
remove
render
render,
rendered
rendering
renders
report
reported
required
requires
resolve
resolving
restores
rewritten
right
role
rollback
rolling
rolls
roughly
round
row
row,
rule
rule;
runtime
runtime.
s:
same
sat
save.
say
saying
says,
scalar
scans
scope
screen
scroll
second
see
sentence
separator
server
server's,
server-side
set
set,
shape
shared
shipped
ships
shop
shrink
silent,
silently
silently.
single-date
site
site,
slash
so
something
something.
spacing
speak.
spellings
splatted
split
split.
spreads
sr-only
stable
stacked
start
start',
start,
starting
state
stays
still
str_contains($value,
stray
string
string,
submission
submit
such
suffixed
supported
surface
survive
survives
system
tag
tag,
tags,
takes
tech.
technology
technology,
tell
tells
template:
text
text-[color:var(--color-wk-danger-text)]">{{
text-[color:var(--color-wk-text)]">
text-[color:var(--color-wk-text-muted)]"
text-[color:var(--color-wk-text-muted)]">&ndash;</span>
text-[color:var(--color-wk-text-muted)]">{{
text-[length:var(--text-wk-lg)]',
text-[length:var(--text-wk-md)]',
text-[length:var(--text-wk-sm)]
text-[length:var(--text-wk-sm)]',
text-[length:var(--text-wk-xs)]
than
that
that:
the
their
them
then
there
there.
therefore
they
this
those
though
three
through
time-picker
timing,
tiny
to
together
together.
touch
trade
translated
travels
trim(
trim($describedBy.'
trip
true
true);
truthy
two
type="date"
type="date">
type="date">,
type="date">`
typed.
typing
unchanged
unconditionally
under
undo
undone.'),
unguarded
unknown
unlike
unreachable
unreachable,
unrelated
untestable.
untouched,
up
use
used
user
user's
user-interface
valid
validated.
validation
value
value.
value="{{
values,
variable
verbatim
version
via
view's
visible
visual
want
was
way
way.
ways
were
what
wheel
when
whenever
where
which
whichever
while
whitespace,
who
whose
why
width
wins.
wire:model="stay"
wiring:
with
without
wk-field:
word
wording
work
works
worth
would
wrapper,
wraps
writes
writing
written
written.
wrong
x-bind:aria-busy="isPending"
x-data="wirekitDateFormatHint({
x-data="wirekitOptimistic({{
x-data="{
x-on:change="commitFromControl()"
x-on:change="e
x-on:change="s
x-ref="control"
x-text="announcement"></div>
x-text="message"
yields
you
zero-dependency
{
{name}[end]
{name}[start]
{{
{{--
||
}
}"
})"
};
}}
}}"
}})"
}},
}}-end"
}}</label>
}}</p>
}}@if($required)<span
}}[end]"
}}[start]"
}}`
—
⚠️
