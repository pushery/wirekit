"Scrollable
"max-height:
"there's
$attributes->getAttributes());
$attributes->merge($inlineStyle
$classes
$fade
$fade,
$fadeAxis
$fadeIsAuto
$fadeValue
$inlineStyle
$inlineStyle]
$label
$maxHeight
$orientation
$overflowClass
$overflowClass,
$scope);
$slot
''
'',
'';
',
'auto'
'auto'.
'auto';
'auto']));
'auto'],
'base',
'both'
'end'
'end',
'fade'
'fade',
'font-[family-name:var(--font-wk-sans)]',
'horizontal'
'label'
'maxHeight'
'none'
'none')
'orientation'
'overflow-auto',
'overflow-x-auto
'overflow-y-auto
'scope'
'start'
'start',
'vertical',
'wk-scroll-fade'
'wk-scrollbar',
'x'
'y';
("Release
($fade
($orientation)
(WCAG
(a11y:
(in_array($fade,
(mask-image,
(none)
(silent
--}}
.wk-scrollbar
/
//
2.1.1
2.1.1),
2.1.1).
:
:focus-within
</div>
<div
=
===
=>
>
?
@elseif($fadeValue)
@endif
@endphp
@if($fadeIsAuto)
@if($fadeValue)
@if(filled($label))
@php
@props([
@props.
A
API
Accessible
Alpine
Area
CONTENT
CSS
Declared
Dev-only
Edge
Falsy
Fully
Inline
JavaScript
LANDMARK.
Masks
Name
Normalize
Nothing
Provides
Pushery\WireKit\WireKit;
Reachability
Removed
Rendering
SCROLL
Scroll
The
UNCONDITIONAL
WCAG
WireKit::resolveClasses('scroll-area',
WireKit::validateProp('scroll-area',
Without
['both',
['style'
[])->class([$classes])
\Pushery\WireKit\WireKit::warnUnknownProps('scroll-area',
])
])),
`??`:
`filled()`
`landmark-unique`,
`role="region"`
`scrollable-region-focusable`
`tabindex="0"`
`tabindex`
a
adds
after
all
all.
allowed
alone
along
also
an
and
apart
are
area
areas
aria-label="{{
around
array_filter([
arrive
as
ask
at
auto-derived
avoid,
axe
axe's
axis
axis:
background
background-agnostic
beats
block,
both
both.
both;
bottom,
by
called
caller
can
cannot
child
client-only
clipped
colored
common
component
component's
config('wirekit.components.scroll-area.fade',
configurable
container
containers
content
content"
continue
contract.
data-fade
data-fade-axis="{{
data-fade="{{
debug
default
deliberately
destination
destination,
destination.
dissolved
dissolves
does
edge
edge(s)
edges
element
emits
empty
empty,
entries
existing
exists
exposed
fade
fade:
fade;
fades
fail.
false
false),
first
fits
flags
focusable
for
for,
forever
frame
from
has
having
here
hint
horizontal
horizontal,
identical.
identify
if
implode('
imports
in
instead
interpolated
into
invalid
is
it
its
itself
just
keeps
keyboard
keyboard-focused
keyboard-reachable
landmark
landmark,
later
limit:
list
live
made
makes
mask
mask,
mask.
masks
match
max-height
may
meant
measured.
measurement
measures
mechanism.
missing
more
more.
n/a
name
named
near
neighbors.
never
no
nobody
not
not.
notes"),
null
null,
of
on
once
one
one.
only
optimistic-ui:
opts
or
orientation.
otherwise
outcomes.
overflow
overflow-x-hidden',
overflow-y-hidden',
overlay).
page
paint
past
pattern
plugin
plus
prod).
prop.
props
pure
qualified:
rather
reach
reader
real
really
region
region,
renders
reports
resolves
right
role
role:
role="region"
rotor
round:
satisfies
scripts
scroll
scroll"
scrollbar
scrolling
server.
set
so
still
style
switch
tabindex="0"
talks
tell
ten
text.
than
that
the
their
them
themed
there
they
this
three
to
top,
true)
unconditional,
unknown
unnamed
use
utility
utility.
validate
value
vertical
very
view's
way
were
what
when
where
which
whose
with
worst
worth
would
wrapping
writes
x
x-data="wirekitScrollFade"
y
{
{$maxHeight};"
{{
{{--
|
||
};
}}
}}"
—
→
