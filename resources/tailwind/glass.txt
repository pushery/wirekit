"in
"visible"
"watch
$wkGlassNonce
'nonce-…'`
'strict-dynamic'
(each
--}}
//
0.015
0.015"
0.04
0.08
10.61
120
2.
20
20,
28px
2px
4.28
4.31
4.79
60
60,
765
</defs>
</filter>
</svg>
<body>.
<defs>
<feDisplacementMap
<feTurbulence
<filter
<link
<script
<svg
<svg>;
=
@endonce
@endphp
@if($wkGlassNonce)nonce="{{
@once
@once,
@php
@stack('meta'),
BENDS
CLI
CSS
CSS,
Compared
EXTERNAL
Extension
FIRST
Glass
Graph
HTML
HTML,
It
JS,
Liquid
Loads
Measured
Measured,
NOTHING:
OTHER
One
Open
REFUTED,
Renders
SVG
That
The
This
Tier
Tier-2
Under
WORSE
WireKit
Wrapped
\Pushery\WireKit\WireKit::cspNonce();
`<link
`<script
`baseFrequency`
`fonts`
`getElementById`
`script-src
`style-src`
`url(#…)`
a
about.
across
action
after
against
all
all.
already
an
and
any
arcs
are
aria-hidden="true">
as
asserted:
asset
asset('vendor/wirekit/glass/wirekit-glass.css')
asset('vendor/wirekit/glass/wirekit-glass.js')
assets,
at
attempts.
backdrop
baseFrequency="0.015
be
became
because
behind
bend
between
block,
body
body,
both
box".
buying
by
canonical
carries
cases
color-interpolation-filters="sRGB">
command's
comment
component
copies
correct
correctness
could
crawler
curves.
dead
defer></script>
definitions
deliberately
delta
demo's
difference
directives
displaces
dissolve
docblock
docs
document,
document.
documentation
does
dots
dotted
down:
drawn
duplicate
duplicated.
earlier
early.
element,
emits
end
every
executed,
extra
eye:
file
filter
filter)
filter,
filters,
first
fixes
for
four
frequency.
from
gets
gives
glass
grid
grid,
guard
harder
has
head"
height="120%"
here
href="{{
id
id,
id="wk-glass-refract"
images,
in
in2="noise"
in="SourceGraphic"
included
injects
insertion
inside
install
instruction
interactive
into
invalid
is
it
it,
it.
it:
its
judged
keeps
layout
layout's
lead
line
link,
links.
lives
look.
lost.
makes
match,
mean
measurably
measures
metadata
missing
mode
more
n/a
needs
neighboring
no
nobody
nonce
nonce,
nonce-based
nonce="{{
not
nothing
numOctaves="1"
number:
of
on
one
one,
one.
only
optimistic-ui:
original
out
over
own
page
page,
parser
path
pattern
pattern,
per
per-channel
phrase:
pitch
pixels
placement
point
policy
presentational
preview
promises
proposed
provides
puts
question
quoting
raising
rather
re-runs
reachable
read
reason
reasoning
recorded
reference
refutes
regular
regular.
rejects
rel="stylesheet">`
rel="stylesheet"@if($wkGlassNonce)
releases.
render
rendered
rendering
renders
renders,
reparented
requirement
resolve
resolved
resolves
result
result="noise"/>
returns
runtime
same
sandbox
say
scale
scale,
scale.
scale="60"
scans
second
section
seed="2"
severity
shape
short
shown
simply
site
six
smooth
so
spell
src="{{
src>`
standalone
started
stays
still
strength
style="position:absolute;width:0;height:0;overflow:hidden"
stylesheet
surface
surfaces
survived
swirls
switches
tag
takes
terminates
tested
than
that
the
there
these
they
thing
this
three
through
tidiness:
to
together.
translates
trip
type="fractalNoise"
unnonced
unstyled.
used
values
very
visibly
was
way
weight
went
what
when
where
which
whole
whose
why
why.
width="120%"
with
without
would
written
wrong
wrong.
x="-10%"
xChannelSelector="R"
xmlns="http://www.w3.org/2000/svg"
y="-10%"
yChannelSelector="G"/>
{{--
}}"
}}">
}}"@endif
~66px
—
⚠️
