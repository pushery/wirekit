!
!==
"--wk-stagger-step:
"no
$attributes->except('style')->class([$classes,
$attributes->get('style',
$attributes->getAttributes());
$classes
$colClasses
$colClasses])
$developerStyle
$hasStagger
$mergedStyle
$scope);
$slot
$stagger
$staggerStep
$stylePieces
$stylePieces);
$stylePieces[]
&&
'')
''));
'',
');
',
'2'
'3'
'4'
'5'
';
'base',
'cols'
'false'
'font-[family-name:var(--font-wk-sans)]',
'gap-[var(--space-wk-md)]',
'grid',
'grid-cols-1
'grid-cols-[repeat(auto-fit,minmax(200px,1fr))]',
'scope'
'stagger'
'wk-stagger'
($cols)
($developerStyle
($hasStagger)
(1rem
(default),
(each
(null
(silent
(truthy)
--space-wk-md;
--}}
//
200px).
75;
75ms
:
</div>
<div
<div>
<x-wirekit::stat>
=
==
=>
>
?
@endif
@endphp
@if($mergedStyle
@php
@props([
@props.
BooleanProp::isFalse
BooleanProp::isFalse($stagger);
Cascade
Declared
Default:
Dev-only
Developer
Fully
Measured
Merge
ON
ONE
Override
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
Renders
See
Stagger
Stats
Token,
Tri-state
WireKit::resolveClasses('stats',
[
[];
\Pushery\WireKit\WireKit::warnUnknownProps('stats',
])
]),
`!==
`--wk-stagger-step`
`.wk-stagger`
`animate`.
`style=""`
a
across
action
alone
an
and
animations
any
asserted:
attribute
auto-derived
auto-emitted
auto-fit
be
block,
change,
children
children.
collapsing
cols
columns
comes
component's
could
countdown
custom
debug
default
developer
direct
dist/wirekit.css
does
early.
effect.
element,
entrance
explicit
false`
feature-grid
file
flags
for
from
full
gap-4
grid
grid:
guard
hardcoded
has
if
implode('
implode(';
imports
in
int
interactive
is
is_int($stagger)
it
later
later-declaration-wins
let
lets
lg:grid-cols-3',
lg:grid-cols-4',
lg:grid-cols-5',
list
live
match
may
ms
multiple
n/a
no
not
null
null,
one.
optimistic-ui:
overrides
presentational
prod).
prop.
props
qualified:
rather
rationale.
raw
reach
reason
recognizes
refutes
rendered
renders
responsive
result
rtrim($developerStyle,
rule).
rules.
second
see
shown
sizes"
sm:grid-cols-2
sm:grid-cols-2',
so
spellings
stagger
stat
step):
step,
step.
stops
string
stringly-false
style
style="{{
take
than
that
the
there
this
trim((string)
true
turn
unbound-attribute
unknown
use
view's
violating
visual
which
whose
with
without
wraps
{
{$staggerStep}ms";
{{
{{--
|
}
};
}}
}}"
—
≥
