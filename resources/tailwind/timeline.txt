!
!!}
!==
"1.
"10
"2
"Just
"after"
"before"
"default"
"heroicon-m-inbox"
"list,
$after
$attributes->class([$classes])
$attributes->class([$sizeClasses]);
$attributes->get('aria-hidden');
$attributes->get('aria-label');
$attributes->get('aria-labelledby');
$attributes->get('role');
$attributes->getAttributes());
$attributes->merge(['style'
$before
$callerAriaHidden
$callerAriaHidden;
$callerAriaLabel
$callerAriaLabelledBy
$callerRole
$classes
$cleaned
$cleaned);
$datetime
$dotColor
$e)
$e;
$effectiveIntent
$intent
$intentLabel
$intentLabel])
$isInformative
$isInformative;
$m):
$m[1]);
$mergedAttributes
$mergedAttributes->getAttributes())->toHtml();
$mergedAttributes->merge($placeholderAttributes)
$mergedAttributes->merge(['aria-hidden'
$name,
$name];
$placeholderAttributes
$placeholderAttributes['role']
$resolved
$resolved);
$resolvedAriaHidden
$scope);
$shouldSetHidden
$size
$size,
$sizeClasses
$sizeMap
$sizeMap[$validated];
$slot
$svgHtml
$svgHtml,
$time
$timeClasses
$title
$validated
$variant
$variant;
&&
'
''
'')
'',
',
'.
'/<svg\b([^>]*)>/i',
'/<svg\b/',
'2xl'
'<svg
'<svg'.$cleaned.'>';
'Rendering
'Run:
'[WireKit]
'after'
'base',
'before'
'danger'
'datetime'
'default',
'false'
'flex',
'font-[family-name:var(--font-wk-sans)]',
'gap-[var(--padding-wk-x-md)]',
'h-12
'h-3
'h-4
'h-5
'h-6
'h-8
'icon'
'img';
'intent'
'lg'
'list-none
'list-style:
'md'
'name'
'relative',
'scope'
'size'
'size',
'sm'
'success'
'text-[length:var(--text-wk-sm)]
'time'
'true';
'true']);
'var(--color-wk-accent)',
'var(--color-wk-danger)',
'var(--color-wk-success)',
'var(--color-wk-warning)',
'variant'
'warning'
'xl'
'xs'
(!
($callerAriaHidden
($effectiveIntent)
($isInformative
($resolvedAriaHidden
($shouldSetHidden)
($size
($svgHtml
('xs')
(\BladeUI\Icons\Exceptions\SvgNotFound
(\Pushery\WireKit\Support\StrictnessGate::shouldThrowOnInvalid())
(\Throwable
(`countdown`,
(array
(button
(dots
(e.g.
(function_exists('svg')
(no
(or
(paths,
(preserving
(provides
(regex
(same
(shouldSetHidden=true),
(silent
(space-y)
(string)
(true
);
+
-
--}}
-webkit-mask-image:
...>
/
//
/>
0.25rem;"
0.5rem;
0;
0;'])->class([$classes])
1
1.4.1
1.5rem);
1.5rem);">
10;
1;
1px
1px;
2.
2rem,
2rem;
3."
3rem.
500s
9999px;
:
:name="$icon"
:status',
</div>
</li>
</ol>
<div
<li
<ol
<ol>
<span
<svg
<svg…>)
<time
<use>,
<x-wirekit::icon
<x-wirekit::icon>
=
===
=>
>
></div>
?
??
@else
@endif
@endphp
@if
@if($after)
@if($before)
@if($icon)
@if($intentLabel
@if($slot->hasActualContent())
@if($time)
@if(filled($datetime))
@if(isset($title))
@php
@props([
@props.
A
A11y
ALSO
AND
API:
AT
ATTEMPTED
Adding
Ahead
An
BECAUSE
BELOW,
Blade
Blade-heroicons'
BladeUI\Icons\Svg
BooleanProp::from($after,
BooleanProp::from($before,
Bottom
CAN
CANNOT
CHANGES
CONDITION
CSS
Carbon
Catching
Check
Content
DO
Dashed
Declared
Default
Dev-only
Dot
Drop
ELEMENT
ENT_QUOTES).'"',
EVERY
Each
Every
FAIL
Fully
Graceful
HAS
HERE,
HTML
HTTP
Hidden
Htmlable,
IN
INSIDE
IS
IT
IT.
Icon
IconResolver,
If
In
It
Logger
MOST
Measured
N
NEVER
NOT
No
Normalized
Nothing
ONE
ONLY
OPENING
OR
OUTER
One
Operating
Optional
PERFECTLY
PROP,
Padding
Pushery\WireKit\Icons\IconResolver;
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
RENDER
Reapply
Render
Renders
Reported
Resolve
RuntimeException
SEE.
SEPARATE
SVG
SVG:
Same
Silent
Size
So
Strip
SvgNotFound
SvgNotFound.
Symmetric
TAG
THE
The
This
Throwing
Timeline
UA
UNBOUND
Uses
Vertical
VoiceOver
WAY
WCAG
WHY
WITHOUT
We
WebKit
When
WireKit
WireKit::resolveClasses('timeline',
WireKit::resolveClasses('timeline.item',
WireKit::validateProp('icon',
With
[
['data-wk-icon-missing'
['status'
\Pushery\WireKit\Icons\IconSetPackages::reportMissingSetOnce((string)
\Pushery\WireKit\WireKit::warnUnknownProps('icon',
\Pushery\WireKit\WireKit::warnUnknownProps('timeline',
\Pushery\WireKit\WireKit::warnUnknownProps('timeline.item',
])
]),
];
__('wirekit::Error'),
__('wirekit::Status:
__('wirekit::Success'),
__('wirekit::Warning'),
__toString
`!important`),
`:has()`
`:nth-last-child(2)`.
`<ol>`
`<span>`
`<span>`,
`<time
`<time>`
`Svg
`[li:last-child_&]:pb-0`.
`after="true"`
`aria-hidden="true"`
`aria-hidden`
`aria-label`
`aria-labelledby`
`class="h-12
`data-wk-timeline-item-content`
`date-separator`).
`datetime`
`default`
`heroicon-m-inbox`
`inbox`
`intent`
`list-style`
`message`,
`none`
`prop="false"`
`role="img"`.
`role="list"`
`variant`
a
a11y
about
above
above.
absent,
accessibility
accessible
action
actual
actually
add
after
against
ago"
ago",
alert
alert's
alias
alias.
align-items:
all
all,
allowed
already
an
anchored
and
announced
announcing
any
anything
app(IconResolver::class)->resolve($name);
applied
are
area.
area:
aria-hidden
aria-hidden,
aria-hidden="'.htmlspecialchars($resolvedAriaHidden,
aria-hidden="..."
aria-hidden="false".
aria-hidden="true"
aria-label
aria-labelledby
array_keys($sizeMap));
arrives
as
as,
asserted:
at
attribute
attribute,
auto-derived
axis:
axis;
back
back-compat
background:
baked
bakes
bare
be
beat
because
becomes
before
below
below.
between
black);
black);"></div>
blade-heroicons
blade-icons
blade-icons,
blade-ui-kit/blade-heroicons.
blade-ui-kit/blade-icons
blade-ui-kit/blade-icons.
block,
body
bootstrapped
border-left:
border-radius:
both
bottom,
branch
branch's
break
browser-default
bug
build
but
button
buttons).
buttons,
by
call
called
caller
caller's
can
cannot
cannot:
canonical
carrier,
carries
carry
case
cases
cast
catch
caught
center;
center;">
centered
changes
changing
check
checks
chevrons,
choice
chose
chronological
clash.
class
class="[li:last-child_&]:hidden"
class="sr-only">{{
class="text-[color:var(--color-wk-accent-fg)]"
class="text-[length:var(--text-wk-md)]
class="{{
cleanly.
close
close-icons,
collapsed
color
column;
comes
compact
compiles
component
component's
components
composer
condition
configured
conflict
connector
connectors);
consequence
console
console).
consuming
container
container.
content
context
continuation
continuous
continuous.
conveys
copy,
could
covered
creates
danger.
dashed
data-wk-prose-skip
data-wk-timeline-item-content>
data-wk-timeline="{{
datetime
datetime="{{
datetime="…">`
debug
debug,
decided
decides.
decimal
decision,
decision:
declared
declaring
decorative
decorative,
default
default.
default:
define('WIREKIT_BLADE_ICONS_WARNED',
defined('WIREKIT_BLADE_ICONS_WARNED'))
degradation:
degrades,
deliberate.
deliberately,
deliberately:
deployment
derive
derive.
dev
developer
developer's
difference
different
direct
display
display:
dist/wirekit.css
documentation
does
dot
dot/icon
dotted
down
drawn
draws
drift
dropdown
dropdowns
drops
each
earlier
early
early.
either
element
element,
elements
else
elseif
embedded
emit
emits
empty
empty-state
ends
engine
ensure
entry
especially
event
events
events.
every
exactly
exhausting
exist
existing
exists.
explicit
explicitly
factory:
fade-in
fade-out
failed,
failure
fallback
fallback,
fallback.
falls
false)
false),
false);
false,
feature
file
files
finds
first
fix
fix.
flags
flex
flex-direction:
flex-shrink:
flex;
flips
followable.
follows
font-[number:var(--font-wk-heading-weight)]
for
forbidden
forcing
found.`
framework
from
function
function).
function_exists('svg'))
gaps).
get
gets
gets,
given
global
glyph
glyph.
goes
green.
guarantees
guard
h-5
h/w
hand-written
hard
harder
has
have
have)
heard
height:
helper
here
hidden
historical
hours
house
human
icon
icon-only
icon;
icons
icons).
icons,
identically
identifier
if
image
implements
implode('
imports
in
including
indent.
indicates
indicator
indicator.
inert
informative
informative.
injected
inline
inner
inside
installed
installed.'
instance
instead
intact
intent
intent.
inter-attribute
interactive
into
invalid
is
is.
isn't
isset($sizeMap[$size])
it
it,
it.
it:
item
item,
item:
items
items"
items.
its
itself
job.
justify-content:
keeping
keeps
kill
label,
labels).
ladder
last
later
layout
leave
like
line
linear-gradient(to
list
list,
list-none
literal
live
loads.
log
logger()->warning(
long
looks
lost
loudly,
m-0
machine
machine-readable
makes
map.
margin
margin:
marker
markers
markers.
marks
mask-image:
match
matches
matching
matters.
may
mean
merge
min
missing
missing,
missing:
modal
modals,
moved
must
n/a
name
name.
named
navigation.
needs
neither
nested
never
no
nobody
noise
non-null
none,
none;
nor
normal
not
nothing
nothing.
now"
now",
now.
null
null)
null,
null;
occurrence
of
offering
on
on.
one
one,
one-time
one.
one;
only
onto
open
opening
opposite
optimistic-ui:
or
ordered
other
our
outcome
outcome,
over
override.
own
own,
own.
p-0
p-0',
package
padding
padding-bottom-zero
padding:
page
page.
pages
parseable
passes
past
path
per
per-set
permit
phrase
pipeline
place
placeholder
plain
post-process
pre-checking
prefix
prefixes
preg_replace(
preg_replace('/\s*aria-hidden="[^"]*"/i',
preg_replace('/\s+/',
preg_replace_callback(
present
presentational
preserves
preset
previously
problem
prod).
production.
project,
prop
prop's
props
provides
pseudo-element
put
qualified:
rather
raw
re-inject
reach
reaches
read
reader
reading
reads
real
reapply
reason
reason:
recommends
red
redundancy
refutes
registered
registry
relative;
reliable
removed
removes
render
render,
rendered
rendered.
rendering
rendering.
renders
replaced
replaces
reported
repository
reproduced
request
require
requires
resolved
resolves
respect
rest
result
return
returns
role
role,
role="img"
role="img".
role="list"
root
root;
row
row.
rule
rung
same
saying
screen
screen-reader-skipping
sees
selector
semantic
semantics
sensible
separate
set
sets
should
show
shown
shows
sibling
siblings;
signals
silently.
single
site
size
size,
size="xs"
sizes
skip
slot,
so
sole
solid
somebody
source
source-baked
space).
spacing
span
spoken
static
stay
stays
still
stopped
stops
storage/logs/laravel.log.
string
string,
string.
strip
style
style="display:
style="display:inline-block;"></span>
style="flex-grow:
style="height:
style="margin-top:
style="position:
style="width:
style="z-index:
styles
stylesheet
succeeded
success
svg($resolved,
svg()
svg(),
table
tag's
tag.
takes
taking
targets
teaches
test
text
text-[color:var(--color-wk-text)]">
text-[color:var(--color-wk-text-muted)]';
text.
than
that
the
their
then
there
thing
third
this
this:
three
through
throw
throws
tile
time,
timeline
title
title,
to
toHtml().
tokens
top
top,
touches
trailing
transitively
transparent,
tree:
true);
truthy
try
two
unknown
unknown-alias
unknown.
unless
until
usage,
use
used
useful
uses
using
utility
valid
validateProp
value
value,
var(--color-wk-accent-fg);"></div>
var(--color-wk-border);
var(--color-wk-border);"
var(--size-wk-xs,
variant
vertical
via
view's
visual
w-12"`
w-12',
w-3',
w-4',
w-5
w-5',
w-5';
w-6',
w-8',
warning
was
way
ways
we
what
when
when:
wherever
whether
which
while
whitespace
who
whose
width:
wins
with
without
word
wording
would
yet.
yields
{
{!!
{{
{{--
|
||
}
},
};
}}
}}"
}}">{{
}};"
}}</span>
}}</time>
}}>
—
→
⚠️
