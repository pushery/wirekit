!
!==
"&nbsp;"
"\u{00A0}")
"any
"choose
"{$targetId}-error"
"{$targetId}-hint"
$announceError
$announceErrors
$attributes
$attributes->class([$classes])
$attributes->class([$wrapperClasses])
$attributes->except(['announceErrors',
$attributes->except(['aria-describedby']);
$attributes->get('aria-describedby'));
$attributes->getAttributes());
$classes
$error
$error,
$errorClasses
$errorId
$errorMessage
$errorMessage)
$errors
$fieldsetDescribedBy
$for
$group
$group->errorId
$group->hint
$group->hintId
$group->key
$group->message($errors
$groupMessage
$hasError
$hasLegend
$hint
$hint)
$hint);
$hintClasses
$hintId
$inAlignedRow
$isHorizontal
$label
$legend
$legend->attributes->class([$legendClasses])
$legend->hasActualContent()
$legendClasses
$legendIsSlot
$name;
$orientation,
$orientationValue
$required
$scope);
$slot
$slot,
$targetId
$wkFieldSet
$wkSpacerBlank
$wrapperClasses
&&
''
',
'<legend'))
'align-fields']);
'alignFields',
'announce-errors',
'announce-errors']);
'announceError'
'announceErrors'
'base',
'block',
'error'
'error',
'false'
'font-[family-name:var(--font-wk-sans)]',
'font-[number:var(--font-wk-body-weight)]',
'font-medium',
'for'
'hint'
'hint',
'horizontal'
'horizontal';
'horizontal']),
'label'
'legend'
'legend',
'mb-1
'mb-3
'min-w-0
'name'
'orientation'
'orientation',
'required'
'scope'
'select-none',
'space-y-1.5',
'text-[color:var(--color-wk-text)]',
'text-[length:var(--text-wk-md)]',
'tracking-[var(--font-wk-letter-spacing)]',
'vertical',
($errors
($hasError
($name
($orientation)
(=
(@aware
(`field.legend`)
(aria-live="polite")
(auto
(avoids
(border
(default),
(default):
(escaped),
(keyed
(label
(markup,
(radio
(rich
(see
(silent
(space-y);
+
--}}
/
//
13.5px
2026-09-04
2026-09-04.
:
:for="$targetId"
:required="$required"
:scope="$scope"
:scope="$scope">
</div>
</fieldset>
</label>
</p>
</x-wirekit::field.set>
</x-wirekit::label>
<button>
<div
<div>
<div>,
<div>.
<div>:
<fieldset
<fieldset>
<fieldset>.
<label
<legend
<legend>
<p
<span
<x-slot:legend>
<x-slot:legend>Permissions
<x-wirekit::badge>New</x-wirekit::badge></x-slot:legend>
<x-wirekit::field.legend>
<x-wirekit::field.set>
<x-wirekit::field.set>.
<x-wirekit::label
=
===
=>
>
?
??
??=
@aware
@aware(['alignFields'
@aware(['announceErrors'
@else
@elseif($group->hint
@elseif($hint)
@elseif($inAlignedRow)
@endif
@endphp
@if($announceError)
@if($errorId)
@if($fieldsetDescribedBy
@if($group->key
@if($groupMessage
@if($hasError
@if($hasLegend)
@if($hintId)
@if($inAlignedRow
@if($isHorizontal)
@if($label)
@if($legendIsSlot)
@if(config('app.debug')
@php
@php($wkSpacerBlank
@props
@props([
@props.
A
APPENDED
ARIA
BEFORE
BELOW
Blade
BooleanProp::from($announceError
BooleanProp::from($required,
Both
CAPTION
CHILD.
ComponentSlot,
DEFAULT
DENSE,
Declared
Dev-only
Emitted
Empty
Error
Everything
FIELDSET'S
FIRST
FieldGroup
FieldGroup::describedBy($group,
FieldGroup::open($name,
Fully
GRANDCHILD,
GROUP
GROUP'S
Gated
Grouped
HERE,
HTML
Horizontal:
How
IDs
IS
IT
If
It
Label
Laravel
Livewire
Measured
Move
NAME,
NOT
Normalized
Nothing
OFFER,
ONLY
OR
OUTER
OWN
One
Only
Overriding
Precedence:
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\Support\FieldGroup;
Pushery\WireKit\WireKit;
REAL
ROLE
Read
Render
Renders
Reported
Required
Rich
Set
Silence
So
Stable
Standalone
Support\SlotAttributes):
THE
TOP
TYPE
Takes
That
The
They
This
Through
U+00A0
UNBOUND
UNBRACKETED
Use
VARIABLE,
Vertical
WCAG-recommended
WHILE
WRITTEN
We
Whatever
When
Whether
Widening
WireKit
WireKit::resolveClasses('field',
WireKit::resolveClasses('field.legend',
WireKit::resolveClasses('field.set',
WireKit::resolveClasses('field.spacer',
WireKit::resolveClasses('label',
WireKit::validateProp('field',
Without
Wrapper
Written
[
['vertical',
\Illuminate\View\ComponentSlot;
\Pushery\WireKit\Support\BooleanProp::from($alignFields,
\Pushery\WireKit\WireKit::warnUnknownProps('field',
\Pushery\WireKit\WireKit::warnUnknownProps('field.legend',
\Pushery\WireKit\WireKit::warnUnknownProps('field.set',
\Pushery\WireKit\WireKit::warnUnknownProps('field.spacer',
\Pushery\WireKit\WireKit::warnUnknownProps('label',
])
]),
`$component`.
`$wkFieldSet`,
`&nbsp;`
`//`
`<span>`
`<x-…>`
`@aware`
`@aware`.
`@if($legend)`
`@props`
`Pushery\WireKit\Components\FieldSet`
`align-items:
`alignFields`
`aria-describedby`
`aria-hidden`
`compileString`
`data-list`
`data-wk-field`
`error`
`filled()`
`for`
`hero`
`hint`
`hint`/`error`
`legend="0"`
`legend="Permissions"`
`legend`
`min-w-0`
`min-width:
`ms-0.5`
`name`
`name`)
`name`,
`prop="false"`
`reserve-message`
`reserve-message`,
`role="group"`
`roles.1`
`roles`.
`select-none`
`sm`
`x-slot:legend`
`x-wirekit::field.legend`
`{{
a
about
above
above)
above.
accepts
accessibility
accessible
across
action
actual
add
address
adopting
after
against
ahead
aligns
all
all.
all:
alone,
alone.
already
also
alternatives,
always,
an
ancestor.
ancestry.
and
announce-error
announce-errors,
announce.
announceErrors)
announced
announcement).
announces
anonymously
answer
answer:
answered
answers
any
appears
application
application.
are
arguments
aria-*;
aria-atomic="true"
aria-describedby
aria-describedby="{{
aria-hidden
aria-hidden:
aria-hidden="true"
aria-hidden="true">*</span>@endif
aria-hidden="true"></span>
aria-live="polite"
array
arrive
arrived
arrives
as
as,
asked
asserted:
at
at.
attribute
attribute.
attributes,
author
auto-derived
auto-generated
avatar
axe
back
badge,
bag
bag,
bag.
bare
be
because
before
behind
being
belong
belongs
below
below,
below.
beside
beside)
between
blank
block
block,
blocks).
border-0
both
bottom
bound
box
box,
box:
boxes,
breadcrumb
bug.
built
but
button
button:
buttons
by
cache
call
caller
caller's
can
cannot
caption
caption,
caption.
caption:
caption;
captions
carries
carry
cast
catalog
changes,
characters
checkbox
checkboxes,
child
child:
choosing
chrome
claim
claimed
class
class="flex
class="flex-1
class="space-y-3">
class="text-[color:var(--color-wk-danger-text)]
class="text-[length:var(--text-wk-sm)]
class="w-1/3
class="{{
classes
classes:
cleared:
collapses
colors
column
comment
comments
compiled
compiles
component
component's
component,
component.
components
composed
composer
conditionally
config('wirekit.a11y.announce_error').
config('wirekit.a11y.announce_error',
config.
configuration
constructor,
container
container's
content
content.
contrast
control
control.
control:
control;
controls
controls.
copying
could
danger-text
dark
dark:
data-wk-field
data-wk-field-part="label"
data-wk-field-part="message"
data-wk-prose-skip
debug
decides
decision
declaration
declared
declares,
decorative
default
defeats
defect
demoted
demotes
dense.
describing
description
design
detection:
developer
dies
docblock
does
does.
double
double-click
down
drag-select
drift:
drifts
drive
dropped
dynamically
e.g.
each
early.
edge
edge.
either
either.
element
element,
element.
elements
else
emitted
empty
end
end`
ends
entity
entry
error
error,
error-bag
error/hint
errors
escaped
escaped,
even
evenly
ever
every
exactly
existed
exists
explicit
explicitly
fail
fall
false
false);
false,
feature
field
field's
field,
field.
field.set:
fields
fieldset
fieldset's
file
filed
files
fill
filled($legend);
fires.
first
fix.
fixes
flags
flex
flex/grid
flips
focus
font,
font-[number:var(--font-wk-heading-weight)]
for
for,
form
form's
formatting.
found
free,
from
gap
gap-[var(--padding-wk-x-lg)]">
gap.
generic
get
gets
given,
global
go
goes
grandchild
grandchild.
grid
grid.
group
group's
group's.
grouping
groups,
guard
half
halves
hand
hand-built
hand-written
handed.
hands
happens
hardcoded
has
have.
having
height,
height.
help
here
here,
here.
highlighted
hint
hint's
hint/error
hint:
hits:
hold.
holding
holds
hook
horizontal
house
how
icon).
id
id="{{
identity
ids
if
implode('
imports
in
index.
indicator
initials
inline
inner
input's
input.
input/select/textarea/checkbox
inside
instanceof
instead
interactive
into
intrinsic
invalid
invisible
invisible.
is
isset($wkFieldSet)
it
it,
it.
it:
items-start
its
keeps
kept
key
label
label's
label,
label-height
labeled
labels
land
lands
later
layout
layout.
layouts.
learn
learns
least
leaves
left
legend
lets
level
levels
library's
library.
like
line
line,
line-height,
line.
lines
lining
link
links
list
literal
literals,
live
logger()->warning('[wirekit]
look
looks
loses
loss
lost.
lower,
m-0',
makes
margin)
margin,
marked
markup
markup,
markup.
match
matches
matches.
matters:
may
mean
meant
message
message,
messages
min-content`
min-w-0
misalignment
missing.
mode,
moment
mouse
move
ms-0.5"
n/a
name
name)
name,
name="{{
named
names
native
needed).
needs
neighbor
neighbors
neighbors'.
never
newline
next
no
no-break
non-breaking
not
note
nothing
null
null)
null);
null)?->first($name)
null)?->has($name));
null,
null;
null])
object
occupy
of
old
on
on.
once,
once.
one
one,
one-line
one.
ones
only
opaque,
opposite
optimistic-ui:
or
order
ordinary
other
other.
otherwise
our
out
over
overridable
overridable,
own
own,
p-0
padding
page
page.
paints
paragraphs,
parent
parser
parts
pass
passed
passed,
pasted.
per
place,
plain
point
pointed
pointing
points
polite
precedence
precedence:
prefix/suffix
presentational
prevents
problem
problem:
prod).
production
project
prop
prop's
prop,
prop.');
props
provide
pt-[var(--padding-wk-y-sm)]">
pull
purpose.
pushes
put
qualified:
quirk
radios
rather
raw
raw"
reach
reaches
reaching
read,
read.
reader
reader:
readers
reading
reads
reads.
real
reason
reasons
rebuilding
reference
reference.
refutes
region
region.
rejected
related
rely
remaining
remove
render
rendered
renders
renders,
replaced:
replaces
reported
reset
resolve
rest
result
return
rich
right,
role",
round-trip
route
row
row,
row.
rows
rule:
runs
same
sat
scan.
scans
screen
screen,
seam
second
see
sees
select
separator,
set
set.
sets
seven
shape:
shapes,
shared
shipped
short,
should
shove
show
shown
shrink-0
shrinking
siblings
silently.
simply
site
sits
sitting
six
sizes
slides
slot
slot,
slot.
so
space
space-y
space-y-1.5">
space.
spaced.
spacer's
spacing
spacing,
spacing.
span
spelled
spelling
spellings
spellings,
stable
stacks
starts
stating,
stay
stays
still
stop
stops
str_contains((string)
stray
string
string,
string.
strips
submit
such
supported
surface
surrounding
survives
tag
tag,
tags
take
takes
tall,
tells
template
test:
text
text-[color:var(--color-wk-danger-text)]">
text-[color:var(--color-wk-danger-text)]">{{
text-[color:var(--color-wk-danger-text)]',
text-[color:var(--color-wk-text)]',
text-[color:var(--color-wk-text-muted)]">
text-[color:var(--color-wk-text-muted)]">{{
text-[color:var(--color-wk-text-muted)]',
text-[length:var(--text-wk-md)]
text-[length:var(--text-wk-sm)]
than
that
the
them
them,
them.
then
there
there)
therefore
these
they
things
this
three
three,
three:
through
through,
time
to
to.
toggles
token
tokens
told.
top
treatment
tree
true
true),
true);
truthiness
truthy
turn
twice,
two
two-line
typography
unaffected
undefined
under
unknown
unlike
until
up
use
use,
used
uses
usual
validation
value
values
variable
vertical
view
view's
view,
visible
visibly
warning
was
way
way.
we
were
what
whatever
when
where
whether
which
while
whitespace,
whoever
whole.
whose
why
wider
wired.
wiring
wiring:
with
without
work.
worse
worth
would
wrapped
wrapper
write
written
wrong,
wrote.
yields
your
{
{{
{{--
|
||
};
}}
}}"
}}">{{
}}</legend>
}}</p>
}}</x-wirekit::label>
}}>
}}>{{
}}@if($required)<span
}}`
—
…controls…
⚠️
