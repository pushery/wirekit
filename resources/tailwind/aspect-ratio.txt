"finish"
$aspectValue
$attributes->getAttributes());
$attributes->merge(['style'
$classes
$ratio
$ratio;
$ratio`,
$scope);
$slot
',
'.$aspectValue])->class([$classes])
'16/9',
'aspect-ratio:
'base',
'ratio'
'relative
'scope'
(a
(silent
--}}
//
:
</div>
<div
=
=>
?
@endphp
@php
@props([
@props.
BOTH
CSS
Declared
Dev-only
Fully
Measured
No
Pinned
Pushery\WireKit\WireKit;
Renders
THROUGH
The
WireKit::resolveClasses('aspect-ratio',
Written
[
\Pushery\WireKit\WireKit::warnUnknownProps('aspect-ratio',
])
]),
`16/9`
`aspect-ratio`
`is_numeric($ratio)
a
accepts
action
alike.
an
and
any
as
asserted:
attribute
attributes
auto-derived
away,
background,
bag,
bare
be
beside
block,
box
branches
browser
by
caller
can.
claimed
comment
component
component's
conversion
could
debug
declaration
describing
difference
does
early.
element,
file
first
flags
folds
for
for.
forms
from
goes
guard
here
identical
implode('
imports
in
interactive
into
invites
is
it
it.
keeps
last,
later
line
list
literal
live
may
means
merge()
n/a
natively
never
no
not
notation
null,
number
on
one
one.
optimistic-ui:
overflow-hidden',
override
own
package's
parser,
parsing,
presentational
prod).
produced
property
property,
props
qualified:
radius)
rather
ratio
reach
reads
real
really
reason
refutes
renders
result
second
shown
silently
so
some:
someone
still
straight
style
style=
styled
suite.
than
that
the
them
there
thing
this
to
took
two
under
unfinished
unknown
use
value
view's
want.
was
which
who
whose
with
{{
{{--
}}
}}>
—
