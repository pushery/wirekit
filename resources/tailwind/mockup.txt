$attributes->class([$classes])
$attributes->getAttributes());
$barLabel
$classes
$hasBar
$scope);
$slot
$title;
$url
$variant
$variant,
$variantValue
'base',
'browser'
'browser',
'code',
'code'],
'phone')
'phone',
'scope'
'tablet']);
'tablet'],
'title'
'url'
'variant'
'variant',
'window',
'wk-mockup',
(a
(silent
+
--}}
/
//
:
</div>
</span>
<div
<span
=
===
=>
?
@endif
@endphp
@if($barLabel)
@if($hasBar)
@if($variantValue
@php
@props([
@props.
ALL
Declared
Dev-only
Frame
Fully
It
Measured
Nothing
Pushery\WireKit\WireKit;
Rendering
Renders
Shown
The
URL
Which
WireKit::resolveClasses('mockup',
WireKit::validateProp('mockup',
['browser',
\Pushery\WireKit\WireKit::warnUnknownProps('mockup',
])
a
about
action
address
an
and
announced.
any
app
app)
are
area.
aria-hidden.
aria-hidden="true">
aria-hidden="true"></span>
around
as
asserted:
at
auto-derived
bar
bar,
bar.
bar:
be
bit
block,
browser
browser,
buttons
buttons.
cannot
carries
chrome
close/minimize
code
code-block)
component's
composition.
content
controls
controls,
could
data-variant="{{
data-wk-mockup
data-wk-mockup-bar
data-wk-mockup-dot></span>
data-wk-mockup-dots>
data-wk-mockup-label>{{
data-wk-mockup-notch
data-wk-mockup-screen>
dead
debug
decoration;
decorative
desktop
does
dots
early.
element,
every
fake
file
filename
flags
for
frame
frame:
from
guard
here
imports
in
in_array($variant,
in_array($variantValue,
interactive
interactive:
is
it
label
later
lights
lights,
list
live
marketing
may
meaning,
must
n/a
no
not
notch
null,
of
one.
optimistic-ui:
or
order
others.
page.
pairs
phone
picture
presentational
press.
prod).
props
pure
put
qualified:
rather
reach
reason
refutes
renders
result
screen-reader
screenshot
screenshot)
shown
slot
snippet,
so
spans,
tab
tablet
than
that
the
them
there
they
this
three
title
title/filename
told
top.
traffic
true)
true);
unknown
use
user
variant's
variants
view's
wear
web
which
whose
window
with
would
{{
{{--
}}
}}"
}}</span>
}}>
—
