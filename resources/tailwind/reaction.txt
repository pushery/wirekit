$active
$active,
$ariaLabel
$ariaLabel])->class([$baseClasses,
$attributes
$attributes->getAttributes());
$attributes->merge(['aria-label'
$baseClasses
$count
$count,
$count]);
$emoji
$emoji,
$optimistic
$optimistic,
$optimisticArgs),
$reactionLayer
$reactionOptimistic
$reactionUsersId
$scope);
$stateClasses
$stateClasses])
$userList
$userList)
$userList))`
$users
'
''
',
'-',
'action'
'active'
'active',
'args'
'base',
'bg-[color-mix(in_srgb,var(--color-wk-accent)_12%,var(--color-wk-bg))]',
'bg-[var(--color-wk-bg-muted)]',
'bind'
'border-[color-mix(in_srgb,var(--color-wk-accent)_35%,transparent)]',
'border-[length:var(--border-wk-width)]',
'border-transparent',
'count'
'cursor-pointer
'debug'
'duration-[var(--transition-wk-duration)]',
'ease-[var(--transition-wk-easing)]',
'emoji'
'false'
'false'"
'font-[family-name:var(--font-wk-sans)]',
'h-7
'hover:bg-[var(--color-wk-bg-elevated)]',
'hover:border-[var(--color-wk-border)]',
'inline-flex
'locale'
'messages'
'mode'
'optimistic'
'optimisticArgs'
'pending'
'phrases'
'reaction-users-');
'reject',
'reverted'
'rounded-[var(--radius-wk-full)]',
'scope'
'text-[color:var(--color-wk-accent-content)]',
'text-[color:var(--color-wk-text-muted)]',
'text-[length:var(--text-wk-sm)]',
'transition-[background-color,border-color,box-shadow]',
'true'
'users'
(bool)
(first
(int)
(silent
--}}
.
//
0
0"
0)
0,
:
:count
</button>
</span>
<button
<span
=
===
=>
>
?
@elseif($count
@endif
@endphp
@if($reactionOptimistic)
@if(count($userList)
@php
@props([
@props.
A
Alpine
Alpine.
Blade
BooleanProp::from($active,
Change
Declared
Dev-only
Extra
FIRST
Fully
Intl.PluralRules
It
Livewire
NOT
No
Normalized
Null
OVERRIDES
Only
Page-unique,
Pre-existing
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
SERVER
Same
Shown
The
UNBOUND
WHICH
WireKit::resolveClasses('reaction',
[
['count'
[],
[];
\Pushery\WireKit\Support\AlpinePayload::from([
\Pushery\WireKit\Support\DomId::unique(null,
\Pushery\WireKit\Support\PluralPhrases::from('wirekit::{0}
\Pushery\WireKit\WireKit::warnUnknownProps('reaction',
])
]),
]);
],
__('wirekit::Could
__('wirekit::Saving'),
`$tickerId`.
`active`
`active`;
`args`
`aria-describedby`
`md5($emoji
`prop="false"`
`trans_choice`
a
able
about.
above
accessible
action
after
against
agreed
all.
always
an
and
announce
announced
answer.
anything
app()->getLocale()),
appended
arguments
aria-atomic="true"
aria-describedby="{{
aria-hidden="true">{{
aria-label
aria-live="assertive"
aria-pressed="{{
array_values((array)
arrives
as
as,
at
attribute
auto-derived
axe
bag
been
before
being
binds
block,
blocked
boolean,
browser
build
built
button
button's
by
call
call,
call.
called
caller's
cannot
capability
carry
case
case,
cast
change
chooses.
class="font-[font-variant-emoji:emoji]"
class="font-[number:var(--font-wk-heading-weight)]
class="inline-flex
class="inline-flex">
class="sr-only"
class="sr-only">
client's
commonest
compiles
component
component's
component.
compute
config('app.debug'),
content.
controls
correct
count
data-wk-optimistic-announcer
debug
decides
default
derived
does
duplicate
each
edge
emoji
empty:
exactly
existed
exposed
factory
false);
false,
feature
five,
flags
flip
flips
follow
forms
from
gap-x-1">
gap-x-1',
gave
give
hand-mount
hardcoded
has
having
helper,
id
id.
id="{{
identical
ignores
implode('
implode(',
implode(…,
imports
in
inside
into
is
is_array($users)
it
it,
it.
items-center
its
later
layer
leaves
list
list,
live
locale
makes
markup,
may
mean
merge
method
moment
name
name.
names
needs
nests
never
new
no
no-JS
node,
not
nothing
now,
null
null,
number
of
on
on.
once
one
one's
one,
one.
only
opposite
optimistic
optimistic-ui:
ordinary
own
owns
page
paint
people
per
person
plan
plus
prod).
prop's
props
px-2',
qualified:
reach
reacted'),
reacted',
reacted|[2,*]
reaction
reactions
reactions|{1}
reader
reads
reason
reason,
region
remember
render
rendered:
renders
replaced
reported
resolved
restore
rollback
rollback.
row
row,
run().
runs,
same
save.
saw,
second
select-none',
separate
server
server-only
server-side
server:
should
show
silently.
single
site
sits
six
so
spreads
state
state.
stays
stopped
str_replace('_',
string,
supported
surface
tabular-nums"
tabular-nums">{{
tell
text
textbook
that
that:
the
there
things.
this
ticker's
time.
to
told
trans_choice('wirekit::{0}
travel
truthy
two
type="button"
undo
undone.'),
unknown
unreachable,
until
up
use
used
value.
variant
via
view's
was
way
what
when
whenever
which
wins).
with
without
would
x-bind:aria-busy="isPending"
x-bind:aria-label="label"
x-bind:aria-pressed="active
x-data="wirekitOptimistic({{
x-data="wirekitReaction({{
x-on:click="toggle()"
x-show="count
x-text="announcement"></span>
x-text="count">{{
zero,
{{
{{--
}}
}}"
}})"
}}</span>
—
