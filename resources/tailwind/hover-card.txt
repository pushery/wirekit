$attributes->class([$wrapperClasses])
$attributes->getAttributes());
$delayHide
$delayShow
$offset
$panelClasses
$scope);
$slot
$trigger
$wrapperClasses
&&
',
'bg-[var(--color-wk-bg-elevated)]',
'border-[length:var(--border-wk-width)]
'bottom',
'delayHide'
'delayShow'
'fixed
'font-[family-name:var(--font-wk-sans)]',
'offset'
'p-[var(--padding-wk-x-md)]',
'panel',
'placement'
'relative
'rounded-[var(--radius-wk-lg)]',
'scope'
'shadow-[var(--shadow-wk-lg)]',
'text-[color:var(--color-wk-text)]',
'text-[length:var(--text-wk-md)]',
'w-72',
'wrapper',
(an
(and
(aria-haspopup,
(avatar,
(button/link)
(buttons,
(panel
(phrasing
(silent
(the
+
--}}
/
//
/components/hover-card#inline-in-running-text.
1.4.13
2.4.3,
200,
300,
8,
</div>
</span>
</template>
<body>
<body>,
<div
<span
<span>
<template
<x-wirekit::hover-card>…</x-wirekit::hover-card>
<x-wirekit::text>…
=
=>
>
@endphp
@focusin="focusin()"
@focusout="focusout()"
@keydown.escape.prevent="closeAndFocusTrigger()"
@keydown.escape.window="open
@mouseenter="mouseenter()"
@mouseleave="mouseleave()"
@php
@props([
@props.
@username
AND
ARIA
Alpine
Alpine's
Both
Card
DOM
Declared
Dev-only
Escape
Floating
Focus
Forwards
Fully
Guarded
HTML5
Hover
Hover-card
INNER
INTO
Its
LEGITIMATELY
Measured:
NOT
OUTSIDE
Only
Pushery\WireKit\WireKit;
So
Surfaced
TRIGGER.
Tab
Teleported
Teleporting
That
The
Trigger
UI
UI.
Unlike
Uses
WCAG
WINDOW,
WireKit::resolveClasses('hover-card',
Without
[
\Pushery\WireKit\Support\AlpinePayload::string($placement)
\Pushery\WireKit\WireKit::warnUnknownProps('hover-card',
])
]),
__('wirekit::Hover
`$refs.panel`
`<div>`
`<div>`.
`<p><span>…<div
`<p>`
`<p>`.
`<span>`
`<span>`)
`@keydown.escape`,
`@mouseenter`
`body`
`open`
`position:
`x-teleport="#wk-overlay-root"`
a
a11y.
able
above,
across
actions).
actually
after
again.
allows
already
an
and
anticipate
are
aria-allowed-attr
aria-expanded)
aria-label="{{
armed;
arms
arrived.
as
at
attributes
auto-closes
auto-derived
avatar
axe-core's
back
back.
backwards
be
because
behavior.
below
bindings
bio,
block,
blur
border-[var(--color-wk-border)]',
both
boundary,
breaks
broke.
browser
but
buttons
by
calculations
cancels
card
card')
card's
card.
cards
carries
case
class="{{
client-only
close()"
component's
content
content)
control
coordinates
could
counterpart
debug
delayHide:
delayShow:
delegated
descendant
developer
dismissible
display
document.
document;
does
dropping
duration-[var(--transition-wk-duration)]"
each
ease-in
ease-out
edges
either,
element
element.
else
encounters
end
entering
entirely
error.
event
ever
every
exists,
fires
first
fixed`,
flags
flow
focus
focus,
focusable;
focusing
follows
for
fragmentation
from
front
full
generic
handler
has
hid
hide
hide-delay
hierarchy.
holds
hover
hover-card
hover-intent
hover/focus),
hover/focus.
implode('
imports
in
inhabitable
inline
inline-block',
inside
interactive
into
irrelevant).
is
is:
it
its
itself
just
keeps
key
keyboard
keystroke.
land
last
later
leave
leaving
lifts
like
lines
links),
list
listens
live
longer
lookup
machine
makes
may
mechanism
moving
must
n/a
named.
names.
never
next
nicety.
no
no-descendant
non-interactive
not
nothing
null,
of
off
offset:
on
one
one.
only
open
opened
opens.
optimistic-ui:
or
order
other
out
out.
overlay
own
owns,
page
pair,
panel
panel></div></span>…</p>`.
parser
pending
phrasing
placed
placement.
placement:
plain
pointer
pointing
popover,
position
positioned
positioning
precedes
previews.
prod).
promise
promises.
props
puts
qualified:
rather
reach
reach.
reachable
read-only
reader
refs
rejects
renders
requires
resolve
rest
reveals
rich
role="dialog"
roll
root
root)
root.
rule.
run
running
sat
scale-100"
scale-95"
screen
screen,
see
sequential
server
show
shown
shows
silent
silently
sits
so
something
somewhere
state
stayed
steps
still
structured
table
tabs
teleport
teleport,
teleported
teleported,
than
that
the
them
them,
there
these
this
through
to
to.
tooltip,
tooltip-like
trigger
trigger's
true:
two
unchanged
under
unknown
usage
use
value
via
view's
viewport-relative
visibility
went
what
whatever
when
which
while
whole
with
without
work
would
wrapper
wrapper,
wrapping
x-cloak
x-data
x-data="wirekitHoverCard({
x-init
x-init="initTriggerAria()"
x-on:keydown.tab="tabFromTrigger($event)"
x-on:keydown.tab="tabWithinCard($event)"
x-ref="panel"
x-ref="trigger"
x-show="open"
x-teleport="#wk-overlay-root">
x-transition:enter-end="opacity-100
x-transition:enter-start="opacity-0
x-transition:enter="transition
x-transition:leave-end="opacity-0
x-transition:leave-start="opacity-100
x-transition:leave="transition
z-[var(--z-wk-dropdown)]',
{{
{{--
})"
}}
}}"
}},
—
…</x-wirekit::text>
