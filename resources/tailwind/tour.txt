!==
"Finish":
"Next"
"Next".
"Tour
$attributes
$attributes->class([$classes])
$attributes->class([$panelClasses])
$attributes->except('aria-label');
$attributes->get('aria-label');
$attributes->getAttributes());
$callerAriaLabel
$classes
$hasCallerName
$index
$level
$level)
$level,
$levelValue
$panelClasses
$placement
$resolvedIndex
$scope);
$slot
$target
$title
$titleId
&&
',
'2',
'3',
'4',
'5',
'6']);
'base',
'bg-[var(--color-wk-bg-elevated)]',
'border-[length:var(--border-wk-width)]',
'border-[var(--color-wk-border)]',
'bottom',
'fixed
'font-[family-name:var(--font-wk-sans)]',
'index'
'level'
'level',
'name'
'p-[var(--padding-wk-x-md)]',
'placement'
'rounded-[var(--radius-wk-lg)]',
'scope'
'shadow-[var(--shadow-wk-lg)]',
'target'
'text-[color:var(--color-wk-text)]',
'text-[length:var(--text-wk-md)]',
'tour',
'w-80',
'wk-tour-step-title-'
'wk-tour-step-title-'.$resolvedIndex,
($levelValue
(0,
(1-6).
(default),
(e.g.
(int)
(left/top:
(silent
(string)
(validateProp
);
*
+
-
--}}
-9999px)
//
0"
0)
0,
0;
1
1,
1])
2,
3
3,
3;
4,
5,
6],
:
:current
:index="N"
:number',
:total'))
</div>
</template>
<button
<div
<h{{
<span
<template
=
===
=>
>
></div>
>{{
?
??
@else
@elseif(isset($title))
@endif
@endisset
@endphp
@if($hasCallerName)
@isset($title)
@keydown.escape.window="active
@php
@props([
@props.
A
ARIA
Alpine
Alpine's
An
Auto-assign
Back/Next
CSS
DIALOG
DOM
Declared
Defaults
Dev-only
DomId
Each
Explicit
Floating
Focus
Fully
GETS
HOW
HTML
Heading
Honoring
IS
ITS
Id
It
Its
JS
JavaScript
Lighthouse.
Live
N".
NAME
No
Nothing
ONLY
Object.assign(floating.style,
Out
Overlay
PreviewRenderer)
Pushery\WireKit\Support\TourStepCounter;
Pushery\WireKit\WireKit;
Reset
SILENTLY,
STEP
See
Set
Step
TEMPLATE
THE
TWICE,
Teleported
That
The
Tour
TourStepCounter::next()
TourStepCounter::next();
TourStepCounter::reset();
UI's
UI.
WCAG
When
WireKit::resolveClasses('tour',
WireKit::resolveClasses('tour.step',
WireKit::validateProp('tour.step',
Without
[
['1',
['number'
[1,
[data-wk-tour-step]
\Pushery\WireKit\Support\AlpinePayload::from(__('wirekit::Finish'))
\Pushery\WireKit\Support\AlpinePayload::from(__('wirekit::Next'))
\Pushery\WireKit\Support\AlpinePayload::from(__('wirekit::Step
\Pushery\WireKit\Support\AlpinePayload::string($name)
\Pushery\WireKit\Support\DomId::unique(
\Pushery\WireKit\WireKit::warnUnknownProps('tour',
\Pushery\WireKit\WireKit::warnUnknownProps('tour.step',
])
]),
__('wirekit::Back')
__('wirekit::Next')
__('wirekit::Tour
`""`,
`$index`
`-1`
`-2`
`<x-wirekit::tour.step>`
`Pushery\WireKit\Support\TourStepCounter`
`aria-label="…"`
`aria-label`
`aria-labelledby`
`data-wk-tour-step="0"`
`filled()`
`heading-order`
`modal.blade.php`
`tour.step`.
a
accessibility
accessible
after
against
ahead
all
all.
alone
along
an
ancestor
and
announcement
announcement:
announcements
anticipate
any
anything
are
aria-atomic="true"
aria-hidden="true"
aria-label="{{
aria-labelledby
aria-labelledby="{{
aria-live="polite"
aria-modal="true"
as
assembled
assigned
async
at
attribute
auto-
auto-assigned
auto-assignment
auto-derived
available,
axe
axe's
back
back.
backdrop
bag
bag,
base
based
be
be,
because
before
behind
below
best-practice
between
bg-[var(--color-wk-accent)]
bg-[var(--color-wk-overlay)]"
block,
blocks
body
both
branches
break
built
but
button's
buttons.
by
bypasses
call
caller
caller's
can
cannot
carried
check
check:
child
chosen
class="flex
class="font-[number:var(--font-wk-heading-weight)]
class="p-[var(--padding-wk-y-xs)]
class="sr-only">
class="text-[color:var(--color-wk-text-muted)]
class="text-[length:var(--text-wk-sm)]
class="wk-overlay-fixed
client-only
component
component's
computePosition()
conditionally
containing
content:
control
controls,
could
counter
current.
cursor-pointer
data-wk-placement="{{
data-wk-prose-skip
data-wk-target="{{
data-wk-tour-step="{{
debug
dedupes
default
defaulted
defect.
depends
derive.
destination,
developer
dialog
dialog's
did-you-mean)
didn't
difference
different
dismiss()"
dist/wirekit.css
docs
document
document-render
does
does.
duration-200"
each
ease-out
element
element,
element.
else,
emitted
empty
empty,
enclosing
error,
escapes
evaluates
evaluation.
ever.
every
everything
fall
fallback
falls
filled($callerAriaLabel);
final
first
first-allowed
fixed
flags
flicker
flip
focus
focus-visible:ring-[length:var(--ring-wk-width)]
focus-visible:ring-[var(--color-wk-ring)]"
focus:outline-hidden
follows
footer
for
fragments
from
full
full-viewport
further
gap-[var(--gap-wk-sm)]">
generated
gets
guessed
h1,
halves
handed
has
heading
heading's
heading,
heading.
here
here:
hit.
hover:bg-[var(--color-wk-accent-hover)]
hover:bg-[var(--color-wk-bg-subtle)]
hover:text-[color:var(--color-wk-text)]
id
id="{{
if
implode('
imports
in
in_array((int)
index
index,
individual
initial
inline
inset-0
inside
inspector
instruction
integer
interpolated
invalid
invisible
is
it
it.
items-center
its
itself
joined
jumps
just
justify-between">
keeps
key
know
label,
label;
land
lands
language.
last
later
lays
leaves
left,
let
level
line
links
lint
list
literal
live
lives
locate
look
looks
loser
lost
markup
match
matters
may
mb-[var(--padding-wk-y-md)]">
mb-[var(--padding-wk-y-xs)]">{{
means:
mechanism
mechanisms:
might
modal
more
most
moves
multi-tour-on-same-page
n/a
name
name.
name:
names
naming
navigation
near
needs
never
next
next()
no
node
not
nothing
null
null,
numbering.
numbers
of
off-screen
often
on
once
one
one,
one.
only
open.
opening
opens
opens,
optimistic-ui:
order
order.
out
outline
outline:
outrank
over
overlay.
overwrites
own
owns
owns,
page
page's
page,
pairing,
panel
parent
parent.
parser
partial
per
per-render
per-request
pick
plugin,
point
popup
position
positioned
positioning
positioning.
positions
precedence
preference
prevents
prod).
produce
product
production
progress
props
qualified:
quieter
random
rather
rationale.
re-minted
re-render.
reach
reaches
read
reader
reason:
record
reference
region
render
reports,
reset
resolves
resolves,
right
role="dialog"
roll
rounded-[var(--radius-wk-md)]
rounded-[var(--radius-wk-sm)]
rule
rule,
run
same
scoped
scrim
second
sees
sense
sentence
sequential
server
server,
server-side
set
sets
several
ships
shorter
shows
sibling
sight
signals
sit
site
sits,
skipped
slot,
slot.
so
sources,
specific
spot.
state
step
step's
step-by-step
step.
steps
stop.
straight
style
stylesheet
sub-component
suffix.
suites
supply
surrounding
sweep
tab
tabindex="-1"
tabular-nums"
tag
tag.
tags
target
telling
text
text-[color:var(--color-wk-accent-fg)]
text-[color:var(--color-wk-text-muted)]
text-[length:var(--text-wk-lg)]
text-[length:var(--text-wk-sm)]
than
that
the
them
then
there
things
this
this,
three
throws
title
to
told
tooltip-like
top
totalSteps
tour
tour's
tours
trade
transform:
translatable
translateZ(0)
true)
two
two:
type="button"
unavailable
under
unique:
unknown
until
up,
use
used
useful
uses,
using
validateProp's
value
value.
values
verbatim,
via
view's
viewport.
visible
wants
warning,
was
well,
well-formed
what
when
whenever
where
which
while
who
whole
whose
winner
winner,
wins
with
without
witness
wizard
wk-overlay-layer-modal
word
worse
would
writes
written
wrong
wrong;
x-cloak
x-data="wirekitTour({
x-on:click="next()"
x-on:click="prev()"
x-ref="overlay">
x-show
x-show="active"
x-show="currentStep
x-teleport="#wk-overlay-root">
x-text
x-text="currentStep
x-text="progressText"></span>
x-transition:enter-end="opacity-100"
x-transition:enter-start="opacity-0"
x-transition:enter="transition
yields
z-[var(--z-wk-modal)]
z-[var(--z-wk-modal)]',
zero.
{
{{
{{--
}
})
})"
}}
}}"
}},
}}</button>
}}</h{{
}}>
—
…
