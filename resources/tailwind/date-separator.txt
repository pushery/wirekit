!==
"7.
"Sep.
"Sonntag,
$attributes->class([
$attributes->getAttributes());
$attributes->has('aria-labelledby'))
$carbonDate
$carbonDate->format($format);
$carbonDate->toDateString()
$carbonDate->translatedFormat('l');
$date
$date->timezone($tz)
$fullDate
$label
$now
$now->timezone($tz)
$reference
$scope),
$slot
$slot->hasActualContent()
$stickyClasses
$stickyClasses,
$timezone
$tz
$tz)
$tz);
$variant,
$variantValue
',
'M
'MMMd',
'UTC');
'base',
'date'
'flex
'font-[family-name:var(--font-wk-sans)]',
'format'
'inline',
'l,
'now'
'py-[var(--space-wk-xs,0.25rem)]';
'scope'
'sticky
'sticky'
'sticky']),
'text-[color:var(--color-wk-text-muted)]',
'text-[length:var(--text-wk-xs)]',
'timezone'
'variant'
'variant',
'yMMMMEEEEd',
($carbonDate->diffInDays($reference)
($carbonDate->isSameDay($reference))
($carbonDate->isSameDay($reference->copy()->subDay()))
($format
($now
($variant)
(silent
--}}
//
2026"
40,
7"
7)
7,
:
<
</div>
</time>
<div
<span
<time
=
===
=>
>
?
??
@endphp
@endunless
@php
@props([
@props.
@unless($attributes->has('aria-label')
A
Carbon
Carbon::now($tz));
Carbon::parse($date,
Carbon::parse($now,
Carbon\Carbon;
Declared
Dev-only
English
F
FIRST
Full
Fully
German
HTML
Human-friendly
ICU
Measured
NAME
No
Only
Parse
Pushery\WireKit\WireKit;
Renders
Same
Sep.".
September
Skeleton,
The
WRONG
WireKit::resolveClasses('date-separator',
WireKit::validateProp('date-separator',
Y');
[
['inline',
\Pushery\WireKit\Support\LocalizedDate::bySkeleton($carbonDate,
\Pushery\WireKit\WireKit::warnUnknownProps('date-separator',
])
]),
__('wirekit::Today');
__('wirekit::Yesterday');
`aria-label`
`translatedFormat('M
`var(--z-wk-sticky,10)`
a
above,
action
after
and
announces,
any
aria-hidden="true"
aria-label="{{
arrangement,
as
asserted:
attributes,
auto-derived
bag
be
below
better
block,
border-[var(--color-wk-border)]"></span>
border-t
caller
caller's
class="grow
class="shrink-0
component's
config('app.timezone',
context
context.
could
date
datetime="{{
debug
default
derives
did
disagrees
discarded.
does
dropped
early.
element,
else
elseif
every
fallback
fallback:
file
flags
font-[number:var(--font-wk-heading-weight)]"
for
format
four
from
full
guard
here:
identical
if
implode('
imports
in
instanceof
instead.
interactive
into
is
it
it.
items-center',
j');
j')`
j,
keeps
kept
label
later
layers
line
list
live
locale
match
matters
may
missing
month
more
n/a
name
no
none:
not
nothing
null)
null,
of
one,
one.
only
optimistic-ui:
order
other
output
parsed
presentational
prod).
props
px-[var(--space-wk-sm,0.5rem)]
py-[var(--space-wk-xs,0.25rem)]'
qualified:
rather
reach
read
reader
readers
reason
refutes
relative
rendered
renders
reports.
resolve
result
role="separator"
screen
sentence
separator
shown
silently,
sits
so
speaker
stands
stays
sticky
string:
support
surface
than
that
the
then
there
this
timezone
token
top-0
translated
turns
two
unchanged.
unknown
use
value
view's
was
what
when
where
which
while
whose
with
worse
writes
writes.
z
z-[var(--z-wk-sticky)]
{
{{
{{--
||
}
};
}}
}}"
—
