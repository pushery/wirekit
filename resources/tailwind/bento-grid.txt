$attributes->class([$classes])
$attributes->getAttributes());
$classes
$gap
$gap,
$gapClass
$gapClass,
$label
$scale
$scale,
$scope);
$slot
',
'2xl'
'2xl']);
'@container/bento',
'auto-rows-[minmax(12rem,auto)]',
'base',
'gap'
'gap',
'gap']);
'gap-0',
'gap-[var(--gap-wk-2xl,2rem)]',
'gap-[var(--gap-wk-lg,1rem)]',
'gap-[var(--gap-wk-md,0.75rem)]',
'gap-[var(--gap-wk-sm,0.5rem)]',
'gap-[var(--gap-wk-xl,1.5rem)]',
'gap-[var(--gap-wk-xs,0.25rem)]',
'gap-[var(--space-wk-2xl,4rem)]',
'gap-[var(--space-wk-lg,1.5rem)]',
'gap-[var(--space-wk-md,1rem)]',
'gap-[var(--space-wk-sm,0.5rem)]',
'gap-[var(--space-wk-xl,2.5rem)]',
'gap-[var(--space-wk-xs,0.25rem)]',
'grid',
'grid-cols-3',
'label'
'lg'
'lg',
'md'
'md'),
'none'
'none',
'scale'
'scale',
'scope'
'sm'
'sm',
'space',
'xl'
'xl',
'xs'
'xs',
($gap)
('bento'),
(Chrome
(and
(see
(silent
(string)
(the
--}}
/
//
111
128).
16.4
:
</div>
<div
=
===
=>
>
?
@2xl
@endif
@endphp
@if($label)
@php
@props([
@props.
A
Accessible
Container
DOM
Declared
Dev-only
Firefox
Fixed
Fully
IS
IT
ITSELF
Match
Measured
NAMED
OWN
On
Pushery\WireKit\WireKit;
RESULT,
Reading
Renders
Rows
Safari
Static
Tailwind
The
There
This
Which
WireKit::resolveClasses('bento-grid',
WireKit::validateProp('bento-grid',
[
['md',
['space',
\Pushery\WireKit\WireKit::warnUnknownProps('bento-grid',
])
]),
`gap`
`space`
a
action
all
allowed
already
amounts
an
and
announces
answer
any
apart,
apart.
appears.
arbitrary
are
aria-label="{{
arm:
arms
as
asserted:
asymmetric
asymmetry
at
auditor
authoring
auto-derived
back
bad
baseline
be
because
bento
bento-cell),
block,
boundary
browser
can
can,
cannot
carries
case
cell
cells
choice,
claim
claims,
class
collapse
collapses
column
column;
component
component's
component:
config('wirekit.components.bento-grid.gap',
container
container-query
content
could
count
covers
crop
data-wk-bento-grid
debug
default
default)
deliberately.
descendant
different
does
drags
drift
early.
either.
element
element,
every
extends
extends.
eye
fallback,
fallbacks)
falls
feature
feature-grid
file
first
fixed
flags
follow
for
from
gap
gap,
get
gets
grid
grid's
grid,
guard
harvests
headings.
here
hierarchy
implode('
imports
in
independent
inline
inside
instead
interactive
interpolated
invalid
invisible
is
it
it.
its
ladder
ladder.
ladders
land
later
leads
let
list
live
lives
loose
map.
match
matrix.
may
mean
mode
n/a
name
names
narrow
never
no
none.
normal
not
null,
of
offers.
on
on:
one
one.
only
optimistic-ui:
or
order
order,
otherwise
out
own
per
person
place
presentational
prod).
prop
prop:
props
qualified:
queries
query
rather
raw
reach
reader
reads
real
reason
reason,
reflow
refutes
region
related
renders
resizable
result
returns
right
role="group"
rung
same
screen
set
should
showcase.
shown
single
size
size.
small,
so
space,
spacing
spans
stack
stacked
stays
strict
strings,
tall
text
than
that
the
their
them.
there
this
three
three-column
throws
tighter
to
token,
track
track,
track.
two
uniform
unknown
use
validateProp
value
view's
viewport.
visual
vocabulary
wall
weight.
when
which
whose
width,
window
would
{
{{
{{--
}
};
}}
}}"
—
