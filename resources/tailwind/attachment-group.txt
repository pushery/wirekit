"3
$attributes->class([$classes])
$attributes->getAttributes());
$classes
$isRow
$label
$orientation
$orientation,
$orientationClasses
$orientationClasses,
$orientationValue
$scope);
$slot
',
'-1'
'0'
'base',
'flex
'flex-col',
'label'
'orientation'
'orientation',
'row'
'row';
'scope'
'stack'
'stack',
'stack']);
'stack'],
'wk-scrollbar
($orientationValue)
(WCAG
(chat
(e.g.
(mail,
(not
(silent
+
--}}
//
2.1.1),
2.1.1,
:
</div>
<div
=
===
=>
>
?
??
@endphp
@php
@props([
@props.
A
Accessible
Blade
Declared
Dev-only
Fully
Pushery\WireKit\WireKit;
Scrolling
The
VALUE,
WCAG
WireKit::resolveClasses('attachment-group',
WireKit::validateProp('attachment-group',
[
['row',
\Pushery\WireKit\WireKit::warnUnknownProps('attachment-group',
])
]),
__('wirekit::Attachments'),
`-1`
`@if`
`match`
`role="group"`
`tabindex`
a
action.
all,
allowlist
alone
an
and
aria-label
aria-label="{{
around
as
at
attachments").
attribute.
auditor
auto-derived
become
before
behind
block,
bubbles,
carries
class
classes
client-only
component's
condition
conditionals
config('wirekit.components.attachment-group.label')
container,
contract
contract:
data-orientation="{{
data-wk-attachment-group
debug
default
delete
detail
does
drift
element
emission.
entry
every
exists
failure
flags
focus
focus-visible:outline-hidden
focus-visible:ring-2
focus-visible:ring-[color:var(--color-wk-ring)]',
for
for.
from
gap-[var(--gap-wk-sm)]',
group
group.
guard
harvests
have
here
hides
holds
horizontally
implode('
imports
in
in_array($orientation,
instead
invisible
is
it
its
keyboard
keyboard-reachable
labeled
later
line
list
lists
live
lives
looks
make
match
match-arm
may
model
must
n/a
name
named
need
never
no
not
nothing
null,
on
one.
one:
operable
optimistic-ui:
overflow
overflow-x-auto
overflow-y-hidden
panels).
passing
plain
prod).
props
purpose:
qualified:
reach
reachable
reaches
reads
reason
region.
resolves
ring.
role
role="group"
row
rows);
said
same
scanner
scroll
scroll-area
scroll-snaps
scroll:
scrolls
server;
snap-mandatory
snap-x
so
so,
source
stack
stack,
statically
stays
stop.
strings,
strips
tab
tabindex
tabindex="{{
ternary
ternary)
that
the
them
there
this
through
tight
to
traceable
true)
unknown
use
variant
vertically
view's
visible
was
way.
which
while
wiring,
with
would
{
{{
{{--
};
}}
}}"
—
