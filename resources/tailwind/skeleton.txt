!
!!}
"the
"wait
$animAttr
$animation
$animation,
$animationValue
$attributes->getAttributes());
$attributes->merge(['style'
$baseShimmer
$c
$c)
$c++)
$colWidths
$colWidths[($r
$cols;
$i
$i++)
$intrinsicHeight
$intrinsicSize
$lines
$lines;
$r
$r++)
$rows;
$scope);
$shimmer
$slot
$wkAnim
$wrapperClasses
%
&&
'');
'',
',
'.($intrinsicHeight).';'])->class([$wrapperClasses,
'.($intrinsicSize).';'])->class([$wrapperClasses,
'100%'
'33%'];
'50%',
'66%'
'66%',
'75%',
'83%',
'animation'
'animation',
'auto
'avatar'
'avatar')
'avatar',
'base',
'block',
'card'
'card')
'card',
'cols'
'data-animation="none"'
'data-animation="none"',
'data-pulse="true"'
'data-pulse="true"',
'false'
'font-[family-name:var(--font-wk-sans)]',
'lines'
'none'
'none']),
'none'],
'pulse'
'pulse');
'pulse',
'pulse';
'rows'
'scope'
'shimmer'
'shimmer'),
'shimmer',
'table',
'text'
'text')
'text',
'type'
'w-2/3'
'w-full'
'width:
'wk-skeleton
'wk-skeleton';
'wk-skeleton-skip-offscreen'])
($animation)
($animationValue
($animationValue)
($rows
($type)
($wkAnim
(`.wk-skeleton-skip-offscreen`),
(animation
(default)
(filter_var($shimmer,
(gradient
(kept
(name
(opacity
(percentage
(see
(silent
(static
(static)
*
+
-
--wk-skeleton-intrinsic-size:
--}}
.wk-skeleton
//
0.5;
0.5rem
0.5rem;
0.5rem`
0.75rem
0.75rem,
0.75rem;
0.75rem;">
0;
1
1)
100%;
100px',
12rem;
16.4
18.0
18.0,
1;
1;">
1px;
1rem;
1rem;">
200px',
200px;'])->class([$wrapperClasses,
25%;
28).'px';
3,
33%;
4,
5,
60px',
60px;'])->class([$wrapperClasses,
66%
75%;
80px',
80px;'])->class([$wrapperClasses,
83%;
8rem;
:
::after
<
</div>
<div
<span
=
===
=>
>
?
@else
@elseif($type
@endfor
@endif
@endphp
@for($c
@for($i
@for($r
@if($type
@php
@props([
@props.
ABOVE
Animation
Animation:
Avatar
Avatar:
Blade
Body
BooleanProp::from($shimmer,
CSS
Card
Card:
DELIBERATE
Declared
Dev-only
ENHANCEMENT
Each
FILTER_VALIDATE_BOOL)
FILTER_VALIDATE_BOOL))
Fully
GPU)
Header
IS
It
JIT
Legacy
Measured
N
NOT
No
Normalized
Nothing
PROGRESSIVE
Per-type
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
Renders
Safari
Separator
Shared
Spacing
TOKENS,
Tailwind
Text:
The
These
This
Those
UNBOUND
Uses
Vary
Visible-only-to-AT
WAI-ARIA
WireKit::resolveClasses('skeleton',
WireKit::validateProp('skeleton',
[
['100%',
['pulse',
['shimmer',
\Pushery\WireKit\WireKit::warnUnknownProps('skeleton',
\Pushery\WireKit\WireKit::warnUnknownProps('skeleton.avatar',
\Pushery\WireKit\WireKit::warnUnknownProps('skeleton.card',
\Pushery\WireKit\WireKit::warnUnknownProps('skeleton.table',
\Pushery\WireKit\WireKit::warnUnknownProps('skeleton.text',
])
]),
__('wirekit::Loading
__('wirekit::Loading')
`--gap-wk-md`
`--gap-wk-sm`
`--wk-skeleton-intrinsic-size`
`.wk-skeleton[data-animation="none"]`
`.wk-skeleton[data-pulse="true"]`
`:shimmer="false"`
`@supports`
`animation`
`background:
`content-visibility:
`gap:
`prop="false"`
`shimmer=false`
`style`
a
above
action
against
align-items:
aligned
all
also
always
an
and
animation
animation).
announce
announcement
any
applies
are
area
aria-busy
aria-label
aria-label="{{
aria-live="polite"
as
as,
asserted:
assistive
at
attribute
attribute.
auto
auto-derived
auto`
avatar
back,
back-compat)
background:
bar
base:
be
before
behind
behind.
belongs
below
beside
bg
bg-[var(--color-wk-bg-skeleton)]
block
block,
body
bool
border-radius:
browser
busy
but
by
call
caller
card
carries
cast
center;
circular
class="flex
class="space-y-2">
class="space-y-3">
class="sr-only">{{
class="{{
color
column
column;
comes.
compiles
component's
composite
config('wirekit.components.skeleton.animation',
container
content
content')
could
count($colWidths)]
count;
custom
custom:
debug
declaration
declared
decreasing/varied
default
default)
defaults
defines
deliberately.
depends
developer's
developers
did
dist/wirekit.css).
does
doesn't
done,
drawing,
each
early.
element
element,
element;
environments
every
existed.
exists
explicitly
exposing
fade,
fake
false
feature
file
filter_var($shimmer,
first
flags
flex-1">
flex-col
flex-direction:
flex-shrink:
flex:
flex;
flip
flipped
flips
floor
floor.
followed
for
from
full-width
gap-2
gap-3">
gap:
geometry
glance.
gradient
guarantee
guard
h-10
h-2
h-3
h-32
h-4
half
handed
have
height
height/width/spacing
here,
here.
here:
hint
if
image
implode('
imports
in
in.
in_array($animation,
inline
interactive
intrinsic-size
is
it
it.
it:
items-center
its
itself
jumping
keeps
keyframes
later
layer;
layout,
leaving
legacy
lets
library's
lighter
like
line
line".
lines
lines.
list
literals
literals.
live
lives
lives.
loading
look
looks
make,
maps
marked
match
matched
max(80,
max-width:
may
mean
mimicking
min-width:
missing,
mixed
mode:
n/a
name:
names
needs
never
new
no
none
none.
normally,
not
not,
nothing
null,
of
off
off-screen
on
on.
one
one.
ones
only
opacity:
opposite
optimistic-ui:
optimization
or
ordinary
override
own
page
paint
pause
per
perfectly
pick
placeholder
placeholder's
placeholder,
placeholder.
placeholder:
presentational
prod).
prop
prop's
property
property.
proportions
props
provides
pulse
pulse)
pulse,
pulse.
put
qualified:
rather
reach
readers
readers.
reads
realism
realistic
reason
refutes
region
region,
remaining
removed
rendering
renders
reported
result
retheme
role="status"
rounded-[var(--radius-wk-full)]"
rounded-[var(--radius-wk-md)]';
row
rows
rule
rules
scale,
scales
scan
screen
screen,
scrolls
see
set
set.
setting
shape
shape;
shimmer
shimmer's
shipped
short
shown
signal
silently.
site
skeleton
skeleton's
skeleton.blade.php)
skip
skip,
slightly
slot
so
stack
state
stayed
stays
still
string,
style
style,
style="background:
style="display:
style="height:
styles
styles)
stylesheet
subtitle)
subtitle).
suggest
support
sweep,
switch
tall
taller,
technology
tells
templates.
text
than
that
the
their
them
theme.
there
these
they
this
title
to
token
travels
true
true)
true);
true,
truthy
tunable
tuned
two
type=text
unknown
use
used
user"
values
var(--color-wk-bg-skeleton);
var(--color-wk-bg-skeleton);"></div>
var(--color-wk-bg-skeleton)`
var(--gap-wk-md);">
var(--gap-wk-sm);
var(--gap-wk-sm);">
var(--radius-wk-full);"></div>
var(--radius-wk-md);"></div>
var(--size-wk-md);
variant
variant.
very
via
view's
w-1/3"
w-1/4"
w-10
w-3/4"
w-5/6"
w-full"
was
way,
way.
were
what
when
where
which
whose
width
width:
widths
wins
with
withhold
without
wrapper
{
{!!
{{
{{--
|
}
};
}}
}}"
}};
}}</span>
·
—
→
⚠️
