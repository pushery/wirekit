"Panel
"calc(100dvh
$asideClasses
$attributes->getAttributes());
$attributes->merge(['style'
$componentClasses
$footer
$header
$hideBelow,
$hideBelowValue
$hideClass
$hideClass,
$label
$maxHeight
$maxWidthClass
$maxWidthClass,
$mobileBehavior,
$mobileBehaviorValue
$resolvedMaxHeight
$scope);
$scrollShadow
$slot
$stickyClass
$stickyClass,
'';
',
'.($offset).';
'.($width).';'])->class([$asideClasses])
'20rem',
'2rem',
'aside'
'aside',
'bg-[var(--color-wk-bg-elevated)]',
'border-[length:var(--border-wk-width)]
'component',
'div'
'false'
'flex
'font-[family-name:var(--font-wk-sans)]',
'hide'
'hide']),
'hideBelow'
'hideBelow',
'inline'
'inline',
'label'
'lg'
'lg',
'lg:max-w-[var(--wk-sticky-panel-w)]',
'lg:sticky',
'max-lg:hidden',
'max-md:hidden',
'max-sm:hidden',
'max-xl:hidden',
'maxHeight'
'md',
'md:max-w-[var(--wk-sticky-panel-w)]',
'md:sticky',
'mobileBehavior'
'mobileBehavior',
'offset'
'rounded-[var(--radius-wk-lg)]',
'scope'
'scrollShadow'
'self-start',
'shadow-[var(--shadow-wk-md)]',
'sm'
'sm',
'sm:max-w-[var(--wk-sticky-panel-w)]',
'sm:sticky',
'top:
'width'
'xl'
'xl']),
'xl:max-w-[var(--wk-sticky-panel-w)]',
'xl:sticky',
($hideBelow)
($hideBelowValue)
($mobileBehavior)
(.wk-scroll-shadow-top/-bottom
(.wk-scroll-shadow-top/-bottom)
(:scrollShadow="false").
(CSS
(WCAG
(a
(any
(axe:
(display:none).
(dynamic
(flex-1
(indefinite)
(literal
(mobile
(not
(silent
(un-stick,
*
+
-
--wk-sticky-panel-w:
--}}
.wk-scroll-shadow
/
//
100%;
100%`,
1px
2
2*offset),
2.1.1)
2.1.1,
214px
240px
3-row
301px
393px
393px,
:
</div>
</{{
<aside>
<div
<this>;
<{{
=
===
=>
>
?
??
@else
@endif
@endphp
@if($scrollShadow)
@if(filled($label))
@if(isset($footer))
@if(isset($header))
@php
@props([
@props.
A
ABOVE
AND
Accessible
Also
An
BOTH
Below
Blade
BooleanProp::from($scrollShadow,
CEILING,
CSS
Changing
Declared
Dev-only
Distance
Explicit
Flex
For
Fully
IS
IntersectionObserver
It
Its
JS.
LANDMARK.
Literal
Measured
NARROWER,
NOT
Non-scrolling
Normalized
OVERLAY
OVERLAYS
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
Reachability
STUCK
Scrollable
Show
So
Tailwind
That
The
They
Two
UNBOUND
UNDER
Unnamed,
WCAG
WireKit::resolveClasses('sticky-panel',
WireKit::validateProp('sticky-panel',
[
['inline',
['sm',
\Pushery\WireKit\WireKit::warnUnknownProps('sticky-panel',
])
]),
`:scroll-shadow="false"`
`<aside
`<aside>`
`<div>`
`@if($scrollShadow)`
`complementary`
`filled()`
`label`
`landmark-unique`
`landmark-unique`).
`position:
`prop="false"`
`tabindex="0"`
`width:
a
above
above.
absent
absolute
actually
address-bar
affordance
affordance.
again.
against
always
an
and
anonymous
anticipate
appears
applied
applies.
applying
approach
are
aria-hidden="true"
aria-label="">`
aria-label="{{
arrive
as
as,
at
at/above
attribute
attribute.
auto-derived
axe
back
back.
background-based
be
because
behaves
behavior,
being
belongs
below
below.
beside
bites
block,
body
body,
body.
border-[var(--color-wk-border)]">
border-[var(--color-wk-border)]',
border-b-[length:var(--border-wk-width)]
border-t-[length:var(--border-wk-width)]
bottom
branch
branches
breakpoint
breakpoint.
breakpoint:
built-in
calc
calc(100dvh
call
caller
came
can
can't
cannot
caps
card
carry
case
cast
ceiling
chain
check
children,
class
class="flex-1
class="h-px"></div>
class="relative
class="shrink-0
class="wk-scroll-shadow-bottom"></div>
class="wk-scroll-shadow-top"></div>
class="{{
classes
cleanly
client-only
collapse
collapses
column
compiles
component's
constrain:
container
containers,
content
content",
cover
covered
data-wk-prose-skip
debug
default
defect.
deliberately
desktop
destination.
does
drives
dvh
each
earlier
edge
empty
end:
engages
entirely
entries
every
everywhere
exactly
exists.
falls
feature
fell
filled($label)
fills
fixed
flags
flex
flex-1
flex-col
flex-grown
flips
floating
flow)
flow,
focus-visible:outline-hidden
focus-visible:ring-[length:var(--ring-wk-width)]
focus-visible:ring-[var(--color-wk-ring)]
focus-visible:ring-inset"
footer
footer,
for
from
full
generates.
gets
given
grew
grid)
growing
h-full
happens
has
have
header
header,
height
height)
here
hidden
hovered
iOS/Android
identically.
identifiable.
implode('
imports
in
inline).
instead
interpolated
is
is:
it
it,
its
itself
jump.
keeps
keyboard-reachable
landmark
landmark,
landmarks
later
layout
leads
leaves
leaving
left
length).
length);
level).
list
literal:
live
looks
makes
match
max-height
max-height:
max-width:
may
mean
meant
measured
min-h-0
min-h-0"
mobile
mobileBehavior
mode:
more
n/a
name
name,
name.
narrow
neighbor
never
no
nobody
normal
not
nothing
notices.
notification-center's
nowhere
null
null,
of
offset
old
on
on.
one
one.
only
only,
opposite
opt-in
opt-in,
optimistic-ui:
or
out
over
overflow
overflow-hidden',
overflow-y-auto
overlays
override
overscroll-contain
overscroll-contain.
own
owns,
page
painted
panel
panel's
panel.
panels
parent,
per
percentage
pinned
plain
plain-CSS
prefix,
prod).
prop's
props
px-[var(--padding-wk-x-md)]
py-[var(--padding-wk-y-md)]
qualified:
rather
reach
reachable
reader
reads
reason
region
remaining
render
renders
reports
resolve
resolves
role:
role="region"
roll
rotor
row/button
rows/buttons
same
sat
scans
scroll
scrollable
scrolled.
scroller
scroller's
scrollport
scrolls.
sees
sentinel
sentinels
sentinels),
server
shadow
shadows
shadows.
shape.
sibling,
sibling.
siblings
side
side's
silently
silently.
site
sitting
smooths
so
source
space.
space:
stacked
stacked,
state
static
stays
sticks.
sticky
sticky`
still
stops
string,
style="max-height:
summary
switch
tabindex="0"
tell
text,
than
that
the
them);
them.
there
therefore
this
this,
through
to
top
top/bottom
true);
true,
truthiness
truthy
two
two-column
un-sticks
unconditional,
unconditionally
unknown
unnamed
use
used
utility
value
variant
view's
viewport
visibility
was
way);
wearing
were
what
when
where
whether
which
wide
wider.
width
width.
width:
with
without
wk-scrollbar
worse
would
wrapper
wrong
x-cloak
x-data="wirekitStickyPanelShadows()">
x-ref="bottomSentinel"
x-ref="scroller"
x-ref="topSentinel"
x-show="bottomShadow"
x-show="topShadow"
x-transition.opacity
your
{
{$offset})";
{{
{{--
}
};
}}
}}"
}};">
}}>
—
