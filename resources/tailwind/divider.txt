!
!$isVertical)
$attributes->class([
$attributes->getAttributes());
$attributes->has('aria-label')
$attributes->has('aria-labelledby'))
$borderColor
$borderColor,
$borderColor])></span>
$isVertical
$label
$orientation,
$orientationValue
$scope),
$variant,
$variantValue
&&
',
'base',
'bold'
'bold']),
'border-[var(--color-wk-border)]',
'border-[var(--color-wk-border-strong,var(--color-wk-border))]',
'border-[var(--color-wk-border-subtle)]',
'border-t
'default',
'flex
'font-[family-name:var(--font-wk-sans)]',
'horizontal',
'label'
'orientation'
'orientation',
'scope'
'self-stretch
'subtle'
'subtle',
'text-[color:var(--color-wk-text-muted)]',
'text-[length:var(--text-wk-sm)]',
'variant'
'variant',
'vertical'
'vertical';
'vertical']),
($orientation)
($variant)
($variantValue)
(no
(silent
--}}
/
//
/>
/>`,
</div>
<div
<hr
<span
=
===
=>
>
></div>
@class(['grow
@else
@elseif($isVertical)
@endif
@endphp
@if(!
@if($label
@php
@props([
@props.
Declared
Dev-only
Fully
HERE
Horizontal
Measured
Pushery\WireKit\WireKit;
Renders
So
That
The
Two
Vertical
WireKit::resolveClasses('divider',
WireKit::validateProp('divider',
[
['default',
['horizontal',
\Pushery\WireKit\WireKit::warnUnknownProps('divider',
])
]),
`:aria-label`
`:label`
`<x-wirekit::divider
`aria-label`
`aria-label`.
`aria-labelledby`
`chat-marker`
`childrenPresentational:
`date-separator`
`separator`
a
accessible
action
alongside
already
an
and
announced
any
aria-label="{{
aria-orientation="vertical"
around
as
asserted:
attribute
auto-derived
be
because
below
block,
border-b-0
border-l',
border-l-0
border-r-0',
border-t',
call
caller
caller,
calling
centered
class="shrink-0
comment
component
component's
contributes
could
data-wk-prose-skip
debug
default
defect
developer
directly
divider
docs
does
early.
element,
emits
emitted
every
explaining
file
file,
flags
for
from
got
guard
had
has
here:
implode('
imports
in
inaudible.
interactive
is
it
items-center',
its
label
label)
label.
label="OR"
later
left
list
live
lived
match
may
n/a
name
name.
no
not
nothing
null,
of
one,
one.
optimistic-ui:
own
page
passes
point
presentational
prod).
props
px-[var(--space-wk-sm,0.5rem)]">{{
qualified:
rather
re-implements
reach
reason
refutes
renders
renders,
result
role="separator"
separator
shape
shown
siblings
site
so
span
still
supplied
suppresses
teaches,
than
that
the
their
there
this
to
true`
twice.
unfixed
unknown
unnamed
use
version.
view's
was
what
which
whole
whose
why,
wins:
with
workaround
worked
{
{{
{{--
};
}}
}}"
}}</span>
—
