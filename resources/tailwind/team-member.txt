!
!==
"+3"
"+7").
"+N"
"5
"A
"Ada
"background-color:
"box-shadow:
$alt
$attributes->class([$classes])
$attributes->class([$groupClasses])
$attributes->getAttributes());
$attributes->merge($avatarStyle
$avatarStyle
$avatarStyle]
$baseClasses
$chipClasses
$chipSize
$chipSize,
$classes
$derivedInitials
$fromInitials
$fromInitialsStyle
$gap
$groupClasses
$initials
$initials);
$initials;
$label
$name
$palette
$remaining
$ringColor
$ringWidth
$ringWidth;
$role
$scope);
$shape,
$shapeClasses
$shapeClasses])
$shape`
$size,
$sizeClasses
$sizeClasses,
$slot
$src
$src)
$statusColor
$statusColor,
$statusLabel
$statusLabel)
$statusRingStyle
$statusRingStyle.';'
$statusSize
$statusSize,
$statusVariant
$statusVariant,
$total
$words
&&
''
'')
'',
'';
''])));
',
'absolute
'alt'
'avatar'
'away'
'base',
'bg-[var(--color-wk-bg-muted)]',
'bg-[var(--color-wk-danger)]',
'bg-[var(--color-wk-success)]',
'bg-[var(--color-wk-text-muted)]',
'bg-[var(--color-wk-warning)]',
'border-[length:var(--border-wk-width)]',
'border-[var(--color-wk-border-subtle)]',
'busy'
'circle'
'circle'),
'dot'
'dot')
'dot'),
'false'
'flex
'font-[family-name:var(--font-wk-sans)]',
'font-[number:var(--font-wk-heading-weight)]',
'fromInitials'
'initials'
'inline-flex
'label'
'lg'
'lg',
'md'
'md'),
'md',
'name'
'offline'
'online'
'overflow-hidden',
'relative
'remaining'
'ring'
'ring')
'ring']);
'ring-2
'role'
'rounded-[var(--radius-wk-md)]',
'rounded-full',
'scope'
'select-none',
'shape'
'shape',
'shrink-0',
'size'
'size',
'sm'
'sm',
'square'
'square']))
'src'
'status'
'statusVariant'
'statusVariant',
'text-[color:var(--color-wk-text)]',
'text-[color:var(--color-wk-text-muted)]',
'transparent',
'true'
'var(--color-wk-danger)',
'var(--color-wk-success)',
'var(--color-wk-text-muted)',
'var(--color-wk-warning)',
'w-10
'w-12
'w-16
'w-2
'w-2.5
'w-3
'w-3.5
'w-6
'w-8
'wk-avatar-group',
'xl'
'xl']),
'xs'
'xs',
("Head
($derivedInitials
($fromInitials
($shape)
($size)
($status
($status)
(RTL-safe)
(WireKit::validateProp('avatar',
(`alt=""`
(count($words))
(default)
(default).
(dist/wirekit.css):
(dot
(e.g.
(for
(int)
(no
(removed
(silent
(string)
(with
+
-
--}}
-1,
.wk-avatar-group
/
//
/>
0
0)
0,
0-8
1
1)),
1).mb_substr($words[count($words)
1.34-8
1.5,
1.79
1.79-4
12c2.21
1],
2,
2.5,
24
24"
2;
2c-2.67
3
4
4-1.79
4-4s-1.79-4-4-4-4
4v2h16v-2c0-2.66-5.33-4-8-4z"
4zm0
:
:from-initials="$avatar
:initials="$derivedInitials"
:src="$avatar"
</div>
</li>
</span>
</svg>
<div
<img
<li
<path
<span
<svg
<x-wirekit::avatar
<x-wirekit::link>
=
===
=>
>
></span>
?
?:
??
@class([
@else
@elseif($initials)
@endif
@endif>+{{
@endphp
@if($alt)
@if($label)
@if($remaining
@if($role)
@if($slot->hasActualContent())
@if($src)
@if($status
@if($statusColor
@if(filled($label))
@php
@props([
@props.
AA-contrast
AND
Accessible
Ada
An
AvatarPalette::for((string)
BOTH
Base
Blade
BooleanProp::from($fromInitials,
Box-shadow
CLASS,
CSS
Combine
Counter
Declared
Dev-only
English,
First
Fixed
Fully
HERE,
Head
Hidden
How
Icon-only
Ignored
L"
Links
Literal
Lovelace,
Measured
Named,
Normalized
Nothing
ONLY
Only
Optional
Overlap
PREG_SPLIT_NO_EMPTY)
Platform"
Platform").
Portrait.
Pushery\WireKit\Support\AvatarPalette.
Pushery\WireKit\Support\AvatarPalette;
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
Renders
Ring
Same
See
Shape:
Size
Sizes
Status
Tailwind
Text
The
Their
Through
Two
UNBOUND
UTF-8.
Uses
WHILE
When
WireKit
WireKit::resolveClasses('avatar',
WireKit::resolveClasses('avatar.group',
WireKit::resolveClasses('team-member',
WireKit::validateProp('avatar',
With
Without
[
['circle',
['dot',
['style'
['xs',
[])->class([$baseClasses,
[];
\Pushery\WireKit\WireKit::warnUnknownProps('avatar',
\Pushery\WireKit\WireKit::warnUnknownProps('avatar.group',
\Pushery\WireKit\WireKit::warnUnknownProps('team-member',
])
]),
]);
__('wirekit::Away'),
__('wirekit::Busy'),
__('wirekit::Offline'),
__('wirekit::Online'),
`:remaining="7"`
`alt=""`
`aria-hidden`
`aria-label`
`class="cirlce"`
`default
`initials`
`prop="false"`
`shape="cirlce"`
`size`
`src`),
`statusVariant="Dot"`
`status`
a
about
above
accessibility
accessible
action
affects
after
against
all.
almost
alone
alone,
alone.
alt
alt=""
alt="{{
always
an
and
announce
announced
any
anyone
anything
application
applies
are
aria-hidden="true"
aria-hidden="{{
aria-label
aria-label="{{
arm
around
array_filter([$fromInitialsStyle,
as
as,
asserted:
assistive
at
attribute
attribute.
attributes
auto-derived
avatar
avatar,
avatars
avatars.
background
background,
base
be
because
behalf
bg
block,
both
bottom-0
bottom-right
bottom-right)
box
box-shadow
box-shadow.
branch
branches:
broken
browse
by
call
caller's
card.
carries
carrying
cast
catalog,
centering,
chip
circle
class
class="block
class="flex
class="min-w-0">
class="sr-only">{{
class="w-3/5
class="w-full
class="{{
classes:
codepoint
collaborators").
color
color.
color:
colored
compare
compiles
component's
concentric
condition
config('wirekit.components.avatar.shape',
config('wirekit.components.avatar.size',
config('wirekit.components.avatar.status-variant',
consistent
content
controls
conveyed
corner
costs
could
counter.
covers
crc32-palette
d="M12
data-wk-prose-skip
data-wk-team-member
data-wk-team-member-links
data-wk-team-member-name
data-wk-team-member-role
debug
decorative
decorative-image
default
deliberate
dense
derive
derived
deterministic
developer
developer's
dims
disc.
docs
does
dot
double-ring
drop
each
early.
element
element,
element.
elements
else
emit
emitted
empty
every
everyone
exact
exactly
existed
fact
fallback
false);
false,
feature
file
fill="currentColor"
flags
flex-col
flips
flush
focus
focusable
font
font-[number:var(--font-wk-heading-weight)]
for
for.
four
from
from-initials
from-initials:
full
fully
gap
gap-[var(--gap-wk-sm)]
gap-[var(--gap-wk-sm)]">
genuinely
gets
glance
goes
group
guard
h-10
h-12
h-16
h-2',
h-2.5',
h-3',
h-3.5',
h-3/5
h-6
h-8
h-full
had
half
happened:
has
hash
hash.
hears
height,
helpers.
here
here,
here.
hid
hidden.
hides
hiding
hiding,
how
icon
if
image
image).
implode('
imports
in
independent
indicator
individual
information
initial,
initials
initials,
initials-fallback
inline
inline-flex
inner
input
inside
instead
instead.
interactive
into
is
it
it,
it.
items-center
its
itself
just
justify-center
justify-center',
keep
key
label
labels
language.
last
later
layer
layout
legal.
like
links
list
live
live-region
lives
lives,
logical
look
looking
looks
make
makes
many
margin
marker
markup.
match
may
mb_*
mb_strtoupper(mb_substr($words[0],
mean
medium
mirrors
mode
mode.
mode;
more
more:
multiplies
muted
n/a
name
name,
names
need
needed,
needs
negative
never
next
no
nobody
non-ASCII
none.
not
not:
nothing
null
null"
null,
null.
object-cover"
of
on
on.
once
one
one,
one.
only
opposite
optimistic-ui:
optional
or
other
outer
overflow
overrides
own
page
page.
pair
pairs
palette
pass
path
people
per
per-app
person
person,
person.
place
places
portrait
preg_split('/\s+/u',
premise
presence
present
presentational
prod).
produced
prominent
prop's
props
provide
put
qualified:
radius,
radius.
rather
reach
reached
read
read,
reader
readers
readers).
reading
reads
real
reason
reasoning
redundant
refutes
region
region,
reliably
rem
removes
render
rendered
rendering
renders
renders.
replaces
result
right
right-0',
ring
ring-[var(--color-wk-bg-elevated)]',
ring-offset-*
role
role="group"
role="img"
role="img",
role="status"
role="status".
roster
roster,
row
same
says
scale
scaling
scanner.
screen
screen-reader
second
see
separated
set
set,
shadows:
shape
show
shown
shown.
shrink-0',
sighted
silence
silently.
silhouette.
site
sits
size
size="xl"
sizing
sizing.
slice
slot
slot.
slot:
small
so
something
span
square
sr-only
src="{{
stack.
standing
state
status
status:
stays
still
stops
string
string,
strings
style
substr()
subtree
surface-color
surrounds
technology
text
text-[color:var(--color-wk-text)]">{{
text-[color:var(--color-wk-text-muted)]"
text-[color:var(--color-wk-text-muted)]">{{
text-[length:var(--text-wk-lg)]',
text-[length:var(--text-wk-md)]
text-[length:var(--text-wk-md)]',
text-[length:var(--text-wk-sm)]
text-[length:var(--text-wk-sm)]',
text-center',
text.
than
that
the
their
theme-independent.
there
these
they
thin
thing
this
those
three
through
throughout:
to
total.
trailing
translated
treatment
trim($name),
trim(implode('
true
truthy
two:
typed
unknown
unlabeled
use
used
utilities
v4).
validator,
values
var(--color-wk-bg-elevated),
variant
variant,
very
via
view's
viewBox="0
visually
wants),
warning,
was
well
were
what
when
where
wherever
which
whichever
white
whole
whose
width,
with
without
worse.
would
you
{
{$gap}px
{$palette['bg']};
{$palette['fg']};";
{$ringColor}";
{$total}px
{{
{{--
}
},
};
}}
}}"
}}">{{
}}</span>
}}>
—
→
