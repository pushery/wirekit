!
!$isFull
!$isPartial;
!==
"."
"4
"4".
"4,0".
"4.0";
"4.2
"Average
"Could
"no
"nothing
"set
"take
$announceError
$announceErrors
$announcedValue
$announcedValue,
$attributes
$attributes->class([$classes])
$attributes->except(['announceErrors',
$attributes->except(['aria-label',
$attributes->except(['id',
$attributes->get('aria-describedby',
$attributes->get('aria-label')
$attributes->get('aria-label'))
$attributes->get('name'),
$attributes->get('name',
$attributes->getAttributes());
$attributes->whereStartsWith('wire:model')
$callerDescribedBy
$clamped
$classes
$clearable
$describedBy
$error
$errors->first($name)`,
$fraction
$fraction)
$fullStars
$fullStars;
$hint
$i
$i)
$i++)
$iconShapes
$iconShapes[$icon]
$iconShapes['star'];
$iconSize
$id
$id);
$id.'-error'
$id.'-hint'
$isEmpty
$isFull
$isPartial
$label
$max));
$max;
$max])
$name
$numericValue
$numericValue,
$numericValue;
$optimistic,
$optimisticArgs),
$optimisticConfig
$ratingSeed
$readonly
$readonly)
$required
$scope);
$shape
$shape['path']
$shape['viewBox']
$slot
$value
$value,
$wrapperClasses
&&
'#'.$id.'-error',
'')
'')).'
''));
'').'
',
'-1'
'-1'"
'.$callerDescribedBy);
'.$max.($clearable
'0
'0'
'M12
'M2
'M5
'_notify',
'_notify'`
'action'
'after'
'announce-errors']);
'announceError'
'args'
'aria-describedby'])->whereDoesntStartWith('wire:model')->class([$wrapperClasses])
'base',
'bind'
'block',
'circle'
'clearable'
'debug'
'diamond'
'error'
'errorRegion'
'false'
'false'"
'font-[family-name:var(--font-wk-sans)]',
'font-[number:var(--font-wk-body-weight)]',
'font-medium',
'h-4
'h-6
'h-8
'heart'
'hint'
'icon'
'inline-flex
'label'
'lg'
'max'
'md'),
'messages'
'mode'
'name']);
'optimistic'
'optimisticArgs'
'path'
'pending'
'rating',
'rating-');
'readonly'
'reject',
'required'
'reverted'
'scope'
'size'
'sm'
'square'
'star'
'star',
'text-[color:var(--color-wk-text)]',
'text-[color:var(--color-wk-text-subtle)]
'text-[color:var(--color-wk-warning)]
'text-[length:var(--text-wk-md)]',
'thumb'
'tracking-[var(--font-wk-letter-spacing)]',
'true'
'value'
'viewBox'
'{
(!hovered
("4
($clamped
($errors
($hint
($optimistic
($size)
(1
(@aware
(APG):
(a
(auto
(bool)
(driven
(e.g.
(float)
(int)
(less).
(more),
(rating
(silent
(the
(when
*
*/
+
-
--}}
/
/*
//
/>
0
0"
0)">
0,
0-2
0-2-2H5z',
0-2-2h-6.31l.95-4.57.03-.32c0-.41-.17-.79-.44-1.06L13.17
0-20z',
0.0–0.99
0;
1
1)
1.18
1.54-.5
1.84-1.22l3.02-7.05c.09-.23.14-.47.14-.73V11z',
10
10-10L12
100
11.54L12
12.28
12l10
14.14
14.76
15.36
16.5
17.77l-6.18
19.58
1;
2
2-2V5a2
2.09C13.09
20
20h2V10H2v10zm20-9a2
21.35l-1.45-1.32C5.4
21.35z',
22
24
24',
24x24
2L2
2a10
2h14a2
2h9c.83
2l3.09
2v14a2
2z',
3
3.25L7
3.41.81
3.78
3.78-3.4
3.81
3a2
3c1.74
4
4-star
4.2
4.42
4.5
4.87
4px
5
5),
5.42
6.26L22
6.86-8.55
6.88L12
7
7.5
7.59
7.59C7.22
7.95
8.45
8.5
8.5c0
9.27l-5
9.27l6.91-1.01L12
9v10a2
:
:aria-checked="rating
:class="(hovered
:max
:required="$required">{{
:tabindex="rating
:value="rating"
</button>
</div>
</label>
</span>
</svg>
<=
<button
<button>
<div
<input
<label
<label>
<label>.
<p
<path
<span
<span>
<svg
<svg>,
<x-wirekit::label
=
===
=>
>
>=
?
??
??=
@aware(['announceErrors'
@click="run(clearTarget({{
@click="select({{
@else
@elseif($hint)
@endfor
@endif
@endphp
@endunless
@for($i
@if($announceError)
@if($callerDescribedBy
@if($describedBy
@if($error)
@if($isPartial)
@if($label
@if($label)
@if($optimisticConfig)
@if($readonly)
@if($required)
@keydown.arrow-down.prevent="stepDown()"
@keydown.arrow-left.prevent="stepDown()"
@keydown.arrow-right.prevent="stepUp()"
@keydown.arrow-up.prevent="stepUp()"
@keydown.end.prevent="selectLast()"
@keydown.home.prevent="selectFirst()"
@mouseenter="hovered
@mouseleave="hovered
@php
@props([
@props.
@unless($readonly)
A
A11y:
ABOVE
AND
ARIA
All
Alpine
Alpine's
An
And
ArrowDown
ArrowLeft
ArrowRight
ArrowUp
BEATS
BY
Blade
Block-level
BooleanProp::from($clearable,
BooleanProp::from($readonly,
BooleanProp::from($required,
BooleanProp::stripFalseHtmlFlags($attributes);
By
CONTROL.
CSP
Change
DECLARED
DENSE,
DOM
Declared
Dev-only
EMPTY
EQUALITY,
Empty
Every
Extra
FILL
FOUR
Fallback
Filled
Fully
GROUP,
HTML
Hidden
Home/End
INHERITED
INSIDE
INTERACTIVE
IS
Icon
Interactive
Interactive:
It
Its
Keeping
Label
Laravel
Let
Livewire
LocalizedNumber::format((float)
Measured
Mirrors
NESTS
NO
NOT
Naming
No
Nonsense,
Normalized
Null
OWN
On
One
Only
Outside
POSITION,
PRESENCE,
Partial
Plain
Preflight
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\Support\LocalizedNumber;
Pushery\WireKit\WireKit;
READONLY
Radiogroup
Read
Readonly:
Rendered
Renders
Reported
Required
SCORE,
SERVER
SVG
SVGs
Same
Set
Silent:
So
Static
StrictnessGate
Strip
Support
Support\DomId
Supports
THIS
TOP
TWICE
That
The
This
Translated,
Two
UNBOUND
Undeclared,
WHICH
What
WireKit::resolveClasses('label',
WireKit::resolveClasses('rating',
Without
Written
[
['value'
[],
\Pushery\WireKit\Support\AlpinePayload::from([
\Pushery\WireKit\Support\DomId::unique($attributes->get('id')
\Pushery\WireKit\WireKit::warnUnknownProps('label',
\Pushery\WireKit\WireKit::warnUnknownProps('rating',
])
]),
]);
],
];
__('wirekit:::value
__('wirekit::Could
__('wirekit::Rating')
__('wirekit::Saving'),
`$clamped`
`$error
`$id`
`$this->validate()`
`:class`
`:error`
`:tabindex`
`:value`
`:value`.
`>=`
`>=`:
`@aware`
`@props`
`_notify()`
`after:
`args`
`aria-describedby`
`aria-label`
`aria-labelledby`
`block`,
`clearTarget()`
`closest('[x-data]')`
`data-wk-server-value`
`disabled="false"`
`error`
`for`,
`id`
`inline-block`:
`input`:
`label`
`ms-0.5`
`name`
`prop="false"`
`rating`,
`rating`;
`required`
`run()`.
`this`,
`value`
`x-data`,
`{{
a
able
about
above
absolute
accepts
across
action
added
adopting
after
after-hook
again,
against
agree
agreed
aliases
all
all,
all.
almost
alone,
alone.
along
already
also
alternative
always
an
and
announce
announce-error
announceErrors)
announced
announced.
announcement
announces
announces,
announces.
answer
any
app
appears
appended
application.
arbitration
are
arguments
aria-atomic="true"
aria-checked
aria-checked="true"
aria-checked="{{
aria-describedby.
aria-describedby="{{
aria-hidden="true"
aria-hidden="true">*</span>@endif
aria-invalid="true"
aria-label
aria-label="{{
aria-labelledby="{{
aria-live="assertive"
aria-live="polite"
aria-required,
aria-required="true"
around.
array_values((array)
arrives
as
as,
ascent
asked
asserted:
assistive
association
assumed
assumed.
asterisk.
at
at,
at.
attribute
attribute.
attributes
attributes,
auto-derived
average).
axe-core's
axes
back
back"
back.
background
bag
bag,
bag.
bag:
bare
baseline-aligns
be
because
been
before
behaves
behavior
behind,
being
belongs
below
below,
below.
between
binding
binding,
binds
binds.
block
block,
blocks
boolean
boot
booted,
both
bound
box
box,
branch
build
building
built
but
button's
buttons
by
byte-identical
call
call,
call.
caller
caller's
cannot
capability
card,
card.
carried
carry
carrying
cast
change
change,
changes
channel
child
choosing
chosen
chosen"
chosen.
claim
clamps
class="cursor-default">
class="inline-flex
class="relative
class="sr-only"
class="text-[color:var(--color-wk-danger-text)]
class="text-[length:var(--text-wk-sm)]
class="transition-colors
class="{{
classes:
clean
clear
clearable:
clickable
clip-path
clip-path,
clipped
collapses
colors
comes
commonest
compare
compares
compiles
complain
component
component's
component,
component.
components
config('app.debug'),
config('wirekit.a11y.announce_error',
config('wirekit.components.rating.max',
config('wirekit.components.rating.size',
config.
conflicting
constraint,
consulted.
container
control
control.
control:
controls
copy
copy,
correct
correct,
corrected
correctly.
could
current
cursor-pointer"
d="{{
danger-text
dark
dark:
data-wk-optimistic-announcer
data-wk-prose-skip
data-wk-server-value="{{
debug
decimal
decision
deduped
default
default)
defect
defines
definition,
deliberate:
dense.
describes
description
deselected
design
designed
developer
did
difference
different
direction
directly
disables
disagree
display
display:block,
displaying
div,
do
documented
documents
does
does.
doing.
double-announce
down
down.
draws
drop
dropped
dropping
drops
duration-[var(--transition-wk-duration)]
duration-[var(--transition-wk-duration)]"
each
early.
effect
either
either,
either.
element
element,
element.
elsewhere)
embeds
emit
empty
empty,
empty:
ends.
entry
equality,
error
error,
evaluates
even
event,
every
everybody
exactly
exceeds
exclude
exist
existed
existing
exists
explicit
explicitly
exposed
expressions
extra
facet
factory
factory's
fail
failed
fallback,
false
false);
false,
feature
field
field's
file
fill
fill-[var(--color-wk-warning)]
fill-[var(--color-wk-warning)]'
fill-none
fill-none'
fill-none'"
filled
filter
finds
fires
first
first,
five
five-star
five.
fix
fix:
flags
fleet,
flex
flex-col
flight
flight,
flips
floor
floor($numericValue);
flush
focus
focus-visible:outline-hidden
focus-visible:ring-[length:var(--ring-wk-width)]
focus-visible:ring-[var(--color-wk-ring)]
folded
for
for.
for="…">
foreground,
form
form,
formatting.
forms.
found,
fractional
from
front.
full/empty
further
gap
gap-0.5"
gap-1',
gave
generic
gesture
give
give,
global
goes
grammar;
grid
group
guard
hand-mount
hardcoded
has
have
hears
hears,
height,
here
here.
here:
hidden
hint,
honest
hover/keyboard
how
hydration
icon
icon:
id
id;
id="{{
identical
if
image
image"
implements
implode('
imports
in
including
indicator
inert.
init.
initial
inline-block
input
input,
input.blade.php.
inset(0
inset-0"
inside
instead
int
integers.
interact
interaction.
interactive
interactive.
interchangeable,
interchangeable:
interpolated
interrupt
into
invalid
invited
is
it
it"
it,
it.
item.
its
itself
jump
just
keeps
kept
key
keyboard
know
knows
knows.
label
label,
label.
labelable
labels
landed
lands
last
last.
later
layer
layer's
leave
leaves
leaving
left
legitimate
legitimately
like
line
line-height)
list
listener
lit
live
lives
localized
looked
looks.
lose
low
lowering
majority
make
makes
mangled
mark
marking
markup
match
max(0,
max:
maxPrecision
maxPrecision:
may
mean
means
meant
measure
measured
message
method
method;
min((float)
mirrors
mis-click;
mode
mode,
model
model,
model;
moment
morph
most
move
ms-0.5"
must
n/a
name
name,
name="{{
named
names
naming
native
nearest
need
needed).
needs
nested
nests
network
never
new
newline
next
no
nobody
node,
non-optimistic
none
not
not,
nothing
nothing.
nothing:
null
null)?->first($name);
null,
null])
number
observeServerValue
observed
of
off.
often
old
omission.
on
on.
once
one
one.
only
opens
operate.
opinion"
opposite
opt
opt-in
optimistic
optimistic-ui:
optional
or
orphan
other
otherwise
out
out.
outrank
outside
over
overlapping
overrides
own
own,
page
page,
page-unique
page.
paint
paint;
parent
parent's
parser
parser,
parses
part
partial
passes
passthrough
past
path
path,
path.
paths
pattern
per
per-star
permanent
permanently,
pick
picking
picture
places
plain
pluralization
plus
point
pointed
points
polite
politely
position
pre-Alpine-init
pre-hydration
pre-morph
precedence:
precision
presentational
prod).
product
promises
prop
prop's
properties
props
purpose
purpose:
pushed
put
puts
qualified:
question
queued
radio
radiogroup
radiogroup),
radiogroup:
radiogroups
radios
rated.
rather
rating
rating,
re-initializes
reach
reachable
reaches
reactive
reactively.
read
reader
readonly
reads
reads:
really
reappeared
reason
reason.
receives
record
records
redundant
reference
refutes
regardless,
regenerated
region
region,
region.
remove
render
rendered
rendered.
rendering,
renders
repeat
repeating
replaces
request
required
resolve
resolved
resolves
result
reverse.
rewrites
rewrites,
right
role="img"
role="radio"
role="radiogroup"
rollback
rollback.
rounded-[var(--radius-wk-sm)]
rounding
roving
row
row,
run
run()
runs
runs.
said
same
save"
save.
say
says
says,
scales
scan
scan;
scope
scope.
score
score.
score:
screen
second
second,
see
see.
seed
seed,
segmented-control
select()
selected
selected,
selection,
sent
separate
separately.
separator
server
server's
server-side
seven
shape
shapes
shipped
should
show
showed
shown
shown,
shows.
siblings.
silently
silently.
simply
simultaneously
single-select
single-select,
sit
site
site.
size
size="sm"
sizes
skip
slot
slot,
snapshot.
so
someone
something
space
span
specific
spelled
spellings
spirit.
spreads
star
star:
stars
stars".
stars',
stars,
stars.
starting
star|:count
state
state.
static
stays
stepping
still
stop
stop,
stop:
stray
string,
stroke-width="1.5"
stroke-width="1.5">
stroke="currentColor"
strut's
style="clip-path:
submission
submit
submit.
submits
submitted
submitting
such
summary
support.
supported
surface
surrounding
survive
survives
synced
tab
tabbing
tabindex
tabindex="{{
tag,
take
taken
taken:
takes
tech
tell
tells
ternary
text
text,
text-[color:var(--color-wk-danger-text)]">{{
text-[color:var(--color-wk-text)]">{{
text-[color:var(--color-wk-text-muted)]">{{
text-[color:var(--color-wk-text-subtle)]
text-[color:var(--color-wk-warning)]
than
that
that.
that:
the
their
them
then
there
there,
these
they
thing
things
this
three
threshold
through
times.
timing,
to
together
tokens
top
trailing
trans_choice('wirekit:::count
transition-colors
trim
trim(($error
trim((string)
true'
true);
truthy
twice
two
type="button"
type="hidden"
unable
unchanged.
unconditionally
undeclared
under
undone.'),
unknown
unlike
unreachable,
untestable.
until
up
update
use
used
uses
valid
validation
validator.
value
value,
value-bearing
value.
value="{{
values
variable
verified
via
view's
viewBox
viewBox.
viewBox="{{
visible
visibly
w-4',
w-6',
w-8',
was
way
way.
way:
wear
were
what
when
whenever
where
whether
which
whichever
while
whitespace,
whole
whose
why
why:
wider
width
window
winning
wins,
wins:
wins;
wire:model
wire:model.
with
without
words
would
wrapper
wrapper,
wrapping
write
writes
written
wrong
wrote,
wrote.
x-bind:aria-busy="isPending"
x-data
x-data="wirekitOptimistic({{
x-data="wirekitRating({{
x-text="announcement"></div>
zero
zero,
{
{{
{{--
||
}';
};
}}
}}"
}}",
}}"/>
}}">
}}%
}})"
}})">
}}))
}}))"
}}-error"
}}-hint"
}}-label"
}}</p>
}}</span>
}}</x-wirekit::label>
}}>
}}@if($required)<span
}}`
—
…"
…").
⚠️
