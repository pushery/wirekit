!
!==
"input,
"invalid"
"this
$announceError
$announceErrors
$attributes
$attributes->except(['announceErrors',
$attributes->except(['id',
$attributes->get('aria-describedby'),
$attributes->get('name');
$attributes->get('required',
$attributes->getAttributes());
$boxClasses
$defaultId
$defaultId,
$describedBy
$error
$errorMessage
$errorMessage)
$errors
$fieldGroup
$fieldGroup,
$groupError
$groupError,
$groupOwnsBagEntry
$hasError
$hint
$id
$id.'-error'
$id.'-hint'
$isInvalid
$label
$labelClasses
$nameAttr
$nameAttr;
$scope);
$sizing
$sizing['box'],
$sizing['dot']
$value
$value)
$variant,
$variantValue
$wkFieldSet
$wkRequiredMarker
&&
'
''
''));
',
'-'
'announce-errors',
'announceError'
'aria-describedby'])->class(['peer',
'base',
'bg-[var(--color-wk-bg-input)]',
'border-[length:var(--border-wk-width)]',
'border-[var(--color-wk-border-strong)]',
'card'
'card']),
'cursor-pointer',
'default'
'default'),
'default',
'dot'
'duration-[var(--transition-wk-duration)]',
'error'
'group
'groupError'
'hint'
'label'
'lg'
'md'),
'peer-checked:border-[var(--color-wk-accent)]',
'peer-checked:peer-hover:border-[var(--color-wk-accent)]',
'peer-disabled:cursor-not-allowed',
'peer-disabled:opacity-[var(--opacity-wk-disabled)]',
'peer-focus-visible:ring-[length:var(--ring-wk-width)]',
'peer-focus-visible:ring-[var(--color-wk-ring)]',
'peer-focus-visible:ring-offset-[length:var(--ring-wk-offset)]',
'peer-focus-visible:ring-offset-[var(--color-wk-ring-offset)]',
'peer-hover:border-[var(--color-wk-border-strong-hover)]',
'radio-');
'relative
'rounded-full',
'scope'
'size'
'sm'
'sr-only'])
'transition-colors',
'value'
'variant'
'variant',
'w-1.5
'w-2
'w-2.5
'w-4
'w-5
'w-6
'wk-field-set']);
'wk-touch-target',
'wkFieldSet'
'wkFieldSet',
(!
($errors
($fieldGroup?->covers($nameAttr)
($fieldGroup?->isInvalid($errors
($groupError
($groupOwnsBagEntry
($isInvalid)
($size)
($variant)
(@aware
(Absolute
(a
(aligned
(bool)
(grouped
(inline
(inline-flex)
(it's
(items-center/justify-center),
(silent
(the
(when
);
+
--}}
.
.=
.peer,
//
/>
2.20.0
2.75rem
20px
2×
:
:has()
</div>
</label>
</span>
<div
<input
<label
<label>
<p
<span
=
===
=>
>
?
??
??=
@aware(['announceErrors'
@elseif($hint)
@endif
@endphp
@if($announceError)
@if($describedBy
@if($hasError
@if($isInvalid)
@if($label)
@if($value
@php
@props([
@props.
A
A11y:
Auto-generate
BOX
Blade
BooleanProp::from($groupError,
BooleanProp::stripFalseHtmlFlags($attributes);
Card
Declared
Dev-only
DomId::unique,
Each
Error
FIRST
GROUP
HERE
HTML
ID:
IDENTICAL
INSIDE
INTO
IS
It
LATER
Laravel
Livewire
Mirrors
NOT
Native
Not
OR
One
PLUS
PRESENCE,
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
Radio's
Read,
SOME
Same
Set
Size
So
Strip
Take
That
The
This
Visual
What
Whether
WireKit
WireKit::resolveClasses('radio',
WireKit::validateProp('radio',
WireKit::warnUnknownProps('radio',
Written
[
['box'
['default',
\Illuminate\Support\Str::slug((string)
\Pushery\WireKit\Support\DomId::unique($attributes->get('id')
\Pushery\WireKit\Support\FieldGroup
\Pushery\WireKit\Support\FieldGroup::describedBy(
])
]),
`:group-error="false"`
`@aware`
`@props`
`card`
`checkbox.blade.php`.
`covers()`
`disabled="false"`
`flex`
`group`
`name`
`name`.
`peer
`required`
`true`.
`value`
`wk-radio-dot`
`{{
a
above
accent
accepts
accessible
after
align-top
align-top';
already
also
always
an
and
announce-error
announceErrors)
announced
announced.
answers
any
appears
are
area
aria-atomic="true"
aria-describedby
aria-describedby.
aria-describedby="{{
aria-hidden="true">
aria-hidden="true">*</span>@endif</span>
aria-invalid="true"
aria-live="polite"
around
arrives
as
at
attribute
attributes,
auto-derived
away
back
back.
background
bag
bag,
bag.
bar
bare
baseline
baseline;
be
became
becomes
before
belongs
beside
between
bg-[var(--color-wk-accent)]"></span>
bind
block-level
boolean
border
border-[length:var(--border-wk-width)]
border-[var(--color-wk-border)]
border-[var(--color-wk-border-error)]';
border.
bordered
both
box
broken.
browser
but
by
call
caller's
can
can't
candidate
cannot
card
card's
cards
carry
causes
centered
changelog
checkbox.
checkbox.blade.php.
circle
circle's
circle,
circle.)
claim
class
class="space-y-1.5
class="text-[color:var(--color-wk-danger-text)]
class="text-[length:var(--text-wk-md)]
class="text-[length:var(--text-wk-sm)]
class="wk-radio-dot
class="{{
classified
clickable
collides,
compatible
component
component's
component,
component.
compound
config('wirekit.a11y.announce_error',
config('wirekit.components.radio.size',
config('wirekit.components.radio.variant',
config.
construction
consumed:
consumes
container
control
control.
control:
controls
controls",
controls;
copy,
cursor-pointer
data-wk-prose-skip
day
debug
declared
decline
declining
default
definition,
delivers
derive
described
description
deselected
design
detection:
did
disables
display
do
does
dot
dot's
dropped
duration-[var(--transition-wk-duration)]
edge
edge,
either
element
element.
elements
elsewhere)
elsewhere.
emitted
empty
enabled
entry
error
error,
every
exactly
explicit
fallback
false
false));
false);
false,
field.set
field.set's
fill
filling
filter
first
flags
flex
flex-centered
flipping
focus
focus.
folds
for
for.
for="{{
forced-colors
form
fourteen
from
full-width,
gap
gap-2
gap-3
gated
global
go
gray,
group
group's
group's,
group-has-[:checked]
group-has-[:checked]:block
group-level
h-1.5'],
h-2'],
h-2.5'],
h-4',
h-5',
h-6',
handler
hardcoded,
has
has-[:checked]:bg-[var(--color-wk-bg-subtle)]
has-[:checked]:border-[var(--color-wk-accent)]
has-[:focus-visible]:ring-[length:var(--ring-wk-width)]
has-[:focus-visible]:ring-[var(--color-wk-ring)]'
hears
help
here
here.
hidden
highlights
hint
hint,
hit-area
hold,
holding
honor.
hover
hung
id
id="{{
identically
ids
ids.
if
implode('
in
inline-flex
inner
input
input,
insets
inside
instanceof
instead
instead.
into
invalid
is
isn't
it
it).
it,
it.
it:
it;
items-center
items-center/justify-center,
items-start
its
itself.
justify-center
keeping
keeps
kept
key
kills
label
label's
label)
label:
lands
layer
layout
leading-tight
left/top
list
live
lives
looks
marker
marks
match
matching
message
message,
message.
min-w-0">
mis-centered
modal)
mode
moves
ms-0.5"
name)
name,
name-derived
native
needs
neither
nested
never
next
no
none:
none↔block
nor
not
note
nothing
nothing.
nudges
null
null)
null)?->first($nameAttr
null)?->has($nameAttr
null,
null;
null])
of
off
offsets
offsets.
on
once
once,
one
one"
one.
only
opposite
opt
optimistic
optimistic-ui:
option.
or
other
out
out.
outranks
over
own
padding
page
page-unique
paint
parent
parser
part
past
pattern).
peer
peer-checked
peer-checked,
peer-checked/focus/disabled
per
per-element
pins
placed
pointed
pointer
pointer-events-none
pointer.
polite
precedence:
preferred
pricing-tier
printing
prod).
promised
prop
props
pt-0.5">{{
pushed
put
px-[var(--padding-wk-x-md)]
py-[var(--padding-wk-y-md)]
radio
radio,
radios
random
rather
re-rounds
reach
reaches
reacting
reacts
reader
reads
real
reasoning,
refuted
region
register
registry
rejection
relative
remove
render
rendered
renders
repeats
reserve,
resolve
rides
right
ring
roll
rounded-[var(--radius-wk-lg)]
rounded-full
row.
rule
runs
same
satisfy.
says
says,
scale
second
see
select
select-none
selected
selected,
selecting
selection
selects:
sentence
separate
server-side
set
set's
set:
sets
shares
sharing
shift
shipped
shipping
shrink-0',
sibling
sibling,
site
sits
sitting
size
so
something
specificity,
spellings
split,
sr-only`
stacked
standalone
start
state,
states
stays
step,
still
stray
stylesheet's
sub-pixel
submit
such
supported
surface
surrounding
survives
tag,
take
takes
tap
target
target,
test
text
text,
text-[color:var(--color-wk-danger-text)]">{{
text-[color:var(--color-wk-text)]
text-[color:var(--color-wk-text-muted)]">{{
textarea,
than
that
the
their
then
there
thing
this
this,
through
time
tinted
to
to,
to:
together.
toggle
toggle/checkbox):
toggles
too,
top
transition-colors
true
true);
true,
turned
two
type="radio"
unaffected.
unchanged).
unchanged.
under
undo
unique
unknown
unlike
upper
use
use,
uses
validation
value
value="{{
variant
variant,
variant:
variants.
via
visually
w-full
was
way.
what
when
which
while
whole
whose
will
wire:model
wiring,
with
without
words
would
written
wrong
{
{{
{{--
||
}
};
}}
}}"
}}">
}}-error"
}}-hint"
}}</p>
}}@if($wkRequiredMarker)<span
}}`
—
