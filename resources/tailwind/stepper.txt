!
!==
"1.
"Completed:
"Completed:"
"Details"
"Details".
"Sent
"Step
"current";
$attributes->getAttributes());
$attributes->merge(['style'
$circleBase
$completedClasses
$connectorClasses
$count
$count,
$count])
$current;
$currentClasses
$description
$follow
$follow)
$i
$isCompleted
$isCurrent
$isCurrent)
$isLast
$isVertical
$itemClasses
$label
$labelClasses
$label])
$listClasses
$orientation
$scope);
$stateClasses
$step)
$step;
$stepAction
$stepHref
$stepIsOperable
$stepIsOperable,
$stepNumber
$stepNumber,
$stepTag
$step['action']
$upcomingClasses
$upcomingClasses);
&&
'
''
'')
',
'.$count.';'])->class([$listClasses])
'a'
'a')
'absolute
'bg-[var(--color-wk-accent)]
'bg-[var(--color-wk-bg)]
'bg-[var(--color-wk-border)]',
'border-[length:var(--border-wk-width)]',
'button')
'button');
'circle',
'current'
'cursor-pointer
'description'
'div'
'duration-[var(--transition-wk-duration)]',
'flex
'flex',
'flex-col
'flex-row
'focus-visible:outline-hidden
'follow'
'font-[number:var(--font-wk-heading-weight)]',
'horizontal'
'horizontal'),
'item',
'label'
'label',
'list',
'list-none
'list-style:
'max-w-full
'mt-[var(--padding-wk-y-xs)]
'orientation'
'relative',
'rounded-[var(--radius-wk-md)]'
'rounded-full',
'scope'
'step'
'steps'
'text-[color:var(--color-wk-text)]',
'text-[length:var(--text-wk-sm)]',
'total'
'transition-colors',
'transition-opacity
'vertical'
'vertical';
'w-8
'w-full',
(!
("Supporting
($follow
($isCurrent
($stepHref
($step['description']
($step['href']
($step['label']
($step['wire:click']
(`--wk-stepper-count`
(active
(and
(config('app.debug'))
(index
(label
(see
(silent
(string)
(the
+
-
--wk-stepper-count:
--}}
-translate-x-[0.5px]'
..,
..].
.var_export($follow,
/
//
0
01-1.06
010
011.06
0;
0l-3.5-3.5a.75.75
0z"
1
1))
1,
1-based,
1.06l-7.5
111.06-1.06L8.674
12.23l6.97-6.94a.75.75
1;
2.
20
20"
3
3."
5.29a.75.75
7",
7.5a.75.75
:
:current
:label',
:total',
:total:
<
</div>
</li>
</ol>
</svg>
</{{
<div
<div>{{
<li
<li>
<ol
<ol>
<path
<span
<svg
<{{
=
==
===
=>
>
?
??
@class([
@else
@endforeach
@endif
@endphp
@endunless
@foreach($steps
@if($description)
@if($follow
@if($isCompleted
@if($isCompleted)
@if($isCurrent)
@if($stepTag
@php
@props([
@props.
@unless($isCompleted)
@unless($isCurrent)
@unless($isLast)
@unless($isVertical)
A
AND
ARIA
Alpine
Blade's
Both
CAN
Check
Circle
Completed:
Connector
Connector:
Current:
Declared
Details
Dev-only
Each
Every
FUTURE
Following
Fully
HTML
Horizontal:
How
Inline
Label
Lives
Livewire
Long
Normalize:
ONLY
Optional
Outer
PLAIN
Pushery\WireKit\WireKit;
Sent
Stepper
Tailwind
That
The
UA
Upcoming:
Vertical:
Visual
WITHOUT
What
WireKit::resolveClasses('stepper',
With
Without
[
['current'
['label'
[],
[overflow-wrap:anywhere]
\InvalidArgumentException('[wirekit]
\Pushery\WireKit\WireKit::warnUnknownProps('stepper',
])
]),
]);
__('wirekit::Completed')
__('wirekit::Progress')
__('wirekit::Step
`--wk-stepper-index`
`<button>`
`__()`,
`aria-current`
`aria-hidden`:
`current`
`cursor-pointer`
`cursor:
`description`,
`display:
`flex-1
`follow`
`href`
`label`
`lang`
`list-none
`min-w-0`
`min-width:
`wire:click`,
`wizard`
a
about
above,
absolutely
accent
accent.
accept
accessible
action
action.
affordance
after
alert
all
all,
allowed,
already
an
and
announced
answer
anything
appears
application
application,
apply
arbitrary
are
are,
aria-current
aria-current="step"
aria-hidden,
aria-hidden="true">
aria-hidden="true"></span>
aria-hidden="true">{{
aria-label
aria-label="{{
array_key_last($steps);
as
at
attribute.
auto-derived
auto`
back,
back.
be
because
become
becomes
before
belongs
below
below).
below,
beside
between
binding
block,
border-[var(--color-wk-accent)]';
border-[var(--color-wk-border)]';
break
breaks
browser
browser-default
browser:
building
but
button
by
can
cannot
caption
carries
carry
case:
catalog
caution.
centered
changes
check
chosen
circle
circles
circles.
class
class="sr-only">{{
class="text-[length:var(--text-wk-xs)]
class="w-4
class="{{
classes
classes.
click
clip-rule="evenodd"/>
code
colon
column
columns
comes
communicated
compact
completed
component
component's
component,
computed
concept,
config('wirekit.components.stepper.orientation',
connectors
contributes
cope
costs
count
count($steps)
current
current),
current).
d="M16.704
data-wk-prose-skip
data-wk-stepper-caption
data-wk-stepper-label
data-wk-stepper-step
data-wk-stepper="{{
debug
decimal
decorative
decorative;
default
default`
described
description
destination
developer
differ
distance
div
docs
document's
documents")
does
don't
drawn
draws
dropped,
duplicate
duration-[var(--transition-wk-duration)]
each
early:
either
element
element.
else
equal
ever
every
everything
expressions
fallback
far:
file
fill-rule="evenodd"
fill="currentColor"
filled
finished
first
flags
flex-1
flex-col
flex-row
flow
flow.
focus-visible:ring-[length:var(--ring-wk-width)]
focus-visible:ring-[var(--color-wk-ring)]'
follows
for
form
form's
from
full
gap
gap-2',
gap-[var(--padding-wk-x-sm)]'
gap-[var(--padding-wk-y-md)]'
get
guess
h-4"
h-8
h-[1px]',
h-[calc(100%-0.5rem)]
hands
has
have
heard:
hears
hears.
helper
here
here.
hidden
hides
holds
horizontal
hover:opacity-80'
how;
href="{{
hyphen,
hyphenate.
hyphens-auto',
identifier:
if
iframe
implicit
implode('
imports
in
indent
indent.
indicator
indicator;
inside
instead
interactive
into
intrinsic
is
is.
is_array($step)
is_countable($steps)
is_string($follow)
it
it.
item
items
items-center
items-center',
items-start
its
joins
jump
justify-center',
keep
keeps
keys
know
label
label's
label,
label.
labels
last
later
later.
left-4
left-[calc(50%+1rem)]
like
line
line.
link
link's
list
list,
list-none
list-style
live
long
longest-word
looking.
looks
m-0
making
many
margin:
mark
marker
markers
markup
matching
may
min-w-0
min-w-0',
min-w-0`
more
moves.
multi-step
multi-word
must
muted
muted.
n/a
name
names
narrower
navigates
navigation
never
new
next
no
none;
none;"
none`
not
null
null"
null)
null);
null,
null;
number
number.
numbered
of
offer
ol.
on
one
one.
one.');
ones
only
only)
operable
operable),
operable,
opposite
optimistic-ui:
or
ordered.
out
outline.
outlined
outside
over
overflowed
overflowing
overflowing.
own
own,
p-0
p-0',
p-0`
package
padding:
page.
paint
paint.
part
passes
past
per
phone
phone.
pins
plain
point
positioned
prefixes
preflight;
preg_match('/^[A-Za-z_$][\w$]*$/',
prod).
progress
property
property,
props
provides
punctuation
pushed
puts
qualified:
rather
reach
reader
reads
redundant
relative',
removes
render
render,
render.
renders
restriction
result
reverse
right
right-[calc(-50%+0.5rem)]
ring).
role="list"
role=list
row
row.
row:
runs
runs,
safe
said
sandbox
says
scanner
screen
semantic
semantics
sent
sets
shape
share
ship
shipped
show
shown.
shows
shrink
shrink-0',
since
single
small
so
some
spacing
span
state
state.
states
states,
stays
step
step's
step).
step,
step.
stepper
stepper's
stepper:
steps
steps.
steps:
still
stretches
string
strip
strips
style
style="--wk-stepper-index:
style="display:
stylesheet
styling
such
switch
syllable,
tag
takes
template.
text,
text-[color:var(--color-wk-accent-fg)]
text-[color:var(--color-wk-accent-text)]
text-[color:var(--color-wk-text-muted)]
text-[color:var(--color-wk-text-muted)]">{{
text-center',
text-start'
text.
than
that
that,
the
their
then
there
there.
therefore
third
this
three
through
throw
to
token)
too-long
top-4
top-8
treatment
trim($label)
true).'
type="button"
under
unknown
untouched.
upcoming
use
v3
v4
variable
variant
variant:
via
view's
viewBox="0
visual
visually
visually,
visually-hidden
w-[1px]
was
way
we
well
what
when
where
wherever
whether
which
why
width,
width.
wire:click="{{
with
with.
within
word
word,
word:
working
would
wrap
wrapper.
wraps
written
x-bind:aria-current="{{
x-show="{{
xmlns="http://www.w3.org/2000/svg"
yesterday"
yesterday",
{
{{
{{--
||
}
}}
}}"
}}">
}}:</span>
}};"
}}</div>
}}</span>
}}>
—
