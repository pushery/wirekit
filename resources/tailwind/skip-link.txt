"#")
"main-content"
$attributes->class([$classes])
$attributes->getAttributes());
$classes
$label
$scope);
$slot
$slot->hasActualContent()
$target
',
'base',
'focus-visible:absolute',
'focus-visible:bg-[var(--color-wk-accent)]',
'focus-visible:font-medium',
'focus-visible:left-[var(--space-wk-sm,0.5rem)]',
'focus-visible:no-underline',
'focus-visible:not-sr-only',
'focus-visible:outline-hidden',
'focus-visible:px-[var(--space-wk-md,1rem)]',
'focus-visible:py-[var(--space-wk-sm,0.5rem)]',
'focus-visible:ring-2',
'focus-visible:ring-[var(--color-wk-accent)]',
'focus-visible:ring-offset-2',
'focus-visible:ring-offset-[var(--color-wk-bg)]',
'focus-visible:rounded-[var(--radius-wk-md)]',
'focus-visible:shadow-[var(--shadow-wk-lg)]',
'focus-visible:text-[var(--color-wk-accent-fg)]',
'focus-visible:top-[var(--space-wk-sm,0.5rem)]',
'focus-visible:transition-none',
'focus-visible:z-[100]',
'label'
'main-content',
'scope'
'sr-only',
'target'
(No
(collapses
(header,
(id="main-content").
(without
+
-
--}}
//
1x1
2px
:
</a>
<a
=
=>
?
@endphp
@php
@props([
Blade's
Defaults
It
Label
Note
PHP
Pushery\WireKit\WireKit;
Tailwind
The
There
This
Visually-hidden
WireKit::resolveClasses('skip-link',
WireKit::warnUnknownProps('skip-link',
[
])
]),
__('wirekit::Skip
`(forced-colors:
`:focus-visible`
`ForcedColorsStateGuardTest`
`box-shadow`,
`id`
`outline-hidden`
`outline-hidden`,
`outline-none`.
`outline-style:
a
above
absolute
accent
accent-fg
across
active
active)`
adds
agent's
all,
already
anchor
and
anticipate:
app-shell
as
asked.
at
baseline
branch
browser
by
byte-identical
carrying
catalog.
chrome
clipped
color.
comment:
comments.)
compiler
compiles
component
content'),
covered,
current
data-wk-prose-skip
default
default;
docs
drops
etc.)
even
exists
explicitly
expose
focus
focus,
focus-visible:not-sr-only
for
for.
forced-colors-mode
foreground
had
high-contrast
high-z-index
holds
href="#{{
id
implode('
in
indicator
inherit
inside
is
it
it.
its
jumps
kept
keyboard
landmark
landmark.
leading
leaves
line
link
list:
literal
main
match
mode
moves
n/a
navigation
never
no
none`
nothing
null
null,
of
on
one
opt-in
optimistic-ui:
out
outline,
outright,
overlays
own
pair.
pill
pops
positioning
prop
radius
readers
rect)
rendered
repaints
reveals
ring
round:
say
scans
server
set
shadow
sits
so
sr-only
suppressed
surface
surfaced
system
tag
tell
that
the
theme
themed
this
to
to.
tokens
top-left
tracks
transparent
use
used
user
users
utility
via
viewport.
was
way
what
when
which
wire
with
wrong
you
{{
{{--
}}
}}"
}}>
—
⚠️
