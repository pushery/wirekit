!
!==
"Email
"could
"invalid"
"on"
$announceError
$announceErrors
$attributes
$attributes->except(['announceErrors',
$attributes->except(['id',
$attributes->get('aria-describedby'),
$attributes->get('name'),
$attributes->get('name',
$attributes->get('required',
$attributes->getAttributes());
$attributes->has('aria-label')
$attributes->has('aria-labelledby');
$describedBy
$error
$errorMessage
$errorMessage)
$errors
$fallbackAriaLabel
$fieldGroup
$fieldGroup,
$groupError
$groupError,
$groupOwnsBagEntry
$hasAccessibleName
$hasError
$hideLabel
$hint
$id
$id);
$id.'-error'
$id.'-hint'
$isInvalid
$knobClasses
$label
$name
$name));
$optimistic
$optimistic,
$optimisticArgs),
$optimisticConfig
$scope);
$sizing
$sizing['knob'],
$sizing['track'],
$sizing['translate'],
$trackClasses
$wkFieldSet
$wkRequiredMarker
$wrapperClasses
&&
'
'#'.$id.'-error',
''
',
'_'],
'absolute
'action'
'announce-errors',
'announceError'
'args'
'aria-describedby'])->class(['peer',
'bg-[var(--color-wk-bg-elevated)]',
'bg-[var(--color-wk-border)]',
'border-[length:var(--border-wk-width)]',
'border-[var(--color-wk-border-strong)]',
'cursor-pointer',
'debug'
'duration-[var(--transition-wk-duration)]',
'ease-[var(--transition-wk-easing)]',
'error'
'errorRegion'
'false'
'groupError'
'hideLabel'
'hint'
'knob'
'label'
'lg'
'md'),
'messages'
'mode'
'name',
'optimistic'
'optimisticArgs'
'peer-checked:bg-[var(--color-wk-accent)]',
'peer-checked:border-[var(--color-wk-accent)]',
'peer-checked:translate-x-4
'peer-checked:translate-x-5
'peer-checked:translate-x-6
'peer-disabled:cursor-not-allowed',
'peer-disabled:opacity-[var(--opacity-wk-disabled)]',
'peer-focus-visible:ring-[length:var(--ring-wk-width)]',
'peer-focus-visible:ring-[var(--color-wk-ring)]',
'peer-focus-visible:ring-offset-[length:var(--ring-wk-offset)]',
'peer-focus-visible:ring-offset-[var(--color-wk-ring-offset)]',
'pending'
'pointer-events-none',
'reject',
'relative
'reverted'
'rounded-full',
'scope'
'shadow-[var(--shadow-wk-sm)]',
'size'
'sm'
'sr-only'])
'toggle-');
'track',
'transition-colors',
'transition-transform',
'translate'
'value'
'w-10
'w-12
'w-3
'w-4
'w-5
'w-8
'wk-field-set']);
'wk-touch-target',
'wkFieldSet'
'wkFieldSet',
(!
($attributes->get('checked')
($errors
($fieldGroup?->covers($name)
($fieldGroup?->isInvalid($errors
($groupError
($groupOwnsBagEntry
($size)
(.peer),
(@aware
(absolutely
(bool)
(kept
(neutral-100)
(neutral-300)
(peer-checked
(screen
(silent
(string)
(the
(they'll
(when
);
+
--}}
->
-translate-y-1/2',
.peer
.peer,
/
//
/>
1.4.11
1ms
1x1,
24px
3.3.1
32x16
40x20
4px
:
</div>
</label>
</span>
<body>.
<div
<input
<label
<label>
<p
<span
=
===
=>
>
?
??
??=
@aware(['announceErrors'
@elseif($hint)
@endif
@endif>
@endphp
@if($announceError)
@if($describedBy
@if($fallbackAriaLabel)
@if($hasError
@if($isInvalid)
@if($label)
@if($optimisticConfig)
@php
@props([
@props.
A
A11y:
AA
AT
Accessible
Arabic
Auto-generate
BOTH
Blade
BooleanProp::from($groupError,
BooleanProp::from($hideLabel,
BooleanProp::stripFalseHtmlFlags($attributes);
CLASS
Change
DIRECTIONS
DOM
Declared
Dev-only
Developer
EMPTY.
English
Error
Every
Extra
HTML
Hebrew
ID
It
Knob
Knob:
Laravel
Livewire
METHOD
MUST
Mirrors
NAME
NOT
Native
No
Nothing
Null
OFF
OR
ORDER
OUTSIDE
Old
One
PER
PRESENCE,
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
READING
RTL
Read,
Render
Rendered
SIZE,
SOME
Same
Set
Size
So
Strip
Support\DomId
Switch
TEXT
TOKEN
Tailwind
Take
That
The
Track:
UNBOUND
Visible
WCAG
WHICH
WRAPS
Where
Whether
WireKit
WireKit's
WireKit-classed
WireKit::resolveClasses('toggle',
WireKit::warnUnknownProps('toggle',
With
Wrapper
Written
[
['track'
[],
\Pushery\WireKit\Support\AlpinePayload::from([
\Pushery\WireKit\Support\DomId::unique($attributes->get('id')
\Pushery\WireKit\Support\FieldGroup
\Pushery\WireKit\Support\FieldGroup::describedBy(
])
]),
]);
],
__('wirekit::Could
__('wirekit::Saving'),
`:group-error="false"`
`<label
`@aware`
`@props`
`args`
`aria-label`
`bg-[var(--color-wk-bg)]`
`covers()`
`data-reduce-motion`
`disabled="false"`
`hideLabel="false"`
`hideLabel`.
`label`
`left-0.5`:
`md`
`name`
`peer
`personalize()`
`required`
`sm`,
`start-0.5`,
`transition-transform`
`true`.
`wk-`
`wk-spinner`
`wk-toggle-knob`
`wk-toggle`
`{{
a
able
above
above,
accepts
accessibility
accessible
action
actionable,
actions
adds
affordance.
after
against
agreed
all.
along
aloud
already
also
always
an
ancestor
anchor
and
animated
animations
announce
announce-error
announceErrors)
announced
announced.
announcement
answers
appears
appended
application's
are
area.
arguments
aria-atomic="true"
aria-describedby.
aria-describedby="{{
aria-hidden="true">*</span>@endif</span>
aria-hidden="true"></span>
aria-invalid="true"
aria-label).
aria-label="{{
aria-live="assertive"
aria-live="polite"
around
array_values((array)
arrives
as
asked
assembled,
associated
at
attribute
attribute,
attributes,
auto-derived
axe-core
axis.
back
background
backgrounds.
bag
bag,
bag.
bare
be
became
because
been
before
behavior
being
belong;
belongs
below
beside
bg-muted
block
boolean
border
both
browser
build
built
but
by
call
call,
call.
caller
caller's
can
cannot
capability
carried
carries
carry
catalog
causes
changed
checkbox
checkbox:
checked.
chrome
circle
clamped
class
class="inline-flex
class="sr-only"
class="text-[color:var(--color-wk-danger-text)]
class="text-[length:var(--text-wk-md)]
class="text-[length:var(--text-wk-sm)]
class="wk-toggle
class="wk-toggle-knob
class="{{
color
column
combobox
come
commonest
component
component's
component,
component.
config('app.debug'),
config('wirekit.a11y.announce_error',
config('wirekit.components.toggle.size',
config.
consumed:
container
contains
context
contract:
contrast
control
control's
control,
control.
control:
controls
controls;
copy,
correctly
correctly.
cursor-pointer">
data-wk-optimistic-announcer
data-wk-prose-skip
debug
declared
decline
declining
decoration.
decorative.
default
default)
deliberately
delivers
depends
descendants,
described
description
designed
detection:
dev-warning
developer
developer's
diameter
direct
direction
direction;
disables
discipline
distance
does
dropped
duration
duration,
edge
either
element
element.
elsewhere)
encodes
end
entry
error
error,
escape
every
everybody.
exactly:
existed
expander
explicit
exposed
factory
fails
failure:
fall
fallback:
false
false));
false),
false);
false,
field's
field.set
field.set's
final
finger
first
fixed-size
flags
flight
flip
flips
floor
flow,
focus
for
for="...">`
for="{{
forced-colors
form
form,
from
full
gap-3
gate
gated
generate
generated.
give
global
group
group's
group's,
group.
h-3',
h-4',
h-5',
h-6',
had
half
hand-mount
hardcoded,
has
hatch
heard.
hears
height
here
here.
hidden
hide
hint
hit-area
humanized
id;
id="{{
identical
identifier
if
implode('
in
in:
inline
inline-flex
input
inset-0',
inside
instanceof
instead
into
invalid
inverted.
invisible.
is
it
it,
it.
it:
items-center
items-center',
its
itself.
keeps
kept
key
key,
knob
know
label
labels
lands
layer
leave
left
lets
level
library's
line.
list
literal
live
load-bearing
logged
marker
markup
match
matches
matching
message
message,
message.
method
minus
mirrors
motion
motion.
moves.
ms-0.5"
name
name)
name="{{
names
native
nearly
needs
neither
network
never
new
no
node
nor
not
not,
nothing
null
null)
null)?->first($name));
null)?->has($name));
null,
null;
null])
of
off
offset
on
once
once,
one
one.
only
only).
operating
opposite
opt
optimistic
optimistic-ui:
or
other
otherwise
out
out.
own
padding
page
page-unique
pair
parent
parser
passes
peer-checked
peer-checked:*
peer-checked:translate-x-*.
per
placed)
points
polite
positioned,
positioning
positive
precedence:
present
printing
prod).
prop
props
provided
purpose
pushed
puts
queue
queueing
random
rather
reaches
read
readable.
reader
readers
reads
reduced-motion
region
region,
region.
regular
rejection
rejects
remove
render
renders
repairing
replacing
required"
requires
reserve.
resolve
resolve.
responses
rides
right,
role="switch"
row
row,
rtl:peer-checked:-translate-x-4'],
rtl:peer-checked:-translate-x-5'],
rtl:peer-checked:-translate-x-6'],
rule
rule,
running
runs
runtime
same
satisfy.
save"
save.
says
says,
scale:
scans
scoped
screen
screen.
second
see
sees
select
select-none{{
selector.
selectors
selects,
sentence
separate
server
server-side
setting
should
show
show.
shrink-0
sibling
siblings
silent
simply
since
site
sits
sitting
size
slides
so
somebody,
source
space-y-1.5"
specific
spellings
spreads
sr-only
sr-only'
sr-only`
start
start-0.5
starting
starts
state
stays
still
stray
string
styles:
stylesheet's
submit
substring
subtree
such
supported
surface
surrounding
survives
switch
switching
system
table
tag,
take
taken
targets
tell
tells
text
text-[color:var(--color-wk-danger-text)]">{{
text-[color:var(--color-wk-text)]
text-[color:var(--color-wk-text-muted)]">{{
textarea
than
that
that:
the
their
themed
then
there
thing
this
this,
through
timing
to
today's
together
toggle
toggle,
token,
toolbar
top-1/2
track
track,
translation
trap
travel
true
true);
true,
truthy
type="checkbox"
ucfirst(str_replace(['-',
unconditionally
under
undone.'),
unknown
unlike
unreachable,
untestable.
untouched.
up
use
uses
uses,
uses.
validation
value
value.
vertical
via
visible
visual:
visually
wants
warning
warnings
was
way
way.
what
when
where
which
while
white
who
whole
whose
width/height
will
win
win,
wire:click,
wire:click.
wire:model
wire:model)
wiring,
with
would
wrapper
written
wrong
x-bind:aria-busy="isPending"
x-bind:checked="value"
x-data="wirekitOptimistic({{
x-on:change="toggle()"
x-ref="control"
x-text="announcement"></div>
{
{{
{{--
||
};
}}
}}"
}}">
}}">{{
}})"
}}-error"
}}-hint"
}}</p>
}}@if($wkRequiredMarker)<span
}}`
~1.04:1
—
≥3:1.
⚠️
