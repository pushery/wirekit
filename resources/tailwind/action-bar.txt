!==
"inside
"toolbar"
$attributes->getAttributes());
$attributes->merge(!$visible
$classes
$isFloating
$mode
$positioningClasses
$positioningClasses,
$scope);
$slot
$visible
',
'base',
'bg-[var(--color-wk-bg-elevated)]',
'border-[length:var(--border-wk-width)]',
'border-[var(--color-wk-border)]',
'display:
'false'
'fixed
'flex
'floating'
'floating',
'font-[family-name:var(--font-wk-sans)]',
'inline-flex';
'mode'
'px-[var(--padding-wk-x-lg)]',
'py-[var(--padding-wk-y-sm)]',
'rounded-[var(--radius-wk-xl)]',
'scope'
'shadow-[var(--shadow-wk-lg)]',
'static'
'static';
'text-[color:var(--color-wk-text)]',
'text-[length:var(--text-wk-md)]',
'visible'
(`x-show`
(default)
(or
(silent
+
-
--}}
-translate-x-1/2
/
//
0
34px
:
</div>
<div
=
=>
>
?
@endif
@endphp
@if($visible)
@php
@props([
@props.
APPEARED,
Action
Alpine-controlled
Announcing
Bar
Best
Blade
BooleanProp::from($visible,
CHANGE
Declared
Dev-only
Drops
English
Fully
Home/End
It
Left/Right
Livewire
Measured
NOT
No
Normalized
OUTSIDE
Out
Playwright,
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
Renders
SERVER-driven
So
Tab
Tab.
The
This
Two
UNBOUND
Under
Up/Down)
Useful
Uses
Why
WireKit::resolveClasses('action-bar',
[
['style'
[])->class([$classes])
\Pushery\WireKit\WireKit::warnUnknownProps('action-bar',
])
]),
__('wirekit::Bulk
`$visible`
`.wk-bottom-nav`;
`.wk-fab`
`:visible="true"`)
`:visible`
`display:
`env()`
`env(safe-area-inset-*)`
`position:
`prop="false"`
`role="group"`
a
above
accessibility
action
actions
actions')
against
all.
aloud
already
always
an
and
announced
announcement
announces
any
appears
are
aria-label="{{
aria-live
aria-live="polite"
arrows
as,
asserted:
at
attribute
auto-derived
automated
available')
axe
bar
bar,
be
below
between
block,
bottom
bottom-[calc(var(--padding-wk-y-lg)_+_env(safe-area-inset-bottom,0px))]
bottom-center
bound,
browser
bulk
buttons,
call
caller's
can
card
carries
cast
catalog
catch
change
changes,
check
children
chrome.
class="sr-only">
comment
compiles
component
component's
component.
composite
composite-widget
content
content.
control
controls
controls,
costs
could
dashboard
debug
dedicated
default
did
display-none
dist/wirekit.css
distinction
do
docs
does
each
early.
element,
elsewhere.
empty
ends.
every
everybody,
exact
exactly
exists
exists.
explained
expression
false
false);
false,
feature
file
filled
fire.
fires.
first
fixed
fixed`.
flags
flip
flipping
flips
flows
for
from
gap-[var(--gap-wk-md)]',
given
green
guard
has
headless
here
home
hover
iPhone,
implode('
imports
in
indicator
inline
inset
inset,
inside
inside,
interactive
interface
introduces
is
is:
it
it,
it.
it:
items
items-center
its
jumping
keeps
keyboard
keys
label
language.
later
layout
left-1/2
library
list
literal
live
make
make;
may
mean
mobile
mode.
model
model.
modes:
moving
n/a
named
never
no
none;']
none`,
not
not:
notched
nothing
nothing.
null,
of
offsets
on
on.
one
one.
opposite
optimistic-ui:
ordinary
otherwise
over
overlay.
own
page
pages
paint
panel
part
path
phone
pinned
point.
populated,
positioning
present
presentational
press
prod).
promise:
prop's
props
puts
qualified:
rather
re-render
reach
reached
reacts
read
readers.
reads
reason
refutes
region
related
renders
reports
resolves
result
role
role="group"
role="toolbar",
roving
rule
run.
same
sat
says
screen
screen-reader
scrolls.
selected.
set
ships
should
shown
shown,
silently.
simply
site
sits
so
somebody,
states
stays
stop
string,
subtree
surrounding
survived
swaps
tab
term.
test
text
than
that
the
their
them
there
these
this
this:
tip
to
to.
told
toolbar
transforms;
translated
translated,
tree
tree".
tree,
true
truthy
unknown
until
usable
use
used
user
via
view's
viewport
viewport-centering
viewport-floating
visibility
visual
walks
was
were
what
when
where
wherever
which
while
whole
whose
why
with
without
wrapper
z-[var(--z-wk-sticky)]'
{{
{{--
}}
}}"
—
→
⚠️
