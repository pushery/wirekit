$attributes->class([$classes])
$attributes->getAttributes());
$cite
$classes
$scope);
$slot
$variant,
$variantClasses
$variantClasses,
',
'accent'
'accent']),
'base',
'border-[var(--color-wk-accent)]',
'border-[var(--color-wk-border)]',
'border-l-4',
'cite'
'default'
'default',
'font-[family-name:var(--font-wk-sans)]',
'italic',
'pl-[var(--space-wk-md,1rem)]',
'py-[var(--space-wk-xs,0.25rem)]',
'scope'
'text-[color:var(--color-wk-text)]',
'text-[length:var(--text-wk-md)]',
'variant'
'variant',
($variant)
(silent
--}}
//
</blockquote>
</footer>
<blockquote
<footer
=
=>
@endif
@endphp
@if($cite)
@php
@props([
@props.
Declared
Dev-only
Fully
Measured
Pushery\WireKit\WireKit;
Renders
WireKit::resolveClasses('blockquote',
WireKit::validateProp('blockquote',
[
['default',
\Pushery\WireKit\WireKit::warnUnknownProps('blockquote',
])
]),
a
action
any
asserted:
auto-derived
be
block,
class="mt-[var(--space-wk-sm,0.5rem)]
component's
could
data-wk-prose-skip
debug
default
does
early.
element,
file
flags
for
from
guard
implode('
imports
in
interactive
is
later
list
live
match
may
n/a
no
not
not-italic">
null,
one.
optimistic-ui:
presentational
prod).
props
qualified:
rather
reach
reason
refutes
renders
result
shown
so
text-[color:var(--color-wk-text-muted)]
text-[length:var(--text-wk-sm)]
than
that
the
there
this
unknown
use
view's
which
whose
{
{{
{{--
};
}}
}}>
—
