$attributes->class([$classes])
$attributes->getAttributes());
$bleed
$classes
$emphasis
$scope);
$slot
$span
$span,
$spanClass
$spanClass,
',
'1x1'
'1x1',
'1x2'
'1x2',
'2x1'
'2x1',
'2x2'
'2x2']);
'@2xl'
'@2xl/bento'
'base',
'bg-[var(--color-wk-bg-elevated)]'
'bg-[var(--color-wk-bg-subtle)]',
'bleed'
'border-[length:var(--border-wk-width)]',
'border-[var(--color-wk-accent)]'
'border-[var(--color-wk-border)]',
'col-span-3
'emphasis'
'false'
'p-0'
'p-[var(--padding-wk-x-lg)]',
'relative
'rounded-[var(--radius-wk-lg)]',
'scope'
'span'
'span',
($span)
(@2xl/bento),
(a
(see
(silent
(the
--}}
//
:
</div>
<div
=
=>
?
@2xl/bento:col-span-1
@2xl/bento:col-span-2
@2xl/bento:row-span-1',
@2xl/bento:row-span-2',
@container/bento),
@endphp
@php
@props([
@props.
A
Blade
BooleanProp::from($bleed,
BooleanProp::from($emphasis,
Declared
Dev-only
Every
Full
Fully
GRID's
How
In
Let
Lift
Match
Measured
Normalized
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
RESULT,
Renders
Tailwind
The
Turns
UNBOUND
WireKit::resolveClasses('bento-cell',
WireKit::validateProp('bento-cell',
[
['1x1',
\Pushery\WireKit\WireKit::warnUnknownProps('bento-cell',
])
]),
`prop="false"`
a
accent
action
against
all
allowed
an
and
any
are
arm
as,
asserted:
at
attribute
auditor
auto-derived
background,
be
below.
bento
bento-grid's
bleed
block,
border
both
box.
call
can
cannot
cast
cell
cell's
claim.
claims
class
classes
collapses
column
column),
columns
compiles
component's
container
content
could
cramped
data-span="{{
data-wk-bento-cell
debug
default
default)
does
drift
each
early.
edge
element,
emits
emphasis
enough
every
exists
failure
false);
false,
feature
file
first
flags
flex
flex-col
flips
for
from
full
gradient:
grid
guard
has
here,
hero
hue
imagery
implode('
imports
in
interactive
into
is
it
its
itself
later
list
literal
live
match
may
mean
mode
much
n/a
narrow
narrow:
neighbors
never
no
not
null,
of
off
on
on.
once
one
one.
only
opposite
optimistic-ui:
or
otherwise
out.
overflow-hidden',
own
padded
padding.
per
presentational
prevent.
prod).
prop's
prop:
props
qualified:
rather
raw
re-engages
reach
reader
reads
real
reason
reflow
refutes
regardless
region
renders
resizable
result
returns
room
rows
run
safe
see
separate
should
shown
silently.
single
sit
site
slivers,
small.
so
span
span-collapse
spans
squeezes
stacked
statically
strict
string,
surface.
survive
tall
than
that
the
then
there
these
this
three
throws
tint
to
tracks
truthy
two
two-column
unknown
unreadable
up
use
used
validateProp
value
view's
viewport,
visually.
was
what
when
where
which
who
whose
wide
width
width).
width.
written
{
{{
{{--
};
}}
}}"
}}>
—
→
