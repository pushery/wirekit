!==
"2
"Code
"Strong"
"Weak"
$announce
$attributes->class([$classes])
$attributes->getAttributes());
$barColor
$classes
$colors
$colors[$wkBar]
$count
$count;
$index)
$label
$label]);
$level
$levelClasses
$levels
$levels));
$max
$max)
$max));
$max):
$max);
$max,
$max]),
$scope);
$showLevel
$step):
$step,
$steps
$steps)
$template
$value),
$widest,
$widestLevel
$wkBar
$wkBar++)
$word
$word):
&&
''
'')
'');
',
'announce'
'base',
'colors'
'count'
'flex
'font-[family-name:var(--font-wk-sans)]',
'grid
'label'
'level',
'levels'
'max'
'scope'
'showLevel'
'text-[color:var(--color-wk-text-muted)]',
'text-[length:var(--text-wk-sm)]',
'total'
'value'
'var(--color-wk-bg-muted)'
'var(--color-wk-bg-muted)';
'var(--color-wk-danger)'
'var(--color-wk-success)';
'var(--color-wk-warning)';
'w-full
($index
($levels[min($steps,
($max
($steps
($steps,
($wkBar
(each
(how
(int
(int)
(is_array($levels)
(nothing
(silent
(string
+
-
--}}
//
0
0),
0,
0;
1)),
1]
4)
4))
4,
5".
:
:total',
:value',
<
</div>
</span>
<=
<div
<span
=
===
=>
>
></div>
>=
?
??
??=
@endfor
@endif
@endphp
@for
@if($announce)
@if($level
@if($showLevel)
@include
@include('wirekit::components.partials.strength-bars',
@php
@props([
A
Alpine
AlpinePayload::from($levels)
AlpinePayload::string($template)
BARS
BooleanProp::from($announce,
BooleanProp::from($showLevel,
CHANGE
DOM:
Dev-only
Each
Fair,
Four
Fully
Good
How
It
JavaScript
Livewire
METER,
NOT
Not
OF
Off
Params:
Pushery\WireKit\Support\AlpinePayload;
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
Rendered
Renders
STRENGTH
Show
Speak
Strong".
Strong;
THE
The
Translated
Weak,
What
Whole
WireKit::resolveClasses('strength-meter',
[
['current'
['label'
[])
[__('wirekit::Weak'),
\Pushery\WireKit\WireKit::warnUnknownProps('strength-meter',
])
]),
__('wirekit:::current
__('wirekit:::label:
__('wirekit::Fair'),
__('wirekit::Good'),
__('wirekit::Strength');
__('wirekit::Strong')];
`:value`
`barColor(i)`
`data-wk-server-value`
`max`.
`password-input`.
`show-level`
`strength-meter`
`x-data`
a
accepts
accessible
across
action
after
alone
already
an
and
announce
announces
any
are
aria-hidden,
aria-hidden="true"
aria-label="{{
aria-live="polite"
aria-valuemax="{{
aria-valuemin="0"
aria-valuenow="{{
aria-valuetext
aria-valuetext="{{
array_map($barColor,
array_map(fn
array_reduce($levels,
array_values(array_map('strval',
arrives
as
at
attribute
bar
bar's
bar.
barColor({{
bars
bars),
bars,
be
be.
before
below
beside
block,
booted
borrowing
both
built
but
by
called,
can
cannot
carries
cell
cell,
changed
changes.
changes:
chosen
class="col-start-1
class="flex
class="h-1
class="sr-only"
class="{{
color
complete
component
construction.
could
count
count($levels))
current
danger
data-wk-server-value="{{
debug
default
defaults
difference
does
drawn
draws
drew
duration-[var(--transition-wk-duration)]"
each
early.
element,
else
elseif
empty
factory
false);
false,
fill
filled
first,
flags
flex-1
fn
follows
for
four
fraction
from
function
gap-1"
gap-[var(--gap-wk-sm)]',
grid:
has
have
here,
how
if
implode('
imports
in
init
instead
intdiv($max,
interactive
invisible
is
is:
is_numeric($value)
it
it,
it.
items-center
its
judge
judges.
keep
kept
ladder
later
left
length
lengths
levels:
list).
lit
live
load.
longest
look
many
markup
max(1,
max:
may
mb_strlen($widest)
mb_strlen($word)
meter
meter's
meter.
meters
meters.
min(max((int)
min-w-0',
morph,
muted.
n/a
name
name,
narrow
never
new
no
node
node,
not
nothing
nothing.
null,
of
on
once,
one
one-cell
one.
only
only,
optimistic-ui:
other
page
per
polite
pre-hydration
presentational
prod).
props
qualified:
quarter
range(0,
range(1,
rather
re-initializes
reach
reader.
reads
refuse.
region
region.
renders:
reserves
result
return
rewritten
role="meter"
role="status"
round((float)
rounded-full
rounds.
row-start-1
same
say
says
scan
scope
screen
screen,
seed
seed,
sees
sentence
server
shared
shares
show.
showing.
shown
shows
shrink-0
since
sized
so
stack
stands
starts,
starts:
state,
static
status
stays
step
step's
step;
steps,
steps.
steps:
strength
strength:
string
style="background-color:
sub-component
subject
success,
surrounding
tabular-nums',
template:
text
than
that
that.
the
them
there
this
through
tint
to
together
top
track,
transition-colors
travels
true);
true,
twin
two
unknown
unlit
up
use
utils/strength.js,
utils/strength.js.
value,
values
view's
warning,
weakest
well.
what
whatever
when
where
which
whichever
whitespace-nowrap"
whitespace-nowrap">{{
whose
wide
width
with
word
word.
word:
words
would
x-bind:aria-valuenow="steps"
x-bind:aria-valuetext="valueText"
x-bind:style="'background-color:'
x-data="wirekitStrengthMeter({
x-modelable="value"
x-text="level">{{
x-text="spoken"></span>
yet
yet)
{
{{
{{--
}
})"
};
}}
}}"
}}">
}})"
}},
}}</span>
—
