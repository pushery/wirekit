!
!!}
"QR
"Scan
"image
$accessibleLabel
$attributes
$attributes->class([$classes])
$attributes->except('aria-label');
$attributes->getAttributes());
$attributes->merge(['style'
$bgColor
$bgHex
$classes
$e)
$ecLevel
$ecLevel);
$fgColor
$fgColor);
$fgHex
$fill
$fill,
$hasQrLibrary
$margin,
$renderer
$resolvedLabel
$scope);
$size).'px;
$size).'px;'])->class([$classes])
$size,
$style
$style,
$svgContent
$svgContent,
$value)
$value,
$writer
$writer->writeString($value,
&&
'#');
'#000000',
'#ffffff',
'',
')
',
'.((int)
'/<svg\b/u',
'<svg
'H'
'L',
'M'
'Q'
'UTF-8',
'accessibleLabel'
'aria-hidden='))
'background'
'base',
'color'
'errorCorrection'
'inline-block',
'margin'
'scope'
'size'
'value'
'width:
($attributes->get('aria-label')
($hasQrLibrary
(\Throwable
(int)
(quiet
(silent
(str_contains($svgContent,
(strtoupper($errorCorrection))
);
--}}
//
0,
1,
1.1.1
2)),
2,
200,
4,
93
</div>
<div
<span>{{
<svg>
=
=>
>
?:
@else
@endif
@endphp
@if($svgContent)
@php
@props([
@props.
A
ALL
Accessible
BaconQrCode
Both
Code
Declared
Defaults
Dev-only
English
Fall
Fallback
Frozen
Fully
German
HTML
Map
Mark
Measured
OUT
Parse
Pushery\WireKit\WireKit;
QR
QR-code
READ
REASON
Renders
Resolve
Rgb
SECOND
SVG
Supports
The
Through
URL
URLs.
WCAG
WireKit"
WireKit::resolveClasses('qr-code',
Without
[
\BaconQrCode\Common\ErrorCorrectionLevel::H(),
\BaconQrCode\Common\ErrorCorrectionLevel::L(),
\BaconQrCode\Common\ErrorCorrectionLevel::M(),
\BaconQrCode\Common\ErrorCorrectionLevel::Q(),
\BaconQrCode\Renderer\Color\Rgb(
\BaconQrCode\Renderer\ImageRenderer(
\BaconQrCode\Renderer\Image\SvgImageBackEnd(),
\BaconQrCode\Renderer\RendererStyle\Fill::uniformColor($bgColor,
\BaconQrCode\Renderer\RendererStyle\RendererStyle(
\BaconQrCode\Writer($renderer);
\Pushery\WireKit\WireKit::warnUnknownProps('qr-code',
])
]),
__('wirekit::QR
`accessibleLabel`
`aria-label`
`focusable="false"`
a
above
accessible
act
action
alternative"
alternative;
an
and
announced
announcement
any
application,
applied
are
aria-hidden="true"
aria-label
aria-label="{{
arrive
as
asserted:
assistive
attribute
authors
auto-derived
available
away.
axe-core
back
bacon/bacon-qr-code.
bag,
be
because
before
belongs
below
bg-[var(--color-wk-bg-muted)]
block,
border-[length:var(--border-wk-width)]
border-[var(--color-wk-border)]
both
both;
branches
browsers
but
caller
caller's
can
cannot
catalog,
catch
changed
class="flex
class_exists('\BaconQrCode\Renderer\ImageRenderer');
code
code"
code')
code'));
code.
color
colors,
comment
component's
concern.
content
correction
could
custom
debug
decides
decorative
default
descriptive
docs
does
early.
echoing
either
element
element,
encourage
enum.
error
every
experience:
expired.
eye
file
first,
flags
floor.
focusable="false"',
for
from
fully
gave:
generates
generic
give
guard
h-full
halves
happens
harmless
has
height:
here
hex
hexdec(substr($bgHex,
hexdec(substr($fgHex,
if
implode('
imports
in
inner
inside
install
instead
interactive
into
invites
is
it
it,
items-center
its
itself
justification
justified
justify-center
keeping
keeps
label
labels
later
leaking
level
levels,
library
like
list
live
lives
load-bearing.
longer
looked
ltrim($background,
ltrim($color,
margin
markup
match
may
module
n/a
name
name.
needed
needs
new
no
non-text
none
not
nothing
null,
null;
objects.
of
often
on
one
one.
only
opaque,
optimistic-ui:
or
ours.
over
override
own
page
parser
pass
passthrough
placeholder
preg_replace(
presentational
privacy
prod).
prop,
prop.
props
purpose-describing
qualified:
rarely
rather
raw
reach
reaches
reader
reason
recommend.
refutes
removing
render
renders
result
role="img"
role="img">
rounded-[var(--radius-wk-md)]
same
says.
screen
screen-reader
separate
server-side
shape
should
shown
skipped.
so
something
square
statement,
stays
still
str_contains($svgContent,
string
string;
strings
support
taken
tech
text
text-[color:var(--color-wk-text-muted)]">
text-[length:var(--text-wk-sm)]
than
that
the
then
there
this
this,
to
try
unknown
use
used
useful
versions
via
view's
violation.
visible
w-full
what
when
where
which
whole
whose
wins
with
with.
without
work
worse
wrapper
write
zone).
{
{!!
{{
{{--
}
};
}}
}}"
}}</span>
—
⚠️
