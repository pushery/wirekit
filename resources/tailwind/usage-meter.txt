!
!==
"100%"
"4
"42
"Approaching
"Loading"
"Orders
"Plan
"Progress"
"Unlimited"
"Unlimited")
"Usage").
"X
"not
"orders",
"seats")
"work
$action
$animation
$animation,
$animationClass
$animationClass;
$animationValue
$ariaLabelAttr
$ariaLabelledbyAttr
$attributes
$attributes->class([$classes])
$attributes->class([$wrapperClasses])
$attributes->class(['w-full
$attributes->except(['aria-label',
$attributes->get('aria-label');
$attributes->get('aria-labelledby');
$attributes->get('id')
$attributes->get('id').'-label'
$attributes->getAttributes());
$barAriaLabel
$barLabelledby
$circumference
$circumference;
$clamped
$classes
$cols
$columns,
$danger;
$dashOffset
$dimensions
$effectiveIntent
$effectiveIntent,
$fillClasses
$fillColor
$fillColor.'
$fillDeterminate
$fillDeterminate;
$fillIndeterminate
$fmt
$fmt($limitNum)
$fmt($usedNum)
$gridClass
$gridClass.'
$heightClass
$intent
$isDanger
$isIndeterminate
$isIndeterminate)
$isOver
$isOver),
$isOver,
$isUnlimited
$isWarn
$label
$labelId
$limit
$limit);
$limitNum
$limitNum;
$max
$max)
$max));
$n);
$over
$percent
$radius
$radius;
$ratio
$rawPercent
$reason
$resetAt
$resetNote
$resetNote)
$scope
$scope);
$showValue
$showValue)
$slot
$strokeColor
$textSize
$title
$titleId
$trackClasses
$unit
$unlimitedLabel
$used;
$usedNum
$value
$value,
$valueExpression)
$variant;
$variantValue
$warn;
$wrapperClasses
%
%)
%).
&&
'
'';
',
'2',
'3']);
'accent'
'accent'),
'accent',
'animation'
'animation',
'aria-labelledby']);
'base',
'bg-[var(--color-wk-accent)]',
'bg-[var(--color-wk-bg-muted)]',
'bg-[var(--color-wk-danger)]',
'bg-[var(--color-wk-success)]',
'bg-[var(--color-wk-text-muted)]',
'bg-[var(--color-wk-warning)]',
'circle',
'columns'
'columns',
'danger'
'danger',
'day'
'day'|'week'|'month'|'year'
'false'
'font-[family-name:var(--font-wk-sans)]',
'gate',
'grid
'h-1',
'h-2',
'h-3',
'info',
'inline-flex
'intent'
'intent',
'label'
'lg'
'limit'
'max'
'md'),
'month'
'neutral'
'neutral',
'neutral']),
'none'
'none')
'none',
'over'
'panel',
'period'
'primary'),
'primary',
'progress-circle-').'-label';
'progress-label-'
'reason'
'relative
'resetAt'
'rounded-[var(--radius-wk-full)]',
'scope'
'shimmer']);
'shimmer'],
'showValue'
'size'
'sm'
'stripes',
'success'
'success',
'text-[length:calc(0.5rem*var(--font-scale-wk))]',
'text-[length:calc(0.625rem*var(--font-scale-wk))]',
'text-[length:var(--text-wk-lg)]',
'text-[length:var(--text-wk-xs)]',
'title'
'track',
'unit'
'unlimitedLabel'
'usage-meter-').'-label';
'usage-meter-panel-').'-title';
'used'
'value'
'valueExpression'
'var(--color-wk-accent)',
'var(--color-wk-danger)',
'var(--color-wk-success)',
'var(--color-wk-warning)',
'variant'
'w-[var(--size-wk-lg)]
'w-[var(--size-wk-md)]
'w-[var(--size-wk-sm)]
'w-[var(--size-wk-xl,6rem)]
'w-full
'w-full',
'warn'
'warning'
'warning',
'week'
'xl'
'year'
(!
($clamped
($cols)
($effectiveIntent)
($n)
($percent
($period)
($size)
($variantValue)
(+
(0..max).
(Component
(This
(Z%)"
(`--text-wk-2xs`
(a
(a11y).
(anything
(aria-disabled
(axe-core's
(background)
(barber-pole)
(circular)
(color
(default
(default)
(determinate
(e.g.
(float)
(int)
(invisible
(its
(links
(matches
(mobile-first).
(never
(no
(not
(numeric)
(ok
(only
(or
(over-limit)
(overrides
(preferred,
(progress)
(ratio>=1.0
(see
(shown
(silent
(slot).
(string)
(structural
(the
(threshold
(true)
(uploads,
(via
(visible
);
*
+
-
--color-wk-info
--}}
-1;">{{
-rotate-90"
.
.wk-progress-indeterminate
/
//
/>
0
0%,
0)
0,
0.0;
0.25
0.6875rem)
0.75
0.8),
0.95);
0;
0`
1
1,
1.
1.0
1.0),
1.0):
100
100"
100)
100,
100.
15.9155
15.9155;
2
2.
2026-09-09.
3
3.
36
36"
4.
4.7
40px
5
5".
94%
:
:aria-label="$barAriaLabel"
:aria-labelledby="$barLabelledby"
:intent="$intent"
:max="$limitNum"
:value="$usedNum"
</div>
</span>
</svg>
<circle
<div
<span
<span>
<span></span>
<span>{{
<svg
<x-wirekit::progress
<x-wirekit::progress>
<x-wirekit::usage-meter>
=
===
=>
>
></div>
>=
?
?:
??
@else
@elseif(!
@elseif($ariaLabelAttr)
@elseif($ariaLabelledbyAttr)
@elseif($isDanger)
@elseif($isIndeterminate)
@elseif($isWarn)
@elseif($showValue
@endif
@endif>
@endisset
@endphp
@endunless
@if
@if(!
@if($isIndeterminate)
@if($isOver
@if($isOver)
@if($isUnlimited)
@if($label
@if($label)
@if($rawPercent
@if($reason)
@if($resetNote)
@if($showValue
@if($showValue)
@if($title)
@if($valueExpression)
@isset($action)
@php
@props([
@props.
@unless($isUnlimited)
A
ABSOLUTE
ALL
ALSO
AND
ARIA
AT
Alpine
Alpine's
An
Auto-generate
BAR
BELOW
Blade
BooleanProp::from($over,
BooleanProp::from($showValue,
Both
Bound
CSP
CSS
CTA
CTA.
Center
Circle
Clamp
Computed
DEPRECATED:
DETERMINATE
Danger
Declared
Deprecated
Derived
Determinate:
Determine
Dev-only
Dimmed
DomId::unique($attributes->get('id'),
Each
Extract
FILTER_VALIDATE_BOOLEAN);
FONT
FOUR
Fallback:
Fill
Fully
GAIN
Gated:
Header:
IS
Illuminate\Support\Str;
Indeterminate:
It
LITERALS,
Livewire
LocalizedNumber::format((float)
MANDATORY
M_PI
Mapping
Measured
Mirrors
More
Mutually-exclusive
NAME
NOT
New
Normalized
Not
Nothing
OUTSIDE
OWN
Omitted
Open
Optional
PHP
PUT
Panel
Pass
Precompute
Pull
Purely
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\Support\DomId;
Pushery\WireKit\Support\LocalizedNumber;
Pushery\WireKit\WireKit;
RANGE
REPLACED
Real
Reason
Renders
Reset
SCALE.
SIBLING
SOME
STABLE
SVG
Same
Sourced
Status
Stroke
TEXT
THAT
THE
THEM
Tailwind
That
The
This
Threshold
Token-driven
Track
Translated
Two
UNBOUND
Under
Validate
Validated
Value
WCAG
WCAG/WAI-ARIA
WERE
WRAPPER,
WireKit::resolveClasses('progress',
WireKit::resolveClasses('usage-meter',
WireKit::validateProp('progress',
WireKit::validateProp('usage-meter',
WireKit::warnUnknownProps('progress',
With
Without
Y
[
['1',
['none',
['primary',
[0,
[danger,
\Pushery\WireKit\Support\AlpinePayload::from($fillDeterminate)
\Pushery\WireKit\Support\AlpinePayload::from($fillIndeterminate)
\Pushery\WireKit\Support\AlpinePayload::from($valueExpression)
\Pushery\WireKit\Support\DomId::unique(
\Pushery\WireKit\Support\DomId::unique(null,
\Pushery\WireKit\WireKit::warnUnknownProps('progress.circle',
\Pushery\WireKit\WireKit::warnUnknownProps('usage-meter',
\Pushery\WireKit\WireKit::warnUnknownProps('usage-meter.gate',
\Pushery\WireKit\WireKit::warnUnknownProps('usage-meter.panel',
])
]),
__('wirekit::Approaching
__('wirekit::Loading')
__('wirekit::Near
__('wirekit::Over
__('wirekit::Progress')
__('wirekit::Resets
__('wirekit::Unlimited')
__('wirekit::Usage');
`(int)`.
`+
`--font-scale-wk`
`.0`,
`<div>`
`DomId::unique`
`FontScaleGuardTest`
`Str::random(6)`
`action`
`aria-label`
`aria-labelledby`
`aria-valuenow`
`danger`
`dist/wirekit.css`
`div`
`id`
`id`,
`intent`
`label`
`personalize()`
`progress`
`prop="false"`
`rem`
`svg`,
`value-expression="percent"`
`value-expression`
`value="40"`
`valueText()`
`value`
`variant`
`variant`.
`wire:poll`
`wk-progress-fill`
`wk-progress-indeterminate`
`wk-progress-track`
`wk-spinner`,
`x-bind:value`
`x-data`
a
a11y).
above
absolute
accent
accent).
accessible
across
action
additive
adds
affordance
after
against
against,
agreed
alert).
alias
alias).
alias.
alone
alone).
alone.
already
always
an
and
animate
animates
animation
announce.
announcement
announces,
another
any
anything
anything,
apart
app
applied
arbitrary
arc
arc,
are
aria-disabled
aria-disabled="true"
aria-hidden="true">
aria-label
aria-label,
aria-label="{{
aria-labelledby
aria-labelledby)
aria-labelledby,
aria-labelledby="{{
aria-value*
aria-valuemax="{{
aria-valuemin="0"
aria-valuenow="{{
arithmetic
around
arrives
arriving
as
as,
asserted:
at
attached
attribute
attributes
auto-derived
available,
axe-core
axis:
axis;
back
back-compat
back-compat,
background
badge
badge's
band
band's
bands:
bar
bar's
bar)
bar,
bar.
bar:
base
be
because
becomes
been
behavior
being
below
below,
beside
binding
block,
blocked
both
bound
branch
breaking
browser
bug.
build
bundle.
but
button
by
cadence
cadence.
call
called
caller
caller's
callers
calling
can
can't
cannot
canonical
carries
carry
carrying
case,
cast
chain.
change
changes
changes.
circle
circle)
circular
circumference
claims
clamps
class
class="absolute
class="font-[number:var(--font-wk-heading-weight)]
class="mb-1
class="mt-1
class="mt-2
class="mt-2">{{
class="pointer-events-none
class="relative
class="tabular-nums
class="text-[color:var(--color-wk-text)]
class="text-[color:var(--color-wk-text)]">{{
class="text-[color:var(--color-wk-text-muted)]
class="text-[color:var(--color-wk-text-subtle)]">({{
class="text-[color:var(--color-wk-text-subtle)]">{{
class="text-[color:var(--color-wk-text-subtle)]">·
class="text-[length:var(--text-wk-sm)]
class="w-full
class="wk-progress-circle-indeterminate
class="wk-progress-fill
class="wk-progress-fill"
class="wk-progress-track
code
collapses
color
color-axis
colored
coloring,
column
coming,
comment
comments.)
compiler
compiles
component
component's
component,
component-level
component-tag
compute
config('wirekit.components.progress.circle-size',
config('wirekit.components.progress.size',
config('wirekit.components.progress.variant',
config('wirekit.components.usage-meter.danger',
config('wirekit.components.usage-meter.warn',
configured
consistently
contract
contract).
control
conveyed
conveys
core
costs
could
count.
count:
current
cx="18"
cy="18"
daily'),
danger
danger/over
danger=1.0,
data-over-limit="true"
date
debug
decided
default
default.
defect
depend
depends
derive
design
determinate
determinate)
determinate:
developer
different
dimensions
dimmed
directory
disagree
dist/wirekit.css)
distinct
docs
does
done
double-announce
drawing
drawn
drew
drifted
drops
duplicate
duration-[var(--transition-wk-duration)]
each
early.
ease-[var(--transition-wk-easing)]'
easy
edge
effective
either,
element
element's
element,
element.
else
else,
empty,
enough
enters
enum;
evaluated
evaluator;
even
every
everywhere
exact
exactly
exceed
exists
exists),
explanation
explanatory
explicit
expression
expression:
extra
extraction,
factor
factory
failure
fallback
falls
false
false);
false,
feature
file
fill
fill).
fill="none"
filter_var($over,
fit
flags
flex
flex-col
flex-wrap
flight"
flips
fn
font
font-[family-name:var(--font-wk-sans)]',
font-[family-name:var(--font-wk-sans)]'])
font-[number:var(--font-wk-heading-weight)]
font-[number:var(--font-wk-heading-weight)]">{{
for
forced-colors
forced.
four
fresh
from
from,
from:
gap-1',
gap-2
gap-[var(--gap-wk-sm)]
gap-[var(--space-wk-md)]
gap-[var(--space-wk-sm)]">
gap;
gate
gate:
gated
generic
geometry:
gets
gives
go
goes.
gray
grid
grid-cols-1
grid-cols-1',
grid-column
grid;
group
group's
guard
h-[var(--size-wk-lg)]',
h-[var(--size-wk-md)]',
h-[var(--size-wk-sm)]',
h-[var(--size-wk-xl,6rem)]',
h-full
had
half
halves
has
have
heading
heading's
heading.
height
here
here.
hint)
hint:
hit
hook
house
id
id)
id,
id="{{
identical
identically
identity
if
implode('
imports
in
in_array($animation,
indeterminate
indeterminate:
indicator
indicators.
inert
inert,
info
inline
inset-0
inset-y-0
inside
instead
intent
interactive
is
is,
is.
it
it,
it:
items-baseline
items-center
its
just
justify-between
justify-center
keep
keeps
kept
keyframes
know.
knowing.
known
label
label's
language
larger
later
layout
layout,
leaves
left
legacy
length
lg
lg:grid-cols-3',
life
light
like
limit
limit".
limit')
limit.
limit;
line
linear
links
list
list,
live
lives
logic.
looked
low-emphasis
made
markers
markup
match
matches
matching
max
max(0,
max(0.0,
max:
max]
may
md
mean
meaningful
means
meter
method.
metric
min
min((float)
minted
mirrors
missing
mode,
moment
month")
monthly'),
morph
most
motion
motion.
multi-column
multiply
must
muted
n/a
name
name)
name,
name.
named
named.
names
natural
needed
neutral
never
new
next
no
nobody
node's
none
normal
not
not.
not:
note
nothing
nothing.
noun)
now
null
null)
null,
null;
number
numeric
of
off
ok.
old
omission
omitted
on
on.
once
one
one,
one.
one;
only
only;
opacity
opacity-[var(--opacity-wk-disabled)]">
opposite
optimistic-ui:
optional
order
origin-center"
other
other;
out,
outer
outside
over
over)
over).
over.
overflow-hidden',
overflowing.
overhead
override
own
owns
package,
page-unique
pairing,
pairs
palette
palette.
palette:
panel
parse.
partial
pass
passes
patched.
people
per
percentage
percentage,
period
pitfalls).
place
plan
plan-paywall
polish:
poll
polls
population
pre-2.4
prefers-reduced-motion,
presentational
pricing-table
primary
printed:
prod).
produced
progress
progress'
progress:
progressbar
progressbar-name
promised
prop
prop's
proper
property
props
provided)
purpose
put
qualified:
quantity,
quotas
r="{{
radial
radial-progress
radial-progress.
radius
ramp
random
rather
ratio
re-renders,
re-renders.
re-resolved
reach
reached,
read
readable
reader
readout
readout:
reads
reads.
real
reason
reason,
reason:
reasoning
recipe.
recommend
recompute
red
redundant
refutes
region
region,
reinforcement
rely
removal
remove
render
render,
render.
rendered
renders
renders.
report
represents
required
reset
resetAt
resolved
resolves
resolving.)
responsive
result
retunes
reuses
richer
right
ring
role.
role="group"
role="progressbar"
rotates
round($ratio
rounded-[var(--radius-wk-full)]
rows,
rule
rule)
rule),
rung
same
scale
scales
scheduled
scope
screen
screens
second.
see
see.
select-none
selects.
semantic
set
set.
sets
settled
shape
shares
shimmer
shipped
shortest
should
show
showValue
shown
shown,
shows
sibling
signaled
silent-wrong-color
silently.
simple
simply
single
sit
site
sits
size
size="sm"
sizes
skipped
slot
slot)
sm
sm:grid-cols-2
sm:grid-cols-2',
small
smallest
smooth
so
soften
source
source.
span
spans
stable
stack
state
static
stay
stayed
stays
still
stops
streamed
streaming).
string,
stripes
stroke-dasharray="{{
stroke-dashoffset
stroke-dashoffset="{{
stroke-linecap="round"
stroke-width="3"
stroke="var(--color-wk-bg-muted)"
stroke="{{
style="grid-column:
style="transition:
style="width:
stylesheet's
sub-component
success
suffix
superseded
supplies
surface
surfaced.
surrounding
sweep).
synonym.
tabular-nums"
tabular-nums">
tag
tag,
tags
text
text)
text-[color:var(--color-wk-danger-text)]">{{
text-[color:var(--color-wk-text)]
text-[color:var(--color-wk-text)]"
text-[color:var(--color-wk-text-muted)]">
text-[color:var(--color-wk-warning-text)]">{{
text-[length:var(--text-wk-sm)]">
text-[length:var(--text-wk-xs)]">
text;
than
that
the
their
them.
theme
themselves,
then
there
there,
there.
they
thing
this
this,
threshold
threshold/intent
through
tick
tick.
tier
time,
title.
to
token
token.
tokens
tokens-only
tokens.
too,
took:
top
track
track,
trailing
transition-[width]
transitions
translator
travels).
true
true)
true);
true,
truncating
truthy
two
type
typography.
unavoidable
unchanged,
unchanged.
unclickable).
unconditional
under
unit
unknown
unlimited
unresolvable
until
untouched
untouched.
up
up.
updates.
upgrade
upgrade/plan-compare
upload
urgent
usage
usage")
usage-meter
use
used
used/limit
used:
uses
utility
v3.0.0
value
value,
value/width
valueText,
values
values.
var(--transition-wk-duration)
var(--transition-wk-easing)"
variants
version
vertical
very
via
view
view's
viewBox="0
viewports.
visible
visual-only".
vocabulary
w-full
w-full',
warn
warning
was
watched
we
weekly'),
were
what
when
where
which
while
who
whole
whose
why
wider
width
will
wins;
wire:poll
with
without
wk-progress-'.$animationValue
wk-progress-indeterminate';
word
wording
wording.
work
would
wrapped
wrapper
wrapper).
written
wrong
x-bind:aria-valuenow="ariaValueNow()"
x-bind:class="fillClasses()"
x-bind:style="fillStyle()"
x-data="wirekitProgress({
x-text="valueText()"></span>
yearly'),
yet
zero
{
{{
{{--
|
||
})"
};
}}
}}"
}}">
}}"></div>
}}%
}}%"></div>
}}%)</span>
}},
}}</div>
}}</span>
}}>
}}@endif
}}@if($unit)
~100
—
→
⚠️
