"Filter"
"Save"
"toolbar"
$align,
$alignValue
$ariaLabel
$attributes->class([$baseClasses])
$attributes->getAttributes());
$baseClasses
$density,
$densityValue
$filters
$gapClass
$gapClass,
$justifyClass
$justifyClass,
$leading
$paddingClass
$paddingClass,
$scope);
$slot
$sticky
$stickyClasses
$stickyClasses,
$trailing
'';
',
'align'
'align',
'ariaLabel'
'base',
'between'
'between',
'comfortable',
'compact'
'compact']),
'density'
'density',
'end'
'end']),
'false'
'flex
'font-[family-name:var(--font-wk-sans)]',
'gap-[var(--space-wk-md,1rem)]',
'gap-[var(--space-wk-sm,0.5rem)]',
'justify-between',
'justify-end',
'justify-start',
'px-[var(--space-wk-sm,0.5rem)]
'px-[var(--space-wk-xs,0.25rem)]
'scope'
'start'
'start',
'sticky
'sticky'
($align)
($alignValue)
($density)
($densityValue)
(`--wk-strip-inset`),
(`flex-wrap
(a
(action
(badges,
(or
(search,
(silent
(typically
*
*/
+
--}}
/
/*
//
10
14rem
40,
:
</div>
<div
=
=>
>
?
@elseif($slot->hasActualContent())
@endif
@endphp
@if($ariaLabel)
@if(isset($filters))
@if(isset($leading))
@if(isset($trailing))
@php
@props([
@props.
A
AND
Adding
BELOW
Blade
BooleanProp::from($sticky,
Declared
Default-slot
Default/trailing
Dev-only
Equal
FIELD,
Filters
Fully
Home/End
It
Leading
Left/Right
Measured
NEVER
NOT
No
Normalized
Nothing
ONCE,
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
Renders
SEARCH
Same
Sized
Tab
The
This
UNBOUND
WITHOUT
Where
WireKit
WireKit::resolveClasses('toolbar',
WireKit::validateProp('toolbar',
With
Without
['between',
['comfortable',
\Pushery\WireKit\WireKit::warnUnknownProps('toolbar',
])
])),
`<x-wirekit::editor.toolbar>`
`flex-1
`flex-wrap`
`min-w-0`
`prop="false"`
`px-*`
`py-*`
`role="group"`
`role="group"`,
`role="toolbar"`
a
above
action
action-bar
actions
after
against
all.
already
an
and
announced
any
anywhere
arbitrary:
are
aria-label="{{
array_filter([
arrows
as
as,
asserted:
at
attribute
auto-derived
axe
bar
bar's
bar,
basis-0
be
behavior
belong
below
beside
between
bg-[var(--color-wk-bg)]'
binds
block,
border
bound.
broken
but
button
button)
button-cluster
buttons)
call
caller
can
cast
catalog
children
chips)
class="flex
cluster
collapse
collapsed
compiles
component
component's
composite
composite-widget
container
content
contents
contract:
control
controls
controls).
controls,
correction
costs
could
could:
cramming
date-separator.
debug
default
default-slot
defaults
defect,
density
did
do
documented
does
doesn't
dumped
each
early.
edge.
element,
ended
ends.
every
extra
fallback:
false);
false,
feature
field
field.
file
fill
filter
filters
filters/trailing
first
fit
flags
flex
flex-1
flex-wrap
flips
flush
for
forcing
four
frame.
from
full
gap-[var(--space-wk-sm,0.5rem)]
gap-[var(--space-wk-sm,0.5rem)]">
green
grows
guard
half-owned
has
here
here,
horizontal
implode('
imports
in
index
inset
inside
instead
interactive
into
is
it
it,
it.
items-center
items-center',
its
jump
justify-between
keep
keyboard
keys
lane
later
layers
layout
leading
leading/
leaves
left
let
line
line,
list
live
longer
look
makes
match
may
mean
min-w-[min(100%,14rem)]">
min-w-[min(100%,14rem)]`
min-width,
mirror
mistake.
mobile.
model
model,
move
moves.
must
n/a
named
named-slot
narrow
narrower
neither
never
next
no
none
nor
not
nothing
nothing.
null,
of
off
old
on
on.
once
one
one)
one.
one:
onto
opposite
optimistic-ui:
other
out
over
own
own,
owns
page
page's
passed
passed,
path
path:
phone
pinned
place
presentational
press
primary
prod).
promise.
promise:
prop's
props
pushes
py-[var(--space-wk-sm,0.5rem)]',
py-[var(--space-wk-xs,0.25rem)]',
qualified:
rather
reach
reached
reaches
read
reader
reads
real
reason
refutes
regression
related
render
renders
reported
resolve
responsive
result
role
role="group"
root.
roving
row
row.
rule
same
sat
says
screen
scroll-track
search
second
sees
selects
selects,
sequence
set
several
shipped
shown
shrinks
silently.
sit
site
sits
slot
slots.
so
space
spare
squeezed
stayed
sticks
sticky
sticky-
still
stop
stops
string,
strip
strong
surface.
tab
tabindex
taking
text
than
that
that).
the
their
them
there
therefore
they
thing
this
to
token
told
took,
toolbar
toolbar's
tooltip
top-[var(--wk-strip-inset,0px)]
touching
trigger
true
truthy
under
uniform
unknown
up
usable
use
used
user
vanished
variant
vertical
via
view's
viewport
visually
w-full">
w-full`)
walks
wanted,
was
way
were
what
whatever
where
which
while
whole
whose
width
with
without
worse
wrap
wrapper
wraps
wrong
z
z-[var(--z-wk-sticky)]
zero
{
{{
{{--
};
}}
}}"
—
