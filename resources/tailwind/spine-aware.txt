$attributes->class([$rootClass])
$attributes->getAttributes());
$rootClass
$scope);
$slot
$spineClass
$spineClass,
',
'base',
'lg',
'scope'
'tier'
'w-full',
'wk-spine-aware',
(brand-bar
(silent
(the
--}}
/
//
</div>
<div
=
=>
@endphp
@php
@props([
@props.
Declared
Default
Dev-only
Fully
Measured
Pass
Pushery\WireKit\WireKit;
Renders
Resolve
See
The
WireKit::resolveClasses('spine-aware',
WireKit::spinePadding($tier);
[
\Pushery\WireKit\WireKit::warnUnknownProps('spine-aware',
])
]),
`--padding-wk-x-*`
`lg`
`md`
`sm`
`tier`
`xl`
a
action
any
applies
are
asserted:
auto-derived
be
block,
brand-bar
but
canonical
component
component's
components
container
content
convention
could
cta
debug
developer-authored
different
docs/extending/spine-contract.md
does
early.
edge
element,
file
first-party
flags
footer
footer.
for
from
guard
header
helper
hero).
implode('
imports
in
inline-padding
interactive
is
joins
later
left
list
live
lockstep
main
may
n/a
no
not
null,
one.
opt-in
optimistic-ui:
other
page-edge
participants
presentational
prod).
produce
props
qualified:
rather
reach
read.
reason
refutes
renders
result
shown
so
spine
spine).
spine-padding
spine-participant
spine.
stay
than
that
the
there
this
tier
tier.
tiers
to
token
unknown
use
utility
valid
via
view's
visually-distinct
which
whose
wirekit:spine-participant
with
{{
{{--
}}
}}>
—
