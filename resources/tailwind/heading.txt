"h{$headingLevel}"
$accent
$as
$as);
$attributes->class([$classes])
$attributes->getAttributes());
$break)
$break,
$breakClasses
$breakClasses,
$classes
$colorClasses
$colorClasses,
$headingLevel
$headingLevel)
$resolvedSize
$resolvedSize,
$scope);
$size
$sizeClasses
$sizeClasses,
$slot
$tag
$tracking,
$trackingClasses
$trackingClasses,
'',
',
'2xl'
'2xl',
'3xl'
'3xl',
'4xl'
'4xl',
'5xl'
'5xl']),
'[overflow-wrap:anywhere]',
'[overflow-wrap:normal]
'[word-break:break-all]',
'accent'
'all'
'all']),
'anywhere'
'anywhere',
'as'
'base'
'base',
'break'
'break',
'false'
'font-[family-name:var(--font-wk-sans)]',
'font-[number:var(--font-wk-heading-weight)]',
'leading-[var(--font-wk-heading-line-height,1.25)]',
'level'
'lg'
'lg',
'md',
'normal'
'normal',
'scope'
'size'
'size',
'sm'
'sm',
'text-[color:var(--color-wk-accent-text)]'
'text-[color:var(--color-wk-text)]';
'text-[length:var(--text-wk-2xl,1.5rem)]',
'text-[length:var(--text-wk-3xl,1.875rem)]',
'text-[length:var(--text-wk-4xl,2.25rem)]',
'text-[length:var(--text-wk-5xl,3rem)]',
'text-[length:var(--text-wk-lg)]',
'text-[length:var(--text-wk-md)]',
'text-[length:var(--text-wk-sm)]',
'text-[length:var(--text-wk-xl,1.25rem)]',
'tight'
'tight',
'tighter'
'tighter']),
'tracking'
'tracking',
'tracking-normal',
'tracking-tight',
'tracking-tighter',
'xl'
'xl',
($break
($level
($resolvedSize)
($tracking)
((int)
(h1–h6).
(int)
(rest
(silent
(string)
-
--}}
/
//
1
1–6
2
2)));
3
4
4.1.
4xl
5,
5xl
6
:
</{{
<{{
=
===
=>
?
??
@endphp
@php
@props([
@props.
A
AN
ATTRIBUTE
An
Auto-size
Blade
BooleanProp::from($accent,
Declared
Determine
Dev-only
Fully
HTML
IN
INT
Measured
Normalized
Pushery\WireKit\Support\BooleanProp;
Pushery\WireKit\WireKit;
RANGE,
RESOLVED
Renders
TO
Tailwind
The
UNBOUND
WireKit::resolveClasses('heading',
WireKit::validateProp('heading',
[
['normal',
['sm',
[word-break:normal]',
\Pushery\WireKit\WireKit::tagName('heading',
\Pushery\WireKit\WireKit::warnUnknownProps('heading',
])
]),
`<h2
`=`
`=`,
`anywhere`
`as="div
`as`
`e()`
`level="2
`prop="false"`
`text`,
a
accepted
action
added
admits
against
alias
all.
already
also
an
and
any
anything
anywhere
apart:
arbitrary
arrived
as
as,
asserted:
at,
attribute
auto-derived
base
based
be
because
becomes
below.
block,
branch
break
browser
call
can
cannot
canonical
carried,
carries
carry
cast
channel
clamped
class
classes
closed
compiles
component's
concatenated
container
copy
could
counts
data-wk-prose-skip
debug
default
designers
documented
does
drift
each
early.
element
element,
else;
escapes
event
exist.
explicitly
extend
external
false);
false,
feature
file
fix
flags
flex
flips
for
from
guard
guarded.
handler.
have
heading
hero
implode('
imports
in
injection
inside
instead.
int
interactive
into
is
it
kit
later
lets
level
link
list
literal
live
make.
match
max(1,
may
md
mean
middle
min(6,
min-content
n/a
name
named
names
neither
never
no
nor
normal
not
nothing
null
null,
of
often
on
on.
one
one.
only
onmouseover=alert(1)
onmouseover=alert(1)"`
opening
opposite
optimistic-ui:
or
out
outlier).
over,
overflow-wrap
own
point
presentational
prod).
prop
prop's
properties
props
qualified:
rather
reach
reads
reason
refutes
rendered
renders
repository,
result
row
row.
rules,
run
same
scale
scale.
set
shipped
shown
silently.
site
size
so
somebody
space
standard
straight
string
string,
such
survived
systems.
tag
tag,
tagName()
template,
tend
text
than
that
the
there
they
this
three
tier
to
toward
truthy
two
typed
typography
unbound
unbroken
unknown
use
used
uses
utilities,
value
values
view's
was
what
when
where
which
whose
width,
with
working
wrap
wrap:
{
{{
{{--
|
};
}}
}}>
—
…>`
→
⚠️
