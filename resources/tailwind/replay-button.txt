"Replay"
"used
"↻"
$attributes->getAttributes());
$attributes->merge([
$el.closest(selector)
$label,
$slot
$slot->hasActualContent())
$target
'aria-label'
'button',
'class'
'label'
'target'
'type'
'wk-replay-button',
(16×16).
(an
(badge[dismissible],
(reveal,
(set
(silent
--}}
/
//
/>`.
0
1
1-5
24
24"
4V7a5
5
5H5a7
5V2L8
6l4
7
7-7Z"/>
</button>
</svg>
<button
<path
<svg
=>
>
@else
@endif
@endphp
@if(!
@if($target)
@php
@props([
@props.
AND
Accessible
Alpine
Alpine's
Alpine.initTree()
BEM
CSP
CSS
ComponentSlot),
Declared
Default
Dev-only
Developers
EMPTY,
Fully
Its
Optional
Pairs
Pass
Replay
Style
That
The
WireKit
[data-replay-target]
\Pushery\WireKit\WireKit::warnUnknownProps('replay-button',
])
__('wirekit::Replay'),
`<x-wirekit::replay-button
`@isset($slot)`
`data-replayable="true"`
`wk-*`
`wk-replay-button`
a
affordance
affordance.
alert[dismissible]).
always
an
ancestor
and
animation
animation-
announce
anticipate
aria-hidden="true"
auto-derived
back.
be
because
before
being
block,
build
button
button.
by
call
calling
can
cannot
capable
card/hero/cta/feature/
chart)
circular-arrow
clear
click
client-only
closest
component
component's
components
constructed
content
content.
contract
convention
d="M12
data-replay-source
data-replay-target
data-replay-target-selector="{{
debug
declarations,
default
demo
demo.
developers
directive
docs
does
early
emitted
empty
etc.
event,
every
existing
explicit
fill="currentColor"
flags
footer[animateIn],
for
from
gains
gated
give
given.
glyph
had
handler
has
height="16"
hold
hooks
icon
icon:
iframe
imports
in
initial
innerHTML
instead.
interaction
is
isn't
it
its
label
later
list
live
lives
matches
may
mount),
must
n/a
namespace
never
no
none
not
nothing
null,
of
on
one
one.
optimistic-ui:
or
otherwise
override
own
owns,
parses.
passing
preview
prod).
props
qualified:
re-bind
re-mount
re-mounts
re-run
reach
readers
reading-progress,
renders
replacing
replay
replaying.
reset
reset:
resources/js/components/replay-button.js:
return
right
roll
root
root.
scope
scopes.
screen
scrollbar
selector
server
sets
site's
slot
snapshot
so
something.
stat[animate],
state
state-mutating
statements
still
stored
that
the
then
there
this
to
true
two
unknown
unset
up
up"
used
uses
value
verb
via
view's
viewBox="0
walks
well-known
when
whether
which
whose
width="16"
with
wrapper
x-data
x-data="wirekitReplayButton"
x-on:click="replay()"
{{
{{--
}}
}}"
—
